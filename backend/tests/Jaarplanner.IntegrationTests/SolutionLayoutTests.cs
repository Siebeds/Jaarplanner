namespace Jaarplanner.IntegrationTests;

/// <summary>
/// Placeholder smoke test so the skeleton solution builds and `dotnet test`
/// reports a green run. Real API integration tests (against a Postgres test
/// container) arrive in later stories.
/// </summary>
public class SolutionLayoutTests
{
    [Fact]
    public void Skeleton_builds_and_tests_run()
    {
        Assert.True(true);
    }
}
