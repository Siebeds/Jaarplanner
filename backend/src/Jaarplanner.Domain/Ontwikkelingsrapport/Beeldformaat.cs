namespace Jaarplanner.Domain.Ontwikkelingsrapport;

/// <summary>The two formats a <see cref="Kindtekening"/> is accepted and stored in (FB-005, R10).</summary>
public enum Beeldformaat
{
    Jpeg,
    Png,
}
