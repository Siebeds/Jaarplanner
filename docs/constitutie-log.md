# Constitution — ratification log

Every ratified amendment of [`CONSTITUTION.md`](../CONSTITUTION.md), one row each, newest last. Moved out of Art. XI on 2026-09-15 ([ADR-0037](adr/0037-lichter-agentproces.md)); the rows up to that date are unchanged apart from their links, which now resolve from `docs/`. The dated amendment notes that used to sit inside the articles are in the constitution as of commit `f5804bc`.

| Date | Ratified by | What |
| --- | --- | --- |
| 2026-06-29 | Siebe De Saedeleir (auteur/directie) | Adopted the Op.stap reference-material refinements as binding: **Art. III** professional-autonomy principle (GAP 0c), **Art. VII.0** taxonomy correction (`Discipline → Domein → Subdomein`, `cluster` nullable — GAP 0a), the **Art. IX** themalaag model (Themadoel/Subdoel + rich attributes + level scoping), and the **Art. IV.8** goal-first authoring with the thema-opbouw wizard as a **committed MVP feature**. Source: `docs/Gap-analyse_Opstap_referentie.md`. |
| 2026-06-29 | Siebe De Saedeleir (auteur/directie) | Closed two Art. XIV open decisions: **Hosting/AI** = Azure + EU region + EU AI data zone ([ADR-0016](adr/0016-azure-hosting-eu-residency.md)); **Identity provider** = Microsoft Entra ID ([ADR-0011](adr/0011-authn-authz-rbac-gdpr.md)). |
| 2026-06-29 | Siebe De Saedeleir (auteur/directie) | Adopted the **UI/UX approach** ([ADR-0017](adr/0017-ui-ux-design-system.md)): Tailwind + Radix UI + shadcn/ui (copied in) as the accessible component layer, design tokens for doelsoort/status/coverage, wireframes-first for the anchor screens, **WCAG 2.2 AA** app-wide. Approach doc: `docs/ux/ui-ux-approach.md`. |
| 2026-07-30 | Siebe De Saedeleir (auteur/directie) | Amended **Art. VI.4** (and Art. X.4 in step) to sanction a **narrow exception** for throw-away local/CI test-database credentials in the repo. *Why:* the practice already existed — `.github/workflows/ci.yml` commits `jaarplanner_ci`, dev-setup docs and story worklogs quote `jaarplanner_local` — so Art. VI.4's flat "no secrets" was being contradicted by precedent rather than by decision, and each audit had to re-argue it from scratch. AI keys, production/staging credentials and any connection string to school data remain **absolutely** excluded. *Surfaced by:* the E2-08 antagonist audit, which correctly flagged `Password=jaarplanner_local` in a worklog as an Art. VI.4 breach with no exception clause to appeal to. |
| 2026-08-19 | Siebe De Saedeleir (projecteigenaar) | Amended **Art. IX.3**'s `Jaarplan` clause to state what a (re)generation may discard: **only a placement that is `Voorgesteld` and not `vergrendeld`**. A placement the teacher decided on survives **without** a lock, and a single-period run narrows which blocks are visited, never what is replaceable. *Why:* the article named the `vergrendeld` flag and said "(excluded from regeneration)" beside it, which read as *the lock is what protects a placement* — and E4-06 had already built the opposite, hiding the lock control on decided **unlocked** placements precisely because locking one there changes no run outcome. *(A decided **locked** placement keeps the control, so the lock stays undoable; whether the UI offers it is E4-06's decision and Art. IX.3 deliberately does not ratify it.)* The article was silent where the product was decided, so every story that touched regeneration had to re-derive the rule from `Themaplaatsing.IsVervangbaar`. *Scope of the ratification:* the **projecteigenaar** ruled it; **FR-7.3 reserves it to directie and their confirmation is outstanding** (question 6 in [`docs/besluiten-gevraagd.md`](besluiten-gevraagd.md)). It is recorded rather than withheld because it is the conservative option and unblocks E4-07; a reversal changes this clause and the teacher-facing strings enumerated under E4-07 together. *Surfaced by:* the E4-07 ruling round's antagonist audit, which found the amendment owed and named the `efecf73` precedent for deferring it. |
| 2026-08-30 | Siebe De Saedeleir (projecteigenaar) | Amended **Art. IX.2** (and the `Klas` clause of Art. IX.3, and the Art. XIV "Thema scope" resolution in step) so that a **`Subthema` is scoped by leeftijd alone**: the `KlasId` leaves the entity, a subthema on K3 holds for every class teaching K3, and a `Klas` states a required `jaarfase` from which its `leerjaar` is derived. Recorded as [ADR-0025](adr/0025-subthema-per-leeftijd.md), which supersedes ADR-0008's per-class scoping of Subthema/Subdoel/Activiteit. *Why:* the article declared both scopes structural, so a school with three parallel K3 classes authored the same subthema three times and the copies drifted, each with its own subdoelen, activiteiten and goal links; a subthema built under K3 groen was unreachable from K3 blauw. **What stays per klas is the planning, not the content.** *Recorded late, and the lateness is part of the record:* the code, the migration and the frontend landed on 2026-08-30 citing "ADR-0025" in their own comments while no such file existed, so for a few hours the repository held an implementation its own governing document contradicted (Art. XI.3). *Surfaced by:* session `hoeken`, which went to build on the change and found the decision unwritten. ~~**Still open:** eleven integration tests encode the abolished concept "a subthema of another klas", and behind three of them sit a guard and an endpoint that were not updated with the model; named in commit `ba4a68a`.~~ **Closed the same day** by the entry below, which also records what the owner decided about the guard. |
| 2026-08-30 | Siebe De Saedeleir (projecteigenaar) | Ruled the **move rule** that the amendment above left without one, and recorded it in **Art. IX.2** under `Activiteit`: **an activiteit may move to another thema, and only to a subthema at the same `leeftijd`.** *Why it needed a ruling rather than a repair:* the old guard compared two `KlasId`s and went with the klas itself, so for part of that day the verb had **no scope check at all** — any caller could post any `subthemaId` and the move succeeded with 200. *Why this answer and not "leave it open":* content is school-wide now, so an age is the only thing left that says who a subthema is for, and a move that changes it hands one class's work to another under a name that no longer says so. **This supersedes the ruling of 2026-08-05** (a move may cross a leeftijd), which was decided when a subthema still named a klas and a graadklas's two ages were the differentiation the model existed for. *Consequence recorded rather than smoothed over:* the 2026-08-05 ruling's own copy line `themabeheer.activiteitVerplaatsLeeftijd` no longer exists in `nl.json`, and neither does any screen that calls the move — the verb and its destination list are **backend-only today**, so this ruling governs an endpoint no teacher can currently reach. *Directie's confirmation is outstanding (question 11 in [`docs/besluiten-gevraagd.md`](besluiten-gevraagd.md)); building proceeds on the owner's ruling, which is the conservative option.* |
| 2026-09-11 | Siebe De Saedeleir (projecteigenaar) | Amended **Art. V.1** with a second route to *gedekt*, and **Art. IX.2** with the entity it rests on: a leerplandoel is also gedekt when a planned **algemene fiche** of the klas carries a decided link to it. Recorded as [ADR-0029](adr/0029-algemene-fiches.md). *Why:* the teachers asked to plan recurring activities outside any thema and link them to goals ("elke maandag turnen op dit uur"), and the owner was asked whether such a goal counts and ruled that it does; without it, the bewegingsopvoeding goals a class works on every week stand in the gap-analyse as missing all year. *What keeps it honest:* the fiche must be **planned** (a fiche that exists only in the settings list proves nothing is taught), it must be **this klas's**, every link on it is a teacher's `manueel` decision (no AI path reaches it, Art. IV.1), and the evidence names it as a fiche so a directie never reads it as a thema. *Directie's confirmation is outstanding*; it is the less conservative of the two answers, because it lets a figure rise, so a reversal must drop the fifth layer from the computation, the payload's `DekkendeFiches` and the export together. |
| 2026-09-11 | Siebe De Saedeleir (projecteigenaar) | Closed the Art. XIV open decision **"Op.stap import"**: the Op.stap goals are imported from **KOV's Op.stap API**, not from per-discipline Excel files, and added **Art. VII.2** to say so. It also records the owner's second ruling of that day: **only G goals for now**, with Z (zwemdoelen) and V (Vlaamse gebarentaal) named as skipped. Recorded as [ADR-0032](adr/0032-opstap-api-als-importbron.md), which supersedes ADR-0006's *source* and keeps its four rules. *Why:* the Excel route never produced a single decreed minimumdoel (E1-12 had waited on a file since 2026-07-28), and the Excel files this repo holds carry no concordance at all, so minimumdoel-level coverage (Art. V.2) was unreachable through them. The API publishes the whole curriculum, the concordance and the decreed text. *Accepted with it:* KOV's usage notes prefer their web components and expect usage data; the owner read them and ruled without asking KOV first. *The cost of the G-only scope, stated:* six minimumdoelen become uncoverable, `6-7.1.6` because only a Z goal carries it and five because no goal does. *Directie has not confirmed this ruling.* Question 1 in [`docs/besluiten-gevraagd.md`](besluiten-gevraagd.md) still asks them for the decreed file it replaces and must be rewritten to report it; that file was held by another session when this was recorded. *Amended in step (Art. XI.1, antagonist round 1 on E1-12):* Art. V.6 now names the API mapping and its HTML conversion as highest-risk logic, and the two clauses of VII.2 that go beyond the rulings are marked as the implementer's. |
| 2026-09-13 | Siebe De Saedeleir (projecteigenaar) | Amended **Art. VII.2** (and the Art. XIV "Op.stap import" resolution in step): the per-discipline **Excel route stays available only until the first API import of the leerplandoelen** (a snapshot applied) and refuses every file after it, with a 409 and without writing anything ([ADR-0032](adr/0032-opstap-api-als-importbron.md) decision 8, amended). Before that it works as it did. *Why:* E1-21's round-1 audit measured what an Excel file does when it is read after the API import. On snapshot 1.2, against the repo's twelve files, it flagged about 4,700 goals *niet meer in Op.stap*, cleared 858 concordances and reverted goals the API had just written to the Excel wording. The owner answered the orchestrator's question with "Bekrachtigen". *The cost, ratified with it:* goals the Excel route loaded outside goal set G (P, S, +, A) stay as the file wrote them and can no longer be refreshed by any route; a removal by KOV is still flagged through the API path, a changed wording is not. *Directie has not confirmed this ruling.* *Clarified by the owner the same day ("Na de leerplandoelen", answering the E1-21 antagonist's round-3 question):* "the first API import" is the first applied import of the **leerplandoelen** (a snapshot), which is what the code checks. Between the minimumdoelen import and that point the Excel route still works, harmlessly. This entry was narrowed in place to say so before it reached `main`. *Amended in step (Art. XI.1):* the functional analysis (*Koppeling leerplandoelen*). **`CLAUDE.md`** (the note above its Excel mapping table and the E1 status sentence) was held by another session when this was recorded and is updated when E1-21 lands. *Surfaced by:* the E1-21 antagonist, round 1 MAJOR 1 and round 2 MINOR 2. |
| 2026-09-13 | Siebe De Saedeleir (projecteigenaar) | Amended **Art. II.6** (and **Art. X.3** in step) so that the **tickets** in `backlog/functionele-backlog/` and `backlog/technische-backlog/` are written in Dutch, and the team tooling that serves them (`tools/backlog-board/`) keeps English identifiers and commands while its messages and board are Dutch and outside the `nl.json` catalogue. Recorded as [ADR-0033](adr/0033-ticketbacklog-en-kanbanbord.md). *Why:* the owner introduced a ticket backlog that a functional architect, not a developer, writes and a tester closes; the owner ruled in session that its tickets are Dutch, and Art. II.6 said the backlog stays English. *Surfaced by:* the TB-001 antagonist audit (round 1), which found the ruling applied in `CLAUDE.md` and the ADR but never recorded here, so the constitution and the code disagreed (Art. XI.3). *Scope:* the owner approved this text as written; the epic backlog, commits, ADRs and worklogs are unchanged. `CLAUDE.md`'s dependent text landed one commit earlier on the same branch (`8543ff9`), in the fix round that found the gap. |
| 2026-09-13 | Siebe De Saedeleir (projecteigenaar) | Extended the **Art. II.6** exception above, on the TB-001 antagonist's round-2 findings: the **guide** that explains the tickets (`backlog/TICKETS.md` and the README in each ticket folder) is Dutch too, and the three ticket **skills** keep Dutch names, following the `groepschat` / `app-starten` precedent where Art. II.2 would otherwise ask for English tooling identifiers. *Also confirmed by the owner:* the previous row and the II.6 text are what he approved, and he waived, **for the branch `feature/ticket-backlog` only**, Art. XI.1's requirement that the dependent text land in the same commit, because the dependent `CLAUDE.md` text landed one commit before the first amendment. The waiver does not change what XI.1 means for any later amendment. The dependent text in `CLAUDE.md` and ADR-0033 is updated in this commit. |
| 2026-09-14 | Siebe De Saedeleir (projecteigenaar) | Amended **Art. VI.1** to name the rights the owner ruled on 2026-09-11, 2026-09-13 and 2026-09-14 ([ADR-0030](adr/0030-rollen-en-rechten-in-de-app.md) §1, statements 1–37). **Directie** sees and edits everything and maintains gebruikers, klassen, schooljaren and rights (FA FR-12.2, the beheerder now being the directie right; there is no separate ICT-coördinator role). **Themabeheer** edits thema's; runs the FR-1 import, including the goal links it writes on themadoelen and subdoelen (only directie may switch on the import's option to delete human decisions); works the thema-opbouw wizard, creating subthema's, subdoelen and activiteiten there only for a thema it builds from scratch and only through the wizard's own actions; and is, with directie, the only right that generates, reviews and adjusts doelsuggesties. The **hoofdleerkracht**, a gebruiker appointed per `(schooljaar, jaarfase)`, several allowed, creates, edits and deletes that jaarfase's subthema's and subdoelen, links goals to its shared activiteiten by hand, and deletes any of them; thema's only with themabeheer. The **leerkracht**, through a many-to-many klastoewijzing, edits the klas's planning, and at that klas's leeftijd edits and creates shared activiteiten and edits streefwoordenschat. The app records who made an activiteit; that maker may delete it while no goal is linked to it, with or without a klas at that leeftijd, and the activiteit stays shared. The article also states which schooljaar counts for the shared content, rules graadklassen provisionally (pending directie's Art. XIV decision, with the klas→leeftijden mapping in one place), and says what a gebruiker with none of the four rights may do. "Configurable" now says what it always meant: who holds a right is data, and what a right allows is one matrix, ADR-0030 §3, which supersedes FA §3.2's table. *Amended in step (Art. XI.1):* the `Thema`, `doelsuggesties[]` and `Activiteit` lines of **Art. IX.2**; a note on **Art. IV.1** and a pointer on **Art. IV.8**; six **Art. XII** entries; the **Art. XIV** "Teacher visibility" bullet (narrowed, not removed) and graadklas bullet (partly answered for rights); FA Bijlage **A.11**, with pointers at §3.1, §3.2, §4, FR-1.1, FR-3.1, FR-4.3, FR-7.2, FR-10.2, FR-12.2, §7, §11, A.5 and A.7; the status of ADR-0008, ADR-0010, ADR-0022, ADR-0026 and ADR-0030; CLAUDE.md; the backlog; and two new questions for directie in `docs/besluiten-gevraagd.md`. *Why:* Art. VI.1 named three roles that no longer described the school. A subthema has had no owning klas since ADR-0025, so the "owning teacher" of ADR-0011 §3 pointed at nobody, and FA §3.2's table gave every leerkracht rights the owner has since withdrawn. *The reversals and the premises, stated:* on 2026-09-11 the owner ruled that every leerkracht generates and reviews doelsuggesties (R8). Shown that one acceptance moves the dekking of every klas that plans the thema, he gave it to directie and themabeheer (R14). Shown that a goal link on shared content moves the dekking of every parallel klas, he gave those links to directie and the hoofdleerkrachten (R19). Told that a subdoel is, in the model, only a link to a goal, he kept subdoelen with them too (R24). The question on the import overstated what it writes; after the correction he confirmed his answer (R27, R34). He limited the wizard's subthema's and subdoelen to a thema built from scratch (R32). Shown that a re-import can delete a hoofdleerkracht's decided subdoelen, he gave that option to directie alone (R35). *How it was ratified:* in session, on the orchestrator's Dutch summary (the four rights, the matrix, the defaults by example, and the round-4 findings). The owner declined the offered option to read the full draft first, and ratified it with the round-4 corrections (R36). Four antagonist rounds ran on the draft. The owner then stopped the audit rounds, so **this ratification commit, which applies the round-4 corrections and R35–R37, was not audited**. *Scope of the ratification:* the rulings (R-items) only. **A row of the ADR-0030 §3 matrix is ratified only as far as the rulings it cites, counted at column level**; whatever a row takes from an I-item or a lettered §4 question is a default and is not ratified here. The defaults Art. VI.1 lists are **not** ratified: I1, I2 (the reading only), I6 with (a), I9 with (d), I12, I13, I15, I16, I17, I18 (the maker assignment only; its delete right is R33), I19–I23, (c) and (e). The build follows them until the owner changes one (R37). Nor is the shape of personal content (part 2, owed with E6-10). *Directie's confirmation is outstanding* on three points: visibility (Art. XIV, question 4 in [`docs/besluiten-gevraagd.md`](besluiten-gevraagd.md)), graadklassen (question 14, never asked before), and the role model as a whole (question 13). |
| 2026-09-14 | Siebe De Saedeleir (projecteigenaar) | Added two defaults to **Art. VI.1** (FA A.11 and ADR-0030 §2 in step): **I24**, a thema-opbouw wizard run ends when themabeheer or directie finishes or closes it, or 14 days after the wizard's last write action in it, whichever comes first; **I25**, while its run is open, the wizard's own write actions may also edit and delete a subthema, subdoel or activiteit that the same run created, and nothing else. *Why:* the round-4 audit of part 1 asked (QUESTION) when I23's "new" window ends and whether the wizard may correct what it just made, and the ratification above left both to E6-05, as defaults owed before the wizard's write actions are built. The combined E6-02/E6-04 build writes those actions, so session `E6-02` put both questions to the owner, who chose these answers. *Scope:* they are defaults like I1–I23: the build follows them (R37), and the owner can change either on its own. I25 widens themabeheer's wizard right from *aanmaken* to editing and deleting within one run, which is why it belongs in this article and not in the ADR alone. `CLAUDE.md` names no individual default and is unchanged. The owner approved this text. |
| 2026-09-14 | Siebe De Saedeleir (projecteigenaar) | Brought the **ontwikkelingsrapport for the derde kleuter** into scope: the first pupil data the app holds. Recorded as [ADR-0035](adr/0035-ontwikkelingsrapport-derde-kleuter.md), whose §1 quotes the 31 rulings (R1 to R31) with the options offered. *Amended:* **Art. I.1** (item 9). **Art. I.2** (pupil-level reporting and grading, each with the ontwikkelingsrapport excepted and bounded; the other three non-goals unchanged). **Art. IV.1 to IV.5** for the AI rewrite, a third kind of AI output: only the teacher's text goes to the AI (R21); every decision is stored, a rejection without its text (R23), while the proposal itself is not stored, which is session design and the premise R23 was asked on; no motivation (R24). **Art. VI.1** (a fifth right, Leerlingzorg, R18; the report outside I9, R17; its rows in the ADR-0030 matrix). **Art. VI.2** and a new **Art. VI.7**. **Art. VI.6**: the owner's waiver of the order, so real pupil data may enter before the register entry, before the parents are informed and before a DPIA (R20, R27), chosen twice against the recommendation with the AVG articles stated. A new **Art. IX.4**. Nine **Art. XII** entries. *Amended in step (Art. XI.1):* the functional analysis (§2.3 *Binnen* and *Buiten scope*, §3.1, a new FR-13, NFR-6, §7, §10, A.11); CLAUDE.md; the ADR-0030 matrix, header and item (e); the status of ADR-0010, ADR-0011 and ADR-0016 and the ADR index; the Art. XIV bullets on graadklassen, teacher visibility and export formats (annotated, none added); E7-06 and the E8 note; question 15 in `docs/besluiten-gevraagd.md`. Also, on the owner's ruling that the amendment may not block what he asked for (R29, which he confirmed in R30), the antagonist, implementer and jaarplan-build instructions, which now treat pupil data outside the report as a violation and check it inside the report against VI.7. *Why:* the K3 teachers write this report by hand three times a year, and the owner wants it where their subdoelen already are. *Scope of the ratification:* the R-items only. Everything else in ADR-0035 §2 and §3, D1 to D5 and D7 to D16 included (D6 was replaced by R26), is the recording session's design and a default. Directie does not edit the K3 set (R31), the one exception to Art. VI.1's rule that directie edits everything. *How it was ratified:* in session, through seven rounds of multiple-choice questions. The fifth came after the antagonist's first audit (3 MAJOR, 8 MINOR), which found session design presented as ratified. The sixth (R30) was the owner's confirmation of the rule the agent instructions carry, and the seventh (R31) his answer on directie and the K3 set. Four antagonist rounds ran on the branch. The small fixes after the fourth were not audited again. *Directie's confirmation is outstanding* (question 15), and as verwerkingsverantwoordelijke the school carries what R20 and R27 waive. |
| 2026-09-14 | Siebe De Saedeleir (projecteigenaar) | Amended **Art. VII.2** and **Art. IX.1** so that a `Minimumdoel` carries the **decree's own ordering** (`leergebied › rubriek › subrubriek`, from `path` of KOV's onderwijsdoelen endpoint) and its **`soort`** (from `type`), and the minimumdoelen register is browsed in that ordering instead of borrowing the discipline of its concorded leerplandoelen (TB-010, [ADR-0032](adr/0032-opstap-api-als-importbron.md) decision 3 amended). *Why:* the owner found the register a long list without overview, asked for the structure the leerplandoelen have, and asked to see how each minimumdoel is linked to the leerplandoelen. KOV publishes the decree's ordering with every minimumdoel and the app did not store it; the borrowed ordering listed 45 minimumdoelen twice and six in a group apart. *How it was ratified:* in session, the owner compared a clickable mockup of both orderings on real data, was told that storing the decree's ordering amends VII.2 and IX.1, and chose it ("oke we gaan voor optie A"), for the minimumdoelen view only ("ik wil wel dat de leerplandoelen sectie blijft zoals het is"). The TB-010 antagonist (round 1, MAJOR 2) found that a choice between mockups is not approval of a text, so the owner was then asked, on the session's Dutch summary of it (the ordering and the kind from `path` and `type`, written only by the import, an unreadable value leaving them empty without refusing the minimumdoel), whether to ratify it, and answered "Bekrachtigen"; asked separately, the owner chose to store the `soort` and show it in the minimumdoel detail, and, told that it would be the only change to the Leerplandoelen part, to make the minimumdoel in the leerplandoel detail clickable. *Not ruled by the owner:* the two measured sentences of IX.1 (nine of the ten leergebieden carry a discipline's name; *Attitudes* is worked out in three disciplines), which the implementer counted on KOV's data of 2026-09-14; IX.1's conclusion from them (the two registers are browsed in their own orderings and neither is read as the other), which follows the owner's choice for the minimumdoelen view only; and the clarifications of Art. VII.0, XII and XIV below. *What it does not change:* the concordance, dekking, Art. VII.0's taxonomy of the leerplandoelen, and the leerplandoelen register. **The leergebied of the decree is not the teachers' leergebied / Wereldoriëntatie**, and the open Art. XIV question about that grouping stays open. *Amended in step (Art. XI.1):* ADR-0032 decision 3 (a dated note); the `Minimumdoel` line of `CLAUDE.md`'s data model; and, on the same audit's MAJOR 1, the two meanings of *leergebied* kept apart in **Art. VII.0**, **Art. XII** (a split entry, with *rubriek*, *subrubriek* and the *soort* of a minimumdoel added) and **Art. XIV**, with the functional analysis in step (Bijlage A.2's rules and A.9's list of new terms). *Directie has not confirmed it.* |
| 2026-09-14 | Siebe De Saedeleir (projecteigenaar) | Added three defaults to **Art. VI.1** (FA A.11, ADR-0030 §2 and the E6 epic in step), the owner's answers to the three questions the E6-02 slice 3 audit raised when every route got its rights check. **I26**, deleting a thema: a themabeheer holder deletes a thema only when it holds no subthema, subdoel or activiteit other than what its own open wizard run created; any other thema only directie deletes. A thema placed in a jaarplan is deleted by nobody, as before. **I27**, the wizard and other people's work: the wizard's own write actions do not delete an activiteit that carries a goal link, or a subthema whose activiteiten carry one, unless the caller also holds the goal-link right at that leeftijd; and they do not change the leeftijd of a subthema the run created while it holds a subdoel or activiteit the run did not create. This narrows I25. **I28**, the wizard's 14 days: only the wizard's own write actions keep a run open (I24); editing the thema or its themadoelen through the ordinary routes does not. *Why:* the matrix of ADR-0030 §3 has no row for deleting a thema, and the delete cascades to subthema's, subdoelen and linked activiteiten that this article reserves to directie and the hoofdleerkrachten; and I25 let the wizard remove goal links a hoofdleerkracht had added since, or carry others' work to another leeftijd, which R19 and I13 withhold elsewhere. The antagonist judged both a contradiction of this article (slice 3 audit round 1, MAJOR A and B) and named the options; session `E6-02` put them to the owner, who chose the conservative answer each time. *Scope:* they are defaults like I1–I25: the build follows them (R37), and the owner can change each on its own. `CLAUDE.md` names no individual default and is unchanged. The owner approved this text. |
| 2026-09-14 | Siebe De Saedeleir (projecteigenaar) | Narrowed the defaults **I26** and **I27** of **Art. VI.1** (FA A.11 and ADR-0030 §2 in step): An activiteit of the run that carries a goal link counts, here and in I26, as one the run did not create, unless the caller also holds the goal-link right at its leeftijd. *Why:* the E6-02 slice 3 audit (round 2, Q4) found that both counted items rather than goal links, so a hoofdleerkracht's `Manueel` link on an activiteit the wizard created could still leave with a thema delete by themabeheer, or move with a wizard re-scope, while I27's own first half already closed that case for the wizard's delete (R19). Session `E6-02` put the two options to the owner, who chose that a goal link protects. *Scope:* still defaults, followed by the build (R37). `CLAUDE.md` names no individual default and is unchanged. The owner approved this text. |
| 2026-09-14 | Siebe De Saedeleir (projecteigenaar) | Clarified the default **I27** of **Art. VI.1** (FA A.11 and ADR-0030 §2 in step): When the wizard changes a subthema's leeftijd, that right is needed at both the old and the new leeftijd. *Why:* the Q4 narrowing said "at its leeftijd", which is ambiguous for a move that gives a linked activiteit two leeftijden: the link leaves the old leeftijd's dekking, which is an unlink there, and enters the new one's, which is a link there. The build already asked the right at both ends, as I13 does for the ordinary re-scope; the E6-02 slice 3 audit (round 3, Q5) asked for text and build to agree, and session `E6-02` put the two readings to the owner, who chose both leeftijden. *Scope:* still a default, followed by the build (R37). `CLAUDE.md` names no individual default and is unchanged. The owner approved this text. |
| 2026-09-15 | Siebe De Saedeleir (projecteigenaar) | Amended **Art. X.7** and **Art. XIII** so that the antagonist audits the diff of a finished story or ticket once, only CRITICAL and MAJOR findings block, and a re-audit checks only the blocking findings still open, in at most two rounds. Added **Art. XI.4** (the text states the rules in force) and moved this log out of Art. XI. Removed from the articles the dated amendment notes, the *why* and *surfaced by* paragraphs and the list of resolved Art. XIV decisions, keeping every rule in force in its ratified wording; dropped the retired `groepschat` from Art. II.6. Recorded as [ADR-0037](adr/0037-lichter-agentproces.md). *Why:* the owner found sessions stuck in long audit loops (E6-02 ran 23 rounds, with 61 MINOR findings against 7 MAJOR) and the governing documents too long to read in every session (this file was 99 KB, `CLAUDE.md` 45 KB). *How:* in session, the owner chose among offered options: MAJOR and up blocks with two rounds, Opus kept, history out with the rules kept literally. The text is ratified when the owner merges TB-021. *Amended in step (Art. XI.1):* `CLAUDE.md` and the antagonist, `jaarplan-build` and `ticket-uitvoeren` instructions. |
| 2026-09-15 | Siebe De Saedeleir (projecteigenaar) | Amended **Art. VI.1**: a gebruiker without a right reads no klas's planning; a new paragraph says who reads which klas's planning (a leerkracht the klassen of her own jaarfase, a hoofdleerkracht those of her appointed jaarfase, themabeheer and directie every klas); the default **I9** is replaced by **Z6** (the rule holds in every schooljaar). Amended **Art. VI.7** (the report's exception now points at that paragraph) and the **Art. XIV** *Teacher visibility* bullet (the owner's rule; directie's confirmation outstanding). Recorded as [ADR-0040](adr/0040-klassen-inkijken-per-jaarfase.md), superseding ADR-0030 default I9. *Why:* on 2026-09-15 the owner asked that a leerkracht see the agendas of klassen of her own klasfase and not of others, and ruled who else reads what (FB-013). *Scope:* Z1 to Z5 are rulings; Z6 (every schooljaar) and Z7 (the reader's own relation lapses with its schooljaar) are defaults. Default (e) no longer includes reading. The text is ratified when the owner merges FB-013. *Amended in step (Art. XI.1):* `CLAUDE.md` (the rights line) and the functional analysis (FR-10.2, A.11). |
| 2026-09-15 | Siebe De Saedeleir (projecteigenaar) | Added the **woordweb**, the brainstorm of the goal-first method's step 3, as personal content: one web of loose words per gebruiker and subthema, read by everyone, edited by its owner and directie, following her across schooljaren. Recorded as [ADR-0043](adr/0043-eigen-woordweb-per-subthema.md), which answers ADR-0030 §4 (a) for the woordweb only. *Amended:* **Art. IV.1** (who decides a woordweb word), **Art. IV.4** (an exception: the AI proposes woordweb words from its own language knowledge, while it is sent only school data), **Art. IV.5** (the woordweb's JSON shape), **Art. IV.8** (AI assist at step 3, only once the web holds a word of the teacher's own), **Art. VI.1** (the woordweb as personal content, and its defaults) and **Art. IX.2** (the `Woordweb` entity). *Why:* on 2026-09-15 the owner asked for the brainstorm step as a woordweb with AI additions (FB-036) and ruled W1 to W7 in session, approving the IV.4 and IV.8 changes explicitly. *Scope:* W1 to W7 are rulings; D1 to D7 are defaults (D7, who reads which words of a woordweb, and the wizard's refusal to delete a subthema that holds one, were added on the antagonist's round-1 findings). The text is ratified when the owner merges FB-036. *Amended in step (Art. XI.1):* `CLAUDE.md` (the data model and the AI flow), the functional analysis (A.7, A.11 and its version table), ADR-0030 §4 (a) and the ADR index. |
| 2026-09-16 | Siebe De Saedeleir (projecteigenaar) | Amended **Art. IX.2** and the **Art. XII** glossary so that a thema's **themadoelen are minimumdoelen** with **no maximum** (the "2–3" bound is gone; at least two stays advisory), bringing along their concorded leerplandoelen at every leeftijd, read and never stored; a leerplandoel themadoel is written only by the FR-1 import. Dropped "2–3" from **Art. IV.8** and from the `doelsuggesties` clause of Art. IX.2 in step. Recorded as [ADR-0046](adr/0046-themadoelen-zijn-minimumdoelen.md), which reverses the owner's FB-009 ruling of 2026-09-15 that a thema keeps no list of its own minimumdoelen. *Why:* a thema spans several leeftijden and a leerplandoel belongs to one, while the minimumdoel is the level that spans them and the one the inspectie tests; the owner also wants many goals on a thema ("max van aantal themadoelen moet weg"). The owner ruled in the same session that the existing leerplandoel themadoelen are deleted by a migration in every environment, and that dekking, the FR-1 import and the thema's AI stay as they are until their own tickets (FB-043). Directie has not been asked. *Amended in step (Art. XI.1):* the functional analysis, A.4 (the themadoel) and A.7 (step 2); `CLAUDE.md` states no count of themadoelen and needed no change. |
| 2026-09-16 | Siebe De Saedeleir (projecteigenaar) | Amended **Art. V.1** so that dekking is computed per klas in **two steps that are both shown**, the **dekkingsprognose** and the **dekking**: a minimumdoel counts only through a thema it is a themadoel of (placed in the klas's plan, for dekking), never through a concorded leerplandoel; a leerplandoel of the klas's jaar/fase through a subthema at its leeftijd (the subthema placed in the agenda, for dekking), an accepted doelsuggestie or a planned algemene fiche; a themadoel that links a leerplandoel and the leerplandoelen a minimumdoel brings along do not count; a klas is measured against the minimumdoelen of its mijlpaal. Amended **Art. V.2**, **Art. IX.3** (the dekking is no longer binary) and the **Art. XII** glossary (concordantie, dekkingsprognose). Recorded as [ADR-0047](adr/0047-dekkingsprognose-en-dekking.md). *Why:* the owner described two steps, what the thema's and subthema's aim at and what the agenda holds, because a school does not plan the whole year at once (FB-045). The mijlpaal mapping and the counting of activiteit doelen and doelsuggesties are defaults the owner may still change. Directie has not been asked. *Amended in step (Art. XI.1):* `CLAUDE.md` (the dekking rule under *Domain model*) and the functional analysis (FR-2.3, FR-9.1, FR-9.3). |
| 2026-09-16 | Siebe De Saedeleir (projecteigenaar) | Amended **Art. VI.3** and **Art. VIII** so that a deployment may pick the **Anthropic Claude API** instead of Azure AI Foundry (`Ai:Provider`), and such a deployment is **not held to an EU processing location**. Recorded as [ADR-0048](adr/0048-claude-api-als-tweede-ai-provider.md) (TB-041), which supersedes [ADR-0016](adr/0016-azure-hosting-eu-residency.md) in part; CLAUDE.md and the functional analysis follow. *Why:* Azure AI Foundry was not available to the owner, so every AI feature failed; the owner asked for a Claude API client on an endpoint he configures and ruled that the EU requirement does not apply to it. *The cost, ratified with it:* on such a deployment the prompts leave the EU, including a rapporttekst rewrite's pseudonymised text about a child; the processing register (Art. VI.6) and the DPIA of question 15 should name Anthropic. ADR-0035 §3.5's "EU only" is superseded in part in step. Directie has not been asked. |
| 2026-09-16 | Siebe De Saedeleir (projecteigenaar) | Amended **Art. IX.2** so that an activiteit's `activiteitType` is **optional**: an activiteit without a chosen soort has none, and no layer fills in a default (the form and the koppelen row start empty, the API stores an absent soort as none). The FR-1 import still requires a soort. The functional analysis follows in A.4 (*Rijke thema-attributen*). Why: the form preselected *Experiment*, so teachers saved a soort nobody chose, and a request without the field silently became *Experiment* too (FB-050). |
| 2026-09-16 | Siebe De Saedeleir (projecteigenaar) | Added the **own activiteit** as personal content, part 2 of the ADR-0030 §5 amendment for activiteiten. A new activiteit a leerkracht creates is hers and follows her across schooljaren; her jaarfase colleagues read it and may use it as an own copy; she links its goals, which count for a klas once the activiteit is planned in that klas's agenda. Recorded as [ADR-0049](adr/0049-eigen-activiteit-van-de-leerkracht.md), which answers ADR-0030 §4 (a) and I6 for activiteiten; personal subdoelen stay with E6-10. *Amended:* **Art. V.1** (an own activiteit's goals count through its own placement, never through its subthema), **Art. VI.1** (a leerkracht creates own activiteiten; creating a shared one and linking goals to one stay with directie and the hoofdleerkrachten; the own activiteit and its defaults), **Art. IX.2** (the `Activiteit` line gains its `eigenaar`) and the **Art. XII** glossary (eigen activiteit). *Why:* on 2026-09-15 the owner ruled E1 to E4 for FB-015 and FB-025, and on 2026-09-16 chose to build FB-015 first. *Scope:* E1 to E4 are rulings; D1 to D9 are defaults of the build. The text is ratified when the owner merges FB-015. Directie has not been asked. *Amended in step (Art. XI.1):* `CLAUDE.md` (the data model), the functional analysis (A.11), ADR-0030 §4 (a) and the ADR index. |
| 2026-09-16 | Siebe De Saedeleir (projecteigenaar) | Added the **subdoelplaatsing**: on the thema page the AI proposes where an open leerplandoel of the thema's themadoelen goes, into an existing subthema of its leeftijd or into a new one. Recorded as [ADR-0050](adr/0050-ai-plaatst-leerplandoelen-in-subthemas.md). *Amended:* **Art. IV.1** (a hoofdleerkracht of the jaarfase or directie decides a subdoelplaatsing), **Art. IV.4** (a second exception: the AI may make up the name and onderzoeksvraag of a thema or subthema it proposes, while every goal it places is a loaded goal it was sent), **Art. IV.5** (the JSON shape), **Art. IV.8** (AI assist at steps 4 to 6 on the thema page), **Art. VI.1** and the **Art. XII** glossary (the hoofdleerkracht asks for and decides them) and **Art. IX.2** (`Subdoelvoorstel` and `Subthemavoorstel`). *Why:* the owner asked for AI proposals that place the leerplandoelen a minimumdoel brings along in existing or new subthema's (FB-057), ruled P1 to P6 in session, and had ruled the Art. IV.4 change for new thema's and subthema's on FB-054 the same day. *Scope:* D1 to D7 are defaults. Directie has not been asked. The text is ratified when the owner merges FB-057. *Amended in step (Art. XI.1):* `CLAUDE.md` (the AI flow and the domain model) and the functional analysis (A.7 and its version table). |
| 2026-09-16 | Siebe De Saedeleir (projecteigenaar) | Amended **Art. IX.2** so that a thema's `doelsuggesties[]` are the AI's proposals of a **minimumdoel** as themadoel, for the mijlpalen the thema's leeftijden meet: accepting one makes it a themadoel, and a rejected one is not proposed again. Amended **Art. V.1** so that an accepted doelsuggestie no longer puts a leerplandoel in the dekkingsprognose or the dekking and counts only through the themadoel it made; the `DoelKoppeling` line of Art. IX.2 no longer names doelsuggesties. Recorded as [ADR-0052](adr/0052-doelsuggesties-zijn-minimumdoelen.md), which supersedes ADR-0047 S2. *Why:* since ADR-0046 a themadoel is a minimumdoel, while the AI at thema level still proposed leerplandoelen that never became one (FB-053). The owner ruled that the existing leerplandoel doelsuggesties, open and accepted, are deleted in every environment. Directie has not been asked. *Amended in step (Art. XI.1):* `CLAUDE.md` (the dekking rule under *Domain model*) and the functional analysis (FR-4.1). |
| 2026-09-16 | Siebe De Saedeleir (projecteigenaar) | Amended **Art. IV.2**, **Art. VI.1** (themabeheer) and the **Art. XII** *Themabeheer* entry so that a thema's doelsuggestie, a proposal of a minimumdoel, is accepted or rejected and never adjusted: another minimumdoel is linked by hand. Amended **Art. IX.2** so that the proposals are shown best fit first, the candidates follow the leeftijden the person chooses (by default the subthema's), and a minimumdoel accepted and later unlinked may be proposed again, while a current themadoel and an open or rejected proposal may not. Recorded in [ADR-0052](adr/0052-doelsuggesties-zijn-minimumdoelen.md) (O6 to O8). *Why:* the owner answered the questions FB-053 left open. Directie has not been asked. `CLAUDE.md` needed no change. |
| 2026-09-16 | Siebe De Saedeleir (projecteigenaar) | Amended **Art. IX.3** so that a jaarplan holds thema placements **with their own first and last day** instead of thema's per planningsblok: the teacher picks the first day, the tool proposes the last from the thema's duration in lesweken, no two placements share a day, a vacation splits a thema into parts, nothing moves when the vacations change (the directie rule of 2026-07-28, kept), and the planning has no planningsblokken. Rejecting a proposal removes it, a vast moment blocks no placement, and the generation is switched off until it is reworked for dates. Amended **Art. IV.2** with the exception that rejecting a proposed thema placement removes it, so no `geweigerd` placement is stored (added on the FB-035 antagonist's round-1 finding, which found IX.3 amended and IV.2 left contradicting it). Amended the **Art. XII** glossary in step (themaperiode, schooldag and lesweek, startthema, vast moment, jaarplan) and the settled-decisions note under Art. XIV. Recorded as [ADR-0053](adr/0053-themaplaatsing-met-eigen-datums.md), which supersedes ADR-0013 and ADR-0020 for the jaarplan. *Why:* the owner asked how to plan a thema of 5 weeks in a period of 6 with the next thema starting in the free week (FB-035), and, refining the ticket, ruled that thema's are not planned in fixed periods at all. *How:* in session, through rounds of multiple-choice questions and a mockup of three plan screens, of which the owner chose the week timeline; the rulings are R1 to R12 in ADR-0053. *This replaces the two-tier block model directie settled on 2026-07-14*; the owner ruled on that himself, and directie has not been asked. *Amended in step (Art. XI.1):* `CLAUDE.md` (the domain model lines on placements and regeneration) and the functional analysis (A.6 and its version table). |
| 2026-09-17 | Siebe De Saedeleir (projecteigenaar) | Added the AI's **doelsuggesties for an activiteit**, recorded as [ADR-0054](adr/0054-ai-zoekt-doelen-bij-een-activiteit.md). *Amended:* **Art. IV.1** (whoever may link an activiteit's goals decides its proposals), **Art. IX.2** (a `Subdoelvoorstel` may also come from a goal accepted on an activiteit) and **Art. VI.1** (only an `aanvaard` or `manueel` link counts as linked for a maker's delete, R25). Why: FB-026 creates the first activiteit links that are not `manueel`, and the owner ruled that a proposal or a rejected goal must not stop the maker from deleting her activiteit. |
| 2026-09-17 | Siebe De Saedeleir (projecteigenaar) | Switched the **AI jaarplan generation** back on for placements with dates, recorded as [ADR-0055](adr/0055-ai-jaarplan-met-datums.md). *Amended:* **Art. IV.5** (plan generation requests thema's with a start week and a motivation), **Art. II.1** (a live example replaces `Planningsblok`), **Art. I.1** items 4 and 6 (a year plan is generated on the free days; regenerating covers the whole plan, no single period), **Art. IX.3** (a generation proposes thema's with a start week and the calendar sets their days; the `Generatieparameters` entity is removed) and **Art. XII** (the *startthema*, *vast moment* and *bewaarde generatieparameters* entries are removed). *Why:* the owner ruled in TB-053 that the model picks thema's and a start week while the server places them on free days, that regenerating one period does not come back (FR-8.2), and that the kept startthema's and vaste momenten, which had no screen since ADR-0053, are removed (FR-5.4). |
| 2026-09-17 | Siebe De Saedeleir (projecteigenaar) | Added the **activiteitvoorstel**: under a subthema the AI proposes activiteiten that work on its subdoelen, to the leerkracht who asked, and an accepted one becomes her own activiteit. Recorded as [ADR-0056](adr/0056-ai-stelt-activiteiten-voor.md). *Amended:* **Art. IV.1** (the asker, or directie, decides an activiteitvoorstel), **Art. IV.4** (a third exception: the AI may make up an activiteit's name, soort, expected outcomes and length, while every goal it links is a subdoel of the subthema and it writes no lesson material), **Art. IV.5** (the JSON shape), **Art. IV.8** (AI assist at step 7, the rijk aanbod), **Art. VI.1** (the activiteitvoorstel as personal content that directie sees and decides, and its defaults), **Art. IX.2** (`Activiteitvoorstel`) and the **Art. XII** glossary. *Why:* the owner asked for AI-proposed activiteiten (FB-025), ruled on 2026-09-15 that an accepted one is the asker's own activiteit, on 2026-09-17 allowed the constitution to be amended so that the AI may think them up, and the same day ruled that directie sees and decides every leerkracht's proposals. *Scope:* D1 to D9 are defaults. Directie has not been asked. The text is ratified when the owner merges FB-025. *Amended in step (Art. XI.1):* `CLAUDE.md` (the AI flow and the domain model) and the functional analysis (A.7 and its version table). |
| 2026-09-18 | Siebe De Saedeleir (projecteigenaar) | Added **the cat**, a proactive AI agent for the kleuterleerkracht, and the **vervanging**. Recorded as [ADR-0057](adr/0057-vervanging-briefing-en-klasfiche.md) (vervanging, briefing, terugkeerbriefing, klasfiche), [ADR-0058](adr/0058-lesvoorbereiding-per-plaatsing.md) (lesvoorbereiding, and Art. IV.4 as one rule), [ADR-0059](adr/0059-de-kat-proactieve-agent.md) (the cat, its signals, background job and chat) and [ADR-0060](adr/0060-activiteitvoorstellen-op-een-aanbod-gat.md) (activiteitvoorstellen on an aanbod-gat, superseding ADR-0056 D6 in part). *Amended:* **Art. I.1** (item 10, the cat), **Art. I.2** (automatic generation of lesson material is no longer a non-goal), **Art. IV.1** (who decides a lesvoorbereiding and the cat's activiteitvoorstel; a proposal brought unasked is decided like one asked for; a chat answer decides and changes nothing), **Art. IV.2** (a chat answer carries no status and is not kept), **Art. IV.3** (a chat answer names what it rests on instead of a motivation), **Art. IV.4** (rewritten as one rule: the AI is sent only school data and loaded goals, may make up content from its own knowledge, and every goal it names or links is a loaded goal it was sent; the three exceptions fold into it, and the rewrite keeps its narrower rule), **Art. IV.5** (the JSON of a lesvoorbereiding, the cat's activiteitvoorstel and a chat turn), **Art. IV.8** (the cat is the one place where the AI acts unasked, and on an aanbod-gat it goes past the goal-first order for one klas), **Art. VI.1** (a vervanging gives a vervanger read-only access to one klas; who asks for, decides and reads a lesvoorbereiding and a klasfiche; the cat's proposals go to the klas's leerkrachten; the reading rule and the rule for a gebruiker without a right name the vervanger; new defaults), **Art. VI.2** (a klasfiche and the chat say that nothing about a child belongs there; no chat is kept), **Art. VIII** (the cat's background job, one instance at a time), **Art. IX.2** (`Klasfiche`, and the cat's `Activiteitvoorstel`), **Art. IX.3** (`Lesvoorbereiding`, `Vervanging`, `Signaal`) and the **Art. XII** glossary. *Why:* in a sparring session on 2026-09-18 the owner shaped an agentic extension, personified as a fat orange striped cat, changed his mind on the non-goal of lesson material and on the AI never acting unasked, and chose phase 1 (TB-056, TB-057, FB-063 to FB-071, FB-031, FB-032). In TB-056 he ruled that a vervanger only reads and that the klasleerkracht corrects the agenda afterwards. *Scope:* the rulings are V1 to V8, L1 to L6, K1 to K7 and G1 to G6; every D-item is a default of the build. The pupil-data rules (Art. VI.2 first sentence, VI.7) are unchanged. Directie has not been asked. *Amended in step (Art. XI.1):* `CLAUDE.md` (the AI flow, the domain model and the advisory rule), the functional analysis (§2.3, FR-10.2, FR-14, NFR-6, §7, A.11 and its version table) and the ADR index; ADR-0056 is marked as superseded in part. |
