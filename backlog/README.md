# Jaarplanner — Backlog & Planning

This is the working backlog for the Jaarplanner build. It is **derived from** and **subordinate to**:
1. [`CONSTITUTION.md`](../CONSTITUTION.md) — binding principles (wins on any conflict).
2. [`docs/Functionele_Analyse_Jaarplanner.md`](../docs/Functionele_Analyse_Jaarplanner.md) — scope (FR/NFR).
3. [`docs/Gap-analyse_Opstap_referentie.md`](../docs/Gap-analyse_Opstap_referentie.md) — ratified refinements.
4. [`docs/adr/`](../docs/adr/README.md) — architecture decisions; [`docs/ux/ui-ux-approach.md`](../docs/ux/ui-ux-approach.md) — UI/UX approach.

> If a story here ever contradicts the constitution, the **constitution wins** — fix the story. When scope is clarified or a decision is made, update the relevant epic *and* the source documents.

## How to use this backlog

- Each **epic** is one `.md` file, aligned to the build order in [`CONSTITUTION.md` §9.3 / Art. VIII](../CONSTITUTION.md).
- Each epic holds **stories** with a stable id (`E<epic>-<nn>`), a checkbox status, acceptance criteria, and references to the FR + Constitution article it satisfies.
- **Update the checkbox when status changes** and keep the progress table below in sync. The Antagonist audits significant changes against the constitution — keep stories honest.

### Status legend
- `[ ]` **Todo** — not started
- `[~]` **In progress**
- `[x]` **Done** — implemented, tested, Antagonist-clean
- `[!]` **Blocked** — waiting on an Open Decision (Art. XIV) or another story

## Epics & progress

| Epic | File | Phase | Stories | Done | Status |
| --- | --- | --- | --- | --- | --- |
| E0 — Project foundation & scaffolding | [E0-foundation.md](E0-foundation.md) | 0 (pre) | 10 | 10 | ✅ Complete again 2026-07-30 — **E0-10 closed**: the app shell no story owned ([ADR-0021](../docs/adr/0021-frontend-routing-and-url-selection.md) recorded first). Gates: 62 frontend tests / 7 files, lint + build clean, whole journey clicked through against live Postgres; antagonist returned VIOLATIONS FOUND and the three blocking findings were fixed. Contrast (SC 1.4.11) and three other deferrals routed to the stories that own them |
| E1 — Curriculum & content fundament | [E1-curriculum-content.md](E1-curriculum-content.md) | 1 | 20 | 14 | ✅ **E1-14 closed 2026-08-04: both landings of the beheer-UI are built** (the schoolbrede laag plus the class-scoped writes), **not merged and not pushed**; **two antagonist rondes, 7 MAJOR between them, all fixed.** Round 1 found reachable **data loss** (switching thema with the edit form open wrote one thema over another) and filed **E1-18**, which the owner ruled the same day (*a school imports decided links*), so the import now writes `Manueel` and imported content counts for dekking. Round 2 found two open forms colliding on DOM ids, raw-GUID server messages at the two new levels, an SC 2.5.3 breach, and that E1-18 had made **three live strings in E1-13's import screen untrue** in front of an irreversible delete. **Closed on the owner's ruling with the round-4 fix round un-re-audited, which the story entry states outright**; FR-3.1's *leerkrachten kunnen* finally has a surface. E1-03/04 blocked on E1-12 (source file from directie); **E1-13 done 2026-08-03** — the import screens exist, so FR-1.1–1.5's display halves and FR-2.5's review notice are satisfied for the first time. Four gate rounds and three fix rounds: round 1 was `FAIL` + 4 MAJOR, and the finding the code-reader graded MINOR was the one that **destroyed two `Aanvaard` links** in a browser; round 4 closed with no MAJOR and a `PASS`. Two things outlived the story: a **model-wide EF key defect** (five child collections could not grow an existing aggregate, so every import after the first answered 500 — invisible because all 13 committing tests ran in-memory), and the round-4 auditor **withdrawing its own round-3 fix** after the implementer showed it was wrong one level down. **E1-14 now owns four items handed over by these gates.** **E1-15 done 2026-07-31** — the Op.stap import is reachable at last (antagonist COMPLIANT, test-runner PASS on the merged tree), but **FR-2.1 is still not satisfied**: a real per-discipline file mixes MD and G rows, so it answers 409 until E1-12 lands. The trigger is built; the import of real data waits on directie. **E1-16 done 2026-07-31** — the Doelen-register (read API + screen) closes the dead nav item E0-10 shipped. Three audit rounds (3 MAJOR + 7 MINOR, then 6 MINOR + 1 QUESTION, then 5 MINOR) and two test-runner passes, the last a real browser pass driving Edge because the Playwright server was down all story; the third audit confirmed **no earlier finding was falsely reported as fixed** and reproduced the mutation checks itself. **E1-17** is new, and it is not this story's: comparing E1-16's link read with E1-15's reference count exposed a layer E1-15 misses |
| E2 — AI-matching thema ↔ doel | [E2-ai-matching.md](E2-ai-matching.md) | 2 | 9 | 8 | ✅ **M2 restored 2026-07-30** — E2-08 landed the FR-4.1 trigger; a teacher can generate suggestions from `/themas`, verified in a browser. **E2-09** (a missing AI config should be 503, not 500) is filed, not blocking |
| E3 — Jaarplan-generatie & kalender | [E3-jaarplan-kalender.md](E3-jaarplan-kalender.md) | 3 | 10 | 9 | 🚧 **E3-09 closed 2026-08-05 by owner decision** — knelpunt-signalering (FR-6.4). The te-vol rule finally has **one** implementation: `BlokspreidingWeergave.IsOverbelast` was already it, but it only reached the *generation* response while the board renders from the jaarplan read, so the kalender carried a provisional count of 3 that contradicted the server for months. That count is deleted and the figures now ride on the plain read. **Two owner rulings**, the second caused by implementing the first: the third signal is a **count plus a route to `/dekking`** rather than the tray the approved wireframe drew (E5-02 already lists those rows, and E5-03/E5-05 own the presentation); and the period **heading** rounds to whole weeks too, because the 2026-07-31 rounding ruling legislated the te-vol sentence and never looked one line up at a heading that had shown one decimal since E3-06 — E3-09 would have printed *"5,7 weken"* directly above *"in 6 weken"*. Gates: **571 unit + 191 integration on real PostgreSQL (0 skipped) + 460 frontend**, format/lint/build clean, driven in a real browser at 1440px and 390px (worst composited contrast **5,73:1**, coverage line **14,55:1**, no decimal week label left anywhere, and the fine tier proven to summarise te vol instead of marking sub-columns). It also fixed **two things not on its list**: the spine flagged te vol with `!` while the column used `▲`, and *"1 weken"* became reachable the moment the figure stopped being a decimal — a plural defect `catalogus.test.ts` **could not** see, because that guard fires on `{aantal}` and the key interpolated `{weken}`. **Two antagonist rounds have run, both VIOLATIONS FOUND, all findings fixed or routed; round 3 is owed and the `[~]` says so.** Round 1 caught a **user-visible falsehood** (the gap sentence claimed the goals appeared in no thema, while the number is *not covered* and a freshly generated plan is entirely `Voorgesteld`), and round 2 caught that all three **Storybook stories crashed** on a `TypeError` because the decorator answered the new `/dekking` request with the *Jaarplan* fixture. E3-01 + E3-02 + E3-05 + E3-10 + **E3-04** + **E3-08** done. **E3-08 closed 2026-08-03** after four build rounds and five gate rounds (final antagonist COMPLIANT, test-runner PASS 9/9 on the merged tree): the kalender zooms between the two ratified tiers, one /rooster drives spine and board, and the fine tier withholds the move affordance in visible text because the server accepts only themaperiode starts. It also retired the provisional te-vol threshold by getting it ruled on, amended ADR-0014/0021 on where the view zoom lives, and surfaced **E7-17** (pnpm lint type-checks zero files). **E3-07 closed again `[x]` 2026-08-04** after the verification its entry said was owed, which was owed because E3-08's three measurements all predated **E4-02** and E4-02 then rewrote the copy on these exact cards. The reopened symptom **does not reproduce** (browser, real PostgreSQL, state reached by pressing *Weigeren* as `beslisVervallen` recommends), and the verification found and fixed **a new instance of the same class**: *"Daarna kan je het thema een **andere** themaperiode geven"* rendered directly above *"dit thema staat in geen enkele periode"*. Five antagonist rounds, final **COMPLIANT**. **Round 1 found 3 MAJOR and none of them were in the code; round 2 found that two reported fixes were not in the tree at all** (a `git checkout` reverting a mutation had discarded them while uncommitted, and the worklog asserted a repair that did not exist); rounds 3 and 4 each defeated the guard against the copy defect with a different reword. The product string had been correct since the first commit. *Left measured but unfixed, and it is E5-02's* (`E5-dekking-export.md:36-37`, owner ruling 2026-08-03): the *Te herzien* notice calls the dekking onbetrouwbaar for a **rejected** stale card while the API answers `isBetrouwbaar=true`, right for `Voorgesteld` and `Manueel` and wrong for `Geweigerd` only. **What it was reopened for, 2026-08-03 (owner ruling)**, on a defect E4-06's gates found in its own copy: on a stale **rejected** card the panel says *"Kies hieronder een periode voor dit thema, of versleep de kaart"* and one paragraph later *"Dit thema is geweigerd, dus je kan het niet verplaatsen"*, with no picker rendered and no drag grip. Two sentences that contradict each other plus an instruction pointing at nothing (the E3-06 rule). Reproduced three times independently. One card state, not a rebuild. *What it did deliver, 2026-07-30, and that part stands:* drag-and-drop plus a non-drag period picker (WCAG 2.2 SC 2.5.7), the removal + confirmation + un-reject affordances three earlier audits pinned on it, and inline re-placement for stale placements. Antagonist returned VIOLATIONS FOUND; all 12 findings addressed, including one that would have let a drag silently turn a rejected thema into covered dekking. **E3-04 `[~]`** — the persistence half is **built and merged 2026-07-31 (PR #18)**: settings kept per (klas, schooljaar), the form loads them, and the generation path reads them so E4 inherits the behaviour. Three fix rounds, two independent gates, **480 unit + 113 integration + 129 frontend, 0 skipped**. It carries a fix that was **not its own**: adding a `Themaplaatsing` to an already-persisted `Jaarplan` threw `DbUpdateConcurrencyException`, so `main` was 500ing on the ordinary FR-8 second-press case — verified independently with a probe against the real model and EF's `IMigrationsModelDiffer`. **Closed 2026-07-31 after the browser pass finally ran.** Both gate agents had died on an org spend limit and Playwright had disconnected, so it was run directly with **headless Chrome driven from Bash** against a live API and database: the retry button's boundary **measures 5,497:1** (its fill contributes 1,178:1, so the border really is the only delineation), the failure notice and the disabled generate button are correct **with the panel shut**, the in-flight retry keeps its notice and reads *"even geduld"*, no overflow at 390px, and the class switch was driven by hand with a second klas — A's pending edit dies with the class and B's own kept setting is what the screen reports. **E3-06 `[~]`** — still owes the directie/teacher session. **E3-03 is no longer blocked** (E5-01 closed 2026-08-03; the story now carries two warnings about what "high coverage" can honestly mean, since a freshly generated plan is all `voorgesteld` and therefore reports 0 by design). **The teacher review is postponed to near the end of the build (owner, 2026-07-30)**, which is what decoupled E3-07 from E3-06; next is **E4**. |
| E4 — Manuele bewerking & (her)generatie | [E4-bewerking-hergeneratie.md](E4-bewerking-hergeneratie.md) | 4 | 8 | 6 | 🚧 **E4-04 done 2026-08-06 — FR-8.1 was reachable since E3-01 and misdescribed itself the whole time.** `GenereerAsync` has always discarded exactly the untouched proposals and kept every decided or locked one; what did not exist was any statement of that **before** the press. The button read *"Jaarplan genereren…"* on the second press as on the first, and the only word about the replacement was past tense. **Seventh reachable-vs-tested instance**, and the mildest: the path worked, it lied about itself. **Two antagonist rondes, both VIOLATIONS FOUND, and both found their MAJOR in the previous round's fix, in the same paragraph, pointing opposite ways** — first the copy promised replacement where the code guarantees only deletion, then the fix denied survival to a **locked** proposal, which `vergrendelUitlegVrij` invites the teacher to create. Fixed by writing what is lost as the **complement** of what is kept, since two lists can drift and a complement cannot. `[x]` on an owner ruling without a third round, stated on the story. **E4-01 done 2026-08-04, `[x]` on an owner ruling of 2026-08-05 without a third audit round, and the story entry says so.** What was missing was **not** on the server: dekking is computed on every read, so nothing behind the API can go stale. It was in the browser, and no test that looks at the kalender could see it. `/dekking` is a **different route**, so while a teacher edits, its query has no observer and TanStack keeps the previous answer; navigation is client-side, so that pre-edit figure is what the screen paints on arrival, with no loading state to say so. Fixed by dropping the class's cached dekking from the shared placement-mutation hook, `removeQueries` rather than `invalidateQueries`, which is the trade-off `DekkingPagina` already makes on a scope switch. Measured in a browser against real PostgreSQL, navigating through the nav rather than reloading: accept **0 → 2 of 14**, move **→ 4 of 14**, remove **→ 2 of 14**, each showing the loading line and never the old figure (the dekking read was deliberately slowed by three seconds in the page, because the stale window is otherwise too short to look at). It also pins the sequence nobody had pinned: five HTTP write-then-read pairs on real PostgreSQL, of which the **stale** case closes a half **E5-01** left unverified, that resolving a stale placement releases the figure it was withholding. **E4-08 filed 2026-08-04** — FR-7.2's ninth verb×object cell, the only one still unbuilt: **an activiteit cannot be moved between subthema's**, so the sole route is delete-and-retype, which loses its `hoek`, its `verwachteUitkomsten` and every hand-made `DoelKoppeling` on it. Filed on an owner ruling after the E4-03 antagonist found that the gap had been routed to **E1-14, which is closed**, and was therefore owned by nothing. **E4-03 done 2026-08-04** — FR-7.2's one genuinely missing verb: **a thema could not be put into a period by hand at all**. `Jaarplan.VoegPlaatsingToe` had exactly two callers, plan generation and the demo seeder, so the only route into a jaarplan was an AI run and *"a fully hand-built plan is possible"* was impossible as written. Eight of FR-7.2's nine verb×object cells were already built (E3-07/E4-02/E4-06 plus E1-14's activiteit and doelkoppeling halves), which is why the code delta is small and the measurement was the work. **Sixth reachable-vs-tested instance**, and the sharpest variant yet: the domain method existed and enforced its invariant correctly, so nothing was missing where a unit test looks. Verified in a browser against real PostgreSQL by watching **dekking go 0 → 2 of 14 with the placed thema named as the evidence**, no AI call and no accept step, which also settles **E4-01's criterion for an *add*** (E4-01 still owns the *move*). Gates: 542 unit + 180 integration (0 skipped), 388 frontend, format/lint/build clean, **axe 0 violations in a real browser**, contrast composited and measured. **The defect worth copying:** focus fell to `<body>` on closing the picker because `setOpen(false)` is batched and the ref was still null, while the comment beside it claimed the opposite. 388 green tests could not see it; one browser probe could. **E4-06 done 2026-08-03** on `feature/e4-bewerking-hergeneratie` (`6755c49`): the lock had a server, an endpoint and a badge, and **no switch** — `api.ts` never called `PUT …/plaatsingen/{id}/vergrendeling`, so *"Vast / Blijft staan bij hergenereren"* was a state no teacher could produce. Fourth instance of the reachable-vs-tested gap (E2-08, E1-15, E0-10). Three fix rounds, three audits, two independent verifications; final gates **PASS + COMPLIANT** on the landing commit with 496 unit + 154 integration + 205 frontend, 0 skipped, and all sixteen `(status, vergrendeld, isVervallen)` states read out of a real browser. **The code half was small and almost all the value was in the copy:** the panel claimed the lock was why a thema survived when it was not, told a teacher who had **rejected** a thema that they had decided to keep it, offered locking as a remedy on a stale card (which would have made the *dekking onbetrouwbaar* state survive every run, where it used to heal itself), and nudged teachers to lock proposals that contribute **nothing** to dekking on a screen with no accept control. None of that was a code defect and every one of them passed every test. **Four owner rulings** are recorded on the story, two of which leave standing obligations: E4-01/E4-02 must build the accept affordance, and E4-05/E4-07 must re-verify six strings when the preserve/overwrite rule lands. **E4-01's coverage half is no longer blocked** (E5-01 closed 2026-08-03) and is largely already satisfied: dekking is recomputed on every read with no cache, which is what "reflected without a manual save" asks for, so what remains is proving it end to end and the screen half that waits on E5-02. |
| E5 — Dekking & export | [E5-dekking-export.md](E5-dekking-export.md) | 5 | 9 | 2 | 🚧 **E5-02 done 2026-08-04 — `/dekking` is a real screen, so FR-9.1 has a user at last.** Every in-scope leerplandoel as gedekt / niet gedekt with the covering thema as evidence, and a summary that **withholds its figure** while a stale placement is unresolved, which is the directie ruling of 2026-07-28 clause 4 finally honoured (E5-01 could only record it as unimplementable). **Three owner rulings landed with it:** E5-09's wireframes-first gate is postponed like E3-06's; a class is measured against **its own jaar/fase** rather than the whole curriculum, which settles the single-leerjaar half of the Art. XIV denominator question; and a **kleutergroep chooses its kleuterjaar**, because `Leerjaar = 0` cannot say which and a derde kleuterklas was reading about a third of the figure it has. The **graadklas** half stays open, same root. Gates: test-runner PASS on all nine claims, antagonist **two rounds** both VIOLATIONS FOUND with all 20 findings addressed, 562 unit + 187 integration + 421 frontend, 0 skipped, twelve mutation checks. **Neither audit round found anything in the product's behaviour** — every MAJOR was in the record around it, twice in prose a previous fix round had written, and round 2's sharpest finding was round 1's MAJOR-1 reproduced by the commit that fixed MAJOR-1. **M5 is not reached:** no percentage (E5-03), no gap-analyse (E5-05), no export (E5-06) and **no minimumdoel level** (E5-04, blocked on E1-12), which is the level the onderwijsinspectie tests. The screen says that last one out loud. **E5-01 done 2026-08-03** — the coverage computation is built and reachable (`GET /api/klassen/{klasId}/dekking`), **513 unit + 165 integration** passing against real PostgreSQL. **Its `[x]` carries a caveat the others do not: the owner ruled it green without a re-audit of the fix round**, so the four MAJOR fixes were verified by tests and by their own author, not by an independent pass. The story entry states that plainly. Two results matter beyond the story: an **owner ruling** settled which of the four `DoelKoppeling` layers count for dekking (all four, with the two class-scoped ones filtered to the klas), after the codebase was found holding **three** conflicting answers — the gap list 4, `OpstapImportService` 3, the kalender card 2; and the mandated Postgres test **caught a live defect on its first run** (a four-way `Concat` EF cannot translate, which the in-memory provider had passed). **E3-07's clause 4 is now implemented**: dekking refuses to report a figure while any placement is stale. **Art. V.2's inspection level is still unreached** — E5-04 stays blocked on E1-12, so M5 is far off. **Antagonist round 1 returned VIOLATIONS FOUND (4 MAJOR, 6 MINOR, 1 QUESTION), all addressed**; it also surfaced a **new Art. XIV open decision** (which leerplandoelen a class is measured against) that gates whether E5-03's percentage means anything |
| E6 — Beheer, rollen & samenwerking | [E6-beheer-rollen-samenwerking.md](E6-beheer-rollen-samenwerking.md) | 6 | 9 | 0 | Todo |
| E7 — Niet-functioneel & overkoepelend | [E7-niet-functioneel.md](E7-niet-functioneel.md) | cross-cutting | 20 | 0 | ⚠️ E7-11 is a deployment gate; **E7-12** `[~]` — advisory cleared, the two CI clauses that stop it recurring are still open; **E7-13** owns an Art. VIII layering leak (`ISchoolcontentParser` port sits in Infrastructure); **E7-14** owns a Postgres teardown fault that leaks a database ~1 run in 5 and reports as an unrelated test failure; **E7-10** — the app-wide `--input` miss it carried is **fixed** (E3-04's UI half re-tokened it to `40 14% 52%` = 3,40:1 on card / 3,21:1 on paper, re-measured in a browser), so what remains on this story is the `secondary` button variant: **no border at all** and a fill measuring 1,16:1 against the white card. It needs a fill or a boundary, not a token; **E7-15** is the naming/casing + comment-layer sweep, **deliberately scheduled last** so its renames cannot collide with in-flight story branches; **E7-16** is new 2026-08-03 and owns the class behind **six** separately-fixed defects: a write path verified only on the in-memory provider is not verified |
| E8 — Fast-follow (post-MVP) | [E8-fast-follow.md](E8-fast-follow.md) | post-MVP | 7 | 0 | Todo |
| **Totaal** | | | **102** | **49** | **48%** |

> **Resolved on 2026-08-06 by recounting, and it is the fifth time this table has produced the same conflict.** `origin/main` arrived with **E4 = 5** (E4-08 merged as PR #32) and a stale **E3 = 8**; this branch had **E3 = 9** (E3-03 closed) and a stale **E4 = 4**. Each side held one correct row and one wrong one, exactly as the notes below predict, so it was resolved **per row** and the total then recounted from the checkboxes rather than from either side's arithmetic.
> *One correction to the counting command itself, because it has been quietly wrong in these notes:* `grep -cE '^- \[x\] \*\*E[0-9]-[0-9]{2}'` is right for the **done** column, but the matching denominator command has to accept **every** marker, including `[!]` for blocked (E5-07 is the one that exposed it). Use `grep -cE '^- \[.\] \*\*E[0-9]-[0-9]{2}' backlog/E*.md`, which gives `10+20+9+10+8+9+9+20+7 = 102`; a `[x~ ]` character class silently reports 98. Done is `10+14+8+9+5+2+0+0+0 = 48`, so `48/102 = 47%`.

> **Resolved on 2026-08-05 by recounting, and this merge is the textbook case the two notes below predict.** `origin/main` arrived with **E3 = 8** (E3-09 closed) and a stale **E4 = 4**; this branch had **E4 = 5** (E4-08 closed) and a stale **E3 = 7**. **Taking either whole side would have silently deleted the other's story**, and taking either total would have been wrong by one in opposite directions. Resolved per row and then recounted from the checkboxes, never from the rows and never by choosing: `10+20+9+10+8+9+9+20+7 = 102` stories, `10+14+8+8+5+2+0+0+0 = 47` done, so `47/102 = 46%`. Command: `grep -cE '^- \[x\] \*\*E[0-9]-[0-9]{2}' backlog/E*.md`.

> **E3-03 closed 2026-08-06 and the table moved to 47, which is what the previous note here promised.** That note said E3-03 was `[~]` because **no independent audit had run** and that the figure would move "when the audit closes". Four have now run, all four returned VIOLATIONS FOUND, and the story is `[x]` **on an owner ruling without a fifth round** rather than on a clean verdict. Recorded in that form deliberately: the earlier wording would have read, to anyone arriving later, as though an audit had eventually blessed it. Recounted from the checkboxes rather than incremented. *(This note first read "the table moved to 47" with an E4 column of 4 and a denominator of 98. Both were correct on this branch and stale by the time it merged: E4-08 landed on `origin/main` in between, taking E4 to 5 and adding four filed stories. Recounted on the merged tree it is 10 + 14 + 8 + **9** + 5 + 2 + 0 + 0 + 0 = **48 of 102**. Left visible rather than rewritten clean, because "my number was right when I wrote it" is exactly how this table has drifted five times.)* **The caveat travels with the story, not with this row** — see the ⚠️ on E3-03's own entry for what a ruled `[x]` leaves open, which is the same caveat E5-01, E4-01 and E4-08 carry.

> **Resolved on 2026-08-05 by recounting, and this merge is the textbook case the two notes below predict.** `origin/main` arrived with **E3 = 8** (E3-09 closed) and a stale **E4 = 4**; this branch had **E4 = 5** (E4-08 closed) and a stale **E3 = 7**. **Taking either whole side would have silently deleted the other's story**, and taking either total would have been wrong by one in opposite directions. Resolved per row and then recounted from the checkboxes, never from the rows and never by choosing: `10+20+9+10+8+9+9+20+7 = 102` stories, `10+14+8+8+5+2+0+0+0 = 47` done, so `47/102 = 46%`. Command: `grep -cE '^- \[x\] \*\*E[0-9]-[0-9]{2}' backlog/E*.md`.

> **The E4-04 merge is the sixth time this table produced the same conflict, and the fifth time each side held one correct row
> and one wrong one (2026-08-06).** `origin/main` arrived with **E3 = 9** (E3-03 merged as PR #33) and a stale **E4 = 5**; this
> branch had **E4 = 6** (E4-04 closed) and a stale **E3 = 8**. Taking either whole side would have silently deleted the other's
> story, exactly as every note below predicts. Resolved **per row** (E3 from `origin/main`, E4 from here) and only then
> recounted over the merged epic files: `10+14+8+9+6+2+0+0+0 = 49` of `10+20+9+10+8+9+9+20+7 = 102` = **48%**. The `Totaal`
> cell was **not** carried across the merge, because it is the one number both sides always get wrong.
>
> **48 / 102 = 47% when E4-04 landed (2026-08-06), recounted from the checkboxes with the command rather than incremented.**
> `grep -cE '^- \[x\] \*\*E[0-9]-[0-9]{2}' backlog/E*.md` → 10 + 14 + 8 + 8 + **6** + 2 + 0 + 0 + 0 = **48**; the denominator
> is unchanged at **102** because E4-04 filed nothing new, which is itself worth one line: it is the first story in a week to
> close without adding an item, because both its audit rondes found their MAJOR **inside this story's own copy** rather than in
> a gap belonging to someone else.
>
> **The `[x]` is an owner ruling taken without a third round, and it is the fifth in a row of that kind** (E5-01, E4-01, E4-08,
> E3-03, now E4-04). Each story says so in its own text, which is the convention that keeps the caveat travelling with the story
> instead of with the percentage. On this one the risk is nameable rather than generic: **ronde 1 and ronde 2 each found their
> MAJOR in the previous round's fix**, in the same paragraph, pointing in opposite directions, so the round nobody ran is the
> one that would have attacked the round-2 rewrite. What offsets it is recorded on the story: ronde 2 re-derived every gate
> itself, it said per finding which of round 1's were genuinely closed instead of crediting the attempt, and the round-2 fix
> changed the sentence's *shape* so that particular failure mode cannot recur by drift.
>
> **46 / 98 = 47% after merging E4-01 into E3-09 (2026-08-05).** Per epic: 10 + 14 + 8 + **8** + **4** + 2 + 0 + 0 + 0 = **46**; denominator `10+18+9+10+8+9+9+18+7 = 98`, unchanged because neither story filed anything new.
>
> **This merge is the third instance of the pattern two notes below, and it behaved exactly as they predict.** Each side held one correct row and one wrong one: this branch knew **E3** was 8 and still thought **E4** was 3; `origin/main` knew **E4** was 4 and still thought **E3** was 7. Taking either whole side would have silently deleted the other story. Resolved **per row** — E3 from here, E4 from `origin/main` — and only then recounted with the command over the merged epic files. The rule holds: *a merge conflict in this table is never resolved by picking a side*, and the `Totaal` cell is never carried across a merge, because it is the one number both sides always get wrong.
>
> **E3-09's own figure went 44 → 45 → 44 → 45 → 46 inside two days, and every move was correct**, which is why it is written out rather than smoothed over. Up when E3-09 was marked `[x]`; back down when its first antagonist round proved that `[x]` unearned (a user-visible falsehood on the screen the story exists to make trustworthy); up again when the owner closed the story after two rounds with every finding addressed; and up once more here, on E4-01. **The lesson on top of the two notes below:** recounting from the checkboxes protects the total only if the checkboxes themselves are earned, and the check for that is the audit, not the arithmetic.
>
> *A first draft of the E3-09 note claimed the opposite of what the count showed* — that recounting had "caught the E3 row still saying 7 while its epic file already held 8" — which was false: the row was correct, and E3-09 is what made it 8. Left visible, because a note whose whole subject is arithmetic drift is a bad place to invent a drift that did not happen.

> **E4-08 closed 2026-08-05, and it is the story that moved this table's denominator four times in one day.** Its four antagonist rounds filed **E7-19**, **E1-19**, **E1-20** and **E7-20**, so `98 → 102` while `done` rose by one, which is why the share went 46% → 45% → 44% → 45% across the day without a single regression. Recounted from the checkboxes at every step: `10+20+9+10+8+9+9+20+7 = 102` stories, `10+14+8+7+5+2 = 46` done. **The `[x]` is a ruled one, not an audited one** (no fifth round), and the story entry says so in its own text.

> **The denominator moved 98 → 102 on 2026-08-05 and the percentage fell 46% → 45% while nothing regressed, for the second time in three days.** E4-08 filed **four** stories its four antagonist rounds surfaced: **E7-19** (a domain guard composing an English message that a Dutch screen renders verbatim, with `(Parameter 'naam')` appended), which the audit found sitting in the **gitignored** groepschat board rather than in the backlog; and **E1-19** (`PUT /api/subthemas/{id}` re-scopes a subthema to another klas and drags every activiteit and doelkoppeling inside it across the class boundary), filed by owner ruling of 2026-08-05 rather than fixed inside E4-08; and **E1-20** (a notice on the klaslaag survives a later successful write that has nothing to do with it), which round 2 found by noticing that E1-14’s round-4 fix never fixed the case its own comment described; and **E7-20** (`aria-expanded` on disclosure triggers is answered three different ways in three files, and no gate can catch a wrong call), which round 4 found by grading the third answer a MAJOR. Denominator up, numerator unchanged, so the share done drops. Recorded because a falling percentage looks like a regression and this one is the opposite: it is the backlog admitting to work it was not counting, exactly as the E7-16 entry below records. **E4-08 itself adds nothing to `done`**: it is `[~]`, because its fix round has had no independent pass and `[x]` means antagonist-clean here. Recounted from the **checkboxes** per the rule below, not from the rows: `grep -cE '^- \[x\] \*\*E[0-9]-[0-9]{2}' backlog/E*.md` gives `10+14+8+7+4+2+0+0+0 = 45`, and the story counts give `10+20+9+10+8+9+9+20+7 = 102`, so the share done rounds to 44%. The **E4 row was already correct at 8/4** (E4-01 and E4-03 landed on `origin/main` before this branch was cut); only E7 and the total needed moving.

> **The total moved 44 → 45 when E4-01 landed (2026-08-05), recounted from the checkboxes rather than incremented.** Command, as the rule below requires: `grep -cE '^- \[x\] \*\*E[0-9]-[0-9]{2}' backlog/E*.md` → 10 + 14 + 8 + 7 + **4** + 2 + 0 + 0 + 0 = **45**; the denominator is unchanged at **98** because E4-01 filed nothing new. `45/98 = 46%`. **Two things about this figure that a later reader should not have to reconstruct.** It is the first `[x]` in this backlog granted on an owner ruling *while its last fix round was unaudited* since E5-01, and both story entries say so in their own text: the caveat travels with the story, not with the percentage. And E4-01 is the story that found the defect **E5-02's own `[x]` had left live** — a dekkingscijfer from before the teacher's edit — which is worth noting next to a rising percentage, because it is the second time in two days that closing one story exposed something in the one before it.
>
> **Merged twice, and the second merge is the clearest example of why this table is re-derived rather than edited (2026-08-04, E5-02).** `E4-03` landed on `origin/main` while E5-02 was in its gates, so the two sides of the merge each held a **correct** table and a **wrong** one: this branch knew `E5` was 2 and thought `E4` was 7/2; `origin/main` knew `E4` was 8/3 and thought `E5` was 1. **Taking either whole side would have silently deleted the other's story.** Resolved per row — `E4` from `origin/main`, `E5` from here — and then the total recounted with `grep -c` over the merged epic files: `10+18+9+10+8+9+9+18+7 = 98`, done `10+14+8+7+3+2+0+0+0 = 44`, `44/98 = 45%`. The `Totaal` cell was left at the pre-merge `97/43` by the resolution itself and had to be corrected, which is the same "resolve a conflict in a *number* by choosing rather than recomputing" that this table retracted on 2026-08-03. **A merge conflict in this table is never resolved by picking a side.**
>
> **Re-derived on the merged tree, not carried over from either side (2026-08-04, E5-02).** This branch forked at `1dfe9b8` and merged `origin/main` (`ba372a4`) back in before its gates ran, so the table had two versions: `origin/main`'s 96/42/44% and this branch's older 94/39/41%. Counted again with `grep -c` over the **merged** epic files: `10+18+9+10+7+9+9+18+7 = 97` stories, `10+14+8+7+2+1+0+0+0 = 42` done, `42/97 = 43%`. Two cells change on this branch: **E7 17 → 18** for the **E7-18** it filed, and **E5 1 → 2** now that **E5-02** is `[x]`. Re-derived again after that flip rather than incremented: `43/97 = 44%`. **E5-02's `[x]` carries a caveat**, recorded on the story the way E5-01's is: its last commit, the kleuterjaar chooser, shipped **without a third audit** on the owner's decision. It is a `[x]` earned by two audit rounds plus a test-runner pass on everything before that commit, and by its author's own tests and browser pass on the commit itself.
>
> *A correction to what this branch said an hour earlier, kept because the mistake is instructive.* It asserted that E7-16 and E7-17 were *"not committed to `origin/main`"* and told whoever lands them to re-derive this row. Both were already committed (`3b523ed`); what was true is that **this branch was 29 commits behind**, which is a different fact with a different remedy. Found by the round-2 audit, which checked `git show origin/main:` rather than taking the claim. **The lesson is the one E4-02 recorded and this branch then repeated: a claim about the repository has a timestamp, and a fetch invalidates it.**

> **What E5-01 unblocked, acted on rather than only noted (2026-08-03).** **E3-03** and the dekkingshelft of **E4-01** were both recorded as "de facto blocked on E5, which is not built at all". That is no longer true, so both were revisited in the same pass that closed E5-01 rather than left carrying a stale blocker: each now names what it can actually measure today and what still waits on E5-02/E5-03 for a *screen*. Neither is claimed done. **E5-04 remains genuinely blocked** on E1-12, and with it any story whose criterion is stated at minimumdoelniveau.

> **Recomputed from the rows on 2026-08-04, not adjusted, and it corrects a cell that was demonstrably one too low.** Counted per epic file with a `grep -c` on the checkbox lines: E0 10/10, E1 18/12, E2 9/8, E3 10/6, E4 7/1, E5 9/1, E6 9/0, E7 15/0, E8 7/0. Those sum to `10+18+9+10+7+9+9+15+7 = 94` stories and `10+12+8+6+1+1 = 38` done, so `38/94 = 40%`. **Two independent things moved and they nearly cancelled:** the technical lead showed at 17:54 on 2026-08-03 that this cell said 37/93 while its own rows summed to 38/93 (two merges each wrote a number that was correct for its own branch, and the conflict resolution *chose* a figure instead of recomputing one), and E1-14’s audit then filed **E1-18**, taking the denominator to 94. So `done` goes 37 → 38 because the cell was wrong, not because a story landed, and the percentage stays 40% by arithmetic rather than by luck. **Pending, and it will move again:** the lead’s **E7-16** and **E7-17** are filed in the working tree but not committed, so E7 counts 15 here and 17 there; when they land this becomes `38/96 = 40%`.

> **The total moved 93 → 94 on 2026-08-03, and the percentage fell 38% → 37% while nothing regressed.** **E7-16** was filed by the technical lead: no story owned the rule that a write path verified only on the in-memory provider is not verified, and the class had been rediscovered **six** times by six sessions (E1 code review 2026-07-27, E3-04, E2-08, E1-13's round-2 gates, E1-13's implementer, and E5-01's first Postgres run, two of them within one hour). Denominator up, numerator unchanged, so the share done drops. Recorded because a falling percentage looks like a regression and this one is the opposite: it is the backlog admitting to work it was not counting. Arithmetic re-derived from the rows: `10+17+9+10+7+9+9+16+7 = 94`, `35/94 = 37%`.
>
> **The total did not move when E4-06 landed, and that is not an oversight (2026-08-03).** E4-06 closing added one; its own gates found a defect in **E3-07**, which the owner ruled reopens, subtracting one. Net zero, and the arithmetic was re-derived from the rows rather than adjusted by hand: `35/93 = 38%`. Recorded because a figure that stays still after visible work looks exactly like a figure someone forgot to update, and this table has had to retract counts twice.

> **E4-03 closing and E4-08 being filed together give 43 of 97 = 44% (2026-08-04), counted with the command rather than incremented.** Per epic file: E0 10/10, E1 18/14, E2 9/8, E3 10/7, **E4 8/3**, E5 9/1, E6 9/0, E7 17/0, E8 7/0, so `10+14+8+7+3+1 = 43` of `10+18+9+10+8+9+9+17+7 = 97`. **The share done therefore drops 45% → 44% while nothing regressed**, the same shape as the E7-16 entry below: numerator up one, denominator up one, and the denominator is the backlog admitting to work it was not counting.
>
> **⚠️ This paragraph replaces one that was false, and the retraction is the useful part.** It read: *"The denominator did not move: E4-03 added no story, and the two scope gaps it found **are recorded against the stories that own them** (an activiteit cannot be moved between subthema's, which is E1-14's surface; and one unparseable enum row taking down `GET /api/themas`, which is E1-14's or E7-15's to file)."* The **E4-03 antagonist audit checked that claim with a `grep` and it collapsed**: neither gap appeared anywhere except E4-03's own entry and worklog, and **E1-14 is `[x]`**, so the first gap had been handed to a story that cannot pick it up. The sentence also contradicted itself in one clause, promising things were *recorded* while calling one of them *to file*. Net effect for the few hours it stood: a story marked `[x]` against an FR it does not fully satisfy, with **no open item owning the remainder**. Resolved by an owner ruling on 2026-08-04: **E4-08** now owns the unbuilt cell. **The enum-casing finding is deliberately *not* a story** (same ruling, and the owner was offered filing it and declined): the *data* was repaired and the repair verified in the same session (`activiteit_type` `experiment` → `Experiment`, one row; `GET /api/themas` went 500 → 200 with 11 thema's; every other enum-as-name column swept and clean), so nothing is outstanding to build. What remains is a **hardening question, recorded under the open items below** rather than routed to a story that does not own it: whether one unparseable row should take down an entire list endpoint. *The lesson, which is not about scope at all:* **"this is recorded elsewhere" is a checkable claim, so check it before writing it** — one `grep` over `backlog/E*.md`, plus a look at whether the destination story is still open. Third instance of this class in three days.
>
> **The total was re-derived from the *epic files* on 2026-08-04, not from the rows, and that found a second error (E3-07).** The cell read **37 / 40%**; counting `- [x]` in all nine epic files gives 10 + 12 + 8 + **7** + **2** + 1 + 0 + 0 + 0 = **40 of 93 = 43%**. Two rows were wrong before E3-07 closed. **E4 said 1 and should have said 2**: E4-02 merged as PR #25 and is `[x]` in its own epic file, but the row was never moved, exactly as E4-02's own session file predicted it would have to be. And the **Totaal** cell was one lower than even the (wrong) rows summed to, the residue of two merges each writing their own +1 and the conflict being resolved by *picking* a number. **The lead's rule was "recount from the rows"; this is the case that shows the rows themselves need checking**, so the rule is now: recount from the **checkboxes**, which are the only place a story's status is actually recorded. Command: `grep -cE '^- \[x\] \*\*E[0-9]-[0-9]{2}' backlog/E*.md`.

> **Resolved on 2026-08-04 by recomputing, and the conflict itself is worth one paragraph, because both sides of it were wrong and in different ways.** `origin/main` arrived carrying **two contradicting `Totaal` rows** (`94 / 35 / 37%` and `93 / 40 / 43%`), which is a botched earlier resolution rather than a disagreement: one of them was left behind. My side carried `94 / 40 / 43%`, correct for a tree that did not yet contain E4-02, E3-07 or the lead's two E7 stories. **So no side could be picked.** Recounted per epic file with a `grep -c` on the checkbox lines: E0 10/10, E1 18/14, E2 9/8, E3 10/7, E4 7/2, E5 9/1, E6 9/0, E7 17/0, E8 7/0. That sums to `10+18+9+10+7+9+9+17+7 = 96` stories and `10+14+8+7+2+1 = 42` done, so **`42/96 = 44%`**. The **E7 row was stale too** (16 where its file holds 17, because E7-16 and E7-17 landed) and is corrected in the same pass, so the rows agree with their files and the total agrees with the rows. *This is the third time in two days that this cell has been resolved by choosing rather than counting. The counting takes one command.*
>
> **The total moved 35 → 36 when E5-01 landed (2026-08-03), and the arithmetic was re-derived from the rows rather than incremented.** Per epic: 10 + 11 + 8 + 5 + 1 + **1** + 0 + 0 + 0 = **36** of 93 = **39%**.
> *Earlier the same day, and kept because the reasoning still applies:* the total did **not** move when E4-06 landed. E4-06 closing added one; its own gates found a defect in **E3-07**, which the owner ruled reopens, subtracting one. Net zero. Recorded because a figure that stays still after visible work looks exactly like a figure someone forgot to update, and this table has had to retract counts twice.

> **Correction (2026-07-27).** A code review of the E1+E2 branch before merging to `main` reopened
> **E1-03**, **E1-04** and **E1-07**: each was marked done with an acceptance criterion that cannot
> currently be met (no `Minimumdoel` is ever created, so minimumdoel-level coverage — the level the
> inspectie tests — returns nothing; and there is no Excel-upload endpoint). Count corrected 27 → 24.
> The root cause the review identified is that **no test ran against PostgreSQL** — the EF in-memory
> provider enforces no FKs, unique indexes or collation, so these defects passed CI. That is fixed:
> `backend/tests/Jaarplanner.IntegrationTests/Postgres/` runs against a real server, and CI now fails
> rather than skipping when no database is configured.

> **Correction (2026-07-29).** Asked what it would take to have an application users can click
> through, a sweep of E0–E8 found that **no story owned the app shell** — routing, a primary
> navigation, and choosing a klas/schooljaar from a list rather than pasting a GUID. The only trace
> of navigation in any planning document is a single line in `docs/ux/ui-ux-approach.md` §3 with no
> story behind it. Meanwhile every feature built so far has been appended to `App.tsx`, which now
> stacks the kalender and the matching review in one flex column, and a klas is selected by pasting
> a GUID into a text input. Added as **E0-10**, which reopens E0 (9/10).
> This is the third instance of one failure mode — **E2-08**, **E1-15**, now E0-10 — and the
> sharpest, because the missing piece is *where the other unbuilt screens were supposed to go*.
> The pattern to name: the stories were written per-FR, and a shell satisfies no FR by itself, so
> nothing claimed it. Worth asking of any remaining infrastructure that no FR names.

## Milestones (MVP)

- **M0 — Skeleton up** (E0): repo scaffolded, Postgres + API + SPA run locally, CI green. — **reached**, and E0 is a complete epic again as of 2026-07-30 (E0-10 closed). It stayed reached throughout the reopening, deliberately: M0's wording is about a running skeleton and that was true the whole time, so claiming M0 had regressed would have been as inaccurate as the claims this backlog keeps having to retract. The shell was a gap in the epic, not in the milestone.
- **M1 — Fundament** (E1): Op.stap goals imported, school thema's imported & manageable, data model live. — ⚠️ **not yet reached** (previously claimed). Two distinct gaps remain:
  1. **The curriculum half.** No `Minimumdoel` can exist, so minimumdoel-level coverage — the level the inspectie tests — returns nothing. Gated on **E1-12**, which needs the decreed-eindtermen source file from directie; that unblocks E1-03/04.
  2. **Everything teacher-facing in E1.** `frontend/src/features/` contains exactly one directory — `matching` (E2). **E1 has no UI at all.** Its stories are `[x]` on server-side evidence, and **seven** FRs name a user action or a display that consequently does not exist: FR-1.1/1.2/1.3/1.4/1.5 (upload, per-row foutmeldingen, preview, add-vs-overwrite choice, sjabloon download), FR-2.5 (the review notice *signaleert*) and FR-3.1 (*leerkrachten kunnen* thema's toevoegen/wijzigen/verwijderen). Tracked as **E1-13** (import flow) and **E1-14** (beheer screens); each affected story now carries an explicit *Scope boundary* note.
  > *Two corrections, both from antagonist audits on 2026-07-28.* First, this line claimed the upload half was "proven end-to-end" — an overstatement: for non-technical teachers end-to-end means teacher → screen, and what was proven was HTTP → database. Second, the correction itself named only **one** teacher-facing gap (FR-1.2) when the same defect sat in E1-05, E1-08, E1-09 and E1-10 — the instance was fixed while the class was left standing. That is the same failure mode as the 2026-07-27 correction below, on its third iteration. **The general lesson, now recorded: an `[x]` earned by a passing API test does not retire an FR whose wording names a user.** Check the verb — *toont*, *signaleert*, *krijgt de gebruiker*, *kan de gebruiker kiezen*, *leerkrachten kunnen* — before marking done.
- **M2 — AI koppelt** (E2): teacher gets accept/reject thema↔doel suggestions with motivation. — ✅ **Reached 2026-07-30, on the evidence the withdrawal demanded.** `E2-08` gave FR-4.1 its missing invocation surface (`POST /api/themas/{id}/doelsuggesties/genereer` plus the trigger panel on `/themas`), and the claim rests on a teacher-visible path rather than a passing API test: the screen was driven **in a browser** against a real API and a real PostgreSQL, where a filter matching no goals reports so without calling the model and a filter that matches reaches the AI. The integration tests now go **through** generation — they assert the list is empty, POST, and then assert on rows only the real controller → real `DoelMatchingService` → real EF store could have written — which is precisely what the seeded tests never did.
  *One residual, accepted by the owner:* a **live** model round-trip is still unverified (no `AzureAI:ApiKey` on the dev machine), so the flow is proven with `IAiClient` faked and everything else real, as Art. IV.5/IV.6 sanctions. Same residual E3-01 accepted for its own Azure call.
  > **Withdrawn 2026-07-28, restored 2026-07-30 — keep the reason, it is the more useful half.** The withdrawal read: the accept/reject/adjust UI existed and worked, but *nothing could ever produce a suggestion for it to show* — `MatchThemaAsync` was invoked from exactly one place in the repository, its own unit tests, so in a deployed app the list would always render `matching.leeg`. **The asymmetry that hid it:** E2-07's wizard-assist endpoints *did* exist, so the epic looked wired up. **The test that hid it:** the integration suite seeded suggestion rows directly, bypassing generation, and was green throughout. A milestone asserting *a teacher can* is not evidenced by a service that compiles.
- **M3 — Plan & kalender** (E3): a year plan is generated and shown in the drag-and-drop calendar.
- **M4 — Volledige controle** (E4): manual edits + full/partial regeneration with locked blocks.
- **M5 — Bewijs van dekking** (E5): coverage down to minimumdoel level, exportable.
- **M6 — School-breed** (E6): admin, roles/permissions, cross-class overviews, collaboration view.
- **MVP complete** = M0–M6 + E7 (non-functional) satisfied.

## Open decisions that gate stories (see [Art. XIV](../CONSTITUTION.md#article-xiv--open-decisions-awaiting-directie))

> **Forwardable version:** [`docs/besluiten-gevraagd.md`](../docs/besluiten-gevraagd.md) states the directie-facing
> asks in plain Dutch, ordered by urgency, with the consequence of leaving each one open. Send that; keep this
> section as the engineering-side record.

Stories blocked on these are marked `[!]`:
- ~~Planningsblok granularity~~ — **RESOLVED (directie 2026-07-14):** two-tier default = themaperiode (4–6 wk) + subthemaperiode (~2 wk), configurable behind a seam (E3-05); never hard-assume months.
- Disciplines first (all vs. starter selection); `cluster` presence per discipline.
- `leergebied`/Wereldoriëntatie surfacing & mapping.
- ~~`jaarFase` code form~~ — **RESOLVED (owner, 2026-08-03): our canonical form is `JK`/`K2`/`K3` + `L1`–`L6`, and the import normalises the other form to it.** The owner's words: *"K3/L1 schrijfwijze is goed."* This is the form the constitution's own mapping table already uses (Art. VII.1 col F), the form `nl.json`'s `matching.jaarFasenPlaceholder` already shows a teacher (*"bv. K3, L1"*), and the form every `jaarFase` literal in the codebase already carries, so the ruling ratifies the de facto answer rather than changing anything. Teacher vocabulary `1K/2K/3K` maps onto it (Art. XIII glossary).
  > **What this ruling does *not* settle, stated separately because conflating the two is how a default gets mistaken for a decision.** Art. XIV asks a *different* question: *"confirm whether Excel col F uses 1K/2K/3K or JK/K2/K3."* That is a question about **what a real Op.stap file contains** — an observation nobody can rule on, only make, and no real per-discipline Op.stap `.xlsx` exists in this repository yet (`assets/` holds PDFs only). The owner has ruled on **our** notation; the file's notation is still unobserved. **The normalisation clause is what makes that gap harmless**: accept both orderings on import and store the canonical form, so whichever way column F is written, nothing downstream has to care. Filed against **E1-12**. Until that clause exists, the observation still matters, because comparison downstream is ordinal (see the concordance note on E1-12) and `3K` ≠ `K3` to a string comparer.
  > *Kept as the record of what the delay cost:* while this sat open, **E2-08 put the answer in teacher-facing copy** (2026-07-29) without anyone ruling on it. The filter matches case-insensitively, so `k3` was forgiven, but `3K` versus `K3` is an *ordering* difference no case-folding rescues. The entry had been tracking a cost that grew from a data change to a data change **plus** a copy change; the ruling arrived before a third screen was added. Same lesson as the Art. II.3 entry below: **a decision whose cost grows every story is a decision that gets taken by default**, and this one had already been taken by a placeholder string.
- Op.stap import source (manual per-discipline Excel vs. automated).
- ~~Minimumdoel source~~ — **RESOLVED (2026-07-28): a separate decreed-minimumdoelen import.** `Minimumdoel` requires an `omschrijving` (Art. IX.1) that **neither** documented Op.stap source carries — the ordeningskader has only Discipline→Domein→Subdomein, and the per-discipline goal Excel's A–M mapping has no such column (Art. VII.1), only the concordance key (B/C/D). Decision: a **second import path** for the decreed eindtermen (ref + leeftijd + nr + omschrijving), keeping Art. IX.1 unchanged and the curriculum read-only (Art. III.1). Tracked as **E1-12** below; it needs directie to supply the source file. Until E1-12 lands, every MD-concorded leerplandoel still fails its FK on insert, so **E1-03/E1-04 stay `[~]`**.
- ~~A leerplandoel `code` that moves to another discipline~~ — **RESOLVED (owner 2026-07-31): refuse the import and inform the uploader.** Surfaced by E1-15: the import diffs *within* one discipline, so a code that already exists under a **different** discipline is not "unchanged" to it, and the realistic cause is a file uploaded under the wrong discipline number. The tool therefore **refuses the whole file** with a Dutch message naming the offending codes and the discipline they currently belong to, changes nothing, and leaves the correction to a human. Reasoning: the code is the stable identity of a decreed goal (Art. III.5), so silently moving one is a curriculum change the app is not entitled to make (Art. III.1), and refusing is the non-destructive option (Art. III.4). If Op.stap ever really does move a code, someone confirms it deliberately — how, is not designed yet and does not need to be until it happens. Implemented in `OpstapImportService.ControleerVoorwaardenAsync` (raised as `OpstapImportFout.CodeInAndereDiscipline`, answered as **409**); the behaviour existed before the ruling, which is precisely why it needed one.
- **Does a `vast moment` constrain a teacher's own hand-placement, or only the AI?** *(new 2026-08-04, surfaced by the E4-03 antagonist audit.)* `JaarplanGeneratieService.GenereerAsync` refuses to place a thema in a period holding a blocking `vast moment` (`geblokkeerdeBlokken`), which is what Art. IX.3's parenthetical *"the service refuses placements in its period"* describes. **Neither manual path consults it:** E3-07's `VerplaatsPlaatsingAsync` never has, and **E4-03 is the first *creation* route into such a period**, offering it in the picker with no annotation. Two readings are defensible and the code currently implements the second by omission: either a vast moment is a *constraint on the plan* (so a hand-placement should at least disclose it), or it is *an instruction to the AI* (so a teacher who deliberately plans over their own oudercontact is doing something Art. IV.1/IV.7 explicitly leaves to them). **What tips it into needing a ruling rather than a default:** the teacher registered that moment themselves in the generation settings, so silence is the tool forgetting an instruction it was given, while a refusal would make the tool override its own user. Cheapest to answer before **E4-05**, which re-reads this copy family anyway; note it also decides whether E3-07's move path needs the same treatment. Nothing hard-assumes either reading today.
- **Art. V.1 says the *link* status decides dekking; the code also requires the *placement* status.** *(new 2026-08-04, surfaced by the E4-03 antagonist audit; the behaviour is E5-01's and pre-dates E4-03.)* Art. V.1 as written conditions the **`DoelKoppeling`** (*"linked (status `aanvaard` or `manueel`)"*) and asks only that the thema be *"placed in the plan"*. `DekkingService.TeltVoorDekking` additionally gates on the **`Themaplaatsing`** status being `Aanvaard`/`Manueel`. That is a real narrowing of a non-negotiable article, and it is recorded by **owner ruling in an epic file** (`E5-dekking-export.md`) rather than by the dedicated amendment commit [Art. XI.1](../CONSTITUTION.md#article-xi--amending-this-constitution) requires. The behaviour is almost certainly the intended one (a `voorgesteld` placement is not evidence of teaching), and it is load-bearing in two directions that are now shipped: it is why a generated plan reports 0% until a teacher decides, and it is the whole justification for E4-03 landing a hand-placement as `Manueel`. **Ratify it into Art. V.1 so the next reader does not have to reconstruct it from a backlog file**; until then every story that leans on "manueel counts" is leaning on an unratified reading.
- **Should one unparseable row take down an entire list endpoint?** *(new 2026-08-04, found while browser-checking E4-03; deliberately **not** filed as a story, owner ruling.)* `ActiviteitConfiguration` persists `ActiviteitType` by name and reads it back with a **case-sensitive** `Enum.Parse`, so a single row holding `experiment` instead of `Experiment` made **`GET /api/themas` answer 500 for the whole list**, taking E1-14's catalogus screen with it. The instance was dirty data, not a defect in either write path (the import and the beheer POST both pass the strongly-typed enum, so nothing in the app can produce it); it was repaired and the repair verified (500 → 200), and every other enum-as-name column in that database was swept and is clean. The open question is only whether fail-fast on corrupt reference data is the behaviour wanted here, or whether a list should degrade around one bad row. Recorded rather than fixed because changing it is a product decision, not a bug fix, and because a guess would be the fourth read-path convention in this codebase.
- Teacher visibility scope; export formats & layouts; coverage depth (binary vs. herhaling/opbouw).
- **Which thema's belong to a class?** *(half-resolved 2026-08-05 by owner ruling, surfaced by the E3-03 antagonist audit.)* **Ruled: a class is not obliged to do every thema the school owns.** In the owner's words: *"elke klas van elk leerjaar kan zijn eigen thema's hebben. VAAK worden de thema's gealigneerd across klassen (bijvoorbeeld voor alle derde leerjaren) maar niet altijd."* So alignment across the classes of one leerjaar is the common case and never an invariant, and the thema-bibliotheek is an **offer** rather than a syllabus.
  > **What that settles, immediately:** E3-03's generation prompt no longer asks the model to place every thema at least once. It asks it to choose the combination that covers the most, and says in as many words that the list is the school's library and not required content for this class. The audit was right to call the old wording a pedagogical default nobody had been asked about.
  > **What it does NOT settle, and this is the part with a cost:** `Thema` is school-wide (Art. IX.2) and **nothing records which thema's a class actually does**, so every generation is handed the whole library for every class. The ruling means that is now known to be wrong rather than merely unexamined: the AI can propose a thema this class does not teach, and a teacher's only remedy is to reject it card by card. Fixing it is a data-model question (a per-class selection, or a `Klas`↔`Thema` link), which is why it stays open here rather than being absorbed into E3-03. It also bears on **E5**: the dekking denominator is already scoped per class, but the *numerator* draws on thema's that may not be this class's. Related: [`CLAUDE.md`](../CLAUDE.md)'s open decision *"Whether a leerplandoel/thema is shared school-wide or per class"*, which this ruling answers for thema's at the **planning** level and leaves open at the **data-model** level.
- ~~**Which leerplandoelen is a class measured against?**~~ — **PARTIALLY RESOLVED (owner, 2026-08-04): a class is measured against its own jaar/fase by default, with the whole curriculum as an explicit switch.** Implemented by **E5-02**: `Jaarplanner.Domain.Curriculum.Jaarfasen.VoorLeerjaar` maps `Klas.Leerjaar` (1–6 → `L1`–`L6`) onto the jaar/fase codes, `Dekkingsbereik` carries the choice, `GET /api/klassen/{id}/dekking?bereik=` defaults to `EigenJaarFase`, and the response states which scope it applied, which codes it used and **how many loaded doelen it left out** (`aantalBuitenBereik`), because a narrower denominator flatters the figure. E5-01's seam was built and Postgres-tested for exactly this, so resolving the ruling was a value at one call site rather than a change to the computation.
  > **What this ruling does *not* settle, and both halves are live rather than theoretical.**
  > 1. **The graadklas / menggroep.** `Klas.Leerjaar` is one ordinal, so such a class cannot state its set. `Jaarfasen.VoorLeerjaar` **refuses** (returns `null`) rather than guessing, and `DekkingService` then widens to the whole curriculum and declares it (`isTerugvalNaarHeelCurriculum`), because a scope narrower than intended would overstate coverage. A teacher sees a sentence saying so. **This remains an open Art. XIV question.**
  > 2. ~~**The kleutergroep, which the ruling's wording did not contemplate.**~~ — **RESOLVED (owner, 2026-08-04): the teacher chooses the kleuterjaar on the screen.** A kleutergroep has `Leerjaar = 0` and nothing says *which* kleuterjaar it is, so the derived scope is `JK` + `K2` + `K3` together. Measured against all three, a derde kleuterklas carried roughly **three times** the doelen it teaches: its figure read about a third of what it is, and its gap list named doelen for two-and-a-half-year-olds it will never teach and does not have to. Surfaced by E5-02's antagonist round 1, which also found that only the leerjaar-7 case had a notice at all.
  > **The rejected alternatives, so the choice is legible:** leaving it wide with an explanatory sentence (the figure stays structurally too low for half the school); renaming the control so it over-claims less (does not fix the figure); and **giving `Klas` a real jaar/fase instead of one ordinal**, which is the right long-term answer but drags in the still-open graadklas decision, a migration, and a beheerscherm that does not exist (E6-03/E6-04). Implemented instead as `?jaarFase=` narrowing **within** what the class has, so nobody can measure a kleutergroep against L6, and the payload carries `beschikbareJaarFasen` beside `gemetenJaarFasen` so a narrowed screen still knows its alternatives. The control renders on *"this class has more than one code"* rather than on *"is this kleuter"* — the second is a question the data model cannot answer, and a graadklas ruling would answer it differently while needing exactly the same shape.
  > **Still open, and it is the same root:** a graadklas / menggroep has no single leerjaar either, and unlike the kleutergroep it cannot even derive a set to narrow from, so it falls back to the whole curriculum and says so. Fixing that properly is the `Klas` change above.
  > **A second E5-02 question, and it is here because three artefacts already said it was (round-2 audit).** **Must the per-row `Gedekt` / `Niet gedekt` verdicts be marked provisional while the plan's figure is withheld?** E5-02 removes the per-group tallies in that state, because group counts are additive and a teacher could add them up to reconstruct exactly the total the directie ruling of 2026-07-28 forbids. **The row chips are just as additive**, so the total stays reconstructible by counting them, and for a doel behind a stale placement the screen prints a definite *"Niet gedekt"* in the one state where the tool says it cannot prove coverage. The chips were kept because removing them leaves the screen unable to show which thema's are affected at all, which is a judgement call rather than the ruling: the ruling speaks of *the figure*. Cheap to settle now, because no export reads these verdicts yet (E5-06).
  > **E5-03 widened this and the entry has to say so, because a commit message is not what E5-04 and E5-06 will read** *(added 2026-08-06 on the E5-03 antagonist's insistence, twice)*. The question above is about **chips a teacher would have to tally themselves**. E5-03 adds *"Alleen ontbrekende"*, and that toggle is **not** suppressed while the figure is withheld: in exactly that state it renders the not-gedekt subset as a list, so **the tool now performs the filtering step on the teacher's behalf**. No figure is printed and every total, tally and percentage stays withheld, so the ruling is not breached on its own terms — but the exposure is one manual count away rather than several, and it changed without anyone deciding it should. **Two ways to settle it, and the first closes both halves at once:** suppress the gaps-only toggle while `!isBetrouwbaar`, or rule that the ruling really does speak only of *the figure* and that per-doel verdicts, chips and filtered lists alike, are outside it. Until then E5-03 ships the second reading by default, which is the position this entry exists to flag.
  > **Still owed by the owner, per Art. XI.1:** a dedicated amendment commit recording the ruling in the constitution (Art. XIV, and the `Dekking` clause of Art. IX.3). The precedent is `efecf73`, which recorded the jaarFase ruling in this file and named the amendment as outstanding rather than making it. **Also falsified by this change and worth correcting in passing:** `Klas.cs`'s own documentation says *"no planning logic keys on this value; it is carried as descriptive data"*, which is no longer true — `Leerjaar` now decides the denominator of every coverage figure.
  > *Kept as the record of what the entry was tracking:* before the ruling, dekking measured every class against the **whole** loaded curriculum, including every other year, every discipline and the illustrative `P`/`S`/`A` doelsoorten. That was never a considered answer, only the available one. It decided whether **E5-03**'s dekkingspercentage means anything and whether **E5-05**'s gap list is usable; both now inherit a scoped denominator and **must read `bereik` before printing a figure**, because one class has two legitimate denominators and a figure that cannot say which it used is not evidence.
  > *One consequence of scoping, stated because it is not only "other years":* the illustrative `P`/`S` doelsoorten carry a **fase** code in Art. VII.1 column F rather than one of the nine jaar/fase codes, so a scoped class does not measure against them at all. They are counted in `aantalBuitenBereik` and the switch is the way to see them.
  > *A second, smaller question rides on the same file (E5-01 audit, filed rather than guessed):* the FR-1 school-content import writes every `DoelKoppeling` as **`voorgesteld`**, and only `aanvaard`/`manueel` count for dekking (Art. V.1). So a school that imports its thema's *with* their doelkoppelingen sees **0% dekking** until a teacher accepts each link one by one. That may be exactly right — Art. IV.2 wants a human to confirm — but it is the first number directie will ever see, and nobody had ruled on it. It carried a gate: *"E5-02 must not put a figure on screen before this is answered."* **RESOLVED (owner, 2026-08-04) and the gate is struck:** **E1-18** (`7e4bde8`) makes the import write **decided** links, so an imported thema's koppelingen count for dekking and the first figure directie sees is no longer a structural 0. E5-02 therefore ships figures legitimately. *Recorded rather than quietly deleted, because the round-2 audit of E5-02 caught this gate still standing on a branch that predated E1-18 and shipped figures anyway: the product was fine and the record forbade what it did.*

### Surfaced by the E3-05 antagonist audit of 2026-07-28 (need a ruling, not a guess)

- ~~What happens to a teaching stretch too short for a full themaperiode?~~ — **RESOLVED (directie 2026-07-28): distinguish a *vakantie* from a *vrije dag*.** The problem was never really "short stretches" — it was that every closure was treated as a period boundary, so Hemelvaart + brugdag and Pinkstermaandag chopped May into slivers and left a one-week "themaperiode" nobody can plan a thema into. The school now classifies each closure when entering the calendar: a **`Vakantie`** (herfst, kerst, krokus, paas) ends a planning period; a **`VrijeDag`** (Hemelvaart, Pinkstermaandag, pedagogische studiedag, facultatieve vrije dag) is a day off *inside* a period and leaves the grid intact. No invented threshold — the answer lives in data the school owns, and "a planningsblok never spans a vakantie" becomes exactly true rather than approximately true. Implemented in E3-05 (`Sluitingssoort`, `Schoolsluiting`); recorded in [ADR-0020](../docs/adr/0020-planningsblok-derivation-rules.md).
  *Residual:* a genuine long gap can still leave a stretch too short for one themaperiode (e.g. a school closing for three weeks mid-year). That yields one short block, asserted by test. It is now a rare edge rather than a routine May occurrence, so it is accepted rather than tracked as open.
- ~~What happens to an existing jaarplan when a school edits its vakantiedata?~~ — **RESOLVED (directie 2026-07-28): flag it loudly; never move it silently.** Blocks are derived, so editing a vacation reshapes the grid and a stored thema placement can end up pointing at a date that is no longer a block boundary. The ruling:
  1. **Never silently relocate a teacher's thema.** The application does not guess a new period. A stale placement keeps its stored date and is marked as needing attention.
  2. **The signal must be impossible to ignore and impossible to dismiss.** Not a toast, not a badge tucked in a corner: a persistent notification on the jaarplan that stays until every affected placement is resolved by a human. "Fix later" is not an option the UI offers.
  3. **It must be resolved as soon as possible**, so the flag names exactly which thema's are affected and offers the re-placement action inline.
  4. **Coverage must not claim what it cannot prove.** A jaarplan with unresolved stale placements cannot report trustworthy dekking, because a thema whose period is unknown is not demonstrably taught in the school year. Until resolved, the dekkingsoverzicht and any export mark the figure as **onbetrouwbaar / te herzien** rather than showing a number that would mislead an inspectie (Art. V.2, and the same spirit as Art. III.4's review report for curriculum re-import).
  *Implementation is split:* **E3-07** detects and persists (placements key on the block's start date, per [ADR-0020](../docs/adr/0020-planningsblok-derivation-rules.md)); **E3-09** renders the non-dismissible flag; **E5** must honour point 4.

### Surfaced by the earlier antagonist audit of 2026-07-28 — E1 remediation (need a ruling, not a guess)

- ~~**Art. II.3 — where do user-facing Dutch diagnostics live?**~~ — **RESOLVED (owner, 2026-07-30): option (c), with the (a) amendment it needs to be coherent.** The ruling, in the owner's words: *"user messages in het nederlands maar system messages in het engels."* So **the language of a message follows who it is for, not which layer produced it**: a message a teacher can act on is Dutch and **may be generated server-side**; a message only a developer or operator can act on is English; UI chrome the frontend authors itself stays in `nl.json`. Ratified as an amendment to **Art. II.3** (`e420648`, a dedicated amendment commit per Art. XI.1).
  **Option (b) is rejected**, which settles the migration cost this entry had been tracking: `problemen[].melding`, `diff.opmerkingen[]` and the planning faults may stay free text, and the **three** tests in `SchoolcontentImportEndpointsTests` that substring-match them do **not** have to be rewritten. The nine strings E3-01 added and the handler that emits them are fine as they are.
  *What the ruling does not license:* sloppy payloads. Where a payload is **presentation** rather than **diagnosis**, structured fields are still the right answer, because only the frontend can format a date in Dutch or pluralise a Dutch sentence. `ParameterRapport` (E3-04) is the worked example: its records exist precisely so the UI writes the sentence, and reverting that to composed strings would now be permitted by Art. II.3 and still wrong.
  *Kept below, unedited, as the record of what this decision cost while it stayed open* — three retractions, one reverted story and two near-misses. The lesson is not about Dutch: **a decision whose cost grows every story is a decision that gets taken by default.**
  <details><summary>The original entry and its history</summary>

  Three documents currently disagree and cannot all hold: Art. II.3/X.3 say Dutch UI text belongs in `frontend/src/i18n/nl.json`; `frontend/src/lib/api.ts` states the client "never echoes a raw backend message to the teacher"; yet FR-1.2's whole value (row numbers, offending column, the unknown codes, the ignored themadoel codes) **can only** be delivered by displaying `problemen[].melding` and `diff.opmerkingen[]` verbatim. Options: (a) amend Art. II.3 to scope it to UI chrome and permit server-generated diagnostics; (b) restructure the payload as machine-readable codes + parameters the UI renders from `nl.json`. Until ruled, the backend keeps growing as a second source of Dutch copy.
  **The conflict is constitutional, and option (a) is an amendment (settled 2026-07-28, third audit).** Art. II.3 reads, in full: *"**All user-facing strings are Dutch** and centralised in `frontend/src/i18n/nl.json`. Never hard-code Dutch text in components."* Art. X.3 repeats it with no component scoping: *"No user-facing Dutch text hard-coded — **everything** in `nl.json`."* A `problemen[].melding` rendered to a teacher is a user-facing string that is not in `nl.json`, so **Art. II.3 does forbid it** — via its first clause.
  > *Retraction.* A previous revision of this entry quoted only the second sentence, concluded "the literal text of Art. II.3 does not forbid it", and priced option (a) as "mostly a documentation clarification". Both were wrong, and wrong in the direction that made the decision look cheaper than it is. Option (a) means amending Art. II.3 to scope it to UI chrome — which **Art. XI.1 requires to be a dedicated amendment commit**, however small the wording change. The `api.ts` convention ("never echoes a raw backend message to the teacher") is an *additional* constraint that is project convention, not constitution; that half of the earlier claim was accurate. The original framing — "three documents disagree and cannot all hold" — was right.
  **Migration cost, so it is visible when directie rules:** under option (b) `diff.opmerkingen` and `problemen[].melding` stop being free text, so every test substring-matching them must be rewritten. That is **three** tests in `SchoolcontentImportEndpointsTests` — `Ongeldige_rij_wordt_precies_gerapporteerd_en_geldige_rij_gaat_door` and `Verwisselde_koprij_importeert_niets` (both on `problemen[].melding`) and `Geldig_bestand_dat_inhoud_laat_vallen_is_niet_volledig_verwerkt` (on `diff.opmerkingen[]`). The file's other four tests assert only booleans, `rijNummer`, emptiness or HTTP status, and survive option (b) untouched. *(An earlier revision said "four" — that was the count of stale `isGeldig` assertion lines fixed in `5d6f087`, a different quantity. A number offered as the measure of a decision's cost has to be counted, not recalled.)*
  **The cost has already moved once (2026-07-29, E3-01 audit).** The conflict is no longer confined to the *import* surface. E3-01 added **nine** new server-generated Dutch user-facing strings on the *planning* surface — in `JaarplanGeneratieService`, `SchooljaarBeheerService`, `Schooljaar`'s argument guards, and `JaarplanController` — plus a **new `PlanningExceptionHandler` whose job is to emit them**. So option (b) now means restructuring two payload families and a dedicated handler, not three import tests. This is the entry doing its job: *a decision whose cost grows every story is a decision that gets taken by default.* Each future story that emits Dutch from the backend should add a line here.
  **The ruling is still free of *UI* rework** — the frontend consumes none of this (zero references, no import keys in `nl.json`), so option (b) is still backend + tests only. That ends when **E1-13** ships, which is why E1-13 is written to isolate rendering behind one formatter module. Ruling before E1-13 is materially cheaper than after, and cheaper today than it was yesterday.
  > **⚠️ The paragraph above came within one line of being false, and E1-13 was not what nearly falsified it (2026-07-29, E3-06 audit).** E3-06's kalender rendered `PlanningsroosterWeergave.Blokindeling` — a Dutch label authored in `GeconfigureerdePlanningsblokIndeling` — straight into the page header. That would have made **E3-06**, not E1-13, the story that put server-generated Dutch in front of a user and put UI rework inside option (b). It was caught in audit and **reverted**: the kalender now explains the grain from `nl.json` and displays no server string, so option (b) remains backend + tests only. *(Citation corrected 2026-08-03 by the E3-08 round-4 audit: this line used to name `kalender.indelingUitleg` as the key carrying that explanation, and **E3-08 deleted that key** as a dead one — zero call sites, the last residue of the reverted render. The grain is now explained by `spine.titel` / `spine.titelFijn`, still from `nl.json`, still no server string, so the **principle** this paragraph records is untouched and only the citation had rotted. Worth noticing anyway: a dangling key name inside the record of an Art. II.3 near-miss is the least useful place to leave one.)* Two lessons worth keeping: the trigger was **not** the story this entry predicted, so "cheaper before E1-13" understates the urgency — *any* new screen can spring it; and a `blokindeling`-style descriptive label is exactly the shape that looks harmless and is not.
  > **E3-04 (2026-07-30) added no new user-facing backend Dutch, and the first draft did** — logged because the near-miss is the useful part. `ParameterRapport` originally returned composed sentences (`"Herfst @ 2026-09-01 (Schoolfeest)"`, `"Oudercontact @ 2026-11-02"`), which would have added **two** more free-text lists to option (b)'s migration surface and introduced a shape this entry had not catalogued: a server-authored **reason clause** (*which* teacher instruction caused a refusal), plus an **ISO date** no Dutch teacher reads. The audit caught it and every entry is now a record (`GeweigerdePlaatsing`, `VastMomentUitkomst`) that the UI renders from `nl.json`. Two things worth carrying forward: the `name @ date` form already used by `Duplicaten`/`Afgewezen` (E3-01) is the drift this was extending, so **those two remain on option (b)'s surface**; and the story that nearly sprang this was again *not* the one predicted — E3-06 nearly did it with a label, E3-04 with a report. **The prompt Dutch is deliberately not counted**, following the precedent that E3-01's own large Dutch `SystemPrompt` was never logged here: a prompt is model-facing, not user-facing.
  > **New backend Dutch added by E3-06** (per this entry's own instruction to log it): `PlanningsroosterService`'s `"Schooljaar {id} is niet gevonden."` fault, and `DemoDataSeeder`'s 7 `Motivaties` + its `Voorbeeldmarkering` prefix. The seeder's strings *are* teacher-visible (they land in `AiMotivatie` and render on a card), which makes them the first **demo-fixture** Dutch to reach a user — a third category this entry did not anticipate, and one that argues for option (c)'s actionable/diagnostic split being insufficient on its own.
  > **New backend Dutch added by E3-07 (2026-07-30), per this entry's own instruction to log it:** 2 `OngeldigeVerplaatsingFout` messages in `JaarplanGeneratieService.VerplaatsPlaatsingAsync` — a target date that starts no period (*"… is geen begin van een periode in dit schooljaar. Kies een periode uit het jaarplan."*) and a thema moved onto a period it already occupies. Both are **teacher-actionable** (pick another period), so under the unratified option (c) they are correctly Dutch; under option (b) they are 2 more strings to restructure. **Option (b) still costs no UI rework:** the frontend renders `kalender.verplaatsMislukt` / `kalender.verplaatsOnbeschikbaar` from `nl.json` and a test asserts the server's `detail` never reaches the screen. *(An earlier revision of this line said the frontend "branches on the 400"; at the time it branched on `isError`, so a 500 or a dropped connection told the teacher to "kies een periode uit dit jaarplan" — a retry that cannot succeed, and the exact conflation the generation panel four lines away carries a comment warning against. Found by the E3-07 antagonist audit and **fixed**: it now branches on `ApiError.status === 400`, so the sentence is true as written and pinned by a test.)* *The trend this entry exists to expose, stated numerically:* the backend's teacher-facing Dutch is now **9 (E3-01) + 8 (E3-06) + 4 (E2-08) + 2 (E3-07)**, plus a dedicated exception handler, accumulated across four stories while the decision sat open. Every one of them was individually defensible, which is the mechanism by which a default gets chosen.
  > **New backend Dutch added by E2-08:** 4 `OngeldigeDoelsubstitutieFout` messages in `DoelMatchingService.VervangSuggestieDoelAsync` (blank code, code not in the loaded Op.stap set, code unchanged, code already linked). All four are **teacher-actionable**, so under option (c) they are correctly Dutch; under option (b) they are 4 more strings to restructure. They reach the teacher only as `ProblemDetails.Detail`, and the frontend **does not render them** — it branches on the 400 and shows `matching.vervangenMislukt` from `nl.json` — so option (b) still costs no UI rework. The generation 422 is English (`"Invalid AI response"` + the parser diagnostic), matching E3-01's jaarplan 422 rather than the Dutch exception handlers, i.e. one more instance of the unratified option (c).
  > **New backend Dutch added by E1-15 (2026-07-31), logged per this entry's instruction even though the ruling has landed:** **8 strings across three files**, counted at the end of the story rather than at the end of its first round. **4** `ProblemDetails.Detail` strings in `OpstapImportController` (the request validations: missing disciplinenummer, no file, not `.xlsx`, unreadable workbook); **3** refusal messages in `OpstapImportFout` (Application), one per `OpstapImportFoutSoort` (the concordance FK, i.e. the E1-12 gap; an unknown discipline number; a code already loaded under another discipline); and **1** new `Title`, `"Import niet doorgevoerd"`, which lives in `Probleemtitels` with the shared titles.
  > *Where those 3 refusals live moved twice, and the count is logged with its history because the churn is the lesson.* As first built they were `Detail` strings in the controller. Fix round 1 moved them into `OpstapImportService`'s new preflight and, in the same commit, **duplicated** them in the SQLSTATE translator, so each case had two near-identical Dutch sentences of which the second is unreachable without a concurrent writer and therefore untestable. Fix round 2 collapsed them to **one factory per case** in `OpstapImportFout`, with a unit test that compares the two renderings structurally, which is the only level at which that divergence is observable. Net effect on this entry: the same 3 sentences, one source each, in `Application` rather than `Api` or `Infrastructure`. A count taken after round 1 would have said 7 in one file, and a count taken mid-round-1 would have said 10 in two; both were briefly true, which is why a figure in this entry needs the commit it was counted at.
  > All eight are **actionable by the person running the import**, which under the ratified Art. II.3 is exactly the Dutch side of the split, and the audience here is directie rather than a teacher: import is reference-data administration (Art. VI.1). Deliberately **left English**: `OpstapRijProbleem.Reden`, the per-row parse diagnostics the response carries. A malformed row in the *official* Op.stap file cannot be fixed by any user of this app, so it is an operator diagnostic — the mirror image of the school-content importer, whose Dutch `melding` describes a row the teacher wrote themselves. That asymmetry is intentional and is the first worked example of the ruling classifying two row-level diagnostics differently based on **who authored the file**; E1-13 clause 6 must not "fix" the English by translating it.
  > *Corrected and extended after the E1-15 audit (2026-07-31), because the count above measured the wrong thing.* Counting only what a story **authors** understates what it puts in front of a user. E1-15 also made **2 pre-existing Dutch `OpstapHerimportDiff.Opmerkingen` notices reachable for the first time** — the out-of-scope-discipline notice and the empty/implausible-file notice in `OpstapImportService` — which had been unreachable free text since E1-05 and are now returned by an endpoint that E1-13 clause 6 renders. Both were **rewritten in the same round** to drop their em dashes (Art. II.5) and the out-of-scope one no longer tells a Dutch reader to edit a configuration key, which mixed the two Art. II.3 audiences in one sentence; the key now lives in a code comment for the operator. Running total of teacher/directie-facing backend Dutch: **9 (E3-01) + 8 (E3-06) + 4 (E2-08) + 2 (E3-07) + 8 authored and 2 exposed (E1-15)**. *The lesson for the next story, and the reason this correction is here rather than in a worklog:* "did I add a Dutch string?" is the wrong question. The right one is "did I make one visible?", and a story that only adds a caller can fail Art. II.5 without touching a single literal.
  > **E1-13 (2026-07-31) is the first story to put any of this on a screen, and it authored no new backend Dutch at all.** Logged per this entry's instruction, and counted the way the E1-15 correction above demands: by what became **visible**, not by what was written. Now rendered to a user for the first time: **`problemen[].melding`** (the school-content parser's per-row diagnostics, 6 distinct sentences plus the composed required-field one), **`diff.opmerkingen[]`** on **both** importers (**4** school-content notices, 2 Op.stap notices), the **3 school-content `ProblemDetails.Detail`** request validations, and **7 of E1-15's 8** strings (the 4 Op.stap request validations and all 3 `OpstapImportFout` refusals; only the `Title` stays unrendered, because the screen writes its own heading). So the "8 authored and 2 exposed" line above is now, in practice, **8 authored and ~15 exposed** — every backend Dutch string in the two import paths reaches a reader.
  > *What that exposure immediately cost, which is the point of keeping this entry:* **4 product-copy defects** that had sat harmlessly in unrendered free text. Three `SchoolcontentImportService` notices carried an **em dash** (Art. II.5, product-wide since 2026-07-30) and the template generator's `Klas` example value carried one too — and that value is *copied into a cell that must match a stored `Klas.Naam`*, so it was becoming data, not caption. One notice used a **`(s)` plural dodge** where Dutch needs two sentences, which no frontend `tAantal` can rescue because the server composes it. One told a teacher *"(een subthema is klas-gebonden, Art. IX.2)"*, mixing both Art. II.3 audiences in one sentence; the article reference now lives in a code comment, exactly as E1-15 did with its configuration key.
  > **Corrected after E1-13's audit (fix round 1): there are five defects, not four, and `SchoolcontentImportService` has *four* reachable notices, not three.** The line above said "all four fixed, with four unit tests asserting the three reachable notices". Both numbers were wrong in the same way. `PasThemadoelCapToe`'s notice, reachable by any file listing four or more themadoelen on one thema, still read *"geankerd (Art. IX.2)"* after the other three were rewritten, and the story's own `AssertLeesbaarVoorEenLeerkracht` had been applied to three of the four. Fixed in fix round 1. A **fifth** defect surfaced in the same sweep and belongs in this count: `OpstapImportService` composed *"De 1 bestaande doelen blijven ongewijzigd"*, the plural bug this repo has now shipped six times, on a notice E1-13 renders.
  > *The lesson is about the shape of the guard, not the count.* A guard applied notice by notice can only cover the notices someone remembered; the replacement drives **one** import that trips three of the four sources at once and asserts the predicates over *every* opmerking the diff carries, so a fifth source on that path fails without anyone remembering. And the sweep that found the miss was **calibrated against the unfixed commit** before being believed: 73 backend product-copy literals plus all 431 `nl.json` leaves, reporting exactly the one known violation there and zero after. A check that has only ever returned zero has not been shown to be able to return one.
  > *One thing the ruling did not anticipate, filed here because it is a new category:* `SchoolcontentRijProbleem` gained a derived **`KolomLabel`**, i.e. Dutch that is neither UI copy nor a diagnostic but a **column heading from the teacher's own file**, read from the single-source column mapping. It travels server-side deliberately: a frontend name→label table would have been a second copy of the Excel layout (Art. III.3). It is Dutch the project writes and a user reads, and it is *not* prose, so option (b)'s codes-plus-parameters framing would have had nothing to say about it.
  > *And one that is Dutch nobody wrote:* the `<input type="file">` button label. On an `en-US` browser it reads "Choose File" on an otherwise Dutch screen. Verified in E1-13's browser check to be the **browser's** string, not ours, by diffing the same page under `--lang=en-US` and `--lang=nl-BE`. Same category as the `<input type="date">` E3-04 ships. Art. II.3 binds copy the frontend authors, so it is out of scope; recorded because it is the kind of English word on a screenshot an audit will reasonably stop at.
  > *Also fixed rather than logged (E1-15 audit):* the same title literal `"Ongeldige aanvraag"` was defined independently in **five** files (three exception handlers, two import controllers), so the API could have started answering two different Dutch titles for one class of fault after any single reword. *Corrected 2026-07-31 (re-audit): an earlier revision of this line said "E1-15 nearly made it six". Verified against git instead of recalled: **4** files carried the literal at `b44c869`, **5** at `57a21b1`, and the fifth was E1-15's own controller. E1-15 made it five; there was never a sixth.* The titles now come from one place, `Api/Infrastructure/Probleemtitels.cs`, which is the Art. II.3 principle applied to the payload the frontend never authors. `Detail` text stays where the fault is raised: only there is the row number, the discipline or the offending code known.
  > **New backend Dutch added by E4-03 (2026-08-04), logged per this entry's instruction even though the ruling has landed: 2 strings, and 0 of them become visible.** Both are `OngeldigePlaatsingFout` factories in Application (the chosen period starts no current block; that thema is already in that period). Both are **actionable by the teacher**, so under the ratified Art. II.3 they are correctly Dutch, and under option (b) they would be 2 more to restructure. Counted the way the E1-15 correction demands, by what a user can *read*: the frontend branches on `ApiError.status === 400` and renders `kalender.plaatsMislukt` / `kalender.plaatsOnbeschikbaar` from `nl.json`, so **neither sentence reaches a screen** and option (b) still costs no UI rework on this surface.
  > *One deliberate non-instance, recorded because it is the shape this entry keeps catching:* the move path's equivalent refusal interpolates `JaarplanGeneratieResponseParser.DatumFormaat` into its sentence, which is an **ISO date no Dutch teacher reads** (the same defect E3-04's first `ParameterRapport` draft had). It is harmless only because nothing renders it. Rather than add a second string that is safe purely by not being displayed, E4-03's two name **no date at all** and say what to do next. Both were also written so that a future decision to render `detail` would not spring an Art. II.5 or date-format defect at the same time.
  > *Not counted as backend Dutch, and worth stating so the boundary stays legible:* E2-08 puts the leerplandoel's own `tekst` into the doelsuggestie payload and renders it. That is **official Op.stap reference content** — domain data the product exists to display, like a thema's naam or a vakantie's naam — not authored UI copy, and `OngekoppeldeDoelenLijst` has rendered the same field since E2-06. The `aiMotivatie` next to it is model output, likewise not authored. This entry is about Dutch *the project writes*; if a future ruling wants to cover displayed domain data too, that is a materially larger question than the one framed here.
  > *…with one caveat that belongs in the demo-fixture category above (logged 2026-07-29, E2-08 audit):* the "domain data, not authored copy" reading holds for **imported** Op.stap content, but `DemoDataSeeder.cs:239` **authors** Dutch straight into `Leerplandoel.Tekst` (`"Voorbeelddoel {i} — demodata voor de review, geen Op.stap-leerplandoel."`), and E2-08's suggestie list now renders it next to real goal text. No new category — it is the same demo-fixture Dutch as the seeder's `Motivaties`, and it predates this story via `OngekoppeldeDoelenLijst` — but it is project-written Dutch reaching a teacher through a field this entry classifies as domain data, so option (b) would either have to exempt it or restructure the seeder. Logged because the line above asks for exactly this.
  **A third option has appeared in the code without anyone choosing it (2026-07-29, E3-01 audit) — ratify or reject it.** E3-01 made its AI-failure 422 **English** on the stated reasoning that translating `"Malformed JSON: …"` would mean inventing new backend Dutch, while the *same commit* added new Dutch teacher-facing text in `KlasBeheerService`. The audit judged the English payload defensible on its own terms (it is internally consistent, and Art. II.3 binds `nl.json`, not `ProblemDetails.Title`) but found the *argument* inconsistent. What the code now actually implements is a rule nobody ratified:
  > **(c) Dutch when the teacher can act on it; English when it is a model or operator diagnostic.**
  That is a defensible principle — a validation message a teacher must act on *should* be Dutch, and an AI-response parse failure is not something a teacher can fix. It is also the reason the backend is currently three-way inconsistent: Dutch titles in three exception handlers, English title+detail in the jaarplan 422, Dutch fault messages from the services. **Decide (a), (b) or (c) explicitly**, because until then each new handler is a coin flip and the inconsistency is being written into the codebase one story at a time. Recommendation: **(c) plus (a)** — ratify the actionable/diagnostic split *and* amend Art. II.3 to scope it to UI chrome, since (c) only makes sense if server-generated Dutch is permitted at all.

  </details>
- **Discipline 9's official name / the 9.x nesting.** The Art. VII.0 list has no bare `"9"` row and never names it, so 9.1/9.2/9.3 are seeded with `parentDiscipline = null` and **nothing** in the codebase ever sets that column. Art. XII meanwhile describes 9 as a subject that *is split*. Directie to supply discipline 9's official name, or confirm 9.1/9.2/9.3 are genuinely top-level. Until then any UI grouping of 9.x is impossible.
- **Does seeding the full taxonomy pre-empt "disciplines first"?** The migration seeds all 13 disciplines unconditionally, without consulting the [ADR-0019](../docs/adr/0019-discipline-selection-config-seam.md) `IDisciplineSelectie` seam. The reading taken is that the seam scopes which disciplines' **goals** are imported, while the discipline rows are the authoritative taxonomy (Art. VII.0) that the FK cannot do without. Consequence to confirm: any UI listing `disciplines` will show all 13, so a later "starter selection" answer becomes a filtering concern rather than a config change.
- **Is "2–3 themadoelen" (Art. IX.2) a range or just a maximum?** Only the upper bound (3) exists in code. A thema imported with 0 or 1 themadoel lands silently, un-anchored, while an over-anchored one is reported. Either the minimum should be reported too, or Art. IX.2 should say 2 is a pedagogical guideline rather than an invariant.
