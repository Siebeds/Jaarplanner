# E4 — Manuele bewerking & (her)generatie

**Phase:** 4 · **Milestone:** M4 — Volledige controle
**Goal:** The teacher keeps full control: anything the AI proposed can be overridden manually, and the plan can be regenerated whole or per period, with locked blocks preserved and a preview before applying.
**Covers FR:** FR-7, FR-8. **Constitution:** [Art. IV.1](../CONSTITUTION.md#article-iv--ai-is-advisory-human-in-the-loop) (human-in-the-loop), [Art. V](../CONSTITUTION.md#article-v--coverage-must-be-provable-dekking).

---

### FR-7 — Manual edits

- [x] **E4-01 — Immediate persistence + live coverage reflection** — *done 2026-08-04 on `story/E4-01-live-dekking` (off `origin/main` `7f6d8d9`, so E5-02 and E4-03 are both in): `c8aacff` (the end-to-end proof), `7074328` (the client half), `a938b1b` (the record), `618f129` + `f52db80` (fix round 1), `5e42a76` (fix round 2). **test-runner PASS; antagonist ronde 1 and ronde 2 both VIOLATIONS FOUND, all addressed** ([`worklogs/E4-01/antagonist.md`](worklogs/E4-01/antagonist.md), [`antagonist-ronde-2.md`](worklogs/E4-01/antagonist-ronde-2.md)). Narrative: [`worklogs/E4-01/implementation.md`](worklogs/E4-01/implementation.md).*
  > **⚠️ This `[x]` carries the same caveat E5-01's did, and it is recorded rather than glossed: the owner ruled it green without a third audit round (2026-08-05), so fix round 2's own fixes were verified by tests, by a browser pass and by their author, not by an independent pass.** What that leaves open is specifically the class both earlier rounds kept finding: prose and copy that claim more than the code does. The mechanism itself was attacked twice and held twice, and every code-level finding was mutation-checked in both directions. Stated because a reader five stories from now cannot tell a ruled `[x]` from an audited one, and this backlog has had to retract counts before.
  Every manual change (move/add/remove) saves immediately and is reflected in the dekkingsoverzicht.
  *Done when:* a drag or edit updates persistence and coverage without a manual save. Ref: FR-6.5, FR-7.
  > **What was actually missing was on the client, and it was invisible to every test that looks at the kalender.** The note below was right that the server needs nothing: dekking is computed on every read (Art. V.1). But the dekkingsoverzicht is a **different route**, so while a teacher edits, its query has no observer and TanStack keeps the last answer; navigation between the two screens is client-side, so that answer is what `/dekking` paints on arrival. A figure computed **before** the edit, with no loading state to say so, and if the refetch then fails it stays on screen beside the error. Fixed by dropping the class's cached dekking from the shared placement-mutation hook (`removeQueries`, **not** `invalidateQueries`: invalidation leaves the stale answer to be painted for the length of one request). That is the trade-off `DekkingPagina` already makes for itself, which renders `isPending` on a scope switch rather than keeping a figure computed over another denominator.
  > *Verified in a browser against a real API and real PostgreSQL, navigating through the nav rather than reloading, because a reload resets the cache the defect lives in:* accept → **0 van 14 → 2 van 14**, move → **4 van 14**, remove → back to **2 van 14**, each with the loading line and never the previous figure. `window.fetch` was patched in the page to delay the dekking read by three seconds, because otherwise the stale window is too short to look at, which is exactly why this would never have been caught by looking.
  > *The sequence half of the criterion is now pinned where it was never pinned:* five HTTP sequences against real PostgreSQL (`DekkingNaBewerkingTests`), each one write followed by one dekking read with nothing in between. Two of them earn their keep beyond this story: the **move** case pins that a drag raises the figure as a side effect (the placement becomes `manueel`), and the **stale** case pins the half **E5-01** left unverified, that resolving a stale placement *releases* the figure it was withholding. Every dekking test before this seeded placements straight through the `DbContext`, which cannot evidence a claim about an edit.
  > **Both gates ran and fix round 1 landed (2026-08-04): test-runner PASS on every criterion, antagonist VIOLATIONS FOUND (1 MAJOR, 6 MINOR, 1 QUESTION), all answered.** *A second antagonist round is owed*, because the fix round touches user-facing copy. **The MAJOR was a comment in this story's own new test**, claiming the card discloses the dekking consequence before a drag: `kalender.verplaatsGevolg` names the status change and the lost motivation, says nothing about dekking, and renders only inside the opened panel, so a teacher who **drags** never reads it. A standing obligation from this story's own entry, described by my comment as discharged. **Owner ruling: write the copy here**, which it now does, in two existing keys and no new one: `beslisUitleg` carries the drag route once above the board, `verplaatsGevolg` carries it at the point of action. A guard in `catalogus.test.ts` pins both, including the two deliberate wordings: **"AI-voorstel"** rather than *thema* (a `geweigerd` card cannot be dragged, so a general promise is false in exactly that state) and *"een eigen keuze telt mee"* rather than *"het telt daardoor mee"* (an already **aanvaarde** placement counted before the move, so naming the move as the cause is the false attribution E4-06 needed three rounds to remove from the lock copy). Read in a browser at 1440px and 390px.
  > **Second owner ruling, and it grew the story: the same stale figure lived on the *link* path, and that is fixed here rather than filed.** A themadoel and an accepted or adjusted doelsuggestie are counted `DoelKoppeling`s, so `/themas` moves the same figure. The **test-runner reproduced it in a browser** and corrected the fix that was about to be written: a link hangs on a **school-wide** thema, so the class-scoped key is too narrow and the whole `["dekking"]` subtree has to go. `useVerwijderThema` deliberately does not drop, and that is a claim about the server: a delete is refused while the thema sits in any jaarplan, and a thema in no jaarplan covers nothing. *The lesson worth keeping:* `useThemas.ts` had **predicted this and named its own successor** (*"whoever adds it should add its key here"*); E5-02 shipped the query and this story exported the key the same day, so the sentence became false while the obligation stayed unmet. A note that names its successor only works if the successor reads it.
  > *Two of this story's own claims were weaker than stated and are now stronger:* the client-half mutation check covered **one of two call sites** (deleting the call in `useGenereerJaarplan` left all 439 tests green), and the three original tests asserted the **cache** rather than the promise. There is now a test that edits on the kalender, unmounts it, mounts `DekkingPagina` on the same client and asserts the overview shows its own loading line and never the pre-edit total. **It fails under `invalidateQueries`**, so the argument that was a docstring is now a test.
  > *One measured cost, recorded rather than argued away (test-runner):* a **lock toggle** cannot change coverage and still forces a loading line for an unchanged figure. Kept, because the alternative is a per-edit rule nobody can state. A **weigering** is not in that category: it resolves a stale placement and therefore releases a withheld figure.
  > **Antagonist ronde 2 (VIOLATIONS FOUND, 2 MAJOR + 9 MINOR) found that fix round 1's copy was in the wrong string, and it is the sharpest finding of the story.** The clause went into `kalender.beslisUitleg`, which renders on **every** tier because a decision is available on every tier. Moving is not: at `Subthemaperiode` the card has no grip and the panel no picker, and at an unrecognised tier nothing can be moved at all, where the promise landed one paragraph under *"thema's verplaatsen kan hier niet"*. **The comment three lines above the render site had said exactly this** (*"'of zelf verplaatsen' is only true on the tier where moving works"*) and the fix walked past it. The clause now lives in `kalender.sleepUitleg`, the `kan` entry of `BORDUITLEG`, which is the mechanism this file already had for the hazard. *The lesson, now written into the file:* a sentence about an affordance belongs in the record keyed on that affordance's state, not beside the topic it shares. Pinned by a test that **renders both tiers** and keys on the clause rather than on the key, plus a catalogue guard that refuses the clause in the tier-independent sentence. Two of ronde 2's questions dissolved with the move: no third paragraph above the board, and the wording is now *themaperiode* like its siblings.
  > *Ronde 2's second MAJOR, also fixed here:* the **importers** only invalidated, so a commit on `/import` left `/dekking` painting a pre-import figure. An import is the write that can move the figure furthest, because it writes both sides of it: counted koppelingen and, on the curriculum side, the denominator. *And two claims of mine that were weaker than written:* the behaviour test failed under `invalidateQueries` at its **cache** assertion, before the screen was ever mounted, so it discriminated nothing that the three earlier tests did not; it now waits on the card and fails on the loading-line assertion instead. The copy guard was mostly verbatim fragments of the string it checked, i.e. the tautology E4-02 recorded; it now pins one property and says plainly that a catalogue test cannot see truth-in-state.
  > *`kalender.vergrendelDekking` was re-read as its own comment demanded and deliberately left alone:* naming the second counting route there was tried and reverted, because that paragraph carries no tier condition, so it stays **under-inclusive rather than false**. Recorded on the string.
  > *Left deliberately unclaimed:* no minimumdoelniveau (E5-04, blocked on E1-12), no change to E5-02's screen, and no new control. "Reflected in the dekkingsoverzicht" is satisfied by the nav item E0-10 shipped.
  > **The coverage half is no longer blocked (2026-08-03, E5-01 closed), and it turns out to be mostly already satisfied.** It was recorded as "de facto blocked on E5, which is not built at all". Dekking now exists and is **computed on every read** with no cache and no invalidation step (Art. V.1), which is precisely what "reflected without a manual save" requires: a drag persists, and the next dekking read already accounts for it. There is nothing to wire up and no recalculation to trigger. **What is left for this story is to prove it end to end** — move a placement, then assert the coverage figure changed — and to decide what "reflected in the dekkingsoverzicht" means while **no dekkingsoverzicht screen exists** (E5-02/E5-03). Do not mark the screen half done on the strength of the API.
  > *One interaction worth knowing about:* a drag sets the placement to `manueel` (`Themaplaatsing.VerplaatsNaar`), and `manueel` **counts** for dekking. So dragging a `voorgesteld` thema raises the coverage figure as a side effect of the move. That is correct under Art. V.1 and it is also a real behaviour a teacher will notice, so it belongs in whatever copy this story or E4-02 writes.
  > ~~*And the standing obligation from E4-06 still stands:* this story and E4-02 must build the **accept affordance**.~~ **Discharged by E4-02 on 2026-08-03**, and struck rather than deleted because the sentence that follows it was the sharpest statement of why it mattered: *until one exists, a teacher cannot move a placement off `voorgesteld` except by dragging it, which means the only route to any dekking at all is a drag.* That is no longer true. "Aanvaarden" and "Weigeren" sit on the card face, and pressing Aanvaarden was measured taking dekking from 0 of 2 to 2 of 2 against a real database. **What is still E4-01's** is the other half of its own criterion: proving a *move* is reflected end to end, and deciding what "reflected in the dekkingsoverzicht" means while no such screen exists. *(Both discharged 2026-08-04, see the notes above. The second clause was overtaken by events rather than answered: **E5-02 merged the same day**, so the question stopped being "what could it mean" and became "does it hold", and it did not until the client stopped caching the old figure. Kept unedited because it dates from when the screen genuinely did not exist.)*

- [x] **E4-02 — Override any AI suggestion** — *done 2026-08-03 on `story/E4-02-aanvaarden`: `3795c16` (story), `57b79c0` (merge of `origin/main` `fcb517f`), `cd6e3e0` / `447fe0a` / `c6b0dde` / `e47e7e8` (fix rounds 1–4). **Four antagonist rounds, all VIOLATIONS FOUND, all addressed** ([`worklogs/E4-02/antagonist.md`](worklogs/E4-02/antagonist.md)); **test-runner PASS on all nine claims, every figure exact** ([`worklogs/E4-02/test-report.md`](worklogs/E4-02/test-report.md)). Narrative: [`worklogs/E4-02/implementation.md`](worklogs/E4-02/implementation.md).*
  Anything proposed by AI can be manually overwritten; status moves to `manueel`.
  *Done when:* overriding a `voorgesteld`/`aanvaard` link sets `manueel` and sticks. Ref: FR-7.1, Art. IV.1.
  > **What this story turned out to be, and it is not what the line above says.** The DoelKoppeling half was **already built** (E2-06/E2-08 send `Aanvaard`/`Geweigerd`/`Manueel`). What was missing is the **decision surface on the kalender**, which is the obligation E4-06's owner ruling of 2026-07-31 assigned to E4-01/E4-02. `Themakaart.tsx` sent exactly one status, `"Manueel"` (the un-reject), while the server, endpoint, `api.ts`, hook and badge all existed. **Fifth instance of the E2-08 / E1-15 / E0-10 / E4-06 pattern.** Two consequences: a generated jaarplan reported **0% dekking**, because only `Aanvaard`/`Manueel` count as placed and E5-01 now computes that; and **`Geweigerd` was unreachable**, so the whole rejected-card branch — which E4-06 spent three audit rounds perfecting — was code for a state no teacher could produce. Both halves of the pair were therefore built.
  > **The design, and the one subtle rule.** Decisions on the card **face**, adjustments behind "Aanpassen": the panel held six actions of two kinds and reviewing a dozen proposals meant opening a dozen disclosures. Weight follows `DoelsuggestieLijst`, **which is why "Verplaatsen" is no longer the default variant**. On a **stale** card the halves part company: **aanvaarden is withheld** (it would label a card "Aanvaard" that covers nothing and still withholds the figure) while **weigeren is offered**, because `DekkingService` counts `IsVervallen && !IsGeweigerd` as unresolved, so a weigering is what *resolves* a stale proposal. The first version withheld both — one flag, two cases — which was round 1's MAJOR.
  > *Verified independently, not self-reported:* pressing "Aanvaarden" moved dekking **0 of 2 → 2 of 2** through E5-01's real computation against real PostgreSQL; rejecting a stale proposal took `isBetrouwbaar` false→true and `onopgeloste` 1→0; the `Voorgesteld → Geweigerd → Manueel` round trip ran **by keyboard only**; contrast measures 8,90:1 and 3,40:1; 314 tests, lint and build clean. *Qualification the verifier added:* that is **leerplandoel**-level only — `minimumdoelen` has no rows (E1-12) and no screen shows the figure yet, so it is verified at the API, not as something a teacher sees.
  > *Hand-offs, written into their destinations rather than named:* **E7-10** (SC 2.5.8's 61×16 targets, SC 2.5.3), **E5-02** (the `herzienUitleg` divergence, plus the owner's ruling of 2026-08-03 that a rejected stale placement leaves the figure trustworthy, so the **copy** is what changes), **E3-07** (this story *enlarges* what it owes: `Geweigerd × vervallen` is now one press away), **E4-01** (accept affordance discharged), **E4-05/E4-07** (two of E4-06's six strings changed; read the file, not the quotation).
  > **The three lessons, because rounds 3 and 4 found no defect in the screen at all — only in prose this story had added.** *(1)* Re-derive every claim about the repo against `HEAD` when you commit it, and write the command beside it: my "`indelingUitleg` is dead" filing was true pre-merge and false after my own merge, and I asserted it twice more, once into E3-06's entry. *(2)* Ask what property a new test adds — mine asserted which key renders where, a tautology, while the property the fix existed for was unpinned, and an auditor restored the defect with the suite green. *(3)* Rewrite the whole sentence or do not touch it: three of four documentation defects were partial in-place substitutions.

- [x] **E4-03 — Manual add/move/remove independent of AI** — *done 2026-08-04 on `story/E4-03-handmatig-plaatsen`. Narrative + full measurements: [`worklogs/E4-03/implementation.md`](worklogs/E4-03/implementation.md).*
  Add/move/remove thema's, activiteiten, and goal links by hand, with no AI involved.
  *Done when:* a fully hand-built plan is possible. Ref: FR-7.2.
  > **FR-7.2 is a 3×3 matrix and eight of the nine cells were already built**, which is why this story is small in code and was worth measuring before writing any. Move, remove, accept/reject and lock a placement all existed (E3-07/E4-02/E4-06); E1-14 had landed the activiteit and doelkoppeling halves hours earlier, writing hand-made links as `Manueel`. The one missing verb was **putting a thema into a period by hand**, and it was missing completely: `Jaarplan.VoegPlaatsingToe` was reached from exactly **two** callers in the repository, plan generation and the demo seeder, so the only way a thema had ever entered a jaarplan was an AI run. *"A fully hand-built plan is possible"* was impossible as written. **Sixth instance of the reachable-vs-tested pattern** (E2-08, E1-15, E0-10, E4-06, E4-02), with a twist: the domain method existed and enforced its invariant correctly, so nothing was missing at the level a unit test looks at. Every layer above it was.
  > **What it lands as, and every consequence is intended:** `Manueel` with no `aiMotivatie`, so it **counts for dekking** (Art. V.1) and **survives a regeneration** (Art. IX.3, it is not `IsVervangbaar`). Fixed to the themaperiode tier, because a placement keys on a themaperiode start (ADR-0020 §3) and a fine-tier start would record five weeks where the teacher aimed at a fortnight, which is E3-08's own reasoning reused rather than a second answer to the same question. `POST …/jaarplan/plaatsingen` is **the only placement endpoint that creates the jaarplan when the class has none** — that is the story, not a convenience.
  > *Verified end to end in a browser against real PostgreSQL:* pressing "Toevoegen" on a class took **dekking from 0 to 2 of 14 with both covered doelen naming the placed thema as evidence**, with no AI call and no accept step. That also demonstrates **E4-01's criterion for an *add***; E4-01 still owns proving it for a *move*. Gates: 542 unit + 180 integration (0 skipped, real Postgres), 388 frontend, format + lint + build clean, axe **0 violations in a real browser** with the picker open, 390px clean, contrast composited and measured (15,42:1 label / 3,21:1 boundary).
  > **The defect worth copying, because 388 green tests could not see it:** `sluit()` called `trigger.current?.focus()` directly, and `setOpen(false)` is batched, so the trigger was still unmounted and the ref null. Focus fell to `<body>` and a keyboard user pressing "Annuleren" lost their place on a sideways-scrolling board, **while the comment beside that line claimed to prevent exactly that**. Found in one browser probe, fixed with a guarded effect, re-measured on both exits, and pinned by a test that was then mutation-checked against the original broken form. Every other new claim was mutation-checked too, in two directions, and **one flaky test was caught before it could mislead anyone** (it sorted the actual against an unsorted expected of two random Guids).
  > *Scope boundary, stated because the matrix is the only place it is visible:* **"activiteit verplaatsen" (to another subthema) is not built** and is not claimed by this story. FR-7.2's verbs apply to all three objects and no endpoint moves an activiteit between subthema's. **It is now owned by [E4-08](#) below**, filed 2026-08-04.
  > **Correction (2026-08-04, antagonist ronde 1, and it is the finding worth keeping).** This bullet originally ended *"That surface is E1-14's"*, and the README claimed both of this story's scope gaps were *"recorded against the stories that own them"*. **Both were false, and a `grep` was all it took:** neither gap appeared anywhere but E4-03's own entry and worklog, and **E1-14 is `[x]`**, so an unbuilt FR-7.2 verb was handed to a story that cannot pick it up. Net effect before the fix: this story sat `[x]` against an FR whose verb list it does not fully satisfy, with **no open item owning the remainder**. The disclosure was honest; the routing was invented. Third instance of this class in three days, and the cheapest to catch: **if you write that something is recorded elsewhere, grep for it before you write it.** Resolved by filing E4-08 (owner ruling, 2026-08-04) rather than by softening the sentence.
  > *What the `[x]` does and does not mean, now stated rather than implied:* it is earned against this story's own *Done when* — *"a fully hand-built plan is possible"* — which was demonstrated end to end (a class with no plan, no AI call, dekking 0 → 2 of 14 with the placed thema as the evidence). It does **not** assert that all nine of FR-7.2's verb×object cells exist. E4-08 holds the ninth.
  > *Obligation handed to **E4-05**:* `kalender.plaatsGevolg` is scoped to *"een hergeneratie van het hele jaarplan"* per E4-06's rule, so it joins that story's list of strings to re-read when the per-period path lands.

### FR-8 — (Re)generation

- [x] **E4-04 — Regenerate the whole plan** — *done 2026-08-06 on `story/E4-04-hergeneratie` (off `origin/main` `59183ad`): `ff47067` (the story), `4926852` (fix ronde 1), `381a9c9` (fix ronde 2). **Two antagonist rondes, both VIOLATIONS FOUND (3 MAJOR + 4 MINOR + 2 QUESTION, then 1 MAJOR + 2 MINOR); every finding fixed or routed.** Narrative + full measurements: [`worklogs/E4-04/implementation.md`](worklogs/E4-04/implementation.md).*
  > **⚠️ Read the `[x]` precisely: it is an owner ruling of 2026-08-06 taken without a third round, so fix ronde 2 has had no independent pass.** Same caveat as E5-01, E4-01, E4-08 and E3-03, and on this story it is a measured risk rather than a formality: **both rounds found their MAJOR in the previous round's fix, in the same paragraph, pointing opposite ways.** What ronde 3 would have attacked is specifically the round-2 rewrite of `kalender.hergenereerUitleg` and the two `genereerVervangen*` keys. Against that: ronde 2 re-ran every gate itself rather than reading them off the worklog, it stated per finding which of round 1's were genuinely closed instead of crediting the attempt, the round-2 fix changed the sentence's **shape** so the failure mode it closes cannot recur by drift, and the state both findings were about was driven in a browser afterwards. Stated because a reader five stories from now cannot tell a ruled `[x]` from an audited one.
  > **Both rondes found the same class of defect in the same paragraph, pointing opposite ways, and that is the story's
  > one durable lesson.** Ronde 1: the copy promised replacement where the code guarantees only deletion. Ronde 2: the
  > fix then said *"AI-voorstellen waarover je nog niets beslist hebt, verdwijnen"*, which is **false for a locked
  > proposal** — undecided by definition, since `vergrendelUitlegVrij` tells the teacher to lock precisely *"zonder er
  > nu al over te beslissen"*, and kept by `IsVervangbaar`. The paragraph then contradicted itself two clauses later,
  > where *"vastgezet"* stands among the survivors, so a teacher who used the lock as advertised read both that their
  > thema disappears and that it stays. **This story's own test rendered that exact fixture and asserted only that a
  > sentence appeared.** *Fixed by changing the shape rather than adding a condition:* what is lost is now the
  > **complement** of what is kept (*"De overige AI-voorstellen verdwijnen…"*, after the survivor list). Two lists can
  > drift from each other, as these did twice in one day; a complement cannot. Pinned by an **order** assertion, the
  > single line that fails when the clauses are swapped with nothing else changed.
  > *One out-of-scope edit, declared rather than slipped in:* `kalender.genereerVervangen` (E3-02's run report, three
  > lines below the disclosure) still said *"zijn vervangen"*, so on an empty AI answer the card read *"De AI stelde
  > geen enkel thema voor."* directly above *"6 eerdere voorstellen zijn vervangen."* Both keys now say
  > *"verdwenen"*. Aligning beat routing: two words for one event on one card is the drift these guards exist to stop.
  > **All three MAJORs were in the disclosure or in prose about it, which is the whole story, so none was cosmetic.**
  > *(1)* The copy said untouched proposals *"worden vervangen door nieuwe voorstellen"* while the discard is
  > **unconditional** on a valid parse and happens before anything is placed: an empty, fully-skipped or fully-blocked
  > answer deletes them and puts nothing back. **The counterexample was inside the commit** — this story's own new
  > endpoint test posts `{"plaatsingen":[]}` and asserts `aantalVervangen 1`. It also stated the *certain* half as a
  > swap and the *uncertain* half flatly, inverting the risk, and was stronger than `vergrendelUitlegVrij`'s
  > *"kan het vervangen"* two keys away. Now *"verdwijnen, ook als de AI er deze keer minder of geen voorstelt"*.
  > *(2)* Replacing the first-run sentence was justified by the board's `beslisUitleg` carrying *"jij beslist"* — but
  > that is gated on `openBeslissingen > 0` by E4-02's own re-audit, so on a **fully decided plan**, the likeliest
  > state to regenerate from, **neither** sentence rendered and nothing said the arrivals are still proposals
  > (Art. IV.1/IV.2). The clause now lives in the string, which no other component can gate, plus a fifth test that
  > asserts its own precondition. *(3)* *"No server test read `aantalVervangen`"* was false in three places (E4-06's
  > lock test asserts it forty lines above mine) and the risk I drew from it was wrong: only the **serialized** name
  > was unpinned, since a property rename breaks the build.
  > *The four MINORs, each a real hole:* the new guard's docblock argued a non-vacuity canary was impossible and then
  > wrote one; the guard's pattern required adjacency, so **FR-8.1's own phrasing** *"opnieuw laten genereren"* —
  > quoted twice in this change — escaped it; the copy promises four survivors and the row-level test covered two,
  > with `Geweigerd` (the survivor a teacher cannot verify by looking) left at the fake-port level; and *"zelf
  > geplaatst"* did not cover a **dragged** thema, which becomes `Manueel` and survives.
  > **Two QUESTIONs are the owner's**, both recorded rather than acted on: a directie ruling on E4-07's
  > preserve/overwrite rule would now falsify a primary-screen sentence as well as E4-06's six card-level ones (this
  > story adds no new commitment, it keeps the *"hele jaarplan"* qualifier); and the *Te herzien* panel does not say
  > that regenerating resolves an undecided stale card by deleting it, though the new copy covers the fact.
  Re-run generation for the entire class plan.
  *Done when:* full regeneration produces a new proposal. Ref: FR-8.1.
  > **The run was already repeatable; what did not exist was any way to know that before pressing.** `GenereerAsync`
  > has discarded exactly `IsVervangbaar` (`Voorgesteld && !Vergrendeld`) since E3-01, kept every decided or locked
  > placement, and returned `AantalBehouden`/`AantalVervangen`, which `Spreidingsoverzicht` has always rendered. But the
  > button read *"Jaarplan genereren…"* on the second press exactly as on the first, and the only statement about the
  > replacement was **past tense, afterwards**. FR-8.1's own wording is *"het volledige jaarplan **opnieuw** laten
  > genereren"* and the word *opnieuw* was nowhere on the screen. A teacher reviewing proposals over an afternoon and
  > pressing again to fill the periods the model skipped would have lost every proposal they had not yet decided on.
  > **Seventh instance of the reachable-vs-tested pattern** (E2-08, E1-15, E0-10, E4-06, E4-02, E4-03) and the mildest:
  > the path was reachable *and correct*, it simply misdescribed itself.
  > **The decision inside the copy, because it is the one a later story could undo.** The disclosure keys on *"does this
  > class have a plan"*, never on *"is anything replaceable"*. The second question is the server's `IsVervangbaar`, and
  > answering it in the client would be a second implementation of one rule on the screen E3-09 spent a whole story
  > de-duplicating. So the sentence states a **rule** (true in every state, including the one where nothing is
  > replaceable) rather than a **prediction**, and a test pins that deliberately. Counting what will change, and
  > offering a cancel, stays **E4-07**.
  > *Two pieces of proof the behaviour never had, and both are E7-16's shape:* a Postgres row-level test that a
  > **decided** placement survives (only the *lock* half had ever met a real database; the decided half was covered by
  > unit tests over a fake storage port, where a removal cannot fail to be a DELETE), and the first test anywhere to
  > read **`aantalVervangen` over the wire** — the frontend read it from a hand-written fixture and no server test read
  > it at all, so a rename would have emptied a sentence on the anchor screen with every suite green.
  > *The catalogue guard was widened from the instance to the class:* the two existing guards key on the key **prefix**
  > and on the word `hergener`, and this story's own copy says *"opnieuw genereren"*, so it was invisible to them by
  > construction. A third guard now covers every `kalender.*` string that mentions running the generation again, in
  > either wording. It found a pre-existing gap on its first run: **`kalender.plaatsGevolg`** (E4-03) makes the same
  > claim from outside both prefixes and nothing pinned it.
  > **Gates, re-run after each fix round and by the auditor itself in ronde 2:** 577 unit + 205 integration (0 skipped, real PostgreSQL), 501 frontend / 20 files, `dotnet format` / lint
  > / build clean, every new claim mutation-checked in the failing direction. Browser pass at 1440px and 390px against
  > a real API and real PostgreSQL with the model stubbed: a class with no plan gets the first-run copy, **the label and
  > the sentence flip within the same session** the moment the first run lands, and on the demo class an accepted and a
  > locked placement survived while five untouched proposals were replaced (*"5 eerdere voorstellen zijn vervangen, 2
  > bestaande plaatsingen bleven staan"*, with the board agreeing). Re-measured after the ronde-2 rewrite on the state both findings were about (a **locked** proposal plus an
  > **empty** model answer, the run that destroys and creates nothing): the board keeps `Verkeer 🔒 Vast` and the
  > report reads *"De AI stelde geen enkel thema voor." / "6 eerdere voorstellen zijn verdwenen." / "1 bestaande
  > plaatsing bleef staan"*, with no *"vervangen"* left on the card. 6 lines / 99px at 390px, composited contrast
  > 8,90:1 (button), 6,08:1 (disclosure) and 5,73:1 (report), no overflow.
  > **The near-miss, recorded because no artifact this project reviews would have caught it.** Mid-pass the browser
  > showed an `Aanvaard` placement being discarded, reproducing over `curl` and over a row inserted straight into the
  > table, on real PostgreSQL, while the xUnit test asserting the opposite passed on the same tree. **The API was
  > running a mutation check**: `IsVervangbaar => !Vergrendeld` had been built, the source restored with `cp`, and the
  > app started with `dotnet run --no-build` — the incremental build did not notice a file restored within the same
  > second as the previous build's output. `git diff` was clean, the suite was green, and the running process was not
  > the source. *Rule out of it:* after a mutation check, force a rebuild before running anything; and a defect that
  > contradicts a passing test on the same tree is a claim about your environment first. The tell was in the first
  > measurement, unread for half an hour: the **lock** was honoured and the **status** was not, which is the exact shape
  > of the mutation and the shape of no plausible EF bug.
  > *Scope boundaries, stated so the next story does not have to infer them:* nothing here claims anything about
  > **per-period** regeneration (E4-05) — both new strings are scoped to the whole plan, like E4-06's six; there is no
  > pre-apply diff and no cancel (**E4-07**, FR-8.3); the model round trip is stubbed, the same residual M2 accepted;
  > and the **`vast moment`** question E4-03 opened (Art. XIV) is untouched.
  > **Inherited obligation from E3-03 (2026-08-05): a new generation path must attach the dekkingsvooruitzicht itself.** FR-5.3's measured half is composed in `JaarplanController.Genereer` rather than inside `JaarplanGeneratieService`, because `DekkingService` reads the plan through `IJaarplanLezer` — which the generation service implements — so a dependency there would close the loop. **If you reuse `POST …/jaarplan/generatie` you inherit it for free; if you add an endpoint, attach it or the panel silently loses its dekking section.** It fails visibly rather than wrongly, which is why the composition was accepted at that layer, but it is an obligation rather than a guarantee. Same note on **E4-05**.
  > *Where that obligation actually lands, now that E4-04 is closed (added while merging E3-03 in, 2026-08-06):* **E4-04 added no generation path.** It presses the one endpoint that already existed, so the vooruitzicht `JaarplanController.Genereer` composes travels with it unchanged and nothing here had to attach anything. The obligation is therefore **E4-05's** in full, which is the story that adds the second path, and it is repeated on that story below rather than left only here.

- [x] **E4-05 — Regenerate a single period** — *built 2026-08-06 on `story/E4-05-periode-hergeneratie`, **merged into `main` as PR #37 (`96a9060`) and pushed**; the branch tip `dfc2535` is an ancestor of `main` and 0 behind it. **Six antagonist rondes, all VIOLATIONS FOUND: ronde 1 2 MAJOR + 7 MINOR + 2 QUESTION, ronde 2 no MAJOR + 5 MINOR, ronde 3 1 MAJOR + 3 MINOR, rondes 4–6 no MAJOR and every finding in the record rather than in the code** — every finding fixed or routed. Narrative + full measurements: [`worklogs/E4-05/implementation.md`](worklogs/E4-05/implementation.md), which has **no section** for the rondes after 3 and still ends on *"Ronde 4 is owed"*, though rondes 4 and 5 did edit it in passing.*
  > **Corrected by the technical lead, 2026-08-19, and the correction is worth more than the fact.** This entry read `[~]`, *"Three antagonist rondes"*, *"merged up to `origin/main` (E5-03 in)"* and *"ronde 4 is owed, which this checkbox says"* for twelve days. All four were false, and the sentence had **frozen at the moment it was written**: it entered the tree in `4aef4ce` (fix ronde 3) and survived `3e299e6` (ronde 4), `713574e` (ronde 5), `57fc357` (ronde 6), the merge of E5-06 in `dfc2535`, and the merge into `main` in `96a9060` — six commits, none of which looked at the summary line one screen above the paragraphs they were rewriting. **The class, not the instance: a self-referential status clause (*"which this checkbox says"*) reads as verified because it agrees with the checkbox beside it, and it goes on agreeing after both have become wrong together.**
  > **What earns the `[x]`, checked rather than taken from the session's own report.** (1) *Merged:* `git branch --merged main` lists the branch; `git rev-list --left-right --count main...story/E4-05-periode-hergeneratie` gives `1 0`. (2) *Six rondes:* `ef43756`, `1d06a43`, `4aef4ce`, `3e299e6`, `713574e`, `57fc357`. (3) *The code has been compliant since ronde 3, and this is the claim I did not take on trust* — the session asserted it in the groepschat (2026-08-07 10:11) and ronde 5 asserted it independently, but the deciding evidence is the diffs: rondes 4 and 5 touch `Themakaart.tsx` only **inside JSX comment blocks** and change one comment line in `Jaarplankalender.test.tsx`; ronde 6 touches `backlog/E3-jaarplan-kalender.md` alone. **No product behaviour has changed since `4aef4ce`.** (4) *Gates on the merged tree* (`dfc2535`): 634 unit + 224 integration, 0 skipped, real PostgreSQL, `dotnet format` / lint / build exit 0, 606 frontend / 24 files. (5) *The acceptance criterion is measured, not argued*: the browser pass ran a regeneration on period 5 against real PostgreSQL and read *"Alleen themaperiode 5 is opnieuw gegenereerd. De rest van je jaarplan is niet aangeraakt."* off the screen, with the drag refusal proven **against a control case** on a free period, which is what makes the negative result mean anything.
  > **The residual, stated rather than tidied away, because closing on a silence is how this backlog got its retractions.** (a) Ronde 6's own fix was never re-audited, and this repo's record is that *"every round's findings were in the fix round that answered the previous one"*. It is not blocking here: ronde 6 edited prose in **another story's** entry (`E3-jaarplan-kalender.md`) and touched no code, so an unaudited ronde-6 fix is at worst a false sentence in E3's record. **Whoever next opens E3-07's or E3-09's entry should read the `57fc357` paragraphs first.** (b) `worklogs/E4-05/` holds only `implementation.md`, and it still ends on *"Ronde 4 is owed"* — so **no ronde after 3 has a section there**, and a reader of the worklog finds no account of them. *(An earlier draft of this clause said they "exist only as commit messages", and that was falsifiable from `git show --stat`: `3e299e6` and `713574e` both edited `implementation.md`. Corrected 2026-08-19 — the true and narrower claim is that the file has no ronde-4/5/6 section and its closing line is wrong, not that it was never touched.)* That is not this story's defect: `E0-10`, `E2-08`, `E3-03`, `E4-04`, `E5-01` and `E1-14` are all missing `test-report.md` and/or `antagonist.md` too, so the gate-record convention this backlog documents has quietly stopped being followed. Filed for the owner as a class, not patched per story.*
  Regenerate one block/period without touching the rest.
  *Done when:* only the chosen period changes. Ref: FR-8.2.
  > **What ronde 1 found, and both MAJORs were in the seam rather than in the new code.** The four things the audit was
  > asked to be hostile about came back clean (nothing auto-applies, no failed run persists, no ordinal is ever a key,
  > the new write paths do run against real PostgreSQL). What it found instead:
  > 1. **Ruling 2 was enforced on the drag and not on the drag's mandated alternative.** The board withheld the drop
  >    target for a blocked period, but the *card's* "Verplaats naar" `<select>` — the control that exists to satisfy
  >    **WCAG 2.2 SC 2.5.7 (Dragging Movements)** — still offered every period. So the one route a teacher without a
  >    mouse has was the one route that proposed a target the server refuses, and the 409 fell through to *"Meld dit
  >    aan de beheerder van de tool"*: the tool blaming itself, for a rule it had just applied on the strength of the
  >    teacher's own setting. The stale-placement notice is the likeliest way in, because such a card sits in no period
  >    and its picker offers all of them. Fixed by keeping the blocked period in the list **disabled, naming the
  >    moment** (a silently shorter list sends a teacher hunting for a period that is plainly on the board), plus a 409
  >    branch on **both** manual routes.
  > 2. **The new button failed SC 2.5.3 (Label in Name, Level A).** Visible *"Deze periode opnieuw genereren…"*,
  >    accessible name *"Themaperiode 3 opnieuw genereren"* — the visible text was not in the name, so speech input
  >    could not reach it. Every other `aria-label` in this feature *appends*; this one *substituted*. **axe cannot see
  >    this**, so the browser pass's "0 violations" was silent about it, which is exactly why it is now a
  >    catalogue-wide guard: any `<key>Label` must contain its visible `<key>`, case-insensitively.
  > *Also fixed:* the bezet explanation rendered at the fine tier where no column carries the marker (a themaperiode's
  > start is also its first sub-block's start); the period-button explanation promised a button a fully blocked year has
  > nowhere; the parameter report was measured plan-wide under a period-scoped heading, so a run on period 5 could
  > report on period 2; a **stale** placement was described to the model as sitting in a period the same prompt had just
  > omitted; `BuitenPeriode` shipped `"Water @ 2026-11-02"` to a teacher and is now structured, so the client says
  > *"Water (themaperiode 4)"*; and the `Manueel` half of the seven widened promises was the one still resting on
  > shared-code reasoning, so it has its own test and the docstring that overclaimed is corrected.
  > **Routed, not fixed here:** the third billable anonymous route is recorded on **E7-11**, whose own dimension it
  > changes (one billable call per *period* rather than per class).
  > **The Art. XIV entry is closed, by the session that held the file (2026-08-07).** It still read *"Nothing
  > hard-assumes either reading today"* while the strict reading was compiled in on three paths, and `backlog/README.md`
  > was **claimed by E5-06** at the moment this was found — so it was left untouched and asked for in the groepschat
  > rather than edited around the lock. E5-06 answered the ASK and struck the entry themselves (`4624c90`, *"Answer
  > E4-05's ASK: the vast-moment Art. XIV item was false on main"*). **Recorded because the outcome is the argument for
  > the protocol:** a refused claim cost this story one paragraph of waiting and produced a correction written by the
  > session that owned the file, rather than a merge conflict in the one file this repo has retracted counts over.
  > **A third owner ruling, 2026-08-06, on the question ronde 1 raised: the settings form says it at the point of
  > choosing.** The audit observed that *"Mag er een thema in die themaperiode?"* / *"Nee, die themaperiode is bezet"* is
  > generic and therefore not false, but that the answer's **meaning changed** — it used to constrain the AI and now
  > removes the teacher's own picker and drop target, while the only disclosure sat on the board, after the fact.
  > **Ruled: one sentence beside that answer**, shown only once the blocking option is actually selected, so it explains
  > a consequence the teacher has chosen rather than warning about one of two options while both are open
  > (`parameters.momentGeenThemaGevolg`). *It is conditional on the date landing in a themaperiode* (ronde 3, MINOR): the
  > render condition is the radio, not the date, and a blocking moment on a vakantie blocks nothing at all.
  > *Recorded here because ronde 3 found it recorded nowhere but in the source comment that implements it* — a
  > user-facing copy change whose only authority was the code doing it, which an auditor cannot tell apart from a
  > decision taken unilaterally.
  > **What ronde 3 found, and it is the sharpest finding of the three rondes because it was inside a fix:** ronde 2's
  > repair gated the move *picker* and added a sentence, and left the instruction that actually promises a picker gated
  > on staleness alone. So on a **stale** card in a year where every period is bezet the panel read *"Kies hieronder een
  > themaperiode … of versleep de kaart"* over no picker, onto a board whose every column is a disabled droppable: both
  > halves false, **the same defect class as the state an owner ruling reopened E3-07 over (there: a stale **rejected** card, where the picker is withheld by the rejection; here: a stale card with every period bezet)**. The new sentence carried the same defect
  > class in a second form (*"nergens **anders** heen"* for a card that is in no period, two lines under a paragraph
  > saying exactly that), which three strings in that same file had already been repaired for. Fixed by gating only the
  > `kan` branch, and the reason is **by construction rather than editorial**: at the other two tiers `doelen` is `[]`
  > by definition (`isGeweigerd || verplaatsstaat !== "kan" ? [] : …`), so `kiesbareDoelen.length === 0` there says
  > *nothing about blocked periods at all* — gating on it would suppress a true sentence on a premise that is not about
  > bezet. Those two branches also carry different information (`herplaatsAnderNiveau` says where re-placing **does**
  > work; `herplaatsNiveauOnbekend` says the view could not be read at all), which is the secondary reason.
  > *(Two earlier revisions of this line were weaker. The first compressed both branches into "say where moving works",
  > false of the second, which says where it does **not**. The replacement argued they would leave the card with "no
  > statement whatsoever" — also not decisive, since the board itself carries `fijnUitleg` at that tier, so the screen
  > is not silent even when the card is. The entry outlives the branch, so it is the artefact that has to carry the
  > argument that actually holds.)* Fixed further by giving the sentence a `*Vervallen` variant. Both pinned, and the round also moved a **vacuous assertion**
  > that appeared to check the claim while standing on a card that could not exercise it.
  > **Inherited from E3-03 via E4-04 (2026-08-06): your new path must attach the dekkingsvooruitzicht itself.** FR-5.3's
  > measured half is composed in `JaarplanController.Genereer`, not inside `JaarplanGeneratieService`, so a second
  > generation route that does not repeat that composition returns a run report with no coverage figures and nothing
  > fails. E4-04 inherited this and discharged it trivially, because it pressed the endpoint that already existed and
  > added no path at all; **you are the story that adds one.**
  > **Inherited obligation from E4-06 (2026-08-03) — six strings become false the moment you add a second discard path.** E4-06 could not promise anything about *"een hergeneratie"* in general, because only the whole-plan path exists, so it qualified every claim to **"een hergeneratie van het hele jaarplan"**. Your story adds the path those sentences deliberately say nothing about. Re-read and re-verify all six in `frontend/src/i18n/nl.json`: `kalender.vergrendeldUitleg` (the "Vast" badge tooltip), `kalender.vergrendelUitlegVrij`, `kalender.vergrendelUitlegVast`, `kalender.vergrendelNietNodig`, `kalender.vergrendelUitlegBeslistVast` and `kalender.weigeringUitleg`.
  > *Coverage is split, so know where each one is pinned:* the first five sit in the `kalender.vergrendel*` family and are caught by the guard in `frontend/src/i18n/catalogus.test.ts`; **`weigeringUitleg` is outside that prefix** and is pinned by a literal assertion in `Jaarplankalender.test.tsx` instead. A change that only touches the sixth will not trip the family guard.
  > **A seventh string joined the list on 2026-08-04 (E4-03):** `kalender.plaatsGevolg`, shown in the hand-placement picker, says the thema *"komt als jouw eigen keuze in deze themaperiode, dus een hergeneratie van het hele jaarplan laat het staan"*. Same qualification and the same reason, and **it is outside the `vergrendel*` prefix too**, so the family guard does not see it either. Re-read all seven, not six.
  > **One substantive question arrives with it, and it is not about copy** (E4-03 antagonist audit, 2026-08-04, now an Art. XIV item in [`README.md`](README.md)): **a `vast moment` blocks *generation* from a period and blocks nothing about a manual placement.** E4-03 added the first *creation* route into such a period and E3-07's move path is equally blind, so a teacher can now hand-place a thema into the period they themselves marked as bezet, with no annotation anywhere. **This story is the cheapest place to settle it**, because a per-period regeneration has to decide what it does with that period anyway, and because you are already re-reading the seven strings above. Do not assume either reading in code.
  > *And, from E3-03 (2026-08-05):* a per-period run must attach the dekkingsvooruitzicht to its own response, for the reason spelled out under **E4-04**. A partial regeneration is also the first path that can change coverage for *part* of a year, so its outlook is measured over the whole plan, not over the regenerated period.
  > ***Answered 2026-08-19 and kept as the statement of the question; two of its clauses are now false and are marked.*** *And the substantive question, not just the copy:* E4-06 hid the lock control on already-decided **and unlocked** placements *(this line said "already-decided", which is over-broad: a decided **locked** placement keeps the control, so the lock can be undone)* because `IsVervangbaar` is `Voorgesteld && !Vergrendeld`, so an `Aanvaard`/`Manueel` placement survives a run without a lock. **If your per-period path may replace a decided placement, that reasoning collapses and the hidden control becomes load-bearing.** ~~That is E4-07's ruling to obtain, not yours to assume.~~ **It is not: the owner ruled on 2026-08-19 that a decided placement always survives, so the reasoning holds and the per-period path may not replace one.** Directie's confirmation of that ruling is what remains open.
  > **Two owner rulings, 2026-08-06, taken before any code was written, because the backlog forbids assuming either.**
  > 1. **A per-period regeneration of a period holding a blocking `vast moment` is refused *before* the AI call, and the reason is visible on the control.** Not a post-hoc report: the teacher does not press a button, wait for the model and receive an empty result. Rejected alternatives, recorded because both were defensible: running anyway and reporting the refusal afterwards (what the whole-plan path does today, but there the run has other periods to fill, so the refusal is a footnote rather than the whole outcome); and treating the explicit choice of that one period as an instruction that overrides the block (which would make the tool ignore an instruction the teacher herself gave, and would make the two generation paths disagree about the same parameter).
  > 2. **The Art. XIV question is settled the strict way: a teacher may not hand-place or drag a thema into a blocked period either.** One rule for human and machine. This **removes a path E4-03 and E3-07 ship today** — both accept such a target silently — so it is a deliberate narrowing of shipped behaviour, not a gap being filled. *Boundary the ruling does not cover and this story therefore does not touch:* nothing is retroactive. A placement already sitting in such a period stays exactly where it is (a teacher may have planned it and then registered the moment), so **the copy may not say the period is empty or that nothing may be there** — only that nothing *new* can be put there while the moment stands. A placement stranded that way is a signalling question and belongs to **E3-09**, not here.
  > *What these two rulings do **not** settle, stated so the next story does not read consent into them:* whether a per-period run may replace a **decided** placement. It may not, and this story does not ask — it applies the existing rule (`IsVervangbaar`: `Voorgesteld` and not `vergrendeld`) scoped to one period, which is exactly what keeps E4-06's hidden lock control non-load-bearing and its six sentences true. **~~E4-07 still owns that ruling.~~ Ruled 2026-08-19 by the owner: a decided placement always survives, so this story's scoping was right and needs no revisiting. Directie's confirmation is what remains open.**

- [x] **E4-06 — Vergrendelde blokken excluded from regeneration** — *built 2026-07-31, closed 2026-08-03 on `story/E4-06-vergrendeling`, merged into `feature/e4-bewerking-hergeneratie` (`6755c49`). Three fix rounds, three audits, two independent verifications. Final gates on the landing commit (`75f326c`, with `origin/main` merged in first): **test-runner PASS**, **antagonist COMPLIANT**, 496 unit + 154 integration + 205 frontend, **0 skipped**, `dotnet format` / lint / build clean, and all sixteen `(status, vergrendeld, isVervallen)` states read out of a real browser at 1440px and exactly 390px.*
  A `vergrendeld` thema/block is preserved across (re)generation.
  *Done when:* locked content survives both full and partial regeneration. Ref: FR-8.4, Art. IX.3.
  > **What this story turned out to be, and it is not what the line above says.** The server half already existed (see the pre-flight below), so the code delta is one API call, one mutation hook, a control, and a button variant. **Nearly all the value came from the copy**, and none of the defects the three audits found were code defects: the panel told a teacher the lock was the reason their thema survived when it was not; it invited a *"Losmaken"* that changed nothing and then contradicted itself; on a **rejected** card it said *"Je hebt dit thema zelf beslist, dus het blijft staan"* when what the teacher had decided was **no**; on a **stale** card it offered locking as a remedy, which would have made the *"dekking onbetrouwbaar"* state survive every regeneration where before it healed itself; and it nudged teachers to lock a proposal on a screen with no accept control, which under E5's binding reading contributes **nothing** to dekking. **Every one of those passed every test**, and the two states carrying the worst of them had never been rendered in a browser by anyone. Worth remembering the next time a story looks like "wire up an endpoint".
  > *One more, because it is the sharpest:* the fix rounds added a catalogue guard to pin the class *"no lock string promises something unscoped about a hergeneratie"*. The guard keys on the word `hergener` — and the newest member of the class deliberately avoids that word, so it was **exempt by construction**, and a comment then claimed a test pinned it when none did. **A guard keyed on a phrase cannot see a sibling that states the same fact while avoiding the phrase.** Now pinned explicitly and the exemption is recorded in the guard itself.
  > *Process note, recorded because it bears on how much the gates prove:* **fix round 3 was written by the orchestrator, not an implementer** — four agents had died on the stream watchdog and the audit had said no build round was needed. The E1-15 precedent permits that **only** because both gates then ran on the result independently, which they did, and the audit was told it was orchestrator-authored so it could judge accordingly. It found exactly one thing: a comment of mine claiming coverage that did not exist. Which is the one failure mode a self-authored round is structurally worst at, and the argument for the rule rather than against it.
  > **Pre-flight finding (orchestrator, 2026-07-31): the server half is built and the story is a *user-surface* story.** `Themaplaatsing.Vergrendeld`, `Themaplaatsing.IsVervangbaar`, `Jaarplan.VerwijderVervangbarePlaatsingen()`, `JaarplanGeneratieService.WijzigVergrendelingAsync` and `PUT /api/klassen/{klasId}/jaarplan/plaatsingen/{plaatsingId}/vergrendeling` all exist, and `GenereerAsync` already discards only replaceable placements. What does **not** exist is any way for a teacher to set the flag: `frontend/src/features/jaarplan/api.ts` never calls that endpoint, yet `Themakaart.tsx` (lines 111 and 401) renders a *"Vast / Blijft staan bij hergenereren"* badge. So today the badge is unreachable state and FR-8.4 has no invocation surface, which is the E2-08 / E1-15 / E0-10 pattern for the fourth time and a breach of the E3-06 rule (never ship a control, or a state, a user cannot produce). This story owns the lock/unlock affordance plus proof that a locked placement survives regeneration; it must **verify** the server half rather than assume it.
  > *Scope boundary:* "partial regeneration" in the *Done when* is **E4-05**, which does not exist (`GenereerAsync` takes no period scope). This story proves preservation across the **full** regeneration path that exists, and E4-05 inherits the obligation to prove it for its own path. Do not mark the partial half proven.
  > **Two owner rulings, 2026-07-31, both taken on the antagonist's findings against `889471d`.**
  > 1. **The lock must say it is not a decision.** The audit found that after this story, locking is the *only* keep-affordance on the kalender: `useWijzigPlaatsingStatus` is called from exactly one place and never sends `Aanvaard`, so nothing on the anchor screen accepts a proposal. Meanwhile a locked `Voorgesteld` placement contributes **nothing** to dekking under the binding reading in [E5](E5-dekking-export.md) (only `Aanvaard`/`Manueel` count as placed), and the copy actively invited teachers to lock instead. **Ruled: add the distinction in copy, inside this story** — vastzetten keeps the thema in place, aanvaarden is what makes it count. The accept affordance itself stays out of E4-06. *Consequence for whoever builds it:* E4-01/E4-02 must add it, and until they do, every locked proposal in a plan is a figure E5 cannot honour. The audit is right that no number will contradict the teacher until E5 exists, which is precisely why the sentence has to carry the weight in the meantime.
  > 2. **The button weights get fixed here, not deferred.** "Losmaken" (reversible) and "Uit deze periode halen" (unrecoverable) were shipped as two visually identical `outline` buttons, adjacent, and this story added the third instance on the commonest card — the unlocked `Voorgesteld` one, which is exactly where deletion happens on a single click with no confirmation. The implementer flagged it and left it; the audit refused to let a self-flag count as a resolution. **Ruled: resolve it in this story**, even though it touches the button hierarchy rather than FR-8.4.
  > **Two further owner rulings, 2026-07-31, taken on the re-audit of the fix round (`ec65209`).** The re-audit closed 8 of the previous 9 findings and raised 1 MAJOR + 4 MINOR, two of which were the owner's to settle:
  > 3. **The dekking sentence is phrased as the teacher's own decision, not as "aanvaard".** `vergrendelDekking` said a thema *"telt pas mee voor de dekking zodra het aanvaard is"*, but E5's binding reading counts **`Aanvaard` and `Manueel`** ([E5-dekking-export.md](E5-dekking-export.md)), and `Manueel` is reachable today by moving the card (`Themaplaatsing.VerplaatsNaar`) while *aanvaarden* has no control anywhere in the product. So the sentence stated a condition that was sufficient but not necessary, and pointed at an affordance that does not exist. **Ruled: phrase the condition as the teacher's own decision**, which covers both statuses and stays satisfiable today, without disclosing unbuilt work. Note for E4-01/E4-02: when the accept control lands, this sentence is one of the strings that must be re-read.
  > 4. **The rejection copy gains one sentence: a weigering survives a regeneration.** A rejected placement stays until someone deletes it and its thema is not re-proposed in that slot, which is the opposite of what *"ik heb dit geweigerd"* leads a teacher to expect. **Ruled: say it.** This also closes the re-audit's MAJOR at its root: the truthful sentence for a locked **`Geweigerd`** placement is that the rejection itself is what keeps the thema out of the AI's reach, so the lock adds nothing and unlocking only removes the label. The defective sentence claimed *"Je hebt dit thema zelf beslist, dus … het blijft staan"* — true about the outcome, false about the decision, since what the teacher decided was **no**.

- [ ] **E4-07 — Pre-apply diff + cancel; manual-edit preservation rule**
  Before applying a (re)generation, show what will change with a cancel option; define and honor how manual edits are preserved.
  *Done when:* the diff is accurate and cancel is non-destructive; the preserve/overwrite rule is explicit. Ref: FR-8.3, FR-7.3 (precise rule: confirm with directie).
  > **RULED BY THE PROJECT OWNER, 2026-08-19: a placement the teacher has decided on always survives a (re)generation.** That is the behaviour that already ships (`Themaplaatsing.IsVervangbaar` is `Status == Voorgesteld && !Vergrendeld`, and both run paths share one filter), so the ruling makes it a decision rather than an accident. Everything below the ruled sentence is derived by whoever wrote it, not ruled.
  > *This block was cut back on 2026-08-19 after three audit rounds found nine MAJOR findings in it, every one in the explanatory prose and none in the ruling.* Each round's fix added a claim, and the claim was the next round's defect: an invented citation, a published grep that did not reproduce its own list, a count of source sites that was short in the sentence saying the previous count was short. **The lesson is the E5-03 corollary, applied to a backlog entry: when a paragraph keeps being wrong, say less rather than say it more carefully.** What survives is what is checkable.
  > *Consequences of the ruling, and their status:*
  > - **E4-07 is buildable.** It does not by itself discharge **FR-7.3's** *"ter beslissing"* clause (FR-8.3 owns the diff and cancel); directie's confirmation is recorded as outstanding in [`docs/besluiten-gevraagd.md`](../docs/besluiten-gevraagd.md). Building on it is safe either way, because it is the conservative option.
  > - **A weigering survives a partial regeneration** on the same principle. `Geweigerd` is not `IsVervangbaar`, and the idempotence key is `Jaarplan.VindPlaatsingOp(themaId, blokNiveau, blokStart)`, so `weigeringUitleg`'s *"hier"* is load-bearing and must not be widened to the whole plan.
  > - **E4-06's hiding of the lock control is NOT ratified here.** An earlier draft ratified it on the claim that locking a decided placement *"changes nothing observable"*, which `JaarplanGeneratieService.cs` itself records as withdrawn (the badge does appear). Left as E4-06 built it.
  > - **No copy is rewritten.** Every string that was priced against this answer is true under it, with the exceptions named below.
  > *Owed, for whoever builds this story:*
  > - ~~**An Art. XI.1 amendment to Art. IX.3**~~ — **done 2026-08-19, in its own commit.** Art. IX.3's `Jaarplan` clause now states what a run may discard, with a ratification-log entry recording that the **projecteigenaar** ruled it while **FR-7.3 reserves it to directie, whose confirmation is outstanding**. The clause it replaced said *"(excluded from regeneration)"* beside the `vergrendeld` flag alone, which read as *the lock is what protects a placement* — the opposite of what E4-06 built. Dependent text in `CLAUDE.md` moved in the same commit, per Art. XI.1.
  > - **Correct the stale comments. These are stated as *predicates with the command that derives them*, not as lists of sites, and that is a deliberate change of instrument.** Three consecutive audit rounds fixed every site a list named and found another the next round, twice inside the file the list already cited by line number. A list is wrong the moment someone writes a new comment; a predicate is not. **Scope every command over `backend/src frontend/src backend/tests frontend/src/**/*.test.*` and the `backlog/` tree** — the earlier lists missed all three of the last two, and one instance lives in a *test name*.
  >   - **(0) The rule is no longer undecided by the owner, but it IS still open with directie.** `grep -rn "E4-07"`. **Fix** hits saying the rule is the owner's to obtain or is not yet ruled — today those are `Jaarplan.cs:149` (*"still open rather than pre-empted here"*) and `JaarplanGeneratieService.cs:118` (*"E4-07's ruling to obtain"*), plus `E4-bewerking-hergeneratie.md`'s own E4-05 hand-over block. **Leave** hits calling it an open **directie** question: they are still true and they are the stated justification for the guards policing this copy family. They become false only when question 6 in `docs/besluiten-gevraagd.md` is answered. *A careless pass here makes three true comments false.*
  >   - **(a) Any claim about *when the lock control is shown*.** The truth is `Themakaart.tsx`'s `toonSlot`: shown on a proposal, and on **any locked** placement (so a decided locked one keeps it, which is how the lock is undone); hidden on a decided **unlocked** one, and on a stale card. Anything saying "hidden on decided placements" without *unlocked* is over-broad. Derive: `grep -rn "lock control\|toonSlot\|slotknop"`.
  >   - **(b) Any paraphrase of the discard predicate.** The amendment names **"untouched"** as the wrong word, and Art. IX.3 says why. Derive: `grep -rniE "untouched|onaangeroerd"` and judge each hit — both words are used across the repo for unrelated things, so this is a read, not a substitution. **`onaangeroerd` appears in a test *name***, which no English-only grep would have found.
  >   - **(c) Any quotation of Art. IX.3's deleted parenthetical.** Derive: `grep -rn "excluded from"` and check whether the hit attributes the phrase to the article. Related and separate: `Themakaart.tsx:854` still says *"the six `kalender.vergrendel*` strings"*, a count the E4-07 entry has since replaced with an enumeration.
  > - **The strings that depend on this rule**, for re-reading if the ruling is ever reversed. Under `kalender`: `plaatsGevolg` (E4-03's), `vergrendeldUitleg`, `vergrendelUitlegVrij`, `vergrendelUitlegVast`, `vergrendelNietNodig`, `vergrendelUitlegBeslistVast`, `vergrendelUitlegGeweigerdVast`, `hergenereerUitleg`, `periodeHergenereerUitleg`, `periodeHergenereerGevolg`, `genereerBehouden`, `genereerBehoudenEnkelvoud`, `genereerVervangen`, `genereerVervangenEnkelvoud`, `weigeringUitleg`, `weigeringUitlegVervallen`. Each read against the ruling on 2026-08-19 and true under it, **with one caveat**: `hergenereerUitleg` and `periodeHergenereerGevolg` promise that a *verplaatst* thema stays, and a drag ending in its own period writes nothing, so such a placement stays `Voorgesteld` and does disappear. That trade-off is already argued at `Jaarplankalender.tsx`, above the regeneration confirmation; it is repeated here because the promise is being ratified.
  >   **The set became sixteen on 2026-08-20, and the caveat moved with the sentence** (E9-08, on an audit finding). That story split `periodeHergenereerUitleg`: the instruction half stayed under that key and **the promise moved to the new `periodeHergenereerGevolg`**, which now renders inside each period's own regeneration confirmation. The list was wrong in both directions for a day — it named a key that no longer carries the rule and not the one that does — and the caveat above was attached to the wrong half. **Art. XI's ratification note binds this enumeration to the Art. IX.3 clause, so a story that moves one of these sentences has to move the list in the same change.** `hergenereerUitleg` itself is unchanged in wording and only changed in *where* it renders, so it stays exactly as ratified.
  > - **Two strings are NOT claimed true here. They are owned by E4-09 below**, not by this block.
  > *What is left to build:* the **pre-apply diff and its cancel** (FR-8.3). The *discard* half is a pure predicate over stored state (`IsVervangbaar`) and is knowable before a run. The *propose* half is the model's output and needs a call to know, so whether the diff shows it at all is E4-07's own design decision and is not pre-empted here. Cancel must leave the plan unchanged, and that is assertable rather than arguable.
  > ***Superseded 2026-08-19 by the ruling above; kept because it records how the question stood, and because two of its claims are now false.*** **The undecided rule is already priced into shipped copy (recorded by E4-06, 2026-08-03), so this decision now costs a copy change as well as a code change.** *(False as of the ruling: it cost none.)* Three audits pushed E4-06's lock sentences to qualify every promise as *"een hergeneratie van het hele jaarplan"* precisely so they would not hard-assume your ruling. Six strings depend on it: the five `kalender.vergrendel*` ones plus `kalender.weigeringUitleg` (listed under E4-05, together with where each is pinned). *(The **six** is E4-06's figure of 2026-08-03 and was already stale then; the current set is the fifteen keys enumerated above.)*
  > ***Also superseded 2026-08-19*** *(the question below was answered by the owner; directie's confirmation is what is still open, so the paragraph is kept as the statement of what they are being asked). Read "untouched" in it as the shorthand it was: the ruled predicate is `Voorgesteld && !vergrendeld`, and the amendment names "untouched" as the paraphrase to avoid.* **The question for directie, stated so it can be answered without reading the code:** when a teacher regenerates, what happens to a placement they already decided on by hand? Today the answer is *nothing* — a run discards only untouched AI proposals (`Voorgesteld && !vergrendeld`) — and E4-06 leaned on that: it **hides** the lock control on *unlocked* `Aanvaard`/`Manueel` placements because locking them changes no regeneration outcome and no dekking *(this clause said "changes nothing observable" until 2026-08-19, which the source itself records as withdrawn: the "Vast" badge does appear)*, and instead tells the teacher in words that their thema stays. If directie rules that a regeneration **may** replace a decided placement unless it is locked, then that hidden control becomes the only protection for hand-made work, and two of those sentences become false. So this is not only FR-8.3's diff: it decides whether an existing screen is honest.
  > *Related, and the reason E4-06 could not settle it:* the same ruling governs whether a **rejection** survives a partial regeneration. E4-06 states in `weigeringUitleg` that a weigering survives a whole-plan regeneration and that the thema is not re-proposed **in that period** (idempotence is per `(thema, niveau, blokStart)`, so the word *"hier"* in that sentence is load-bearing and must not be dropped).

- [x] **E4-08 — Een activiteit naar een ander subthema verplaatsen** — *added 2026-08-04 (owner ruling, on the antagonist MAJOR against E4-03): the ninth cell of FR-7.2's verb×object matrix, and the only one still unbuilt. **Built and closed 2026-08-05** on `story/E4-08-activiteit-verplaatsen`, off `origin/main` `3e646da`: `950b009` (server), `2c7ea32` (screen), `777f9e6` (browser pass), `afde19d` + `35e2dc1` (fix round 1), `1cf91d6` (record), `c697d4f` (two owner rulings), `ce1cadc` (fix round 2), `38a64f8` (round-2 record), `39f9eaa` + `9dbe204` (fix round 3), `db0705e` (fix round 4). **Four antagonist rounds, all VIOLATIONS FOUND, every finding fixed or filed** ([`worklogs/E4-08/antagonist.md`](worklogs/E4-08/antagonist.md)). *(An earlier revision of this line stopped at `35e2dc1` and headed its gates block there, while HEAD was two commits later and one of them changed product code. Round 2's MINOR 8: a story that asks to be blessed has to name the tree it is asking about. A first attempt at the repair invented `ce1cadc`'s hash **before the commit existed**, which would have been the same defect in a new form; it was written as a placeholder and filled in by the commit after.)*  **Closed 2026-08-05 by owner ruling**, after four rounds; see the caveat immediately below.*
  > **⚠️ This `[x]` carries the same caveat E5-01's and E4-01's do, and it is recorded rather than glossed: the owner ruled it green without a fifth round (2026-08-05), so fix round 4's own fixes were verified by tests, by mutation checks and by a browser pass, but not by an independent pass.** What that leaves open is specific and nameable, because three consecutive rounds found it: **defects introduced by the previous fix round.** Rounds 2, 3 and 4 each found exactly that, and in two of the three the defect was invisible to a green suite (a mutation check that passed because the test's own setup had already done the thing under test; a control that was a no-op for screen-reader users only, which `axe` has no rule for). Fix round 4's surface is small by comparison, which is the ground the owner drew the line on: one render condition, one deleted branch, one new control and six comment or record edits. **Every MAJOR across all four rounds is fixed and mutation-checked**, and the four findings not fixed here are filed as E7-19, E1-19, E1-20 and E7-20. Stated because a reader five stories from now cannot tell a ruled `[x]` from an audited one.
  Move an activiteit from one subthema to another by hand, with no AI involved.
  *Done when:* a teacher can move an activiteit between subthema's without deleting and retyping it. Ref: FR-7.2, [Art. IX.2](../CONSTITUTION.md#article-ix--core-data-model-functional).
  > **Why this exists as its own story.** FR-7.2 reads *"thema's, activiteiten en doelkoppelingen handmatig **toevoegen, verplaatsen of verwijderen**"*, which is a 3×3 matrix. E4-03 measured all nine cells before writing any code and found eight built: E1-14 landed create/edit/delete for activiteiten and doelkoppelingen, E3-07/E4-02/E4-06 own move, remove, accept, reject and lock for a placement, and E4-03 built the one missing add. **The ninth is moving an activiteit between subthema's**, and no endpoint does it: `ActiviteitenController` has `PUT {id}` and `DELETE {id}`, while creation is `POST /api/subthemas/{subthemaId}/activiteiten`, so the parent is chosen at creation and never afterwards. Today the only route is delete-and-retype, which loses the activiteit's `hoek`, its `verwachteUitkomsten` and **every `DoelKoppeling` on it** (`Manueel` links a teacher made by hand). That is the same "a guard whose remediation does not exist" shape E3-01 had to fix for `Klas` deletion.
  > **Where it belongs, and why it is filed under E4 rather than E1.** The FR is FR-7.2, which is E4's, so the traceability lives here. The **surface** is E1-14's themabeheer screens (`/themas` → thema → subthema → activiteit), so whoever builds it works in that feature, not on the kalender. Filed in the epic that owns the requirement rather than the epic that owns the pixels, which is how every other cross-cutting story in this backlog is placed.
  > **Watch the scoping rule (Art. IX.2), because it is what makes this more than a foreign-key update.** A `Subthema` is scoped **per klas and per leeftijd** while a `Thema` is school-wide, so a move can cross a class boundary: moving an activiteit from a K3 subthema to an L3 one silently reassigns class-scoped content. Decide and state whether a move is restricted to subthema's of the same thema, or the same klas, or is free with the consequence disclosed. Do not guess this one in code.
  > ### **RESOLVED (owner, in session 2026-08-05): same klas required, any thema allowed.**
  > The entry above told the builder not to guess this, so it was asked before any code was written. Three options were put to the owner, who chose the second:
  > 1. *only within this thema and this klas* — exactly the destinations already on screen, nothing new to load;
  > 2. **same klas, also another thema** — the klas boundary stays hard, a move may cross a thema. **Chosen.**
  > 3. *free, with the consequence disclosed* — may cross a klas.
  >
  > Recorded here rather than only in code comments (antagonist round 1): a ruling that lives in comments is not findable by the next story. The rejected options are kept, because they are what makes the choice legible.
  > **Enforced as a domain invariant, not as a UI rule.** `Subthema.VerplaatsActiviteitNaar` refuses a destination in another klas, and `Activiteit.VerhuisNaar` is `internal`, so nothing reaches the FK past that guard. The picker *agrees* with the server rather than being the only thing between a teacher and a refusal.
  > **What the ruling brings with it, measured rather than argued:** dekking counts an activiteitkoppeling through the thema its subthema hangs under (`EfDekkingOpslag` layer 4), so a move that never leaves the klas can still take a doel out of this class's figure. An integration test drives it 1 → 0 against real PostgreSQL, and the panel says so where the action is.
  >
  > ### What the story turned out to be
  > **The server half is one FK, and the links travel for free**, because `Doelkoppelingen` is an owned collection keyed on the activiteit. That is what makes a move different in kind from delete-and-retype, which loses `hoek`, `verwachteUitkomsten` and every `Manueel` link a teacher made by hand. Almost everything else was on the screen, and **the three findings that mattered came from running it rather than from reading it**:
  > 1. **An integration test caught a Dutch sentence with an English artefact glued to it.** `ArgumentException(message, paramName)` appends `(Parameter 'doelSubthema')` to `Message`, the service forwards `Message` as the 400's `detail`, and E1-14's forms render that `detail` verbatim. This is E1-14's round-4 MAJOR one screen over. Fixed, and pinned by a test asserting the property over *every* message the method can refuse with rather than over the two sentences. The same trap in `Subthema.Require` is filed as **[E7-19](E7-niet-functioneel.md)** rather than fixed here.
  > 2. **The confirmation has to name the destination, and it has to live at section level.** A cross-thema move takes the activiteit off this screen, so the row that performed it is gone before anyone could read a notice inside it, and a successful move is otherwise indistinguishable from a delete.
  > 3. **A browser pass found a refusal that still offered what it said was gone.** With the destination deleted by a colleague, the panel said *"Dit subthema bestaat niet meer"* while the picker one line above still offered that subthema and still had it selected. Same shape as the stale card that reopened E3-07. Both halves fixed: the shared mutation wrapper refreshes on any failure rather than only on a 404, and the chosen destination is derived from the list rather than trusted from state.
  >
  > ### Antagonist round 1 (on `777f9e6`): VIOLATIONS FOUND, 2 MAJOR + 6 MINOR + 2 QUESTION. All addressed in `afde19d` + `35e2dc1`.
  > **MAJOR 1 is E1-14's code as much as this story's, and it is the sharpest finding.** `maakSubthema.isSuccess` is a **latched** TanStack flag, not an event, and nothing in the feature calls `.reset()`. E1-14's round-4 fix was a render-phase `if (notice && maakSubthema.isSuccess) clear`, which therefore fires on the very render that *raises* a notice, for the whole mount after one successful subthema create. Create a subthema, then move an activiteit: the confirmation never painted, in the one flow where it is the only feedback there is. It silently killed E1-14's own *"iemand anders heeft het verwijderd"* notice the same way. **Every test wrote once**, which is why 455 green tests said nothing. Clearing is now the create path's `onSuccess`, and there is exactly one such call site, so the "keep the rule in one place" argument that motivated the latch is unaffected.
  > **MAJOR 2: the panel could offer an empty picker under a message telling the teacher to choose.** With the panel open and the list emptied (by the new refetch-on-failure rule, or by an ordinary window refocus) it rendered a label, a select holding only its placeholder, and a disabled submit. **A passing test asserted that state as correct**, which is precisely how this class became sticky in E3-07. There is now an explicit sentence and the test demands it. Hoisting the failure notice out of that branch was part of the same fix: the empty-state sentence had replaced it, so a teacher was told what the state is without being told their move had not happened.
  > *The six MINORs, each fixed:* the destinations read verifies the klas instead of letting an unknown one answer `[]` (which the picker read as "nowhere to move to" and hid the control, rendering an infrastructure state as a claim about the school's content); the query breaks its ordering tie on `ThemaId` and the grouping is keyed on a map rather than on adjacency, because `Thema.Naam` has no unique index and two thema's sharing a naam produced two groups with the same id and the same label; a stale failure no longer greets the next open of the panel; the coverage sentence ties its claim to the thema being in this class's jaarplan, which is the condition layer 4 actually applies; the panel state got an axe pass, since it introduces the first `select`/`optgroup` on the card; and heading, select, submit and cancel are named per activiteit, because the open state is per row so two panels can be open at once.
  > *One fix was **removed** rather than kept:* a `verplaats.reset()` on the trigger. A mutation check showed it changed no test, and reading the close paths confirmed it cannot fire. Insurance that cannot fire is the shape E1-14 shipped once as `key={alWeg ?? "nieuw"}`.
  > *And looking once more at the fixed screen found one more instance of the same class:* both server refusals ended *"Kies een ander subthema."*, which lands directly above *"Deze klas heeft geen ander subthema…"* when the vanished destination was the last one. The server now states the fact and the screen states the remedy, since only the screen knows whether an alternative exists to point at.
  >
  > ### Two lessons about the guards, because three of them did not bite when first written
  > *(1)* A duplicate-name guard comparing **accessible** names passes on two visibly identical buttons, because an `aria-label` keeps the names unique; and the same guard at **section** scope fails on correct code, because "Wijzigen" and "Verwijderen" appear twice there by E1-14's deliberate design. It is now scoped to the row and asserts both properties, with two panels genuinely open. *(2)* Asserting a `select`'s value after setting it to a missing option proves the **DOM**, not the component: a select refuses such a value by itself. The fake now reproduces the race (refuse **and** remove) and the assertion is on the submit's enabled state. **Ten mutation checks in total, and three of them only started biting after being rewritten.**
  >
  > ### Antagonist round 2 (on `c697d4f`, the fix round): VIOLATIONS FOUND, 2 MAJOR + 7 MINOR + 1 QUESTION. **Both MAJORs were introduced by fix round 1.**
  > It first verified, against the code rather than against my table, that **all ten round-1 findings are genuinely in the tree** and that all four gates reproduce on HEAD. Then it found that the pattern held for the third story in a row: the fix round is where the next defect lives.
  > **MAJOR 1 was caused by the fix for round 1's MAJOR 2.** Replacing the empty picker with a sentence took the **cancel button** with it, because the cancel lived inside the "there are destinations" arm. So in the very state that fix exists for, and in the loading and list-error states too, the panel had no control that closed it: no cancel, no submit, no Escape, and a trigger above it that only ever set a state which was already set. A panel with no way out beside an enabled control with no effect. The button row now sits outside every branch, the submit stays conditional and the cancel does not, and the test asserts the property over *every* state the panel can be in.
  > **MAJOR 2: the confirmation was a `role="status"` region mounted together with its text.** This codebase has written the opposite rule into its source twice, and **E4-06 shipped exactly this and found it silent in the one case that mattered**. It matters more here than in either earlier case: for a cross-thema move the row leaves the screen, so that sentence is the only evidence a teacher gets, and a screen-reader user could not tell a successful move from a delete. The region is now mounted with the section with only its text conditional, as `Schoolcontentimport.tsx` and `Themakaart.tsx` do. The auditor was explicit that it could not verify silence without a real screen reader and graded it on the contradicted rule instead, which is the honest form of that finding.
  > **The most useful MINOR was 3: my `geldigeKeuze` fix was untestable in its own fixture.** The refused destination was the klas's only candidate, so the picker was replaced by the empty-state sentence and the submit was absent *whatever* the component computed. Reverting the derivation left all 459 tests green. There is now a fixture with a second destination still on offer, and the assertion is on the submit staying disabled on a stale id.
  > *And MINOR 4 caught the same shape one level up:* **nothing asserted that a notice is ever cleared**, so the mechanism that replaced round 4's latched guard was unpinned exactly as the latched guard had been. It also found that rewriting that comment had deleted the repo's only record of the defect round 4 actually described (a successful delete of B still showing "iemand anders heeft het verwijderd" about A), which is **still live**: filed as **[E1-20](E1-curriculum-content.md)** and restored to the comment.
  > *The five textual MINORs, all fixed:* a comment still calling ruling 5 an open question after the owner had ruled and E1-19 existed; two comments asserting a grouping mechanism the fix round had replaced (the client groups on a keyed map now, so the SQL tie-break is about row order and the client half is defence in depth pinned only server-side); a unit-test comment claiming a property over *future* refusals that a hand-written list cannot have; this entry recording gates on a commit that was no longer HEAD; and the leeftijd sentence printing unconditionally, which is now tied to a different-age destination actually being on offer, because the ruling was that the crossing must be *disclosed*, not that the sentence must always appear.
  > *The QUESTION is routed, not answered:* the two new endpoints carry no `[Authorize]`, which the auditor verified is consistent pre-existing drift (only `OpstapImportController` has one) and belongs to **E6 / FR-10.2**. It also noted, unprompted, that `CLAUDE.md` still says E3-07 is reopened `[~]` while both this branch and `origin/main` carry it `[x]` — not this story's file, and passed to the technical lead.
  > **Twenty-five mutation checks across the story, seven of which only began biting after being rewritten. One took three attempts to be honest**: `hidden` as a Tailwind class is invisible to jsdom (no CSS), and a first-occurrence replace hit the *delete* panel's identical class list instead of the move panel, failing two unrelated tests while leaving the code under test untouched. **Fifteen mutation checks across the story.**
  >
  > ### Antagonist rounds 3 and 4: **VIOLATIONS FOUND** both times, 1 MAJOR each, and both MAJORs were in the *fixes* rather than in the build
  > Full table of all twenty-eight findings and their outcomes: [`worklogs/E4-08/antagonist.md`](worklogs/E4-08/antagonist.md).
  > **Round 3's MAJOR is the most transferable thing this story produced: a mutation check that passed because the test's own setup had already performed the thing under test.** The notice-clearing test clicked *Nieuw subthema* first, and that handler already clears notices, so the assertion never observed the `onSuccess` site round 1's fix had installed. Deleting either clearing site alone left the suite green; only both together failed. So the mechanism whose absence had made a confirmation never paint was unprotected, and **E1-20's text told the next author it was pinned.** The rule to carry: when a mutation check bites, check *which* assertion caught it.
  > *Round 3's other two, same family:* a property test named over *"every state the panel can be in"* drove **three of four**, and the missing loading state hid a live mutation (a submit with no picker); and `kanLeeftijdWisselen` could print above *"er is nu geen bestemming om uit te kiezen"*, because TanStack's `isRefetchError` keeps `data` while `isError` is true, so a failed **refetch** renders an error branch with stale candidates in hand.
  > **Round 4's MAJOR is the assistive-technology half of round 2's MAJOR 1, and it stood through two further rounds because every test and every browser pass looks at pixels.** The trigger announced `aria-expanded="true"` and did nothing when pressed in that state: a screen reader heard *"uitgevouwen, knop"* and activating it called `setVerplaatsen(true)` on a state already `true`, with no `aria-controls` to reach the panel it announced. **`axe` has no rule for a lying `aria-expanded`**, and the panel's axe run passed the entire time. Fixed by adopting the shape this repo already had twice (`aria-controls` + a real toggle), sharing one close routine with the cancel so closing from the trigger also resets. Three mutations bite it.
  > *Round 4's sharpest MINOR was my own repair of round 3:* adding *"Probeer het opnieuw."* to the list-error sentence with **nothing to press** is precisely the half-measure `Themakiezer`'s own fix round had rejected, and it is worse here, because closing and reopening this panel issues no request at all (the query is section-scoped, stays mounted with the row, and its retries are spent). The sentence states the fact and a button does the refetch.
  > *Also fixed in round 4:* three of four notice-clearing behaviours survived mutation, so the "mutually exclusive by construction" claim is now asserted in **both** directions (two tests plus a one-shot-404 fixture, because a successful cross-thema move takes the row off the screen and leaves nothing to move again); the panel's 404 guard was **deleted** as unreachable, per this story's own rule; `|| verplaatsen`'s justification had expired once round 2 made the cancel unconditional; and the list-error paragraph was the only one in the panel without a measure cap.
  > **Filed rather than fixed: [E7-20](E7-niet-functioneel.md)** — `aria-expanded` on disclosure triggers is answered three different ways in three files, each defended in its own comment, and no gate can catch a wrong call. Round 4 found the third answer *as a MAJOR*, which is the argument for writing the rule down where a builder meets it.
  >
  > ### Gates, re-run on every landing commit and independently by the round-2, round-3 and round-4 auditors
  > 575 unit + 201 integration against **real PostgreSQL**, 471 frontend / 20 files, **0 skipped**, `dotnet format` + eslint + `tsc` + `pnpm build` clean. Browser passes at 1440px and at exactly 390px against a live API and real PostgreSQL, run **four times**: one after the build and one after each fix round. They covered the colleague-deletes-the-destination race, a graadklas-shaped scenario, and finally the disclosure semantics plus the list-error state, which needed `Network.setBlockedURLs` on a **cold** document because the app's own QueryClient retries with backoff and a warm cache hides that state entirely. Measured: submit 8,90:1, empty-state sentence 5,80:1, disabled submit 2,16:1 (an inactive component, so outside SC 1.4.3, recorded because it is the state the panel opens in). The only two elements sitting past 390px are the nav's own `overflow-x:auto` scroller, and `document.documentElement.scrollWidth` reads 390, which is why that probe is the wrong one to use.
  >
  > ### Two further owner rulings, 2026-08-05, both taken on the audit's QUESTIONs
  > 4. **The *leeftijd* crossing is permitted, and the panel has to say what it means.** The audit was right that the earlier ruling covered only the **thema** boundary and that this half had been inferred by the builder. Ruled: allowed, because Art. IX.2 makes `(subthema × leeftijd)` the differentiation axis and this is the graadklas case the model exists for; **and** the panel states the consequence in words rather than leaving it to the age printed in an option label, since choosing another leeftijd changes which age group teaches the activiteit from then on. Implemented as `themabeheer.activiteitVerplaatsLeeftijd`, which also restates the klas boundary in the same breath: a teacher told about one scope will wonder about the other. The code comment now records the ruling instead of flagging the inference.
  > 5. **The subthema re-scope route stays as it is, and is filed rather than fixed here.** `PUT /api/subthemas/{id}` accepts a different `klasId`, and `WijzigScope` drags every activiteit and doelkoppeling inside the subthema across the class boundary. Ruled: leave it, do not widen this story, and **file it** so it does not live on as a code comment. It is **[E1-19](E1-curriculum-content.md)**, in the epic that owns the endpoint, and it needs its own ruling first (refuse the crossing, or permit it as an explicit correction that discloses how much content and whose dekking figures move). The invariant's comment here stays narrowed to the verb it actually guards, so this story claims nothing about the system it has not proven.
  >
  > ### Owed before this can go `[x]`
  > **A round 5**, on the reasoning rounds 3 and 4 have now demonstrated twice each: this story's fix rounds are where its remaining defects live. Fix round 4's surface is genuinely small (one render condition, one deleted branch, one new control, six comment or record edits), so the round is cheap; that is an argument about cost, not about need. Round 1's two questions were ruled on by the owner, and every other finding across four rounds is either fixed or filed as E7-19, E1-19, E1-20 or E7-20.
  >
  > *And check the read paths before assuming it is one write:* `SchoolcontentBeheerService` resolves activiteiten through their owning subthema, so a move changes what `GET /api/themas/{id}/voor-klas/{klasId}` returns for **two** classes. E7-16 applies: this is a write path, so it needs a test against real PostgreSQL and not only the in-memory provider.

- [ ] **E4-09 — Twee schermzinnen beloven behoud in een periode waar de hergeneratie wél doorgaat** — *filed 2026-08-19 by E4-07's ruling round, on the antagonist's instruction not to park a live suspicion in a paragraph*
  Two strings promise that what already stands in a period stays, in a context that reads as being about a generation run. In `frontend/src/i18n/nl.json`: `kalender.bezetteperiodesUitleg` (*"Wat er al stond, blijft staan"*) and `parameters.momentGeenThemaGevolg` (*"Wat er al staat, blijft staan"*). But the whole-plan discard is unconditional and plan-wide — `Jaarplan.VerwijderVervangbarePlaatsingen()` is `VerwijderVervangbare(_ => true)`, with no exclusion for a bezet period — so an untouched `Voorgesteld` placement in such a period **is** discarded by a run.
  *Why both, and why one story:* an earlier draft filed only the first and asserted the second was true, one paragraph apart, on wording that differs by a single letter (`stond` / `staat`). They stand or fall together. `momentGeenThemaGevolg` is the more exposed of the two: it renders inside the generation card at `Generatieparametersformulier.tsx:680`, above the button it may be wrong about, and `Generatieparameters.test.tsx:692` pins it as *"the boundary that keeps it honest: nothing already planned is removed."*
  *Read from the code, not measured.* **The first job is to reproduce it or refute it**, because the copy may be defensible on its render condition: both sentences may be about the *bezet marker* (you just chose this, and choosing it does not empty the period) rather than about a run. If that reading holds, the fix is to make the frame explicit; if it does not, the sentences are false on a shipped screen.
  *Reproduction:* generate a plan → add a **blokkerend** vast moment inside a period that holds an untouched `Voorgesteld` placement → regenerate the whole plan → read both sentences against what happened. Against real PostgreSQL and in a browser, since the claim is about what a teacher sees.
  *Done when:* both strings are true on every branch that renders them, and a test pins the case that is wrong today. Ref: FR-8.1, Art. II.3, Art. IV.2, and the E5-03 rule.
  > *Not E4-05's to fix, though the first string is its own:* E4-05 is `[x]` and merged, and reopening a closed story for a defect that turns out to span two features and two epics' copy would hide the shape of it. Filed as its own story on this backlog's own precedent (E1-17, E1-19, E1-20, E7-18, E7-19 were all filed rather than left in a paragraph).
