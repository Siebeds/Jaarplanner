# Functionele backlog (FB)

Tickets van de functioneel architect, aangemaakt op `main` met de skill `ticket-aanmaken`, altijd als `nieuw`. Alleen
de eigenaar wijzigt daarna hun status. Structuur, statussen en het bord: [`../TICKETS.md`](../TICKETS.md).

Dit bestand is uitleg, geen ticket: het bord en `tickets.mjs` slaan `README.md` over.
