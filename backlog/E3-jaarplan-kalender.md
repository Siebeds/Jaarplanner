# E3 — Jaarplan-generatie & kalender

**Phase:** 3 · **Milestone:** M3 — Plan & kalender
**Goal:** Generate a full year plan per class (thema's with their goals spread across the school year) and show it in an interactive drag-and-drop calendar — one of the two anchor screens.
**Covers FR:** FR-5, FR-6. **Constitution:** [Art. IV](../CONSTITUTION.md#article-iv--ai-is-advisory-human-in-the-loop), [Art. VIII](../CONSTITUTION.md#article-viii--tech-stack--architecture-binding-choices) (anchor screens), [Art. IX.3](../CONSTITUTION.md#article-ix--core-data-model-functional).
**UX & a11y:** this is an anchor screen — follow [`docs/ux/ui-ux-approach.md` §4](../docs/ux/ui-ux-approach.md) and [ADR-0017](../docs/adr/0017-ui-ux-design-system.md); WCAG 2.2 AA incl. keyboard-operable DnD.

---

### FR-5 — Generation

- [x] **E3-01 — Jaarplan generation service (structured JSON, advisory)** — *done 2026-07-29; test-runner PASS, 3 antagonist rounds, CI green with 0 skipped*
  *Gates:* CI run [30438090488](https://github.com/Siebeds/Jaarplanner/actions/runs/30438090488) — **412 unit + 73 integration passed, 0 skipped.** Three audit rounds found 1+3 MAJORs, all fixed: a `Klas` cascade that silently destroyed accepted and locked placements behind a code comment asserting a guard that did not exist; a delete guard whose remediation no endpoint supported (my instruction, not the implementer's); a `[PostgresFact]` that could never pass because a parameter placeholder sat inside quotes; and an unguarded `DELETE /api/themas/{id}` above a new Restrict FK, on another false comment. See [worklog](worklogs/E3-01/implementation.md) and [test report](worklogs/E3-01/test-report.md).
  > **A defect this story shipped, found on 2026-07-31 by E3-04 and recorded here because it belongs to this aggregate.** `Themaplaatsing.Id` is assigned in its constructor while the owned-collection key defaulted to EF's `ValueGenerated.OnAdd`, and `DetectChanges` decides Added-vs-Modified for an untracked entity in a **loaded** parent's collection from whether the key is set. So a placement added to an already-persisted `Jaarplan` was tracked as **`Modified`**, `SaveChanges` issued an UPDATE for a row that does not exist, and the request failed with `DbUpdateConcurrencyException`. **That is the ordinary FR-8 case: the second time a teacher presses "Jaarplan genereren".** It was invisible to *"CI green with 0 skipped"* because every green path either created the plan and its placements in one `SaveChanges` or regenerated with an answer that added nothing (empty, refused or duplicate), and no browser session reached it because generation 500s here with no `AzureAI:ApiKey`. Fixed in E3-04's branch with `ValueGeneratedNever()` on `themaplaatsingen` (plus the two new tables and `schoolsluitingen` as hardening) — metadata only, verified via `IMigrationsModelDiffer` to produce **no schema diff** — and pinned by `JaarplanPersistentieTests.Een_plaatsing_toevoegen_aan_een_bestaand_plan_slaagt`. *The lesson is about the gate, not the line:* a suite whose second-save path is never exercised reports green on a feature that fails on its second use.
  *Residual, accepted:* the **live Azure round-trip** is unverified — the real `AzureAiFoundryClient` is now driven offline over a stub `HttpMessageHandler` (13 executed cases covering URI, `api-key` header, `response_format`, `EnsureConfigured` and non-2xx), but no test authenticates against a real deployment. Only a manual run or a staging smoke check closes it.
  *Scope that landed here out of necessity, recorded not hidden:* a `SchooljarenController` (create/read — E6-03 narrowed accordingly) because a required `Klas.SchooljaarId` with no creation path would have made class creation unreachable; and `DELETE …/jaarplan/plaatsingen/{id}` (a slice of FR-7/E3-07) because the delete guard above needed a remediation that existed.
  *The lesson this story taught, recorded because it recurred:* CI was pushed on the **story branch before merging**, which is the only reason two defects were caught — 9 persistence tests failing on a `varchar(32)` overflow, and **6 previously-green E1-07 tests broken by the route change**. Both gates had reported "no regression" and both were right about what they checked; neither could see it, because those tests **skip** locally. A skipped test cannot visibly regress. The implementer added `TestSeedPastInHetSchemaTests` — 10 unskippable `[Fact]`s that build the EF model and compare seed helpers against real mapped column lengths — so that class of defect now fails locally.
  Generate a per-class plan: thema's + goals across planningsblokken; returned as validated JSON; persisted as a proposal (not auto-applied).
  *Done when:* a class yields a reviewable generated plan via the faked + real AI client. Ref: FR-5.1, Art. IV.
  *Also owns (assigned 2026-07-28):* Art. IX.3's "**`Schooljaar` contains multiple klassen**". E3-05 implemented only the vakantie-/periodestructuur half of `Schooljaar`; the Schooljaar↔Klas containment (and the `Jaarplan` entity itself, with its `vergrendeld` flag per thema) belongs here. Flagged by the E3-05 audit as previously unowned by any story.

- [x] **E3-02 — Spreading heuristics** — *done 2026-07-29; the claim is split into an asked half and a measured half, deliberately*
  Respect number of available blocks, logical order (e.g. seasonal thema's in season), and balanced goal distribution.
  *Done when:* generated plans place seasonal thema's sensibly and spread goals. Ref: FR-5.2.
  **The acceptance criterion as written cannot be tested, and pretending otherwise would have been the easy lie.** The AI client is a fake in every test (Art. IV.6), so "the model spreads well" is unfalsifiable here — asserting it would assert the fake. The story is therefore delivered as two halves that *are* verifiable:
  1. **Asked** — the prompt carries all three FR-5.2 properties and the figures each needs to be satisfiable: use as many different blocks as possible; a thema's `DuurWeken` must not exceed the block's weeks (so blocks now print weeks, not just days); place a season-suggesting thema in a block whose dates fall in that season; distribute the goals evenly (so each thema now prints its goal *count*, not just the codes). The block count is stated outright rather than left to be tallied. Pinned by `De_prompt_vraagt_spreiding_en_geeft_de_cijfers_die_daarvoor_nodig_zijn`.
  2. **Measured** — `Spreidingsrapport` describes what actually came back: blocks used vs available, empty periods, distinct goals per block, and any block whose thema's need more weeks than it spans. Returned on `JaarplanGeneratieResultaat` and surfaced in the UI. Seven unit tests feed it deliberately clumped and deliberately spread plans.
  *Deliberately threshold-free.* The report carries **no** pass/fail verdict and generation never retries or rejects on a bad spread (Art. IV.1) — an earlier sketch did retry until the spread passed a bar, which would have made the tool the decider and buried a model weakness in a loop. Nothing in the FA or the constitution defines an acceptable spread, so a limit in code would answer by fiat a question that belongs to the school — the same reasoning that keeps E3-10 question C open. The UI says so explicitly: *"Wat een goede spreiding is, beslist de school — de tool weigert niets."*
  *Two honest notes on seasonality.* `Thema` has **no season/period attribute**, so "seasonal thema's in season" can only come from the thema's own naam/invalshoeken plus the block's dates. The prompt is worded to reason from exactly those and still forbids outside sources (Art. IV.4) — but if the school wants this to be reliable rather than inferred, a `Thema.voorkeursperiode` field is the answer, and that is a data-model change for E6-03/directie, not something to invent here. Second, `IsOverbelast` is computed from `DuurWeken`, **not** from a thema count — three 2-week thema's fit a 6-week period, two 6-week ones do not — which is why it is a different signal from the kalender's provisional "te vol" count.
  *It is reachable from the browser, not just the API.* The kalender now carries the wireframe's **"Jaarplan genereren…"** button with the report rendered beneath it. Offering generation before E3-07 exists is safe because generation only ever *adds* proposals and never discards a decision or a lock; the destructive half (moving/removing) stays with E3-07 and its confirmation. This closes the gap E3-01's controller flagged — *"a teacher-facing screen is E3-06's job, so today this is reachable by an API client and not yet from a browser"* — which E3-06 had left open by shipping read-only.
  *A defect found by actually clicking it:* with no `AzureAI:ApiKey` set the client throws and the endpoint returns **500**, and the first version of the UI reported *"de AI gaf geen bruikbaar antwoord"* — blaming the model for a configuration fault and inviting a pointless retry loop. The UI now branches on status: 422 (model answered badly, retry is sensible) vs anything else (*"nu niet beschikbaar … meld dit aan de beheerder"*). Both state that nothing changed, which Art. IV.5 guarantees. **Worth escalating separately:** a missing API key arguably deserves a 503 from the backend rather than a 500, which would let the frontend stop inferring intent from a generic server error. Not done here — it touches E2-01's client and the shared exception handler.

- [~] **E3-03 — Aim for full coverage over the year** — *built 2026-08-05 on `story/E3-03-volledige-dekking` (off `origin/main` `2a29297`). `[~]` rather than `[x]` for one reason only: **no independent audit has run.** Every gate this session could run itself is green (584 unit + 200 integration with **0 skipped** against real PostgreSQL, `dotnet format` clean, `pnpm lint` clean, `pnpm build` clean, 478 frontend tests, plus a browser pass at 1440px and exactly 390px), but this repo's own record says a self-review is not a substitute for the antagonist: the independent pass has repeatedly caught defects in the fixes written to address its own earlier findings. Worklog: [`worklogs/E3-03/implementation.md`](worklogs/E3-03/implementation.md).*
  Generation targets complete dekking of the leerdoelen across the school year.
  *Done when:* a freshly generated plan reports high coverage in E5 (gaps surfaced, not hidden). Ref: FR-5.3.
  > **The acceptance criterion as written is unreachable, and the story decides deliberately what to measure instead.** A freshly generated plan reports **0 covered**, by design and permanently: every placement a run creates is `voorgesteld` (Art. IV.2) and only `aanvaard`/`manueel` counts as taught (Art. V.1), precisely so the AI cannot grant coverage (Art. IV.1). "A freshly generated plan reports high coverage" could therefore only be satisfied by weakening Art. V.1. **Decision: the run reports a `Dekkingsvooruitzicht`** that carries both figures side by side: what the plan covers today, and what it *would* cover if the teacher accepted every proposal standing in it. The second is explicitly a prospect and never called dekking, which is the Art. V.2 line this story is built along.
  > **Delivered as the two halves E3-02 established**, for the same reason: the AI client is a fake in every test (Art. IV.6), so "the model achieved good coverage" is unfalsifiable here and asserting it would assert the fake.
  > 1. **Asked** — the prompt gained a `Dekking` section: cover as many *different* leerplandoelen as possible; place every thema at least once *as long as it fits in the blocks*; when two thema's compete for a period, prefer the one whose doelen are not yet covered elsewhere; do not repeat a thema while another has no block. Subordinate to the fit rule by placement and by wording, so it never licenses an overfull period. The user prompt now states `Aantal thema's`, beside E3-02's block count, which is what makes "place every thema" checkable and makes the one case the rules arbitrate (more thema's than blocks) visible at a glance. **Deliberately absent: any target, percentage or curriculum.** A bar would ask the model to judge its own coverage, which is the retry loop E3-02 refused to build, using a denominator it cannot see. Pinned by `De_prompt_vraagt_volledige_dekking_en_zet_er_geen_streefcijfer_in`, which asserts the absences too.
  > 2. **Measured** — `DekkingService.BerekenVooruitzichtAsync`, returned on the generation response and rendered under the spreading report. It lives on `DekkingService` rather than beside the generator so that *every* rule that decides coverage — which of the four link layers count, which placement statuses count, what a stale placement does, which goals are in scope — is applied by exactly the code that computes the real dekking. A leaner copy next to the generator is how the two would drift, and this codebase has paid for that class twice already (the te-vol threshold, the four link layers). The scope resolution was extracted so both figures read the denominator from one method; `Het_gedekte_cijfer_is_hetzelfde_getal_als_de_dekkingsberekening_geeft` compares the two on one arrangement rather than trusting that.
  > **The denominator needed no invention: the owner's ruling of 2026-08-04 already settled it.** A class is measured against its own jaar/fase, so the outlook reports `bereik` + `gemetenJaarFasen` beside the figures and the panel names the scope. `aantalBuitenBereik` is deliberately **not** rendered here: the obligation to count what a narrowed denominator leaves out sits on the screen that offers the whole-curriculum switch (E5-02), and a number a teacher cannot act on is not a disclosure. The graadklas fallback widens and says so.
  > **What the browser pass found, and it is a copy defect rather than a layout one.** The first draft's explanatory line read *"Een thema telt pas mee voor de dekking zodra jij het aanvaardt"* — which is **E4-06's owner ruling 3 all over again**: `manueel` counts too, so aanvaarden is sufficient but not necessary, and a teacher who dragged a thema into a period would have been told their own decision did not count. It now reuses `beslisUitleg`'s ruled-on negative framing (*"Zolang een thema een AI-voorstel blijft, telt het niet mee voor de dekking"*). Second, the two figures both began with "Gedekt", so the line was tightened to *"Als je alle voorstellen aanvaardt: 9 van 14."* Measured in headless Chrome over CDP (Playwright's profile was held by another session) across **five states × two widths**: contrast 14,55:1 on the figures and 5,73:1 on the muted lines against `bg-paper`, no overflow at 390px, and the withheld state prints **no** number at all rather than a 0.
  > **Two states got their own sentence instead of a number**, because both would otherwise read as the opposite of what they mean: a plan with an unresolved stale placement (*"we cannot say"*, not *"it covers nothing"*, per directie 2026-07-28) and a class with 0 doelen in scope (*"nothing loaded yet"*, not *"alles gedekt"*).
  > **A finding that outlived the story, filed against E7-16:** `EfDekkingOpslag.HaalDekkendeKoppelingenAsync` throws `NotImplementedException` on the EF **in-memory** provider while running fine on Npgsql, which is E7-16's lesson pointing the other way. It broke five pre-existing in-memory endpoint tests the moment the generation endpoint started computing coverage; that fixture now stubs `IDekkingOpslag` and every figure it thereby cannot observe is asserted against real PostgreSQL instead.
  > *Also worth knowing:* `pnpm test` stayed green with **four broken type annotations** in test fixtures, and only `pnpm build` (`tsc -b`) found them. That is **E7-17** exactly as filed, met in the wild.
  > **Antagonist round 1 (2026-08-05): VIOLATIONS FOUND — 3 MAJOR, 5 MINOR, 3 QUESTION. All addressed in fix round 1; two of the MAJOR were false statements to a teacher, and neither was a code defect.**
  > 1. **The gap sentence was false in the ordinary state right after AI matching.** It read *"Zit in geen enkel gepland thema: 22"*, but the ceiling widens the **placement** status set while the **link** filter (aanvaard/manueel) stays where it lives, in SQL. So a doel that a placed thema carries through a still-`voorgesteld` doelsuggestie was reported as sitting in no planned thema at all. The number is unchanged and defensible; what it may be said to mean is narrower, so the line is now *"Ook dan nog niet gedekt: 22"* and the doc's invariant *"accepting proposals cannot reduce it"* was corrected (accepting a **doelsuggestie** can). Pinned by a Postgres test, because a fake port cannot express a link the port never returns.
  > 2. **The panel's figure never refreshed, on the screen where the invalidating actions happen.** It came from the mutation result, which nothing invalidates, while `usePlanMutatie` drops the dekking cache on every placement edit. Accept one card and the panel still said *"Nu gedekt: 0 van 34"* beside a live line that had already moved: **E4-06's "one card says two things", promoted to the figure a directie reads.** Now withheld rather than refreshed, keyed on comparing the plan the response carried with the plan on screen (`plaatsingssignatuur`), because "did anything actually change" is the only question that matters and a mutation counter would answer a different one.
  > 3. **Two denominators on one screen, and my stated reason was factually wrong.** The report refused a `jaarFase` narrowing "because the chooser lives on the dekkingsoverzicht". **It does not:** E3-09 put a `Jaarfasekiezer` on the kalender. A narrowed kleutergroep would have read one figure over K3 and, a few pixels away, one over JK+K2+K3. The chosen code now travels with the run (`POST …/generatie?jaarFase=`) and changes only what the figures are measured against, never the run.
  > *The five MINOR, each fixed:* the block was titled *"van dit voorstel"* while measuring the whole plan (including placements the run only kept); the withheld sentence was hard-plural where the sibling notice already has a singular; the 0-of-0 sentence claimed no leerplandoelen were loaded at all when hundreds may sit outside the scope; the terugval test asserted a string the component rendered for a different reason, so the fallback now has its own sentence and the component reads the flag; and **no test exercised the production wire** (DI + `EfDekkingOpslag` + Npgsql + the serialiser) — `aantalOnbereikbaar` is a derived getter, so a serialisation change could have dropped the gap line with every test green. `PostgresApiFactory` gained an AI stand-in and there is now a Postgres-backed endpoint test.
  > *Two of the three QUESTIONs were answered in the code:* the layering justification overstated its case (the cycle rules out a generator dependency, not an Application-layer orchestrator — that was a judgement about ceremony and now says so), and the "ceiling ≥ figure" invariant is a per-moment property across two non-transactional reads, which the doc now admits. **The third is the owner's and is open:** *"Plaats elk thema minstens één keer"* is issued over the whole school-wide thema-bibliotheek, i.e. it assumes every thema the school owns belongs in every class's year. Nothing auto-applies, so nothing is decided behind anyone's back, but it is a pedagogical default no FR states.
  > *Verified after the fix round:* **586 unit + 202 integration, 0 skipped**, 481 frontend, lint/format/build clean, and **fourteen** panel states re-read in a browser at both widths (five states plus the two new ones, desktop and 390px), including the stale state driven by a response whose plan really differs from the one on screen.
  > **Owner ruling, 2026-08-05, on the audit's open QUESTION: a class is not obliged to do every thema the school owns.** *"Elke klas van elk leerjaar kan zijn eigen thema's hebben. VAAK worden de thema's gealigneerd across klassen (bijvoorbeeld voor alle derde leerjaren) maar niet altijd."* The prompt asked for the opposite (*"Plaats elk thema minstens één keer"*), which asserted a pedagogical default no FR states. **Rewritten to ask for selection rather than exhaustion:** choose the combination of thema's that together covers the most, and the list is explicitly named as the school's library rather than required content for this class. Pinned in both directions, because the old sentence is individually plausible and would read as an improvement to anyone re-adding it. The `Aantal thema's` figure stays, on a new justification (it shows whether this is a selection problem at all), and the comment says so rather than letting an expired reason survive.
  > *The half the ruling exposes and does not close, filed in [`README.md`](README.md)'s open decisions:* `Thema` is school-wide and nothing records which thema's a class does, so generation is handed the whole library for every class. That is now a known defect rather than an unexamined default, and it needs a data-model answer, not a prompt.
  > **No longer blocked (2026-08-03, E5-01 closed).** This story was recorded as blocked because "its criterion measures coverage in E5, unbuilt". The measurement now exists: `GET /api/klassen/{klasId}/dekking` returns per-doel coverage plus `aantalGedekt`/`aantalLeerplandoelen`, computed on read, so a test can generate a plan and then assert on the figure without any screen. **Buildable today.**
  > **Two things to read before starting, because they change what "high coverage" can honestly mean.** First, a freshly generated plan places thema's as **`voorgesteld`**, and only `aanvaard`/`manueel` count for dekking (Art. V.1, so that the AI cannot grant coverage). **A freshly generated plan therefore reports 0 covered by design**, and this story's criterion as literally worded is unreachable: it has to measure either the *potential* coverage of what was placed, or coverage after acceptance. Decide that deliberately and say which. Second, the **denominator is an open Art. XIV decision** (see [`README.md`](README.md)): today it is the whole loaded curriculum, so "high coverage" for one class is arithmetically impossible while a K3 class is measured against every L1–L6 goal. A percentage target set before that ruling will encode the wrong denominator.

- [x] **E3-04 — Pre-generation parameters** — *closed 2026-07-31. Both halves merged 2026-07-30 (PR #13); the persistence half the owner's ruling asked for merged 2026-07-31 (PR #18), worklog section "Persistence half": the settings are kept per (klas, schooljaar), the form loads them, and generating saves them. Three fix rounds, two independent gates. The teacher review is no longer a gate (postponed to the end of the build).*
  > **The browser pass, and how it had to be done (2026-07-31).** Both gate agents died on an org monthly spend limit and the Playwright MCP server had disconnected, so the mandatory browser check was run by the orchestrator directly with **headless Chrome driven from Bash** against a live API, a dedicated PostgreSQL database and the demo seeder. Two harness pages loaded the real app in a same-origin iframe (headless Chrome on Windows clamps `--window-size` to ~504px, so a "390px screenshot" is a cropped 504px layout and reads as an overflow bug that is not there); a temporary Vite middleware forced `GET …/jaarplan/parameters` to fail. **Sampling had to use a `MutationObserver` rather than timers**, because `--virtual-time-budget` pauses virtual time while a fetch is pending, so an in-flight state cannot be polled. All temporary files reverted, the scratch database dropped.
  > **What it proved, measured rather than computed:**
  > - **The retry button's boundary is 5,497:1** against the composited backdrop `rgb(248,233,233)` (SC 1.4.11 floor 3:1). The fill contributes only **1,178:1**, so the border genuinely is the sole delineation, and the earlier `border-input` at 2,86:1 was a real miss. Alert sentence **5,497:1** at 12px; button label **15,424:1**.
  > - **The MAJOR is fixed where it mattered: with the panel shut.** `aria-expanded="false"`, the trigger reads *"(instellingen niet geladen)"*, exactly one `role="alert"` is visible without opening anything, it wraps no control, and the generate button is `disabled`. Evidence: [foutstaat](../docs/ux/wireframes/e3-04-r4-foutstaat-verstuurd.png).
  > - **The in-flight retry state is real**, which the previous round's finding said it could not be: on click the notice **stays**, the button becomes `disabled` and reads *"Opnieuw proberen, even geduld"*, and after the response resolves the notice clears, the trigger returns to *"(niets ingesteld)"* and generating is enabled again.
  > - **The class switch was driven by hand** with a second klas whose stored settings were a blocking *Oudercontact*. Setting a startthema for klas A gave *"(1 startthema)"*; switching to klas B gave *"(1 vast moment)"* — B's own kept setting — with A's startthema gone from every period select. The pending edit dies with the class.
  > - **No overflow at 390px** in either state (`scrollWidth == clientWidth == 390`), and the round-3 chevron fix holds: glyph at x37/y630 beside the label at x56/y627, where it used to sit orphaned above it at y645. Zero nested tab stops in the trigger.
  > *Observed, pre-existing, deliberately not fixed here:* `kalender.genereer` is *"Jaarplan genereren…"* — an ellipsis on an idle primary action, which conventionally signals either "in progress" or "opens a dialog", and this button does neither. It dates from E3-02 and `genereerBezig` is a separate string, so it is a copy question for whoever owns that label, not a defect this story introduced.
  Teacher supplies parameters before generation (**bv.** vakanties, vaste momenten, gewenste startthema's).
  *Done when:* parameters measurably influence the result. Ref: FR-5.4.
  > **The `bv.` is restored to this story text on purpose (2026-07-30).** FR-5.4 reads *"De leerkracht kan parameters meegeven vóór generatie (**bv.** vakanties, vaste momenten, gewenste startthema's)"* — *bijvoorbeeld*, so the three are illustrative, not an enumerated set to implement item by item. This story's earlier wording dropped it and read as a definite list, and the implementation was then built against the paraphrase rather than the FA, which the constitution ranks above the backlog. It cost a long defence of a position the source grants in six characters. **Quote the FA when the FA is the criterion.**
  *Built (backend only, 2026-07-30) — worklog: [`worklogs/E3-04/implementation.md`](worklogs/E3-04/implementation.md).* `JaarplanGeneratieParameters` + `VastMoment`, a prompt section, `ParameterRapport`, enforcement in `JaarplanGeneratieService`, and an optional request body on `POST …/jaarplan/generatie` so the E3-02 kalender button keeps working unchanged.
  - **Vakanties are deliberately not a parameter, and the FA says so itself.** FR-12.1 assigns them to another role and another surface: *"De beheerder kan schooljaren aanmaken en de vakantie-/periodestructuur instellen."* They are persisted school data on `Schooljaar` (E3-05) and blocks are derived from it, so accepting them again would create a second source of truth for the school calendar.
  - **The structural half of that argument is narrower than first claimed.** `Schooljaar.Lesperiodes()` cuts only on `BreektPeriode`, so a **`Vakantie`** genuinely cannot be spanned by a block. A **`VrijeDag`** sits *inside* a block by design (ADR-0020 §5) and is currently **not expressed to the model at all** — and the prompt prints `Planningsblok.AantalDagen`, a raw calendar span, while `Spreidingsrapport` measures the same block in **open days**. So the model can be told a block is longer than the report will measure it, satisfy the prompt's own fit rule, and still be flagged `IsOverbelast`. That is pre-existing E3-01/E3-02 drift, **not introduced here**, but it is the load-bearing premise of this story's headline decision. **Filed as its own clause below rather than fixed in passing**, because the fix (print open-day weeks from `Schooljaar.TelOpenDagen`, the method that exists precisely because two callers disagreeing about block length was already a defect) touches the E3-02 prompt and deserves its own gate.
  - **The two parameters that do exist are different in kind, on purpose.** A *gewenst startthema* is a **preference**: it reaches the prompt and the report says whether the model complied. A *vast moment* that blocks is a **constraint**: the service refuses placements landing in its period. Enforcing the preference would mean the tool placing a thema no model proposed, leaving its provenance unstatable — `voorgesteld` would be false, and `manueel` survives regeneration and would strand a parameter the teacher had since changed.
  - **A closure is not a vast moment.** Anything that closes the school (vakantie, Hemelvaart, Pinkstermaandag, pedagogische studiedag) is a `Schoolsluiting` on the schooljaar under FR-12.1. An earlier revision listed *"pedagogische studiedag"* as a `VastMoment` example, contradicting ADR-0020 §5 outright and recreating for its neighbour the exact second-source-of-truth the vakantie exclusion was reasoned from. The boundary is now a stated rule, so a UI cannot offer two forms for one fact.
  *Audit (2026-07-30) — VIOLATIONS FOUND, no CRITICAL; six findings had to be fixed before any UI could land on this, and all six are fixed:*
  1. **Plural `GewensteStartthemas` had single-thema semantics.** One sentence named one block for a comma-joined list, instructing the model to put several 4–6 week thema's in one themaperiode — against the system prompt's own *"zoveel mogelijk verschillende planningsblokken"* and its fit rule — and the report then guaranteed a false *"niet gehonoreerd"* entry for any teacher who named two. Now **positional**: the i-th name targets the i-th block, which is what makes a plural list mean anything and makes the documented *"in the order given"* true instead of decorative.
  2. **`BlokkeertPlaatsing` defaulted to `false`**, i.e. to the only outcome that produces **no signal at all**: the persisted plan is byte-identical to a run with no parameters and the report is empty, so it is indistinguishable from *"your instruction was honoured"*. A form that forgot one checkbox would have shipped a control with no effect, which CLAUDE.md's E3-06 rule forbids outright. The default is gone and the field is `[JsonRequired]`, so omitting it is a 400.
  3. **A refused placement was thrown away.** Unlike an unknown thema name or date, it is fully *resolvable* — the thema, the block and the model's `motivatie` all exist — so discarding it left a thema planned nowhere, destroyed the motivation, lowered the dekking Art. V exists to prove, and gave the teacher nothing to act on. And `HeeftAandachtspunten` **excluded** it, so a UI honouring the documented contract would have hidden the loss. Both fixed; the refusal now carries the motivation for E3-07's re-placement.
  4. **The report could blame the model for the tool's own refusal.** Ask to open with Water *and* block the first period, and the service refuses Water and filed it under *"the model declined"*. New `TegenstrijdigeStartthemas` keeps the teacher's self-conflict apart from model non-compliance. Untested before; now pinned.
  5. **Composed Dutch presentation strings** (`"Herfst @ 2026-09-01 (Schoolfeest)"`) carrying an ISO date no Dutch teacher reads, destined for a screen: the shape E3-06 was reverted for, and two more free-text lists on option (b)'s migration surface. Every entry is now a **record** (`GeweigerdePlaatsing`, `VastMomentUitkomst`) and the UI composes the sentence from `nl.json`.
  6. **Two vaste momenten in one period lost the second one entirely** from the report — the same defect `OnplaatsbareVasteMomenten` exists to prevent, one case over. `ToegepasteVasteMomenten` now reports every resolved moment.
  *Fixed while gating, and worth recording separately because it was not this story's:* `Postgres/PostgresTestDatabase` dropped its test database `WITH (FORCE)`, which terminates lingering backends and needs superuser or `pg_signal_backend` — privileges the local and CI `jaarplanner` roles do not have. It failed about **one full-suite run in three** with `42501: permission denied to terminate process`, on whichever Postgres test finished first, because `ClearAllPools()` returns when the *client* releases its handles, not when the server has closed those backends. Now a plain drop retried on `55006`, which needs no privilege beyond ownership. **A test that fails one run in three is indistinguishable from a real regression**, and this project has already paid twice for gates that could not be trusted.
  *Gates:* **437 unit + 81 integration passed, 0 failed, 0 skipped**, run against real PostgreSQL and repeated **four consecutive times** to prove the flake above is gone. `dotnet format` clean, build 0 warnings. Reachability is asserted over HTTP (`Een_vast_moment_uit_de_request_body_weigert_een_plaatsing`), not only in unit tests — the M2 lesson.
  *Waived deliberately:* `JaarplanGeneratieParameters` is bound straight from the body by `[FromBody]`, so an Application type is the request contract, while the same controller declares nested API records for its other two bodies. Responses being Application types is established precedent here; requests were not. Left as-is for now and recorded so it is a choice rather than an oversight.
  > **Widened on purpose (owner ruling via adjudication, 2026-07-31), because the persistence half made the type carry `IValidatableObject` too** — validation whose only effect comes from `[ApiController]` invoking it, i.e. more MVC-awareness in an Application type than the binding alone. **The waiver now covers both.** Reasoning: no framework *dependency* is added (`IValidatableObject` is `System.ComponentModel.DataAnnotations`, and the Application project already references nothing new for it), the behaviour is proven over HTTP rather than only in unit tests, and moving the contract to a nested Api record plus a mapping is a larger change than the thing being waived — which would be reversing a decision to avoid recording it. If a later story does move request contracts into `Api`, this type goes with them; until then it is a **choice**, restated rather than quietly grown.
  **The UI half landed 2026-07-30**, after the E2-08 merge settled the jaarplan screen. `Generatieparametersformulier` (the form) + `Parameteroverzicht` (the report), inside the existing generation card.
  - **Collapsed by default.** A teacher generates a year plan once or twice a year, so a permanently-open two-list form would be the largest object on the anchor screen for a task almost nobody is doing right now. Generating without parameters stays one click; the disclosure's summary states whether anything is set, so a parameterised run is never a surprise.
  - **Startthema's render as one row per period, not as a list of names.** Each entry **names its own period by that period's start date** (`{ blokStart, themaNaam }`), and each row is labelled *"Periode 1 · 1 sep – 1 okt"*, so the row and the wire agree about which period is meant. *(The contract was positional until 2026-07-31 — the i-th name targeted the i-th block — which is what made the labelling load-bearing rather than merely helpful; see the closed open item below.)* This is why the form takes the derived grid and not just the thema names. **It also takes the grid's `niveau`:** a kept setting keys on the *generation* tier (`Themaperiode`), and once E3-08's zoom fetches `Subthemaperiode` the board's blocks are no longer the periods those settings name, so the form checks the tier instead of assuming it.
  - **The thema comes from a picker** (`GET /api/themas/bibliotheek`), which makes the server's `onbekendeStartthemas` case much *harder* to reach: a teacher cannot mistype a name they never type. It is **not unreachable** — the list is cached with no `staleTime`, so a rename or delete between fetch and generation still lands there, which is why the report still handles it. (An earlier draft of this entry claimed "unreachable"; the code comment was right and the backlog was stronger than the code.) E1-14 still owns a real thema-beheer screen.
  - **"Mag er een thema in die periode?" has no pre-selected answer**, mirroring the server's `[JsonRequired]`. Radios rather than a checkbox precisely *because* a checkbox has a default. A moment with a name, a date **and** an answer is sent; anything less is not, and a row that is begun but unfinished is named as such in the collapsed summary (see the audit fixes below).
  - **Every period is a live row, and a gap is simply "no preference".** Clearing one period leaves the others exactly as they were. *(Until 2026-07-31 the rows grew one period at a time and clearing a period cascaded into the later ones — both forced by the positional contract, since a gap would have shifted every later choice one period earlier. With a date key there is nothing left to prevent: the growing list, the clear-cascade and the "a gap must be unexpressible" rule are all gone, along with the copy that explained them.)*
  *What looking at the screen caught that 94 green tests did not — all three fixed:*
  1. **Seven period rows, six of them disabled.** The first version rendered every period, so a task where a teacher realistically names one or two thema's presented a column of dead controls, and a disabled select reads as broken rather than as not-yet-reachable. Now one row that grows, which also made the sentence explaining the top-to-bottom rule unnecessary: prose is the first thing to cut.
  2. **"1 startthema's, 1 vaste momenten"** in the disclosure summary. Wrong Dutch for the commonest count, and the **third** time this project has shipped this exact plural bug (E3-06's *"1 thema's staan"*, then four more strings in the E3-06 code review). Routed through `tAantal`, with the zero parts omitted rather than printed as "0 vaste momenten". **This fix was incomplete when first claimed** — the audit found a fourth instance still shipping in the same commit (`rapportOnbekend`, finding 2 below). Fixed as a class now, not as an instance: *the same failure mode this backlog recorded on 2026-07-28, and the reason "fixed as a class" has to be checked rather than asserted.*
  3. **The refusal block's heading was singular while its closing line was plural** for the same single thema. Same defect one sentence apart. Also pluralised.
  *A defect a test caught that the code hid:* `Parameteroverzicht` omitted `geweigerdDoorVastMoment` from its emptiness check, so a run whose **only** outcome was a refused placement rendered *nothing at all* — hiding the most actionable message on the screen, since that thema ends up planned nowhere. In practice a refusal implies an applied moment, so the case looked unreachable; asserting it directly showed it was not.
  *Verified in a real browser against live PostgreSQL* (API :5284, Vite :5273): the form produced exactly `{"gewensteStartthemas":["Water"],"vasteMomenten":[{"naam":"Schoolfeest","datum":"2026-09-15","blokkeertPlaatsing":true}]}` on the wire, the thema picker listed the seven real seeded thema's, the unanswered-question warning fired, and there is **no horizontal overflow at 390px**. Evidence: [formulier](../docs/ux/wireframes/e3-04-parameters-formulier.png), [rapport](../docs/ux/wireframes/e3-04-parameters-rapport.png).
  *Contrast measured in the browser, not assumed:* the attention text used by the report and the undecided-moment warning is **9.37:1**.
  > **The `--input` token is fixed, here, and the reasoning that first deferred it was wrong (audit round 2).** The four new controls used `border-input` at **1.42:1** against SC 1.4.11's 3:1 floor, and this entry argued the choice was between *deviating for four controls* and *leaving it to E7-10* — recording "they join the existing failing population rather than introduce a new defect" as the honest move. The audit's reply is correct and worth keeping: that is a claim about **consistency**, not about **conformance**, and the option set was false. A verified one-token value was already in hand, changing it is not a deviation but *the* fix, it repairs every existing failing control at once, and it is one line. `--input` is now **`40 14% 52%`** = **3,40:1 on card / 3,21:1 on paper**, re-measured in the browser across every consumer. Recorded because the shape of the error is reusable: *a story that re-describes a defect as a population is absolving itself.*
  > Still open for **E7-10**: the `secondary` button variant (*Manueel aanpassen*) has **no border at all** and a fill measuring **1,16:1** against the white card it sits on. Not touched here; it needs a fill or a boundary, not this token.
  *The residual, stated rather than buried:* **no live AI round-trip was exercised**, because this environment has no `AzureAI:ApiKey` and generation returns 500 (the UI correctly says *"nu niet beschikbaar … meld dit aan de beheerder"* rather than blaming the model). The report was therefore rendered in the real browser against a **stubbed response**, and end-to-end enforcement is covered by the integration test over HTTP with a stub AI client. Same residual E3-01 recorded for the Azure client: only a manual run or a staging smoke check closes it.
  *Audit round 2 on the UI half (2026-07-30) — VIOLATIONS FOUND; every blocking finding fixed:*
  1. **The collapsed summary asserted an instruction that would not be sent.** It counted any moment with a name and a date, ignoring the blocking question — so a teacher who left that unanswered, collapsed the panel and generated saw *"(1 vast moment)"* while the run sent nothing and the report said nothing. The warning that explains it lives *inside* the panel, which is closed by default. That is the backend audit's *"indistinguishable from your instruction was honoured"* defect re-created one layer up, and it falsified this component's own argument for being collapsible. Counts now derive from what will be sent, and unfinished rows are named separately (*"1 nog onvolledig"*). Pinned by a test that collapses the panel first.
  2. **The plural bug shipped again in the commit that announced fixing it.** `rapportOnbekend` was the one report line with no `…Enkelvoud` sibling, so a single unknown thema read *"Deze thema's kent de school niet"* — the **fourth** instance. The claim "pinned" was an overclaim; it is now true, and the corresponding *"found by looking"* bullet above says *incomplete* rather than fixed.
  3. **The thema picker used the wrong endpoint.** `GET /api/themas` returns `ThemaWeergave` with `Subthemas`, i.e. **every class's** class- and age-scoped subthema's, subdoelen and activiteiten — a whole subtree, to fill a dropdown with names. `/api/themas/bibliotheek` exists precisely to avoid that (Art. IX.2, no cross-class bleed) and this was the first frontend consumer of either. Also now `enabled: open`, so the collapse saves bytes as well as pixels.
  4. **A stub 404'd silently in a neighbouring test.** `Jaarplankalender.test.tsx` documents that an unrouted URL *"404s loudly"*, but `/api/themas` was unrouted and the form is closed there, so the only symptom was a React `act()` warning attributed to a component that file does not test. Routed.
  5. **The em-dash guard could not fail.** It asserted over `{}` and over a `document.body` that `afterEach` had already emptied, covering three hard-listed keys while reading as catalogue-wide. Now one assertion over the whole of `nl.json` — which also protects every other feature.
  6. **The committed evidence screenshot showed the defect the story claimed to have fixed** (*"1 startthema's"*): it was simply never retaken after the fix. Retaken. Zero user risk, direct credibility risk in a story whose argument is *"I looked at it"*.
  7. **The state keyed on block position, so the form could desync from the grid.** `blokken` refetches on window focus, so a beheerder shrinking the year left a choice for a period that no longer existed — which was then sent, and the server filed it under `TegenstrijdigeStartthemas`, telling the teacher they had marked that period bezet themselves. Now keyed on the block's **start date**, the same stable key the rest of the system uses (ADR-0020 §3).
  8. **Smaller ones:** the clear-cascade now states its consequence *before* it can be triggered rather than only after; `aria-labelledby` on a plain `<div>` named nothing and is replaced by `aria-controls` on the trigger; the trigger names its content (*"Vooraf instellen: startthema's en vaste momenten"*) because the old label named none, which was the real discoverability gap; *"Je voorstel blijft staan"* became *"Je keuze"* since **voorstel/voorgesteld** is the AI suggestion status on this very screen (Art. II.4); the form now says the settings apply **to this run**, since nothing persists them; and a false test comment claiming a no-parameter run returns `null` is corrected, with a test added for the `ParameterRapport.Geen` shape the server actually sends.
  *Corrected overclaim, kept visible:* an earlier draft said *"a half-answered moment is not sent, and the screen says so instead of dropping it silently."* A moment with a name but no date was in fact dropped with **no** message. Now the summary names any begun-but-unfinished row.
  *Gates:* **97 frontend tests / 9 files**, `pnpm lint` and `pnpm build` exit 0. Backend untouched by the UI commits; the **459 unit + 91 integration, 0 failed, 0 skipped** figure is the full suite measured after merging `main` (so it includes E2-08's tests), not a re-run of the 437+81 recorded for the backend half.
  **The persistence half landed 2026-07-31** (branch `story/E3-04-persistentie`; worklog section *"Persistence half"*). It is what the owner's ruling above asked for, and it changed the contract as well as the schema:
  - **`Generatieparameters` per (klas, schooljaar)**, with `BewaardStartthema` and `BewaardVastMoment` as owned collections, one migration, a unique index on the pair, one startthema per period in the database, and cascade-on-klas-delete. The school-year half of the key exists because every stored value is a **date**.
  - **The request now keys a preference on `blokStart`, not on array position** — the open item below is closed accordingly, and everything the positional contract forced on the UI is gone.
  - **Both request arrays are `[JsonRequired]`, and two preferences for one period are a 400.** Since a body *replaces* the kept settings, an omitted array and a silent de-duplication both destroy durable teacher input with nothing in the report to say so. Same argument that made `blokkeertPlaatsing` required. Posting **no body** still means "use what is stored".
  - **A stranded kept preference is named in the form (outside the collapse) and in `ParameterRapport.VervallenStartthemas`**, never dropped and never moved to a neighbouring period (directie 2026-07-28). It stays in the request, so reverting the vakantie edit restores it.
  - **A concurrent second run no longer 500s *on the settings row*.** Two runs starting together both create the row; the loser's insert is discarded behind the storage port and it writes into the winner's row (last write wins), instead of surfacing a raw Postgres `23505`. **The scope of that sentence is deliberate** — see the two residuals recorded under audit round 4 below, because an earlier draft of this bullet claimed the whole class.
  - **A pre-existing EF defect was fixed on the way:** owned-collection Guid keys defaulted to `ValueGeneratedOnAdd`, so a new `Themaplaatsing` added to a **loaded** plan was tracked as `Modified` and any second generation run that placed a thema threw `DbUpdateConcurrencyException`. `ValueGeneratedNever()` on `themaplaatsingen`, both new tables and `schoolsluitingen`; metadata only, no schema diff. See the E3-01 note above.
  *Audit round 3 (2026-07-31, on the persistence half) — VIOLATIONS FOUND (1 MAJOR, 7 MINOR); all fixed:*
  1. **MAJOR: a failed settings load made the collapsed screen assert the opposite of what would happen.** With the settings GET failing, the trigger read *"(niets ingesteld)"* while the run sent **no body**, so the server applied whatever it had stored: a teacher with a saved blocking vast moment read the exact opposite of the truth. The mitigation had been placed *inside* the collapse, which is where the same defect hid the first time. The failure is now stated outside it, the summary distinguishes *unknown* from *absent*, and **generation is refused until the settings are known** — a run whose parameters the screen cannot state is a run nobody can consent to. Pinned by a test that never opens the disclosure.
  2. **The summary counted a stranded preference twice** (once as a startthema, once as "zonder periode"), so one setting read as two. The clauses partition the set now.
  3. **The stranded notice matches its E3-07 sibling**: a labelled `region` with an sr-only `role="status"` line, non-dismissible. *Not* one big `alert` — it holds a button per entry, and a live region wrapping controls re-announces everything on every interaction, which is exactly why `TeHerzien` was changed in E3-07. (The finding said the sibling was an `alert`; that was true of E3-06 and stale since E3-07.)
  4. **The parameter form is told which tier its blocks belong to.** The stranded check compared against whatever grid the kalender passed, which agreed only because `/rooster` defaults to `Themaperiode`; E3-08's zoom would have flagged **every** kept preference as *"zonder periode"* and offered rows the server reports as vervallen. Now checked against the generation tier and pinned by a test, **before** E3-08 lands.
  5. **An empty submission no longer writes an empty row**, and the comment claiming it never did is gone: the form posts a body on every run, so every class was getting one.
  6. **Backlog drift, which is why the bullets above were rewritten**: this entry still described the positional contract, the growing rows and the clear-cascade as current, and still listed the positional question as open. A reader consulting the backlog (which CLAUDE.md designates as the thing to consult) was being misinformed about the shipped contract.
  7. **A worklog mechanism was wrong** (`DoelKoppeling`'s shadow key is a **Guid**, not an int) — right conclusion, wrong reason, and the reason is the part that gets reused. Corrected in the worklog.
  *Gates (fix round):* `dotnet format` clean, build 0 warnings, **478 unit + 112 integration passed, 0 failed, 0 skipped** against real PostgreSQL; **125 frontend tests / 9 files**, `pnpm lint` / `pnpm build` exit 0, 0 React `act()` warnings.
  *Audit round 4 (2026-07-31) — VIOLATIONS FOUND (1 MAJOR, 5 MINOR, 3 QUESTION); all fixed or recorded. The test-runner passed the whole story in a real browser in the same round.*
  1. **MAJOR: the refusal gated the generate button but not the form.** With the settings GET failing a teacher could still open the panel and set a startthema for any period behind an action that could never fire — and worse, an edit there would post a body that **replaces** the kept settings, so one startthema would silently delete a stored blocking vast moment they never saw. The same one-line gate also closed a display/request desync: an errored TanStack query is stale, so it refetches on window focus, and a retry that succeeded overwrote the typed fields *without* re-reporting them, leaving the parent holding the earlier edit. Gated now, so no edit can exist for the load effect to clobber.
  2. **The failure state had no way forward.** Its copy said *"herlaad de pagina en probeer opnieuw"* — which the query client's own three retries had already done before the notice could appear — and it lacked the escalation sentence its sibling `kalender.genereerOnbeschikbaar` ends with. Now an **"Opnieuw proberen"** button (a *sibling* of the `role="alert"`, never a child, so the live region does not wrap a control) plus copy that states the consequence and names the beheerder. The absence of *"herlaad"* is asserted, so the copy cannot drift back.
  3. **`IsUniekeSleutelSchending` matched a substring, not the table.** `Contains("generatieparameters")` also matches both child tables' FK indexes (`…GeneratieparametersId_BlokStart`, `…GeneratieparametersId_Datum`), so a child-table `23505` would have been reported as the lost race and 500'd blaming a duplicate settings row that never existed. Matches the table exactly now, and the false comment is gone.
  4. **The race recovery had never been exercised as a composition** — a unit test against a fake with no EF, plus an EF test that *reproduced* the service's four calls. Now also `De_service_overleeft_de_verloren_race_op_de_echte_opslag`: the real service on the real `EfJaarplanOpslag` against real Postgres, with the winning run spliced into the race window by a delegating decorator.
  5. **The generation tier was duplicated across the wire with nothing binding the halves** (`types.ts`'s `GENERATIEBLOKNIVEAU` vs `JaarplanGeneratieService.GeneratieNiveau`, coupled by a doc comment; the rooster endpoint test pins the *rooster* default, a different decision). A backend test now reads `types.ts` and compares the literal, so moving the tier fails a test instead of silently degrading the form to "another tier" forever.
  6. **390px:** the chevron floated alone above its own label once the trigger wrapped. Chevron and label are one non-wrapping inner flex item now.
  *Two residuals recorded rather than fixed, both about concurrency:*
  - **The jaarplan race is still open, and it is E3-01's aggregate.** `LaadOfMaakJaarplanAsync` does the identical unguarded load-or-create for `Jaarplan`, which also has a unique index on `KlasId`, and `BewaarAsync` catches nothing — so two simultaneous **first-ever** runs for one class still 500 one step past the settings row, and two simultaneous regenerations can still lose the `VerwijderVervangbarePlaatsingen` delete. Not fixed here because the resolution is genuinely different: a plan write is not "last write wins" in any obvious sense.
  - **Last write wins is ratified, and its residual is invisible rather than wrong.** It is byte-for-byte the semantics of two runs a second apart, which the owner ruled in. What nothing says out loud: the **winning** run generates from settings the database no longer holds by the time it finishes, and neither run's `ParameterRapport` mentions that a concurrent run replaced them.
  *One round-3 finding was **withdrawn** by both gates rather than waived (2026-07-31), and the distinction matters for the record:* the demand that the stranded notice be `role="alert"` rested on its E3-07 sibling being one, and `git log -S` shows `TeHerzien` moved to region + sr-only `status` in `57a4c5e` — the commit addressing E3-07's own audit, for exactly the reason the dispute gave. The implementation was right and stands; the finding was wrong.
  *Gates (fix round 2):* `dotnet format` clean, build 0 warnings, **479 unit + 113 integration passed, 0 failed, 0 skipped** against real PostgreSQL; **127 frontend tests / 9 files**, `pnpm lint` / `pnpm build` exit 0.
  *Open, and nobody has ruled:*
  - ~~**Are pre-generation parameters remembered?**~~ — **RESOLVED (owner, 2026-07-30): yes, the generation settings are kept.** This was a **feature change, not a note**, and it is **built (2026-07-31)**; all five clauses below are answered, four of them done and the fifth deliberately not:
    1. ✅ `Generatieparameters` is **persisted state per (klas, schooljaar)** — a domain entity with two owned collections, a table each, one migration, a unique index on the pair and cascades from `klassen` (Art. IX.3). The school-year half of the key is load-bearing: every value stored is a **date**, so a row must never be read for another year. First data-model change since E3-05, as predicted.
    2. ✅ The form **loads** the saved settings and shows them, and a run posts them back, so an untouched form sends exactly what was saved. There is deliberately **no "Bewaren" button**: a body *replaces* the kept settings and no body *uses* them, so clearing a field and generating is how you clear a setting.
    3. ✅ **A run that is handed no parameters reads the stored ones**, on the single generation path — so E4-04/E4-05 inherit it by calling the service rather than by adding it. A period marked bezet stays bezet on the next run.
    4. ✅ `parameters.uitleg` was rewritten in the same commit: it now says the settings stay saved for this class and that removing something and generating again is how you clear it.
    5. **Not acted on, as instructed.** `gewenste startthema's` stay **advisory**. Whether a durable preference should now be *enforced* is a separate decision about the tool placing a thema no model proposed, and it is not taken as a side effect of persistence.
  - ~~**Which record does a teacher use for a schoolfeest**~~ — **RESOLVED (owner, 2026-07-30): a schoolfeest is a `VastMoment`.** So the boundary the code already stated is ratified: a **closure** (vakantie, Hemelvaart, Pinkstermaandag, pedagogische studiedag) is a `Schoolsluiting` on the schooljaar under FR-12.1, and a day the school has committed **inside** a teaching period is a `VastMoment` supplied by the leerkracht under FR-5.4. The instructional copy in the form is therefore correct and no longer an unratified assumption rendered to users. No code change needed — which is the useful part: the audit had flagged this as a boundary stated to teachers before the school agreed it, and the ruling closes that without a migration.
  - ~~**Should the startthema contract be positional at all?**~~ — **RESOLVED (2026-07-31, with the persistence half): no. Both the storage and the request key on `blokStart`.** The contract is now `{ blokStart, themaNaam }[]`, matching the report, the placement key and `PUT …/plaatsingen/{id}/blok` (ADR-0020 §3). **Persistence is what settled it:** a *stored* ordinal is strictly worse than a transmitted one, because it survives exactly the vakantie edits that invalidate it, and keeping storage on dates while the request stayed positional would have meant a position↔date mapping at the boundary — which is where the bug would live. Everything the positional contract needed is gone with it (the growing list, the clear-cascade, the gap rule), and de-duplication moved from per-name to per-period. **Two consequences worth knowing:** a stored `blokStart` can stop being a period boundary, which is reported in the form *and* in `ParameterRapport.VervallenStartthemas` rather than dropped or moved (directie 2026-07-28); and two entries for one period are now a **400** rather than a silent de-duplication, since a body replaces the kept settings and quietly keeping the first would delete the second for good.
  - **The open-day vs calendar-day prompt gap** described above: the model is told block lengths the spreading report will contradict.
  - ~~**Does this UI need the same directie/teacher review E3-06 is `[~]` for?**~~ — **RESOLVED (owner, 2026-07-30): the review is postponed until nearly the whole build is done.** Two consequences, and the second matters more than the first.
    1. **E3-06 and E3-04 do not close on a review gate any time soon.** They stay `[~]`, honestly, for months rather than days. That is bookkeeping and it is fine, but nobody should read either checkbox as "blocked on me".
    2. **It unblocked E3-07, which has since shipped.** Its clause said *"E3-04, or E3-07 after the review, is next"*, written when the review looked imminent; holding drag-and-drop for a session that is deliberately late would have stalled the rest of E3 and then E4. E3-07 was built on the **approved E3-10 wireframe** plus the directie ruling of 2026-07-28 on stale placements, and closed the same day — so this ruling is the reason that story exists rather than a note about it.
    *The risk this accepts, stated plainly:* everything from E3-06 onward is now built against one owner's judgement and a paper wireframe, with the teacher check arriving after most of it is written. That is the owner's call to make, and it is the right one if the alternative is stalling. But it inverts ADR-0017's wireframes-first intent, so the later the feedback lands, the more of it will be **rework rather than direction** — and E3-10's five open questions (period rhythm, a thema spanning two periods, what makes a period *te vol*, the ongeplande-doelen tray, what belongs on a card) will each have been answered by default in code by then. Worth one deliberate re-read of those five when the session is finally scheduled. It lives *inside* E3-06's screen, so marking E3-04 done while its host screen waits on that review would be inconsistent. **Answer it once, for both.** This is the question that currently keeps E3-04 open (see below).

### FR-6 — Calendar

- [x] **E3-05 — Planningsblok model & calendar grid** — *audited 2026-07-28 (VIOLATIONS FOUND), findings fixed, CI green 2026-07-28*
  Model the school year as configurable planningsblokken; **do not hard-assume months** — support themaperiode (4–6 wk) / subthemaperiode (~2 wk). Belgian school year Sept→June.
  *Done when:* the block unit is configurable behind a seam; default is documented, not compiled-in. Ref: Art. IX.3, Gap A.6.
  *Decision (directie 2026-07-14, Art. XIV resolved):* default is the **two-tier** model — themaperiode (4–6 wk, coarse) + subthemaperiode (~2 wk, fine); zoom levels (E3-08) map to these tiers; unit configurable behind the seam, default documented not compiled-in.
  *Built (2026-07-28):* `Planningsblokniveau` (Themaperiode | Subthemaperiode — **no `Maand` member, guarded by a test**), `Planningsblok` (niveau/ordinaal/start/eind, no calendar unit), `Schooljaar` + owned `Schoolsluiting` (named `Schoolvakantie` when first built; renamed later that day when it had to cover Hemelvaart and studiedagen too) with `Lesperiodes()`, the `IPlanningsblokIndeling` seam and its config-driven implementation (`Planning:Blokindeling`, default themaperiode 5 wk / subthemaperiode 2 wk documented in configuration space), plus migration `SchooljaarEnVakanties`. **Blocks are derived, never stored** — persisting them would bake the granularity into rows. See [worklog](worklogs/E3-05/implementation.md).
  *Audit + fixes (2026-07-28):* the antagonist returned **VIOLATIONS FOUND** (6 MAJOR). Fixed: blocks are now **distributed evenly** per teaching stretch (greedy chopping produced 1-week "themaperioden" outside the ratified 4–6 wk range), the **fine tier nests inside the coarse tier** (they were independent chops, so a subthemaperiode could straddle a boundary), identity is now **`(Niveau, Start)`** and the false "`Ordinaal` is stable" claim is gone, the default is documented in **`appsettings.json`**, and the derivation rules are recorded in **[ADR-0020](../docs/adr/0020-planningsblok-derivation-rules.md)** (superseding ADR-0013's "configuration on the `Schooljaar`" clause). 22 unit tests pass locally — including the three properties the audit showed were unasserted (block duration, tier nesting, ordinal instability); 3 persistence tests still need CI.
  *Closed (2026-07-28), CI run [30357426252](https://github.com/Siebeds/Jaarplanner/actions/runs/30357426252):* the 3 `SchooljaarPersistentieTests` (vakantie round-trip, unique schooljaarnaam, cascade delete) run green against real PostgreSQL — 42 integration passed / 0 skipped, 328 unit passed. Note they had in fact been passing since the first push; the red CI that appeared to block this story was four unrelated E1-07 assertions left stale by that story's audit fix, now corrected. The story was gated on a failure it did not cause.

- [~] **E3-06 — Calendar/agenda view of the plan** — *first clickable draft built 2026-07-29; awaiting the directie/teacher review that closes it*
  Render the year plan as a calendar/agenda over the school year.
  *Done when:* a generated plan renders per block. Ref: FR-6.1.
  *Built (2026-07-29):* `frontend/src/features/jaarplan/` — the ribbon of unequal periods with vakanties as literal gaps, block width proportional to `aantalLesdagen`. Reachable from the running app via `JaarplanPagina` (klas-id input, same shape as the E2-05 review page — this project has three times shipped a feature nobody could reach). Backend: `GET /api/schooljaren/{id}/rooster` exposes the **derived** grid (`IPlanningsroosterService`), because a ribbon drawn from placements alone cannot show an empty period, and re-deriving the grid in TypeScript would duplicate the ADR-0013 seam and disagree with it the moment `Planning:Blokindeling` changes.
  *Deliberately absent **as of 2026-07-29**, so the review was not misled:* no drag-and-drop (E3-07 owns it **and** the confirmation protecting an accepted/locked placement — shipping the grip without the guard ships the destructive half first), no zoom toggle (E3-08), no ongeplande-doelen tray (E3-09). A control that does nothing teaches a review the wrong thing.
  > **⚠️ All three of those absences are now filled, and the teacher review has not happened yet (corrected 2026-08-04 by E3-09).** The reviewer will therefore see a screen this paragraph describes wrongly, which is worse than the omission it was written to prevent: **E3-07** shipped drag-and-drop plus a non-drag picker, **E3-08** shipped the zoom toggle, and **E3-09** shipped the knelpunt-signalering. The date is now stated in the sentence rather than left implied, because "deliberately absent" reads as a present-tense claim about the screen in front of you. E3-09's own story records that it deliberately did **not** build the *tray* the wireframe drew: the count and a route to `/dekking` replace it (owner ruling, 2026-08-04), so a reviewer looking for a tray should be told it is not coming rather than that it is missing. *This entry was already flagged as stale in E3-09's story text and was owned by "whoever lands E3-08"; E3-08 landed without fixing it, so the fix is recorded here with that history rather than silently applied.*
  *Answered-by-accident risks, all made visible rather than silent:*
  - ~~**"Te vol" (question C)** is a single named constant `VOORLOPIGE_TE_VOL_DREMPEL = 3` in `Periodeblok.tsx`, and the UI's own tooltip says the threshold is provisional.~~ — **answered by owner ruling 2026-07-31 and implemented by E3-09:** te vol is `sum(DuurWeken) > ` the period's open days in whole weeks, computed server-side. The constant is deleted and the copy no longer calls the rule provisional. *(The file was `Periodekolom.tsx`, not `Periodeblok.tsx`, from E3-06's own redesign onward.)*
  - **The doelsoort mix on a card (question E)** is **not** rendered: the jaarplan API returns `doelcodes` but not each code's doelsoort, so the wireframe's `MD 4 · G 6 · + 1` cannot be computed without inventing it. The honest count is shown instead. Adding it needs a backend join — a review decision, not a silent one.
  - **`AantalLesdagen` counts weekends.** `Schooljaar.IsLesdag` excludes only closures and nothing in the codebase models weekends, so "5,1 weken" is calendar weeks net of closures — which is exactly the arithmetic the approved wireframe used (31 ÷ 7 = 4,4). Whether `IsLesdag` should exclude weekends is a **domain** question; a second definition in the UI mapper would be the drift this project keeps paying for. Ask the teachers whether "5,1 weken" reads as five teaching weeks.
  *Stale placements are rendered, never dropped:* a placement whose `blokStart` matches no derived block goes into a non-dismissible `role="alert"` naming each thema (directie 2026-07-28). `vervallenPlaatsingen` also catches one the server did **not** flag but whose date matches no block, so a disagreement between the two views surfaces instead of a thema silently vanishing from the plan.
  *Demo data:* `DemoDataSeeder` (Infrastructure) seeds the real 2026-2027 Belgian calendar, one klas and seven `Voorgesteld` placements so the review needs no Azure AI call. Opt-in via `Demo:Seed`, set in `Properties/launchSettings.json` — **not** `appsettings.Development.json`, which `WebApplicationFactory` loads: putting it there ran the seeder inside every integration test and broke `SchoolcontentBeheerEndpointsTests` on a thema-name collision.
  **Viewable now, without a database — Storybook** (`pnpm storybook` → *Jaarplan/Jaarplankalender*). Three stories render the **real component** against the genuine 2026-2027 Belgian calendar with `fetch` stubbed in a decorator: `VolledigJaarplan`, `MetVervallenPlaatsing`, `NogNietGegenereerd`. Screenshots kept as review evidence: [ribbon](../docs/ux/wireframes/e3-06-kalender-volledig.png), [stale-placement notice](../docs/ux/wireframes/e3-06-kalender-vervallen.png). This is a review aid, **not** proof the screen works: it bypasses the API, the DI container and the database.
  **✅ Verified end-to-end against a real database (2026-07-29).** Postgres 17.10 was installed natively (no Docker — see the new section in [`docs/dev-setup-secrets.md`](../docs/dev-setup-secrets.md)), migrations applied, the seeder ran, and the kalender was opened in a browser against the live API: 7 periods, 4 vakantie gaps, the te-vol flag on periode 3, two empty periods, and every motivation carrying its *"Voorbeeld (geen AI-antwoord)"* marker. `/health/ready` returns 200. Screenshot: [live kalender](../docs/ux/wireframes/e3-06-kalender-live.png). **The full suite ran green for the first time on this machine: 412 unit + 78 integration, 0 failed, 0 skipped.**
  *What looking at it caught that the tests did not:* the stale notice read *"1 thema's **staan** niet meer in een periode"* — wrong Dutch for a count of one, and one is the common case. Fixed with separate singular copy and now asserted. Worth recording as the general point: 31 green tests, a clean axe pass and a zero-warning lint said nothing about whether the sentence was grammatical. For a Dutch UI aimed at non-technical teachers, somebody has to read the screen.
  *Audit round 2 (2026-07-29) — six MAJOR findings fixed, all of them things that would have corrupted the review:*
  - **The card said "doelen gedekt".** `doelcodes` are *links*; Art. V.1 makes a doel gedekt only once its thema is placed — so for a **stale** placement, sitting in no period, the figure proved nothing, and it was printed directly beneath the notice saying this plan's dekking is untrustworthy. Now "N doelen **gekoppeld**", and a stale card says *"Dekking onbekend zolang dit thema geen periode heeft"* instead of a number.
  - **Two WCAG 2.2 AA contrast failures** (`slate-400` on `slate-50` = 2.45:1; `slate-500` on `slate-100` = 4.35:1, both 12px against a 4.5:1 threshold). Replaced with the `muted-foreground`/`foreground` tokens. **jsdom cannot evaluate contrast**, so the axe test never could have caught these — treat "axe passes" as covering structure, never colour.
  - **The "te vol" threshold disclosure lived in a `title` tooltip** — invisible on touch, unreachable by keyboard, unread by most screen readers. The backlog and commit both claimed the UI "says out loud" that the threshold is provisional; it did not. Now visible text beside the flag.
  - **Hand-written motivations were indistinguishable from AI output**, in `AiMotivatie`, under "Waarom hier?", at the session convened to judge whether AI motivations are useful — so feedback on fixture prose would have been recorded as feedback on the AI. Every seeded motivation is now prefixed *"Voorbeeld (geen AI-antwoord) — "*, in the persisted value so no UI can lose it.
  - **The demo could not show what the review asks about.** Seeded thema's had no themadoelen (every card read "Nog geen doelen gekoppeld") and one thema per period, so the te-vol flag could never fire. The seeder now creates clearly-marked `DEMO-*` leerplandoelen + two themadoelen per thema, and places three thema's in periode 3 so question C's own illustration is visible. It deliberately leaves one period empty, and deliberately seeds **no** stale placement (that would put a permanent alert on every demo — to see it, edit a vakantie date).
  - **Rendering `blokindeling` would have pre-empted the open Art. II.3 ruling.** See the ⚠️ note under that entry in [README](README.md): E3-06, not E1-13, nearly became the story that put server-generated Dutch in front of a user. Reverted; the grain is explained from `nl.json`.
  *Also fixed:* the seeder could take the **host down** on the likeliest first run (`CanConnectAsync` succeeds against an existing-but-unmigrated database, then the first query throws `42P01`, and an exception from `StartAsync` aborts startup) — now wrapped, logged and skipped; the uniquely-indexed `Klas.Naam` is guarded separately and renamed to `"L3 — derde leerjaar (demo)"`; registration additionally requires `IsDevelopment()`; `AantalLesdagen` → **`AantalOpenDagen`**, because it counts weekends and the TypeScript mirror had already silently re-glossed it (the *domain* question of whether `IsLesdag` should exclude weekends stays open for the teachers — only the lying field name was fixed); a `tabIndex` making every inert card a tab stop was removed; and two tests that could not fail were removed or made real (an axe pass over an empty ribbon, and a timezone assertion identical to the line above it).
  *Done when (added):* the session happens and is captured — date, attendees, what they said — and the five E3-10 questions get answers. See the obligations above.
  **This story's output is the artifact directie *and teachers* assess** *(owner, 2026-07-29 — see the [E3-10 header](../docs/ux/wireframes/e3-10-kalender.md))*. The E3-10 approval authorises building the **first clickable draft**; the teacher review runs against *this*, not against the paper wireframe. So:
  - **Build it to be changed.** Prefer the cheapest structure that can be clicked through honestly over one hardened against requirements the review has not confirmed. This is the one story where under-building is the correct risk.
  - **Do not let the draft silently answer E3-10's five open questions** (period rhythm, a thema spanning two periods, what makes a period *te vol*, the ongeplande-doelen tray, what belongs on a card). Where something must be picked to be clickable, pick visibly and reversibly and add it to the review list — an implementation detail that becomes a decision nobody made is the failure mode this project keeps hitting.
  - **Capture the session when it happens:** date, who attended, what they said. E3-10's `[x]` currently rests on a test-report that evidences *"Reviewed with directie/teachers"* with the string *"Approved 2026-07-28"* — its own conclusion. That is thin for the directie half and must not be repeated for the teacher half.
  *Carried from the wireframe:* the year is a **ribbon of unequal periods with vacations as literal gaps**, block width proportional to teaching days — a uniform month grid is refused (Art. IX.3 / ADR-0013). Doelsoort must not be carried by colour alone (Art. XII, WCAG 2.2 AA).

- [x] **E3-07 — Drag-and-drop (`@dnd-kit/core`)** — *built + audited 2026-07-30: antagonist returned VIOLATIONS FOUND (2 MAJOR, 10 MINOR, 1 QUESTION), **all 12 addressed**, gates green, verified in a browser against PostgreSQL.* **Reopening closed 2026-08-04 by owner ruling** on `story/E3-07-kaartstatus` (`92c7dcb`), after the verification the entry below said was owed: five antagonist rounds, final verdict **COMPLIANT**, gates re-derived by the auditor rather than taken from the worklog (**315 vitest / 15 files, 0 failed, 0 skipped**, lint + build clean). See the *Reopening verified* clause. **Reopened 2026-08-03 by owner ruling** on a defect E4-06's gates found in this story's own copy and its own picker: see the *Reopened as a defect* clause below. Everything else about the story stands; the reopening is one card state, not a rebuild.
  Drag thema's between periods.
  *Done when:* dragging moves an item and persists immediately (links to E4-01). Ref: FR-6.2.
  *Scope correction (2026-07-30, antagonist audit):* this line read "Drag thema's **/activiteiten** between periods", and the activiteiten half was neither built nor recorded as deferred. It is corrected rather than deferred, because **Art. IX.3 has no activiteit placement**: a jaarplan holds *thema's* per planningsblok, so there is nothing for an activiteit to be dragged between. Placing activiteiten within a thema, if the school ever wants it, is content management (E1-14 / E6-05), not calendar planning. Flagged because this project's documented failure mode is the unrecorded deferral, and a promise in a story line is exactly where one hides.
  **The teacher review that E3-06 owes was waived for this story by the project owner (2026-07-30):** *"start on e3-07, i give you permission, no teacher assessment is needed."* Recorded here because the E3-06 entry sequences E3-07 after that review, so without this line the two entries contradict each other. E3-06's own obligation to capture the session is **unchanged** — it was not waived, it was decoupled.
  *Built:* `PUT …/jaarplan/plaatsingen/{id}/blok` + `Themaplaatsing.VerplaatsNaar` + `OngeldigeVerplaatsingFout`, and on the frontend a `DndContext` board with a draggable grip **plus** an `Aanpassen` panel per card (period picker, removal with confirmation, reverse-a-rejection). See [worklog](worklogs/E3-07/implementation.md) and [test report](worklogs/E3-07/test-report.md).
  *The story was full-stack, which the wording hid:* **no endpoint to move a placement existed.** E3-01 shipped GET/generatie/status/vergrendeling/DELETE — nothing that changed a placement's period. "Drag-and-drop" read as frontend work and was half backend.
  *A move sets the placement to `Manueel` and clears its `AiMotivatie`.* The field is defined as the model's reason for placing the thema *here*; once "here" is the teacher's choice, keeping it credits the model for their decision (Art. IV.3 inverted). **This lands E4-02's rule early** — a drag *is* "overriding an AI suggestion", so E4-02 should find placements already covered. It also makes the placement non-`IsVervangbaar`, so a regeneration cannot undo a move made by hand (asserted).
  *Two routes, because WCAG 2.2 SC 2.5.7 requires a single-pointer alternative to any dragging movement.* The browser check then found a second reason it is not optional: **a drag from Periode 6 to Periode 2 could not be completed at all**, because the two columns are separated by the board's horizontal scroll. The picker did it in two clicks. Over a 7-period year the picker is the *primary* route and the drag is the nudge-to-a-neighbour shortcut — worth knowing before E3-08 designs zoom levels around dragging.
  *The grip is deliberately not a tab stop and is hidden from assistive tech.* dnd-kit's `KeyboardSensor` steps by pixels across a ribbon of unequal columns, which is not a followable interaction; a tab stop that lifts a card and cannot reliably drop it is the "control that does nothing" banned after E3-06. The panel is the keyboard route.
  *One existing E3-06 test was rewritten, deliberately:* it asserted the stale notice contained **no buttons at all**, which is how it proved non-dismissibility — and which no version of E3-07 can satisfy, since the same ruling requires inline re-placement *in that notice*. The assertion now pins the **full set** of controls, so a dismiss or "later" affordance still fails it.
  *Added instances of a known defect, logged not hidden — and fixed by a sibling before they shipped:* the new `variant="outline"` buttons inherit `--input`, measured at **1.42:1** against this story's base commit. **E3-04 fixed the token to `40 14% 52%` (3,40:1) on the same day**, so on `main` these buttons pass and never shipped failing. Kept rather than deleted because the mechanism is the lesson: two stories measured the same token one day apart and only one of them repaired it. Everything this story authored passes, measured in a browser with alpha composited: the picker's border is `border-ink-zacht` at 6.08:1 and the destructive confirm is 9.93:1. *The new `destructive` variant uses `attentie-ink` for **margin and hierarchy**, not because `attentie` fails — white on `attentie` is **4.53:1**, a pass by 0.03, which E7-10's own rule calls too thin to build on. An earlier revision of this line claimed 4.31:1 and "fails"; that was an arithmetic slip, caught in audit and corrected here, in the worklog and in the code comment.*
  *The audit's two MAJOR findings, kept because they are the useful half* (full record in [antagonist.md](worklogs/E3-07/antagonist.md)):
  1. **A move was justified by a reversibility claim that is false.** The code said a move needed no confirmation because the teacher "can drag it back"; dragging back restores the date and *nothing else* — the AI motivation and any `Aanvaard` decision are gone. The same worklog asserted "destroyed, not archived" and "reversible" two decisions apart. Fixed by making the code honest instead of the claim: the picker now discloses the consequence **before** the move, and the false comments are corrected. Still no confirmation dialog: a move is a small unrecoverable edit where a delete is a total one.
  2. **Dragging a `Geweigerd` placement silently granted it dekking.** This story built an explicit, explained control to reverse a rejection, then let a drag do the same `Geweigerd → Manueel` transition with no explanation — and it is the **only** transition here with an Art. V.1 consequence, because a rejected placement teaches nothing while a manual one does. A sideways nudge would have moved a thema from "not taught" to "taught" in an inspectie-facing figure. Now refused at every layer (400 before the no-op branch, no grip, no picker, and the panel says why), with three tests.
  *And one correction to **evidence**, which matters more than the code it justified:* the story claimed white on `attentie` measures **4.31:1 and fails**. It measures **4.53:1 and passes** — an arithmetic slip that had already reached a code comment, two backlog entries, the worklog and a commit message. `attentie-ink` is still right, on margin (0.03 is not a margin) and hierarchy. Also fixed: a test-report line asserting a border colour that was never measured and that the code does not paint for an empty period.
  *Surfaced, owned by nobody:* **"Manueel" is jargon for a non-technical teacher.** It is shared copy from E2's matching screen so it was not renamed here, but a drag now *produces* this status, so this story makes the word far more common than E2 ever did. "Zelf gekozen" / "Zelf geplaatst" reads plainer. Needs a copy decision, not a code change.
  *Binding constraints from [ADR-0020](../docs/adr/0020-planningsblok-derivation-rules.md) + the directie ruling of 2026-07-28:*
  - A placement **keys on the block's start date**, never on `Ordinaal`. The ordinal is a display position and shifts when the schooljaar's vacations change; keying on it would silently relocate a teacher's thema.
  - When a vacation edit reshapes the grid, a placement whose stored date is no longer a block boundary becomes **stale**. The application **must not guess** a new period for it.
  - A stale placement raises a **persistent, non-dismissible** notification that names the affected thema's and offers re-placement inline. The UI offers no "later" — see E3-09 for the rendering.
  - While any placement is stale the jaarplan is *te herzien*, and **E5 must not report a dekking figure** for it (Art. V.2).
  *Done when (added):* a vacation edit that invalidates a placement produces the flag, no thema has moved on its own, and coverage refuses to report a number until it is resolved.
  *Done when (added 2026-07-29, from the E3-01 audit):* **a teacher can remove a thema from a period, and reverse a rejection.** E3-01 discovered that `Geweigerd` makes a placement non-replaceable, so a rejection survived every regeneration with no undo — reject a thema in a period, change your mind, stuck. E3-01 lands the API (`DELETE …/jaarplan/plaatsingen/{id}`) because a delete-guard it introduced instructed teachers to do something no endpoint supported; **this story owns the UI affordance** and must make removal discoverable rather than implied. Ref: Art. IV.2 (accept/reject/**adjust** must all be meaningful).
  *Why this clause exists:* E3-01's worklog claimed the gap was documented "so E3-07 cannot miss it", but it was recorded in three source files and E3-01's own worklog and **not here** — the story that has to act on it. A deferral recorded only in the deferring story is a deferral that gets lost.
  *Reopened as a defect, found from outside this story (E4-06's implementer + antagonist, 2026-07-31; owner ruled the reopening on 2026-08-03) — it needs a fix, not just a note.* **Reproduced three times independently**, most recently on E4-06's landing commit: a stale rejected card renders `kalender.herplaatsKies` while the panel contains **zero** `<select>` elements. On a **stale rejected** card the panel renders `kalender.herplaatsKies` (*"Kies hieronder een periode voor dit thema, of versleep de kaart"*) and, in the very next paragraph, `kalender.weigeringEerstTerugdraaien` (*"Dit thema is geweigerd, dus je kan het niet verplaatsen"*). Two sentences that contradict each other on one card, and the first points at nothing: the period picker is suppressed for a rejected placement (`Themakaart.tsx`, `doelen = isGeweigerd ? [] : …`) and the card carries no drag grip, so it breaches the E3-06 rule as well. Photographed at 390px during E4-06's round-2 browser pass. **E4-06 deliberately did not fix it** — it is this story's copy and its picker, and E4-06 only had to keep its *own* new key off that state, which it did. The likely shape of the fix is to suppress the re-placement instruction while a placement is rejected and let the *terugdraaien* step come first, but that is a copy-and-flow decision for whoever picks it up, not a guess to bake in here.
  > **That shape is no longer a guess: E3-08 shipped it, and the symptom is measured gone at both tiers (2026-08-03). The checkbox is deliberately left `[~]` for the owner.** The owner ruled on 2026-08-03 that E3-08 fix its own contribution to this state (`kalender.herplaatsAnderNiveau`, a string E3-08 added, made the fine tier *worse*: it converted a local contradiction into a cross-view instruction that could not be kept, because it is the **rejection** and not the tier that withholds the picker). The fix that answers that ruling also removes `herplaatsKies` from a rejected card at **both** tiers, so a stale rejected card read, **at the time E3-08 shipped it**, *"Dit thema is geweigerd, dus je kan het niet verplaatsen. Draai hieronder eerst de weigering terug. Daarna kan je het thema een andere themaperiode geven."* plus its remedy button, with no picker and no grip. **That quotation is no longer what the card says, and the citation is corrected here rather than left to rot a third time (2026-08-04):** this sentence is now two, split on `isVervallen`, and the stale card gets `kalender.weigeringEerstTerugdraaienVervallen`, which is the same text **without *andere*** — see the *Reopening verified* clause at the end of this entry for why. *(Quoted in full after the round-5 audit caught this sentence saying "reads **only**" while quoting the string in its pre-clause form — a partial quotation of `nl.json`, in the very round whose own finding was a citation that had rotted.)* Measured **three times by different hands** (the E3-08 implementer, the merge round, and the test-runner independently), each time in a real browser against real PostgreSQL with the state created by a direct DB write, and with a `Manueel` control card proving the suppression is scoped to the rejection rather than blanket. The owner also ruled that the remedy's **second** step be named, so the card gained a second-step clause naming no view and therefore true at both tiers. **Both instances of this quotation are corrected together (2026-08-04), because fixing one and leaving the other is the exact defect the correction announces it has stopped:** since the E3-07 reopening the clause reads *"Daarna kan je het thema een themaperiode geven."* on a **stale** rejected card and *"Daarna kan je het thema een **andere** themaperiode geven."* on one that really is in a period.
  > **⚠ E4-02 enlarged what this story owes, on 2026-08-03, and wrote it here rather than only in its own entry.** The reopening is about a stale **rejected** card. Until E4-02 that state took a rejection *plus* a vakantiedata edit by the school, which is why the entry above describes it as something E4-06's *gates* found rather than something a teacher meets. **It is now one press away on the anchor screen:** E4-02 put "Weigeren" on a stale proposal's card face and `kalender.beslisVervallen` recommends that press, because a weigering is the one action that resolves a te-herzien plan (`DekkingService` counts `IsVervallen && !IsGeweigerd`). So whatever this story still owes, it owes on a state teachers will now reach routinely rather than exceptionally.
  > *What E4-02 already fixed, so it is not counted twice:* `kalender.weigeringUitleg` closed with *"het thema komt dan als jouw eigen keuze in deze themaperiode"*, which is false on a stale card, and it contradicted `weigeringEerstTerugdraaien` two lines above it on the same card. Split into `kalender.weigeringUitlegVervallen`, and an existing test turned out to have been **pinning the defect** for the stale case (it lives in this file's `describe("… (E3-07)")` block but was authored by **E4-06**, `81b4ed9` — verified with `git log -S`, because the hand-off that fixed a mis-routing had mis-routed the credit). That is one instance, found because E4-02 created the path to it. **It is not a verification of this story's criteria**, which is the narrow question the owner still owns below.
  > **Why this entry does not flip itself.** One symptom on one screen is not this story's acceptance criteria, and E3-08 has no mandate over E3-07's. So the question the owner still owns is narrow: *does this reopening close on the evidence above, or does E3-07 owe a verification of its own?* Recorded here rather than settled, because a side effect that nobody verified against the reopened story's own criteria is exactly the shape this backlog keeps having to retract. Evidence: [`worklogs/E3-08/implementation.md`](worklogs/E3-08/implementation.md) (merge section) and [`test-report.md`](worklogs/E3-08/test-report.md) (round 4, item 7).
  > **Reopening verified, and it owed one after all (2026-08-04). The checkbox is still the owner's.** The narrow question above is answered: E3-08's three measurements **all predate E4-02**, which then landed (PR #25) and rewrote the copy on these exact cards, so the evidence described a tree that no longer existed. **The reopened symptom was re-verified by this story rather than inherited from E3-08** — deliberately not called a verification of "this story's own criteria", because two lines above this entry defines that phrase as something larger, and E3-07's actual criteria (a drag moves and persists, removal is discoverable, a rejection is reversible; FR-6.2) were **not** re-measured here. What was re-measured is the one card state the owner reopened the story over, on `1dfe9b8`, in headless Chrome against real PostgreSQL, with the state reached the way a teacher now reaches it (press *Weigeren* on a stale card, which `kalender.beslisVervallen` recommends): **the reopened symptom does not reproduce** (no `herplaatsKies`, 0 `<select>`, no grip), with the stale `Voorgesteld` card in the same plan as a control proving the suppression is scoped to the rejection rather than blanket.
  > *And the verification paid for itself:* it found **a new instance of the same class**, invisible to both authoring stories. `weigeringEerstTerugdraaien` closed with *"Daarna kan je het thema een **andere** themaperiode geven"* while the paragraph directly beneath it, `weigeringUitlegVervallen` (authored by **E4-02**, `447fe0a`), said *"dit thema staat in geen enkele periode"*. *Andere* presupposes exactly what the next sentence denies. **Not a case nobody could have seen:** `git show 447fe0a:frontend/src/i18n/nl.json` already contains the E3-08 clause, so E4-02 wrote its sentence directly beneath it, and the pair survived E4-02's four antagonist rounds and its test-runner PASS. Fixed by splitting on `isVervallen`, as the paragraph two lines below already splits `weigeringUitleg`; the non-stale sentence is deliberately untouched. Worklog: [`worklogs/E3-07/reopening-2026-08-04.md`](worklogs/E3-07/reopening-2026-08-04.md).
  > *One defect was measured and deliberately **not** fixed here* (owner, 2026-08-04: verify separately, route afterwards). The non-dismissible *Te herzien* notice calls the dekking onbetrouwbaar for a **rejected** stale card while `GET …/dekking` answers `isBetrouwbaar=true` / `0` unresolved — right for `Voorgesteld` and `Manueel`, wrong for `Geweigerd` only, because `kalenderFormat.vervallenPlaatsingen` has no status filter where `DekkingService` has `IsVervallen && !IsGeweigerd`. **It is not unowned: E5-02 holds it** (`E5-dekking-export.md:36-37`, owner ruling **2026-08-03**), and the measurement is what this session adds to it.
  *Done when (added 2026-07-29, E3-01 audit — the compensating control):* ✅ **built.** **deleting a placement that is not `Voorgesteld`, or that is `vergrendeld`, requires an explicit confirmation naming the thema and the period.** E3-01's `DELETE …/plaatsingen/{id}` deliberately ignores status and lock, and the audit sustained that reading: Art. IV.2's *adjustable* presupposes revisable, Art. IX.3 scopes `vergrendeld` to "excluded from **regeneration**", and the endpoint names the exact object so the caller cannot be unaware. What makes that safe is **specificity**, not a status check — the round-1 defect was a *cascade* destroying placements as an unnamed side effect of deleting something else. But it means the only protection for accepted, locked teacher work is a UI confirmation, in a codebase with **no soft-delete and no audit trail**, where the loss is unrecoverable. This clause exists because that obligation would otherwise be recorded nowhere, and this project's record is that unrecorded obligations do not get built (M2 withdrawn; "clear diagnostics" documented but never implemented).

- [x] **E3-08 — Zoom levels (jaar ↔ periode/blok)** — *built on `story/E3-08-zoomniveaus`, branched fresh from `main` (`0de4851`) at the owner's instruction. **Closed 2026-08-03** after **four build rounds and five gate rounds**: final antagonist verdict **COMPLIANT**, test-runner **PASS 9/9** on the merged tree, browser-measured against real PostgreSQL. Gates at landing: **229 vitest / 12 files, 0 skipped**, lint 0, `tsc -b` 0, backend provably untouched (three-dot).*
  > **What it is.** A two-option segmented control (`Weergave: Themaperiodes / Subthemaperiodes`) above the spine, driving **one** `/rooster` fetch so the spine and the board can never disagree about which period is which. At the fine tier a thema renders **once**, in the sub-block whose start equals its `blokStart`, because which weeks inside a period it occupies is not modelled and drawing a span would invent data. Sibling sub-columns say they belong to a themaperiode that holds a thema rather than claiming nothing is planned. **No move affordance at the fine tier**, stated in visible text: `VerplaatsPlaatsingAsync` accepts only themaperiode starts, and a drop on a parent's first sub-block would be honest about the request and dishonest about the effect. The tier lives in **component state**, with ADR-0014 and ADR-0021 amended to say so (see below).
  > **Both obligations E3-04 left here are met**, and meeting the first one properly took two rounds: the parameter form withholds period *rows* at the fine tier but must not stop making a *claim* about the run, and `parameters.anderNiveau`'s copy follows the control's label rather than the reverse.
  > **The two defect classes worth carrying forward, both introduced by a fix round answering an audit finding.** (1) *A screen that says nothing about a run is not neutral; it defaults to the reassuring reading.* Withholding the stranded-preference claim at the fine tier made the summary count an orphaned kept startthema as **valid**, with generation enabled — identical state, opposite statements at the two tiers. Fixed by deriving one `periodestaat` that governs both the gate and the claims. (2) *An errored TanStack refetch keeps its data*, so `if (isError) show the fallback` describes a state the user is not in: alt-tabbing during a blip announced *"je ziet nog de themaperiodes"* over a board drawing subthemaperiodes. Fixed by deriving the fallback from `data === undefined`. Both were found by the gates, not by the tests.
  > **Three things it changed outside itself.** It **retired the provisional te-vol threshold** by getting the owner to rule on it (see E3-09 above; nine places the old definition lived, four of which an audit had to find). It **amended ADR-0014 and ADR-0021** to record that the view zoom *and* E3-07's drag state live in component state, covering a precedent that had contradicted a one-day-old ADR and that no audit had noticed. And it surfaced **E7-17**: `pnpm lint`'s `tsc --noEmit` half checks **zero** files, because `tsconfig.json` is solution-style, so every "lint clean" claim in this repo proved ESLint only. No shipped `[x]` is undermined (the lead measured the blast radius), and the real type gate is `pnpm build`.
  > **Left for E3-09, each with its fix named:** the fine tier still inherits the per-column te-vol mark, against the ruling this story recorded; a **zero-block grid swallows the zoom control and every rooster notice**, and after the copy was shortened that hoist is load-bearing rather than cosmetic; generation is still offered on a schooljaar with zero themaperiodes (pre-existing E3-04, `periodestaat` is its home); a backend enum gaining a third tier still lands on the teacher's screen as *"de tool kon deze weergave niet lezen"* — the new exhaustive mapping catches the TypeScript half only; and E5-01's finding that `vervallenPlaatsingen` has no status filter, so a stale **rejected** card counts toward the non-dismissible notice while dekking calls that same card resolved.
  > *Worklog: [`worklogs/E3-08`](worklogs/E3-08/implementation.md), with all five gate verdicts persisted beside it. One of those verdicts records the orchestrator's own failure: the round-3 reports were written to disk **after** the fix round had been briefed from a summary of them, which is the asymmetry the independent pass exists to prevent.*
  Switch the view between a year overview and a finer period/block view. **No unit hard-named** — follows the planningsblok seam from E3-05; do not presuppose months while Art. XIV is open.
  *Done when:* level switching works without losing state. Ref: FR-6.3 (exact levels: open), Art. IX.3/XIV.
  *Two obligations E3-04 leaves here (2026-07-31), both about the tier it fixed:*
  1. **This story is the one that makes another tier reachable**, so it must check E3-04's parameter form at the finer tier: kept startthema's key on the **generation** tier (`Themaperiode`), so at `Subthemaperiode` the form deliberately renders no period rows and says `parameters.anderNiveau` instead. Verify that reads sensibly beside the new control, and that the kept settings still travel unchanged.
     > **Corrected 2026-07-31, mid-story, and the correction is the interesting part.** This clause used to read *"no period rows **and no stranded claim**"*, and E3-08 built exactly that — which turned out to be a defect both gates found independently: with the stranded *claim* withheld, the summary stopped excluding the stranded entry and counted it as a **valid** startthema, so identical state read `(1 zonder themaperiode)` at one tier and `(1 startthema)` at the other, with generation enabled. What obligation 1 actually wanted was to withhold the **rows** (a period row keyed on a tier the board is not showing is unfillable); it never wanted the screen to stop saying what the next run will do. The clause is fixed here rather than only in the code, because a story text that contradicts the shipped fix is how a later reader "fixes" the fix back. *The general lesson, since this is the second time this project has hit it:* an obligation phrased as "renders nothing" is a licence to make no statement, and a screen that makes no statement about a run is not neutral — it defaults to the reassuring reading.
  2. **`parameters.anderNiveau` has to agree with the control's own label.** It used to say *"zet de kalender terug op het hele jaar"*, naming a control that did not exist; it now describes the state (*"zolang de kalender het hele schooljaar toont"*). When the zoom lands, either its label matches that wording or the copy follows the label.

- [x] **E3-09 — Knelpunt-signalering** — *closed 2026-08-05 by owner decision on `story/E3-09-knelpunten`. The te-vol rule now has one server-side definition, the kalender reads it instead of guessing, the third signal is a count plus a route rather than the tray the wireframe drew, and a kleutergroep chooses the jaar/fase its coverage figure is measured against. **Two antagonist rounds, both VIOLATIONS FOUND (3 MAJOR + 10 MINOR + 2 QUESTION, then 1 MAJOR + 11 MINOR + 2 QUESTION); every finding fixed or routed, and both QUESTIONs ruled on. Read the `[x]` precisely: no round returned COMPLIANT, because each round audited the previous round's fixes and found something new. The owner closed it on that basis (see below).***
  Visually flag overloaded blocks and goals that appear nowhere. Signal with icon/label, not colour alone (a11y).
  *Done when:* an over-full block and an unplaced goal are both visibly flagged. Ref: FR-6.4, ADR-0017.
  > **BUILT 2026-08-04, still `[~]` — antagonist round 3 is owed** on `story/E3-09-knelpunten`, branched from `origin/main` (`7f6d8d9`, i.e. with E5-02 and E4-03 in). Gates: **571 unit + 191 integration against real PostgreSQL (0 skipped)**, **460 frontend / 20 files**, `dotnet format --verify-no-changes` clean, `pnpm lint` and `pnpm build` clean, and the whole screen driven in **headless Chrome against a live API and a real database**, at 1440px and at exactly 390px.
  > *This paragraph opened with "**DONE** … The story is checked off; the antagonist audit is the one gate not yet run" until **round 2** caught it. By then two audits had run, both returned VIOLATIONS FOUND, and the checkbox three lines above already said `[~]`. One entry asserting two statuses at once is exactly what round 1's MAJOR-3 was about, surviving its own fix — so the correction is recorded here rather than applied silently.*
  >
  > **Two owner rulings were taken during the build, and the second one exists because the first one's implementation exposed a defect the 2026-07-31 ruling had not looked at.**
  > 1. **The third knelpunt is a count and a route, not a tray (owner, 2026-08-04).** The approved E3-10 wireframe draws *goals placed nowhere* as its own tray beside the board, and that was right on 2026-07-28 when nothing else could show it. **E5-02 has since shipped `/dekking`**, which lists every in-scope leerplandoel with its coverage, its doelsoort and the thema's that cover it, and **E5-03/E5-05 own that presentation**. So the board states the fact (*"14 leerplandoelen komen in geen enkel thema van dit jaarplan voor"*) and links to the screen that owns the list. Reasoning the owner weighed: a tray here is a second, poorer rendering of the same rows on the screen whose standing problem is too much prose above the board, it would run to hundreds of entries after a full import, and its length is governed by the **still-open** Art. XIV denominator question. **The deviation from the approved wireframe is deliberate and is recorded here rather than in a commit message**; the wireframe itself is not retouched, because it is the record of what directie reviewed (same reasoning as place 9 below).
  > 2. **The period heading rounds to whole weeks too (owner, 2026-08-04).** This was *not* foreseen by the te-vol ruling and was found by implementing it. The 2026-07-31 ruling made `BeschikbareWeken` a whole number precisely so no screen could print *"6 weken nodig, 5,4 weken beschikbaar"* beside *"niet te vol"* — but it legislated the te-vol **sentence** and never looked one line up, at the column heading, which had rendered `openDagen / 7` to **one decimal** since E3-06 and is the figure in the approved wireframe (*"4,4 weken"*). E3-09's own copy put the two side by side: a period of 40 open days would have read *"5,7 weken"* in its heading and *"in 6 weken"* directly beneath it. Two numbers for one period length, which is the exact self-contradiction the earlier ruling exists to prevent, one level up from where it was looking. The owner ruled the heading follows the rule. **Cost accepted explicitly:** a period carrying three vrije dagen now labels identically to an unbroken one, and the {@link Jaarspine} still sizes its segments on exact open days, so two segments of visibly different width can share a label. The width stays the honest signal of teaching time; the label is the figure the rule uses. *Worth carrying forward: a ruling that fixes "the screen could print X" should be checked against every figure already on that screen, not only the one being added.*
  >
  > **What was built.** The rule now has **one** implementation and it is server-side. `BlokspreidingWeergave.IsOverbelast` was already it; what was missing is that it only ever reached the *generation* response, while the board renders from the jaarplan read plus `/rooster` — which is why the kalender carried a threshold of its own for months. `JaarplanWeergave` gained `Blokken` (the same `Spreidingsrapport.Meet`, same filters, same arithmetic, reused rather than reimplemented leaner) and `ThemaplaatsingWeergave` gained `DuurWeken`, so the board can also answer *"would this period become te vol?"* **during** a drag. `VOORLOPIGE_TE_VOL_DREMPEL` is deleted along with the count-based copy; `formatteerWeken` became `wekenInBlok`, returning a number because `tAantal` needs a count. The two calendar faults carry a glyph **and** words (`▲`); the coverage line carries **words alone**, deliberately, because its sentence is self-explanatory in monochrome and the glyph it first had was invisible (see *What looking at it changed*). Never colour alone in all three cases: WCAG 2.2 AA SC 1.4.1, per Art. VIII and ADR-0017.
  >
  > **The three signals are deliberately not one uniform banner, and that is the design decision this story actually turned on.** Their consequences differ, so their weight does: a **stale placement** keeps E3-07's heavy bordered region with cards and actions, because a human *must* resolve it and it is the one that is not dismissible; **te vol** is medium weight in the same `attentie` hue but without the heavy border, because it is a judgement a teacher may accept and Art. IV.1 says nothing is blocked; and **goals taught nowhere** wears `dekking-niet-gedekt`, not `attentie`, because it is not a calendar fault at all (the periods are fine, there is simply curriculum this plan does not teach) and it points off-screen. Art. VIII / ADR-0017 allow exactly **one** attention hue, so the three are separated by weight and by tokens that already mean what each signal is about, never by inventing a third chrome accent. **No wrapping "Knelpunten" container:** that screen already carries three boxes, and a fourth box announcing a grouping the reader can see is the accessory to remove.
  >
  > **The nine places, all discharged.** 1 the constant is gone, with its `catalogus.test.ts` exemption **deleted** rather than reworded — the exemption predicted its own end (*"whoever changes it owns adding the singular"*) and the guard's own "keeps its exemption lists honest" test fails on an entry whose key no longer carries `{aantal}`, which is that guard working. 2 the figures reach the board on a plain page load, verified through real Postgres. 3 the copy states weeks needed against weeks available. 4 the drag preview adds **the dragged thema's own weeks**, not 1, so it can no longer promise room that the flag then denies. 5–8 the four doc comments that asserted the two signals were deliberately different no longer say so. 9 the wireframe's question C is closed in the `.md`; **the mock is deliberately still not retouched.**
  >
  > **Two things E3-09 fixed that were not on its list.** The **spine's te-vol glyph** was `!` while the column and the explanation used `▲`: defensible while te vol was two loosely related things, and one signal wearing two glyphs on one screen once the rule has a single definition. The width argument that justified a bare marker is untouched, since `▲` is no wider. And the **`kalender.weken` plural**: with whole weeks *"1 weken"* became reachable, on the short block a long mid-year closure leaves behind, and it was not reachable while the figure read *"1,0 weken"*. `catalogus.test.ts` **could not have caught it** — that guard fires on `{aantal}` and this key interpolated `{weken}` — so the key was renamed to `{aantal}` and now falls under the guard. That is the seventh instance of this repo's Dutch plural defect and the first found by changing a *number format*.
  >
  > **Measured in a browser, against a live API and real PostgreSQL** (`jp_e309`, demo seed, 1440px and exactly 390px): the flagged period reports **"Te vol: 18 weken thema's in 6 weken"** and the read path returns 18 needed against 6 available for that period, which independently confirms the `DemoDataSeeder` comment corrected in this story — its first draft said *"6, 6 and 5 weeks, so 17 weeks"* and the three thema's landing there are each **6**, so 18. Every period heading renders whole weeks and **zero** decimal week labels survive anywhere on the page. At the fine zoom **no column carries the flag** and the summary line reads *"Te vol: themaperiode 3. Ga naar de weergave Themaperiodes…"*, which is the 2026-07-31 tier ruling working. The gap line renders with a working `/dekking` link, 226 ms after the board. Contrast, composited over the real backgrounds rather than assumed: worst case **5,73:1** (the spine's date label), the te-vol lines at **9,39:1** and the coverage line at **14,55:1**. No horizontal overflow at 390px outside the board's own deliberate scroller.
  >
  > **What looking at it changed, which no test and no probe had objected to.** The coverage knelpunt was first built symmetrical with the te-vol explanation above it: a tinted band (`bg-dekking-niet-gedekt/10`) with a `◦` as the non-colour carrier. Every gate was green, the composited contrast measured **12,34:1**, and the axe run with all three knelpunten on screen was clean. In a real browser it **inverted the hierarchy this story is built on**: that tint composites to a distinctly pink full-width slab, and it was the loudest thing on the page, so the *coverage fact* shouted while *te vol* beside it, the signal a teacher may actually have to act on today, murmured in cream. The `◦` meanwhile rendered as a dot indistinguishable from a bullet, so SC 1.4.1's "never colour alone" was satisfied on paper and not on screen. Both fixed by **removing**: a 2px left rule in the dekking colour, no fill, no glyph, with the sentence itself as the carrier — *"zijn nog niet gedekt door dit jaarplan"* needs no icon to be read in monochrome, which is a stronger guarantee than an icon nobody can see. Contrast after the change: **14,55:1**. *The lesson is the standing instruction's own: a passing axe run and a green contrast figure both said this was fine, because neither can see that an element is too loud **relative to its neighbour**.*
  >
  > **What the fixture change proves that the assertion does not.** The kalender test file's `maakJaarplan` now *derives* the per-block load from the placements instead of taking a hand-written list, and the Storybook fixture does the same. A hand-kept list would drift from `plaatsingen` the first time one was added and show a te-vol flag on a period whose cards do not account for it — in the story that is the picture people trust. The `/dekking` read is also routed in **every** stub in that file, not only the tests about it: unrouted it fell through to the 404 branch and the knelpunt line rendered its *"could not be checked"* state on every screen the file asserts against, **including the axe runs**, which would then have been measuring a permanent error state nobody meant to put there.
  >
  > **What is deliberately absent, so no later story credits itself with it and no reader reads an absence as an oversight:**
  > - **no ongeplande-doelen tray and no filtering of it** (owner ruling 1 above; **E5-03/E5-05** own the list, and the review question *"does the tray need filtering at real volume"* is void because there is no tray);
  > - **no gap count while dekking is untrustworthy.** An unresolved stale placement makes the server withhold `aantalGedekt` (directie 2026-07-28, point 4), and a plan that cannot report dekking cannot report a gap in it either. Neither the count nor the failure line renders then: the *Te herzien* notice above is already naming what has to happen first;
  > - **no te-vol signal on a zero-block grid**, which is correct (a year with no periods has no period that can be over-full) and which also suppresses the gap line, which is a judgement rather than an accident. Unreachable until **E6-03**; the reasoning for all three placements is in a comment at the call site, because this was filed to E3-09 by E4-02's round-3 audit and a filing discharged silently is a filing that gets re-opened;
  > - **no change to the te-vol rule at the subthemaperiode tier** beyond summarising it (owner, 2026-07-31);
  > - **no lesweek model.** The owner explicitly ruled that out of scope on 2026-07-31: `TelOpenDagen` still counts weekends, so rounding up is a leniency rather than a measurement. That remains true and is now visible in one more place, the heading.
  >
  > **Antagonist round 1 (2026-08-04, owner-authorised in session): VIOLATIONS FOUND — 3 MAJOR, 10 MINOR, 2 QUESTION.** All three MAJOR and every actionable MINOR are fixed in round 1; the two QUESTIONs are the owner's. **The story stays `[~]`**, because this backlog's own legend defines `[x]` as *antagonist-clean* and round 2 has not run, and because this repo's record is that a fix round is exactly where the next defect lives.
  >
  > **The three MAJOR, kept in full because two of them are about me rather than about the code.**
  > 1. **The gap sentence was false in the commonest state on the screen.** It read *"komen in geen enkel thema van dit jaarplan voor"* while the number is `aantalLeerplandoelen - aantalGedekt`, and dekking additionally requires the **placement** to be `Aanvaard`/`Manueel` (Art. V.1). A freshly generated plan is entirely `Voorgesteld`, so it reports 0 covered: the demo seed put *"14 leerplandoelen komen in geen enkel thema van dit jaarplan voor"* directly beside seven cards each announcing *"2 doelen gekoppeld"*, carrying exactly those 14 codes. **That contradiction was on screen during this story's own browser pass and I read it as a pass**, which is the part worth keeping: looking at a screen only finds what you are looking for, and I was checking the treatment rather than the truth of the sentence. Now *"{n} leerplandoelen zijn nog niet gedekt door dit jaarplan"*, the vocabulary `/dekking` already owns. No clause explains why the figure is high, because `kalender.beslisUitleg` already sits above it saying a proposal does not count toward dekking.
  > 2. **The plural defect was reintroduced two lines after being diagnosed.** `"Te vol: {nodig} weken thema's in {beschikbaar} weken"` prints *"in 1 weken"* on the short block a long mid-year closure leaves behind, which this story's **own** test (`wekenInBlok(7) === 1`) documents as reachable. Eighth instance, authored in the commit that announced fixing the seventh, through the very escape hatch that commit had just described. Fixed with `tAantal` on the available figure; `benodigdeWeken` reaches 1 only when the available figure is 0, which needs a block of no open days, so it gets no fourth key.
  > 3. **`[x]` was unearned.** Taken before the audit, against this file's own legend. Corrected above.
  >
  > **`catalogus.test.ts` is now structural rather than name-based, and that is MAJOR 2's real fix.** The guard found counts by the *placeholder name* `{aantal}`, which is why `{weken}` escaped it and then `{nodig}`/`{beschikbaar}` escaped it again. It now finds them by **the noun that follows a placeholder**. Calibrated rather than trusted: it returned **three** failures before the fixes and zero after, and the third was a **pre-existing ninth instance nobody knew about** — `kalender.spreidingBlokken`, *"{gebruikt} van {totaal} themaperiodes gebruikt"*, ungrammatical on a year deriving a single period. Fixed rather than exempted, because a brand-new guard whose first real catch is waved through teaches the next reader that the list is advisory.
  >
  > **Other round-1 fixes worth naming.** The **spine joined te vol by ordinal** across two query responses while the board joined by start date, one file after `belastingPerStart` documented the rule it broke; it now joins on the start. **`/dekking` was unrouted in `Generatieparameters.test.tsx`**, so ~30 tests and an axe run in that file were silently measuring the knelpunt's failure state, which is precisely the trap the sibling file's own comment warns about, and my record had claimed the routing was done in "every stub". Two of this entry's own figures were wrong: the frontend count (451, actually 453 after the fix round) and the coverage line's contrast, where the summary quoted **12,34:1**, the value of the *discarded* tinted band, rather than the shipped **14,55:1**. And the entry claimed all three knelpunten carry an icon in one paragraph while describing the removal of the third one's icon in another.
  >
  > **Two MINORs deliberately not fixed here, with reasons.** The repo-wide **`Art. XII` miscitation** for the colour/a11y rule (Art. XII is the *glossary*; WCAG 2.2 AA lives in Art. VIII and ADR-0017) is corrected in every line this story touched and **filed to E7-15** for the rest, because `CLAUDE.md` and several other stories carry it and a sweep is that story's job. And **NFR-3**: this line downloads the whole per-goal `DekkingWeergave` to render one integer, and `useDekking` sets no `staleTime` by design, so it refires on every kalender mount. The 226 ms figure quoted above is a **14-goal** measurement and does not generalise to a full Op.stap import; a count-only projection is the fix and it is not in this story.
  >
  > **Owner ruling, 2026-08-05, on the audit's first QUESTION: the kleuterjaar chooser belongs on the kalender too.** The figure this story put on the anchor screen had no denominator on it. `Klas.Leerjaar` is one ordinal, `0` says "a kleutergroep" without saying which kleuterjaar, so `Jaarfasen.VoorLeerjaar` derives `JK + K2 + K3` and a derde kleuterklas is measured against roughly three times the doelen it teaches. E5-02 gave `/dekking` the control; E3-09 shipped the figure without it. The owner chose the chooser over labelling the scope in the sentence.
  >
  > **`Jaarfasekiezer` is imported from `features/dekking`, not reimplemented**, and it already carried the right condition: it renders on *"this class has more than one available code"* rather than on *"is this a kleutergroep"*, which is a question the data model cannot answer and which the open graadklas decision would answer differently while producing the same shape. It gained one prop, `uitlegKey`, because its caption names what the narrowing affects and that differs per screen: on `/dekking` *"dan meet dit overzicht alleen…"* is true, on the kalender "dit overzicht" would point at the board, which the narrowing does not touch.
  >
  > **The narrowing now travels into the link, which is what finally makes an earlier claim true.** This file had asserted that asking for `EigenJaarFase` meant *"the number here is the number there"*; that held only until a teacher touched anything, because `DekkingPagina` keeps its scope in search params and the link went to the bare route. It now carries `?jaarFase=`.
  >
  > **Measured in a browser against real PostgreSQL, on a kleuterklas created for it and seeded with 18 goals across JK/K2/K3** (the demo database holds only `L3` goals, which is itself why one of the gates below exists). Unnarrowed: *"18 leerplandoelen zijn nog niet gedekt"* with *Alle drie* pressed. After pressing **K3**: *"6 leerplandoelen…"* and the link becomes `/dekking?jaarFase=K3`. **18 → 6 is the defect the ruling was about, demonstrated live** rather than argued. The L3 class shows no chooser at all. No overflow at 390px.
  >
  > **Two gates on the control that only looking found, both now tested.** It does not render when there is **no figure for it to govern**: with a curriculum holding no kleuter goals the sentence is absent and a chooser over nothing is the E3-06 rule again, one step removed, and that is the ordinary state until **E1-12**. And it **stays** once narrowed even if the chosen year holds nothing, or the control that produced that state would disappear and leave no way back to *Alle drie*. Separately, `beschikbareJaarFasen` is **latched** in a ref: narrowing creates a new query key, `useDekking` deliberately keeps no previous data (E5-02's choice, not this story's to change), so the control was unmounting for the length of the request and vanishing from under the cursor that had just clicked it. *That ref also has to be declared with the other hooks:* placed beside the derivation that uses it, it sat after this component's early returns and React answered with "Rendered more hooks than during the previous render" and five test files of nothing.
  >
  > **Beyond the chosen option, and flagged rather than slipped in:** the option the owner picked left the **graadklas** case unlabelled, since there is nothing for that class to choose. A class whose `Leerjaar` maps to no jaar/fase is silently measured against the **whole** curriculum, so it would read a number several times too large with no way to tell, which is the same defect as the audit's first MAJOR. It now gets one sentence, `kalender.dekkingTerugval`, mirroring what `/dekking` already renders for that exact state. Say so if that is unwanted.
  >
  > **One observation from looking, not changed because the layout is the owner's:** the chooser sits between the zoom switch and the jaarspine, matching the preview that was chosen, while the sentence it governs is roughly 300px lower, below the generation card. Every other control on this screen sits with what it affects. Moving it directly above the knelpunt line would restore that; it is a placement question, not a defect, so it waits for a decision rather than being taken.
  >
  > **Antagonist round 2 (2026-08-05): VIOLATIONS FOUND — 1 MAJOR, 11 MINOR, 2 QUESTION. All fixed or routed; round 3 owed.** It first verified what this repo has been burned by before: **all three round-1 MAJOR fixes are genuinely in the tree**, no repeat of E3-07's asserted-but-discarded fixes. It also **mutation-tested the widened plural guard** (three failures at `1b58101`, zero at head, and it confirmed independently via `git log -S` that the third was authored by E3-02 and therefore genuinely pre-existing), confirmed **no `/dekking` regression** from sharing `Jaarfasekiezer`, and re-derived every figure in this entry.
  >
  > **The MAJOR: all three Storybook stories crashed, and this story broke them.** The decorator's URL chain ended in `: plan`, so the new `/dekking` request was answered with the **`Jaarplan`** fixture; `apiFetch` casts rather than validates, so `beschikbareJaarFasen` was `undefined` and the kalender died on `.length`. Behind it a second, independent break: `OngeplandeDoelen` renders a `<Link>` and the decorator had no router, which the *first* commit introduced — the two test harnesses were given `MemoryRouter` and the story was not. **E3-09 edited that file in both commits and this entry cited it as evidence of care** (*"a story is the picture people trust"*); it was a picture of an error page, and the E3-06 teacher review these stories exist for is still outstanding. Three fixes, since any one alone leaves it broken: route `/dekking`, add the router, and **stop dereferencing a field the cast does not guarantee** — a screen whose worst case is a missing signal must not have a worst case of a missing screen. Verified by driving all three stories in headless Chrome: they render, the te-vol flag reads *"15 weken thema's in 6 weken"*, the knelpunt line reads *"11 leerplandoelen zijn nog niet gedekt"*, and the `/dekking` link is present.
  >
  > **Two MINORs were the record contradicting itself about its own status** — this paragraph's own opening, and the README cell claiming `[~] pending round 2` and *"the antagonist audit is owed and the `[x]` says so"* in one breath. Both corrected in place with the reason, because that is precisely what round 1's MAJOR-3 was about, surviving the fix for it.
  >
  > **Three false comments, all mine, all corrected.** `KNELPUNT_DEKKING` still quoted the sentence the fix round **deleted as a falsehood** and named it the present a11y carrier: the fifth false doc comment on this story and the first authored by the round that fixed the other four. And two backend comments still promised *"N,N weken"* on screen (`Spreidingsrapport`, one line above the line this story changed; `IPlanningsroosterService`, documenting the very field that feeds the heading) — the whole-week ruling reached the code and missed its own neighbours, which is the second instance of the lesson this entry already carries.
  >
  > **Three MINORs were real behaviour, not prose.** `kalender.dekkingTerugval` was gated on the fallback flag alone, so two reachable states rendered *"the line below is about the whole curriculum"* with **no line below**; it is now gated on the figure actually rendering, the discipline `teVolUitleg` and `beslisUitleg` already follow. Narrowing to a kleuterjaar holding **no goals** produced total silence, which this very component's doc calls the one direction the signal must never fail in; it now says so, and **only when the teacher chose that scope** — unnarrowed the same emptiness means no curriculum is loaded, which is the import screen's story. And both captions promised *"de regel hieronder"* while that line sits below the strip, the generation card and up to three paragraphs; **reworded rather than moved**, because the placement is the owner's open question and the copy was wrong either way.
  >
  > **`JAARFASE_PARAM` moved to `app/routes.ts`** beside `DEKKING_PAD`, for the reason that constant's own note gives: importing it from `DekkingPagina` pulled a page subtree into the jaarplan feature and left the two features one import from a cycle. Defined once, re-exported where it was.
  >
  > **Two tests added for paths that were unpinned, both found by the auditor asking "can this fail?".** The plural guard's own first catch, `kalender.spreidingBlokken`, was fixed at the call site with nothing asserting the singular *sentence* — the guard proves a key exists, never that anything calls it, so reverting `tAantal` to `t` would have left everything green. And the `/dekking` side of the shared chooser's caption was unpinned, so changing the default `uitlegKey` would have put the kalender's wording on a screen with no such line.
  >
  > **One claim of mine was overstated and is now correct.** *"A state nothing can produce"*, about `benodigdeWeken` reaching 1, is false: a one-day stretch between two vakanties covered by a vrije dag does yield a block of zero open days. What is true is that nothing in the product can author such a schooljaar **until E6-03** — which is how the sibling zero-block cases on this same screen were already phrased.
  >
  > **The NFR-3 concern was claimed as "routed" while it lived only inside this entry, which closes.** That is the **E4-08 mistake one story earlier** (routed to a closed story, therefore owned by nothing). It is now filed on **E7-03** with the count-only projection named, and with two multipliers the earlier note had missed: `main.tsx` uses a bare `new QueryClient()`, so `refetchOnWindowFocus` re-downloads the full per-goal payload on **every window focus**, and every narrowing is a new key with no kept previous data. *The Art. XII routing to E7-15 was checked and was genuine.*
  >
  > **Two QUESTIONs for the owner, neither introduced by this story.** The prompt tells the model a period is *"4,4 weken"* (`AantalDagen / 7`, raw calendar span) while the te-vol verdict measures the same period as **5** (`ceil(open days / 7)`): pre-existing since E3-02, lenient in the safe direction, and the input that decides whether the flag fires at all. And whether `kalender.dekkingTerugval` should stay — the auditor judged adding it right in principle, since without it the graadklas case reproduces round 1's MAJOR-1 exactly.
  >
  > **Closed 2026-08-05 by owner decision, and what the `[x]` rests on is stated rather than implied.** This backlog's legend defines `[x]` as *antagonist-clean*, and **neither round returned COMPLIANT** — round 1 found 3 MAJOR, round 2 found 1 MAJOR in round 1's own fixes. Every finding from both rounds is fixed or routed to a story that outlives this one, both QUESTIONs are ruled on, and the owner judged that enough. That is a legitimate call and it is theirs; recorded this plainly because an `[x]` earned differently from every other `[x]` in this file should say so, and because a third round would have audited a third batch of new code with no reason to expect it to be the last.
  >
  > **Owner ruling, 2026-08-05, on round 2's first QUESTION: the prompt states the te-vol figure.** `JaarplanGeneratiePromptBuilder` told the model a 1 sep – 1 okt period was *"4,4 weken"* (`AantalDagen / 7`, raw calendar span to one decimal) while the flag a teacher reads measures the same period as **5** (`ceil(TelOpenDagen / 7)`). Pre-existing since E3-02 and lenient in the safe direction, but it is the input that decides whether the model even tries to fit a thema, so the generator was steered by a stricter number than the verdict it would be judged against. The prompt now uses `WekenCapaciteit`, the same arithmetic. **The days stay the calendar span deliberately:** the model is asked to reason about seasons and about "a moment in the school year", for which the real dates are the truth; only the *capacity* figure follows the rule. That closes the tenth place a block's length in weeks was computed, and it is the one the story's own "nine places" list had missed.
  >
  > *The test for it was vacuous twice before it worked, which is the more useful half.* The whole backend suite stayed green after the change, which already said no test pinned that figure. The new assertion compares the prompt against `Spreidingsrapport.Meet`'s own `BeschikbareWeken` rather than a literal, so the two cannot be changed apart — and then **the first mutation passed**: reverting to integer division prints no separator either, so it proved only that the mutation differed from the fix, not that it reproduced the original. Reverting to the *real* original (`ToString("0.0", InvariantCulture)`) does fail it. The companion "no fractional weeks anywhere" assertion had the same flaw in miniature: it matched a **comma** while the old form printed a **point**, so it could not have caught the code it was written against. Both fixed; both found by mutating rather than by reading.
  >
  > **Owner ruling, 2026-08-05, on round 2's second QUESTION: `kalender.dekkingTerugval` stays.** The sentence a graadklas reads about being measured against the whole curriculum is confirmed, so the addition that went beyond the 2026-08-05 chooser ruling is now ratified rather than tolerated.
  >
  > **What is left undone and now belongs to other stories, so nothing closes with this entry:** the **chooser's placement** (it sits with the zoom control while the line it governs is below the generation card — the copy no longer claims otherwise, so this is purely a design question); **NFR-3's count-only projection**, filed on **E7-03**; the repo-wide **`Art. XII` miscitation**, filed on **E7-15**; and the **lesweek model** the owner deferred on 2026-07-31. E3-08's own handover items 2 to 5 were **deliberately left where they are** at the owner's instruction (2026-08-05): the E3-08 entry still assigns them to this story, and that inconsistency is knowingly accepted rather than overlooked.
  >
  > Nothing was merged. The branch is pushed at the owner's instruction so each audit ran on the commit that would land.
  *Third signal (added 2026-07-28, directie ruling):* a **stale placement** after a vakantie-edit (see E3-07). **E3-07 (2026-07-30) already delivered the notice's *action* half** — each stale card carries the `Aanpassen` panel whose picker offers every period, with nothing pre-selected, so the application still never guesses (clause 1). What is left for this story is the *rendering* treatment alongside the other two knelpunten, not the re-placement mechanism. Unlike the other two knelpunten this one is **not dismissible** and must persist until a human resolves it — an over-full period is a judgement call a teacher may accept, a placement pointing at a date that no longer exists is not.
  *From the approved E3-10 wireframe:* an over-full period is flagged in place (border + icon + the word "Te vol"); goals placed nowhere get their own tray, because something absent cannot stand out inside the ribbon. Open from the review: whether the ongeplande-doelen tray needs filtering at real volume (hundreds after a full import).
  > ~~**What makes a period "te vol"?**~~ — **RESOLVED (owner, 2026-07-31): it is arithmetic on the school's own data, not a count of thema's.** A period is *te vol* when the placed thema's need more weeks than the period offers: `sum(Thema.DuurWeken) > ` the period measured in **open days** (`Schooljaar.TelOpenDagen`). That is not a new rule — it is `BlokspreidingWeergave.IsOverbelast`, which E3-02 already computes and which the kalender has been contradicting with a provisional count of 3 since E3-06. One word, one definition. The reasoning: a count cannot tell three 2-week thema's (which fit a 6-week period) from two 6-week ones (which do not), and every input the arithmetic needs is data the school supplied rather than a threshold this project invented.
  > **Nine places this story owns. The first five were named when the ruling was recorded; the last four were found by the E3-08 antagonist audit the same day, and the count is left visible because the miss is instructive:** a ruling that lists "the places X is written down" is making a completeness claim, and this one was wrong by four while sounding exhaustive. One of the four is the wireframe **directie actually opens**.
  > 1. **`VOORLOPIGE_TE_VOL_DREMPEL` goes** (`frontend/src/features/jaarplan/kalenderFormat.ts`), and with it the *"voorlopige drempel"* clause in `kalender.teVolUitleg` and the exemption for `kalender.teVol` in `frontend/src/i18n/catalogus.test.ts`. That exemption comment named this exact coupling and said *"whoever changes it owns adding the singular"* — this is that story, and the singular is now reachable (one 8-week thema in a 6-week period is te vol at `aantal = 1`).
  > 2. **The numbers have to reach the kalender on a plain page load.** `benodigdeWeken` / `beschikbareWeken` / `isOverbelast` exist today only on the **generation** response (`Spreidingsrapport`), while the board renders from the jaarplan GET plus `/rooster`. So this story includes a read-side change, and the rule must stay in **one** place: computed server-side from the domain, never re-implemented in TypeScript. Re-implementing it is precisely the drift `Schooljaar.TelOpenDagen` was extracted to end (E3-02 code review, finding 4).
  > 3. **The copy changes with the definition.** `"Te vol: {aantal} thema's"` states a count the rule no longer uses. It must state what a teacher can act on: the weeks needed against the weeks available.
  > 4. **The drag preview must predict with the same rule.** `kalender.wordtTeVol` in `Periodekolom` currently adds 1 to a count, so it needs the dragged thema's own `DuurWeken` — otherwise the board promises one thing before the drop and reports another after it.
  > 5. **One doc comment becomes false and must be corrected, not left.** `Spreidingsrapport.IsOverbelast` documents itself as *"**not** the same signal as the kalender's provisional te vol threshold"*. After this ruling it is the same signal, and a comment asserting a difference that no longer exists is the failure mode this repo has now recorded three times.
  > 6. **`frontend/src/features/jaarplan/types.ts`** — the `Spreidingsrapport` interface doc justifies itself by *"the same reasoning that keeps the kalender's te vol threshold provisional"*.
  > 7. **`frontend/src/features/jaarplan/Spreidingsoverzicht.tsx`** — *"the same reason the kalender's te vol limit is still marked provisional"*.
  > 8. **`frontend/src/features/jaarplan/Periodekolom.tsx`** — the component doc: *"The threshold is still the provisional one from review question C"*.
  > 9. **`docs/ux/wireframes/e3-10-kalender.html`** — question C is still asked in the approved artifact, and its mock flag reads *"Te vol — 3 thema's"*. The **question** is synced to the `.md` (2026-07-31); the **mock is deliberately left alone**, because it is a picture of the design directie approved on 2026-07-28 and retouching it would falsify the record of what was reviewed. Its em dash is likewise historical: Art. II.5 binds shipped copy, not an archived wireframe.
  > *Also stale, and owned by whoever lands E3-08:* E3-06's record two entries above still lists *"no zoom toggle (E3-08)"* under *"deliberately absent, so the review is not misled"*, and still names the constant's home as `Periodeblok.tsx` (it is `Periodekolom.tsx`). The teacher review is postponed, so that sentence describes a screen the reviewer has not seen yet and will mislead them the moment the zoom lands.
  > *A thema without a usable `DuurWeken` is not a case:* `Thema.DuurWeken` is `RequirePositive` in the domain and required by the import parser, so there is no null branch to design around.
  > **Two further rulings the E3-08 audit forced, both taken by the owner on 2026-07-31, and E3-09 must not re-open either:**
  > - **Te vol exists at the themaperiode tier only, and the fine view summarises it.** E3-08 makes a second tier reachable, and the arithmetic applied naively there flags **every filled sub-column** (`beschikbareWeken ≈ 2` against a thema's whole 4–6 weeks), which is a board that signals nothing. So the property belongs to the tier the placement keys on (ADR-0020 §3). At `Subthemaperiode` the signal is **one line above the board** naming the te volle periodes, not a mark inherited by up to nineteen columns: the "explanatory prose never repeats per row" rule applies to warnings too, and a per-column mark would also invite the reading that *this fortnight* is overbooked, which is not what was measured.
  > - **The comparison is made in whole weeks, rounding the available side up.** `BeschikbareWeken` is `TelOpenDagen / 7`, and `Schooljaar.IsLesdag` excludes **only closures**, so a Saturday counts as an open day while a vrije dag costs a seventh of a week — and `DuurWeken` stays nominal. Left alone, a 6-week thema stops fitting a 6-week period the moment Hemelvaart and a brugdag fall inside it, i.e. te vol would fire on the commonest calendar a Flemish school has. Rounding up means **single free days never make a period te vol, while a vakantie still does** (it breaks the period outright, so it never had to be modelled here anyway). The domain's own comment on `TelOpenDagen` already flagged this: *"this counts weekends … Whether it should is an open question for the school."*
  > *Which number rounds, spelled out after the E3-08 round-2 audit asked (2026-07-31), because "compare in whole weeks" had two readings and E3-09 would have had to guess:* **`BeschikbareWeken` itself becomes a whole number** (`ceil(TelOpenDagen / 7)`), not just the comparison. If only the comparison rounded, the wire would keep a one-decimal `double` and the screen could print *"6 weken nodig, 5,4 weken beschikbaar"* beside *"niet te vol"* — self-contradictory copy of exactly the kind this ruling exists to end, and place 3 of the list above requires that sentence to be printed. **Consequence to state out loud: this changes verdicts a teacher already reads today.** `Spreidingsrapport.IsOverbelast` is rendered by `Spreidingsoverzicht` after every generation run, so periods currently reported as overbelast on a fractional shortfall will stop being reported. That is the intended effect of the ruling rather than a side effect, and it is the reason the change belongs in one place server-side.
  > *Deliberately NOT done, and recorded so it is not mistaken for an oversight:* the honest fix is to model the lesweek (a weekend concept plus teaching days per week) and compare in teaching days. The owner weighed it and ruled it out **of this ruling**, because it touches `Schooljaar`, `TelOpenDagen` and every caller including the jaarspine's proportional widths, which today treat a Saturday as teaching capacity. It needs its own story rather than a side-step inside E3-09.

- [x] **E3-10 — Kalender wireframe + teacher feedback (wireframes-first)** — *wireframe **approved** 2026-07-28; teacher assessment runs against the E3-06 draft (owner, 2026-07-29)*
  Low-fidelity wireframe of the kalender reviewed with directie/teachers **before** building E3-06/07.
  *What the `[x]` means, precisely (clarified 2026-07-29 after an antagonist audit flagged the gap):* directie (auteur) approved the wireframe as the basis for building the **first clickable draft**. **The teacher half of ADR-0017's review is deliberately sequenced onto that draft — E3-06 — not skipped.** A paper wireframe is a poor artifact to hand a non-technical teacher when the interaction is the point. The audit read the absent teacher record as evidence the review had been skipped and owed; that inference was wrong, and the record was thin enough to invite it. E3-06 now carries the obligation to capture the session.
  *Done when:* a wireframe is approved and informs the build. Ref: ADR-0017 (wireframes-first), `docs/ux/ui-ux-approach.md` §4; NFR-2.
  *Drafted (2026-07-28):* [`docs/ux/wireframes/e3-10-kalender.html`](../docs/ux/wireframes/e3-10-kalender.html) (open in a browser, no build step) with the rationale and the five review questions in [`e3-10-kalender.md`](../docs/ux/wireframes/e3-10-kalender.md). Low-fidelity by design — greyscale, doelsoort as lettered grey chips, so reviewers critique structure not palette. Central idea: the year is a **ribbon of unequal periods with vacations as literal gaps**, block width proportional to teaching days — a uniform month grid is refused (Art. IX.3 / ADR-0013).
  *Approved (2026-07-28):* the wireframe is approved as the basis for E3-06/E3-07. The five open questions (period rhythm, thema spanning two periods, what "te vol" means, the ongeplande-doelen tray, what belongs on a card) were **not** individually answered at approval — they stay open as build-time decisions and are carried into E3-06/E3-09; see [worklog](worklogs/E3-10/implementation.md).
  > **Question C is answered (owner, 2026-07-31): "te vol" is `sum(DuurWeken) >` the period's open-day weeks.** Recorded in full under E3-09, which owns the five places the old provisional count is written down. Four of the five review questions remain open, so the caution above still stands for the rest.
  *It already earned its keep:* feeding real 2026-2027 dates through the E3-05 grid exposed three **1-week "themaperioden"** — outside the ratified 4–6 wk range. See E3-05's antagonist verdict.

> ### Code-review round (2026-07-29) — seven findings on E3-06 + E3-02, all fixed
>
> Run after both stories had already merged to `main`. Every finding was verified against the code before acting; the first was reproduced with a failing test before the fix.
>
> 1. **HIGH — the jaarplan error state was unreachable.** `rooster` is chained behind the schooljaarId the jaarplan returns, so while that id is unknown the rooster query is *disabled* — and a disabled TanStack Query v5 query reports `isPending === true`. With the pending guard placed before the error guard, a failed jaarplan fetch showed *"Jaarplan laden…"* forever and `kalender.fout` was dead code. The realistic trigger is the only route into the screen today: a teacher pastes a klas-id that does not exist. Guards reordered; regression test added.
> 2. **The UI told teachers "er is niets gewijzigd" after wiping their proposal.** `VerwijderVervangbarePlaatsingen()` runs *before* the new placements are added and is persisted either way, so a valid-but-empty run deletes the previous proposal and then reported no change — false about the teacher's own data. `AantalVervangen` is now reported and rendered; the false clause is gone. (The 422/500 messages keep it, where it is true.)
> 3. **The spreading report counted rejected placements.** `IsVervangbaar` is `Voorgesteld && !Vergrendeld`, so `Geweigerd` survives regeneration — and was making a period count as *used*, and even as *overbelast*, on the strength of a thema the teacher had thrown out. New domain predicate `Themaplaatsing.IsGepland`; the kalender's "te vol" count applies the same rule (the card is still shown — visible, not counted).
> 4. **The two screens disagreed about how long a period is.** The report divided the raw calendar span by 7 while the ribbon used **open days**, so a 5-week thema could be reported as fitting a period the screen beside it called 4,9 weeks. Counting now lives in one place, `Schooljaar.TelOpenDagen`, used by both.
> 5. **`?niveau=99` returned 500.** ASP.NET Core binds any integer to an enum, so it passed model validation and threw unmapped in the seam — on a public GET. Now a 400; the named form always 400'd, which is why only the numeric form is asserted.
> 6. **The seeder guarded the klas name but not thema names.** `Thema.Naam` is not uniquely indexed, so a collision throws nothing — it silently creates a second "Water", and generation resolves the model's answer *by name* (`g.First()`), which would bind a developer's plan to the demo thema and make their own unreachable. Colliding names are now skipped, carrying each thema's block index with it so the layout cannot shift.
> 7. **The plural bug was fixed as a class, not an instance.** Four more strings rendered "1 thema's voorgesteld" / "1 bestaande plaatsingen bleven staan" — and a test *pinned* the ungrammatical one, so fixing the copy would have looked like a regression. Added `tAantal(aantal, enkelvoud, meervoud)` to the i18n helper and routed all five call sites (including the earlier `herzienTitel` ternary) through it.
>
> *Two of these are worth remembering beyond their fix.* A disabled query reporting `isPending` makes guard **order** load-bearing, not stylistic. And a test that pins wrong user-facing copy is worse than no test: it defends the defect.
