# E1 — Curriculum & Content Fundament

**Phase:** 1 · **Milestone:** M1 — Fundament
**Goal:** The data model exists; Op.stap leerplandoelen are imported (read-only) with the correct taxonomy and concordance; the school's thema's/subthema's/activiteiten are imported and manageable — including the richer themalaag (themadoelen, subdoelen, rich attributes) and level scoping.
**Covers FR:** FR-1, FR-2, FR-3. **Constitution:** [Art. III](../CONSTITUTION.md#article-iii--curriculum-data-integrity--professional-autonomy-non-negotiable), [Art. VII](../CONSTITUTION.md#article-vii--opstap-taxonomy--excel--model-mapping), [Art. IX](../CONSTITUTION.md#article-ix--core-data-model-functional).

---

### Data model

- [x] **E1-01 — Curriculum entities (read-only)**
  `Discipline` (string `nummer`, optional `parentDiscipline`, 9.x split), `Leerplandoel` (code unique, doelsoort enum, jaarFase, domein, subdomein, **cluster nullable**, tekst, voorbeelden?, toelichting?, woordenschat?, `minimumdoelRef`), `Minimumdoel` (ref, leeftijd K-/4-/6-, nr, omschrijving). Grouping key `(domein, subdomein)`; identity = `code`.
  *Done when:* migrations create the tables; entities are immutable from app code paths (Art. III.1). Ref: Art. IX.1, VII.0.
  *Note (2026-07-27, qualified 2026-07-28):* stays `[x]` — its AC is tables + immutability, both met and covered by executed unit tests. Two reference-data gaps found by code review were fixed/logged separately: the `disciplines` table had **no rows** and nothing to create them (its FK broke the first import) — now seeded from the Art. VII.0 authoritative list; `minimumdoelen` is still empty (see E1-03/E1-04). The 9.x nesting is seeded with `parentDiscipline = null` because the authoritative list has no bare `"9"` row and does not name it.
  ⏳ **The seed itself is not yet verified anywhere.** Its only evidence is `ReferentiedataIntegriteitTests`, which **skips** without a real PostgreSQL, and the migration has never been applied to any instance. Held to the same standard as E1-07: treat "seeded" as claimed-but-unproven until CI runs it green.

- [x] **E1-02 — School-content entities (autonomous, level-scoped)**
  `Thema` (school-scoped: naam, invalshoeken?, `duurWeken`, `kernwoordenschat[]`, `rijkeWoordenschat[]`), `Themadoel` (school-scoped, 2–3, links to leerplandoel/minimumdoel), `Subthema` (class/age-scoped: probleemstelling?, onderzoeksvraag?, `duurWeken`), `Subdoel` (class/age-scoped, per `(subthema × leeftijd)`), `Activiteit` (class/age-scoped: `activiteitType` enum, hoek?, verwachteUitkomsten?), `DoelKoppeling` (status enum + `aiMotivatie`).
  *Done when:* migrations created; scoping enforced (Thema/Themadoel/kernwoordenschat school-wide; Subthema/Subdoel/Activiteit per class & age). Ref: Art. IX.2.
  *Note (2026-07-27, qualified 2026-07-28):* stays `[x]` — migrations exist and scoping is enforced, both covered by executed unit tests. Code review found the class scope was **unusable in practice**: nothing could create a `Klas`, so every subthema was rejected/dropped. Addressed by the `Klas` CRUD endpoint (`IKlasBeheerService` + `POST /api/klassen`), which E3 needs anyway for per-class plan generation.
  ⏳ **That endpoint is not yet verified anywhere.** Its only evidence is `KlasEndpointsTests`, which **skips** without a real PostgreSQL. Held to the same standard as E1-07: claimed-but-unproven until CI runs it green.

### FR-2 — Op.stap import

- [~] **E1-03 — Op.stap Excel parser (ClosedXML), single-source mapping** — *reopened 2026-07-27 (code review): the `Minimumdoel` half of the AC is not met*
  Parse one Excel per discipline using the A–M mapping in **one place** in code (Art. III.3, VII.1). Handle hidden/empty columns; nullable cluster.
  *Done when:* a discipline file produces correct `Leerplandoel`/`Minimumdoel` rows. **High-risk logic — unit-tested thoroughly** (Art. V.6).
  *Open:* the parser produces **`Leerplandoel` rows only — never a `Minimumdoel`**. It reads the concordance *key* (col D, or B+C) but the per-discipline goal Excel has **no `omschrijving` column** (Art. VII.1), so there is no source for the decreed minimumdoel text and no code path inserts one. **Blocked on an Art. XIV decision: where do minimumdoel rows come from?** Also: `ResolveMinimumdoelRef` emits partial keys like `"K-"` when col D is blank and only one of B/C is filled.

- [~] **E1-04 — Doelsoort recognition & concordance** — *reopened 2026-07-27 (code review): concordance cannot yield minimumdoel-level coverage*
  Map doelsoort enum (MD/G/+/P/S/A); build `minimumdoelRef` = B+C; link minimumdoelen ↔ leerplandoelen.
  *Done when:* concordance is queryable; coverage at minimumdoel level becomes possible (feeds E5). Ref: FR-2.2/2.3.
  *Open:* `Leerplandoel.MinimumdoelRef` is a **`Restrict` FK** to `minimumdoelen.Ref`, and the `minimumdoelen` table is never populated (see E1-03). So every MD-concorded row **fails to commit** (SQLSTATE 23503), and minimumdoel-level coverage — the level the **onderwijsinspectie** tests (Art. V.2) — can return nothing. Doelsoort mapping itself is done and tested; the concordance *chain* is proven to work as soon as a minimumdoel exists (`ReferentiedataIntegriteitTests`), so this is purely the missing data source.

- [x] **E1-05 — Re-import without clobbering plans**
  Re-importing updated Op.stap data updates reference data but **does not auto-overwrite jaarplannen**; flags what to review.
  *Done when:* a re-import diff/notice is produced; existing plans intact. Ref: **FR-2.5 server side only**.
  *Scope boundary (2026-07-28, corrected by the third audit):* FR-2.5 says the tool *"**signaleert** wat herbekeken moet worden"* — the same verb class as FR-1.2's *toont*. The diff/notice is **computed in-process and returned to its caller — of which there is none.** `IOpstapImportService` is DI-registered but referenced by no controller and by no test outside `OpstapImportServiceTests`, so the Op.stap (re-)import cannot be triggered by an HTTP client, a CLI or a job at all. FR-2.5 also requires that *"de doelen **opnieuw ingeladen** kunnen worden"* — that half fails too, and so does **FR-2.1's initial import**. The missing invocation surface is **E1-15**; the display half is **E1-13** clause 6.
  > *An earlier revision of this note said the notice is "produced and **returned**", implying a reachable caller. That was the same category of overstatement as the "proven end-to-end" it was written to replace — narrowing a claim to "server side only" does not rescue it when the server side has no entry point either.*

- [x] **E1-06 — Discipline selection (starter set vs all)** — *data-driven seam built ([ADR-0019](../docs/adr/0019-discipline-selection-config-seam.md)); the actual disciplines-first choice (Art. XIV) stays runtime config (`Opstap:DisciplineSelectie`), not compiled in*
  Make the imported discipline set configurable; isolate behind a seam so neither "all" nor a subset is hard-coded.
  *Done when:* the choice is data-driven, not compiled in.

### FR-1 — Thema/activiteit import

- [x] **E1-07 — Excel upload + validation + per-row errors** — *reopened 2026-07-27 (code review); closed 2026-07-28 on green CI*
  Upload `.xlsx` of thema's/subthema's/activiteiten; validate required columns/fields; clear per-row error messages.
  *Done when:* invalid rows are reported precisely; valid file proceeds. Ref: **FR-1.1 and FR-1.2, server side only**.
  *Scope boundary (clarified 2026-07-28, widened by the third audit):* this story owns the **server side** — parse, validate, report, commit. Both cited FRs name the user and both display halves are deferred to **E1-13**: FR-1.1 *"**De gebruiker kan** een Excel-bestand (.xlsx) opladen"* (clause 1) and FR-1.2 *"**toont** duidelijke foutmeldingen per rij"* (clause 2). The `[x]` here means the endpoint reports precisely and is proven from HTTP to PostgreSQL; it does **not** mean a teacher can upload anything or see any of it. Neither FR is satisfied until E1-13 lands. *(FR-1.1 was left un-narrowed by the previous round — the selective fix the round claimed to have stopped.)*
  *Progress (2026-07-28):* all three gaps **implemented** — `SchoolcontentImportController` adds `GET sjabloon` / `POST voorbeeld` / `POST` (which also makes E1-09's template generator reachable); header validation is now **positional** so a reordered template is refused instead of importing wrong data; and unknown goal codes plus a 4th themadoel are **reported** rather than aborting the import as a 500 (the cap is now checked in both preview and commit, restoring "preview == commit"). See [worklog](worklogs/E1-07/implementation.md).
  *Closed (2026-07-28), CI run [30357426252](https://github.com/Siebeds/Jaarplanner/actions/runs/30357426252):* the Postgres-backed endpoint tests now run green — **42 integration passed / 0 skipped**, 328 unit passed. They had been *failing*, not merely unexecuted: four of them still asserted the old single `isGeldig` property after this story's own audit fix (finding 3) split the response into `isBestandGeldig` + `isVolledigVerwerkt`, so CI was red on every push from 2026-07-28 09:11 onward while the backlog recorded only "awaiting CI". Assertions corrected, and a seventh test now pins the distinction the split exists for: a workbook naming a non-existent klas parses clean (`isBestandGeldig` true) yet drops its subthema (`isVolledigVerwerkt` false) — previously unasserted.
  *Lesson (same root cause as the E1 reopening):* a test that can only run in CI is not evidence until CI has run it. "Awaiting CI" and "passing in CI" were conflated for five pushes.

- [x] **E1-08 — Import preview + add/update-or-overwrite on re-import**
  Show a preview before commit; on re-import let the user choose add vs. update/overwrite.
  *Done when:* preview matches committed result; re-import modes work; **the overwrite path preserves (or explicitly warns before discarding) teacher-set `DoelKoppeling` statuses** (`aanvaard`/`geweigerd`/`manueel`) so a re-import never silently destroys human decisions. Ref: **FR-1.3/1.4 server side only**, Art. IV.2 (mirrors E1-05's non-destructive stance).
  *Scope boundary (2026-07-28, antagonist audit):* both cited FRs name the **user**, not the API. FR-1.3: *"Vóór het definitief inlezen **krijgt de gebruiker** een voorbeeldweergave"*; FR-1.4: *"Bij herimport **kan de gebruiker kiezen** tussen toevoegen of bestaande gegevens bijwerken/overschrijven."* A `POST …/voorbeeld` returning JSON is not a user receiving a preview, and a `modus` field on a request body is not a user choosing. Both display halves are deferred to **E1-13**, including the add-vs-overwrite choice and the Art. IV.2 warning before discarding human decisions — which is the one place in this flow where a silent default would destroy teacher work.

- [x] **E1-09 — Downloadable import template**
  Template `.xlsx` matching the import structure, incl. fields for themadoelen, subthema onderzoeksvragen, two-tier woordenschat, activiteittype, duurWeken.
  *Done when:* template downloads and round-trips through E1-07. Ref: **FR-1.5 server side only**, Gap A.4. *Note: final columns gated on Art. XIV "Thema/activiteit Excel structure".*
  *Scope boundary (2026-07-28, antagonist audit):* FR-1.5 wants the template *"downloadbaar, zodat duidelijk is hoe het bestand eruit moet zien"* — a teacher needs somewhere to click. `GET …/sjabloon` serves the file correctly and is proven by test, but no UI links to it. The download surface is deferred to **E1-13**.

- [x] **E1-13 — Import-UI: upload, preview & per-row foutmeldingen op het scherm** — *added 2026-07-28 (antagonist audit): FR-1.2's display half was unowned. **Done 2026-08-03.** Built on `story/E1-13`, branched fresh from `main` at the owner's instruction. **Four gate rounds, three fix rounds**, closing on `antagonist` 0 MAJOR + `test-runner` PASS, both re-running every suite themselves.*
  > **What this closes, precisely.** A teacher can now, on screen: download the sjabloon and upload an `.xlsx` (FR-1.1/1.5); read the per-row problems with **row number and offending column** and the opmerkingen for content that was dropped (FR-1.2); see `isBestandGeldig` and `isVolledigVerwerkt` as **two statements that are never collapsed into one "OK"** (the E1-07 audit's finding 3, at the layer where it would have reached a user); review the preview before committing (FR-1.3); choose add versus update and be warned, unchecked by default, before an overwrite discards teacher-set `DoelKoppeling` statuses (FR-1.4, Art. IV.2); and read the Op.stap re-import review notice (FR-2.5). **It does not close FR-2.1:** a real per-discipline file still refuses with 409 until **E1-12** loads the decreed minimumdoelen, and the screen now says so before you upload rather than after.
  > **Gate history, because the shape of it is the useful part.** Round 1: `antagonist` **VIOLATIONS FOUND** (4 MAJOR, 4 MINOR), `test-runner` **FAIL** on clauses 4, 5 and 6. The two gates found the same top defect by different routes, and *the finding the code-reader graded MINOR was the one that deleted teacher data*: the discard opt-in stayed armed after a re-check, so with the threatened set emptied by a concurrent session the flag was sent with no control anywhere on screen, and two `Aanvaard` links became `(none)`. Round 2: all eight findings verified closed on evidence the auditor re-derived rather than accepted, **no MAJOR and no CRITICAL** left, and clause 5 was proven three ways including a *positive* control (tick and commit, and the links really are discarded) which a fix that had merely stopped sending the flag would have failed.
  > **Round 2 still returned FAIL, on a defect four days older than this story, and it is the fourth instance of one pattern.** Against real PostgreSQL a commit that adds a new subthema or activiteit to an **existing** thema answers 500: `DbUpdateConcurrencyException`, *"expected to affect 1 row(s), but actually affected 0"*, because the batch issues `UPDATE subthemas`/`UPDATE activiteiten` for rows the run is creating. So the importer can create and can no-op, and **cannot grow existing content, which is the normal case from the second import on.** Same class E3-04 diagnosed and fixed for its own tables (a child collection whose Guid key defaults to `ValueGeneratedOnAdd` is tracked as `Modified` when added to a *loaded* aggregate): `ValueGeneratedNever` is present in the `Generatieparameters`, `Jaarplan` and `Schooljaar` configurations and in **none** of the schoolcontent ones. E3-04 named it a class and fixed the instances it met. **Why it is fixed here rather than filed:** three of this story's own clauses end in a commit a teacher cannot complete, and closing on "the server side is proven" while the user cannot finish is the exact claim this backlog has already retracted for M1 and M2. Why no test saw it: all 13 committing import tests run on `UseInMemoryDatabase`, which has no relational rows-affected check.
  > **Two owner rulings, 2026-08-03.** (1) **FA §3.2 stands as written:** a leerkracht may import thema's and activiteiten; only the Op.stap goals are beheerderswerk. So `/import` keeps one route for both roles with the beheerder marking on the **Op.stap section**, and E6-02 gates the section rather than the route. (2) **A `verdwenen` review report may be lost when a re-check fails.** Clearing the previous outcome before a new run answers is correct, because no stale report may ever sit on screen; the cost is that a failed second check leaves the beheerder with no way back to the report short of a successful re-run. Accepted deliberately. Whether such a report should be *durably* re-readable stays the open directie question about acknowledging a disappeared leerplandoel, and does not belong to this story.
  > **This story changed a response contract E1-05 owns (recorded 2026-08-03, fix round 3, audit QUESTION 7).** `OpstapImportService`'s empty-input guard now fires on `inkomend.Count == 0` **irrespective of existing rows**; it previously required `&& bestaand.Count > 0`. So a first import that reads nothing answers `overgeslagen: true` / `toegepast: false` with a notice, where it used to answer `overgeslagen: false` / `toegepast: true`. **E1-05's old condition is therefore no longer its specification**, and this line exists so a reader of E1-05 does not treat it as one. No curriculum data behaviour changed (nothing was written either way) and the destructive-over-reaction protection for `bestaand.Count > 0` is untouched. The reason for the change is the E3-06 rule: the screen otherwise offered "Doelen inlezen" and then reported success for zero doelen. The round-3 audit judged this within E1-13's scope; it was recorded only in a code comment and a commit message until now.
  > **The claim on this line was made twice, and the first one vanished.** It was originally taken as an *uncommitted* edit in the shared main working tree; another session moved `HEAD` there and wiped it, so this story read `[ ]` in `main` for an hour while it was actively being built and could have been picked up twice. That is exactly the `maintree` hazard the groepschat protocol names. The lesson, recorded because it will recur: **a claim that is not committed is not a claim.** `story-E1-13` is now also held as a lock in `.claude/coordination/claims/`.
  The teacher-facing screen for the school-content import: pick an `.xlsx`, see the preview diff, and **read the per-row problems and opmerkingen on screen** — with the sjabloon download alongside it.
  *Why this story exists:* E1-07 built and proved the whole server side, but FR-1.2 reads *"De tool valideert het bestand … en **toont** duidelijke foutmeldingen per rij."* Nothing showed them: `frontend/src/features/` contained only `matching`, no component referenced `schoolcontent-import`/`problemen`/`opmerkingen`, and `nl.json` had no import keys. The audit found no story in E1–E8 owned it, so closing E1-07 would have retired FR-1.2 with half of it unbuilt. Users are **non-technical teachers** — a diagnostic that exists only in a JSON response does not satisfy the FR.
  *Done when:* a teacher can, on screen —
  1. download the sjabloon (FR-1.5) and upload a filled `.xlsx` (FR-1.1);
  2. read the per-row problems (row number + offending column) and the opmerkingen for content that was dropped (FR-1.2);
  3. see a distinction between **`isBestandGeldig`** (it parsed) and **`isVolledigVerwerkt`** (nothing was dropped) — the two **must not** be collapsed into one "OK", which is exactly the defect E1-07's audit finding 3 rejected, and this is the layer where it would reach a teacher;
  4. review the preview *before* committing (FR-1.3);
  5. **choose add vs. update/overwrite on re-import (FR-1.4)**, and be warned before an overwrite discards teacher-set `DoelKoppeling` statuses (Art. IV.2) — never a silent default;
  6. **read the Op.stap re-import review notice (FR-2.5)** — *what must be reviewed* after curriculum reference data changed, from `OpstapHerimportDiff`. Note this is the **other** importer: a different flow and DTO from the school-content one above, so it may warrant its own screen; it is listed here because it is the same *kind* of surface and must not be orphaned again. Requires **E1-15** to exist first, since nothing can trigger an Op.stap import today.
     > **E1-15 landed (2026-07-31), so clause 6 is now buildable. Four things it hands you, and one trap.** The endpoint is `POST /api/opstap-import` with `POST /api/opstap-import/voorbeeld` as the non-writing preview; both take multipart `bestand` (.xlsx) + `disciplineNummer` and answer `{ isBestandGeldig, isVolledigVerwerkt, problemen[], diff, toegepast }`. (a) `diff.vereistReview` is the flag to key the notice on, and `diff.opmerkingen[]` carries the Dutch reason a file did nothing. (b) `problemen[].reden` is **English on purpose** (an operator diagnostic about the *official* file — see the Art. II.3 entry in `README.md`); do not translate it, and consider whether a teacher should see it at all or only a directie. (c) The preview refuses exactly what the commit refuses, so you may trust a 200 preview. (d) The refusals are **409** (minimumdoelen not loaded yet, i.e. E1-12; or codes belonging to another discipline) and **400** (unknown discipline, bad request), each with a Dutch `detail` — branch on the status, as `kalender`/`matching` do, rather than on `isError`. (e) **Two error envelopes on one endpoint**, added by E1-15's fix round and surfaced by its test-runner: the integrity refusals travel through `IProblemDetailsService`, so they carry `type` and `traceId` as well, while the controller's own 400s answer `{detail, status, title}`. Additive and RFC-valid, so not a defect, but a parser that assumes one shape will trip over the other.
     > **A second trap, and this one is a copy problem rather than a data problem.** The 409 does not distinguish *"the file you uploaded is wrong"* from *"the application is not ready for this file yet"*, and while E1-12 is open it is almost always the second: a real Op.stap file mixes MD and G rows, so its concordance keys refuse until the decreed minimumdoelen are loaded. Rendered raw, a directie member can only conclude that the file they just downloaded from Op.stap is broken, and the next thing they do is re-download it. Distinguish the two on screen: the missing-minimumdoelen case is a system state with a named next step (load the decreed minimumdoelen first), not a rejection of their file. `problemen[]` versus the refusal `detail` is the seam to key that on.
     > **The trap:** `vereistReview` **never clears once a discipline has lost a goal.** It is `true` whenever `Verdwenen` or `VerdwenenMaarGekoppeld` is non-empty, and a flag-and-keep row stays absent from every later file, so every subsequent re-import of that discipline keeps reporting it. Pre-existing importer behaviour (E1-05), **deliberately not changed by E1-15** — filed here because this is the story that would render it as a permanent "te herzien" banner nobody can dismiss, which is the E3-09 mistake in a different flow. Either scope the notice to *this run's* changes, or give the reviewer a way to acknowledge a disappearance; the second needs persisted state and therefore a decision, so raise it rather than inventing one.
  Ref: FR-1.1/1.2/1.3/1.4/1.5 (display halves, deferred from E1-07/E1-08/E1-09), FR-2.5's review notice (from E1-05), Art. II.3, Art. IV.2, ADR-0017.
  *Depends on:* E1-07 `[x]` (the endpoint + response contract).
  *Gated by an open decision — read before building:* the **Art. II.3 diagnostics ruling** (see `README.md`, unresolved). `problemen[].melding` and `diff.opmerkingen[]` are today **server-generated Dutch free text**, while Art. II.3/X.3 require Dutch UI copy to live in `nl.json`. Option (a) permits displaying them verbatim; option (b) restructures the payload as codes + parameters the UI renders from `nl.json`. **Do not hard-assume one** — if the ruling has not landed, isolate the rendering behind a single formatter module so option (b) becomes a change in one place rather than throughout the UI.

### FR-3 — Beheer

- [x] **E1-10 — CRUD for thema/subthema/activiteit + goal links**
  Add/edit/delete at each level; link an activiteit/subthema to one or more leerdoelen; manage 2–3 themadoelen per thema.
  *Done when:* CRUD respects level scoping; goal links persist with status. Ref: **FR-3.1/3.2 server side only**.
  *Scope boundary (2026-07-28, antagonist audit):* FR-3.1 is the most explicitly actor-naming FR in the set — *"**Leerkrachten kunnen** thema's, subthema's en activiteiten toevoegen, wijzigen en verwijderen"* — and there is no UI for any of it. The REST surface and its level-scoping invariants are done and tested; the teacher-facing beheer screens are deferred to **E1-14**. Note E6-05 (thema-opbouw wizard) covers *guided creation* of a new thema and does not cite FR-3.1, so it does not own *wijzigen/verwijderen* of existing content.

- [x] **E1-14 — Beheer-UI voor thema's, subthema's & activiteiten** — *added 2026-07-28 (antagonist audit): FR-3.1's interaction half was unowned. **Both landings are built 2026-08-04** on `story/E1-14-themabeheer` (landing 1 `64021d9`, landing 2 `93e2b4a`), **not merged and not pushed**. Split at the owner's request so the level boundary could be reviewed before the second half was built on it; the owner clicked landing 1 through in a browser before landing 2 started. **Closed 2026-08-04 by owner ruling, after four antagonist rondes (landing 1; landing 2 + E1-18; the round-2 fix round; the round-3 fix round) and 12 MAJOR between them, all addressed.** **What the ruling knowingly accepts, stated so nobody has to reconstruct it:** the round-4 fix round has had **no independent pass**, and on this story that is a measured risk rather than a formality, because three rounds in a row the fix round produced the next round's findings. The owner drew the line deliberately: every MAJOR is addressed, the gates are green, and the two remaining MINORs are written down with their reason. Two things the next author should know rather than rediscover: **`ongekoppeld.uitleg`'s first clause** still says *"aan een thema gekoppeld"* while a doel can leave that list on one class's activiteit link, and **no browser pass covered landing 2's final state** (rounds 3 and 4 are static, and round 4 says colour, layout and whether an injected `role="alert"` is announced are all outside its reach).
  > **Landing 1, what it delivers and what it deliberately does not.** `/themas` is now the beheer surface: a list of the school's thema's plus one thema at `/themas/:themaId`, nested like the Doelen register so a thema stays deep-linkable (ADR-0021). Thema aanmaken, wijzigen and verwijderen work, the 2-or-3 themadoelen are managed with a searchable leerplandoel picker over E1-16's `GET /api/leerplandoelen`, and the class-scoped half (subthema's, subdoelen, activiteiten with their links and statuses) is **rendered read-only** and says in visible text that changes go through Import today, rather than showing controls that do nothing (the E3-06 rule). Landing 2 owns those writes and removes that sentence.
  > **The backend needed nothing**, which is worth stating because it is the opposite of this epic's usual finding: E1-10's REST surface was complete and correct, including a `Themaplaatsing`-aware refusal on delete and `heeftVoldoendeThemadoelen` for the guideline. This was a UI story end to end.
  > **The level boundary is enforced by which endpoint each half reads** (Art. IX.2), not by which fields a component chooses to render: the school-wide half comes from `…/bibliotheek`, which contains no class's content at all, and the class half from `…/voor-klas/{klasId}`. `GET /api/themas` is deliberately never called, because it would put every class's content in the tab one component away from being rendered. Both properties are pinned against **the requests made**, not against the screen.
  > **Owner ruling 2026-08-04: the AI-doelsuggesties live on the thema detail.** E2-08 had shipped its trigger behind a thema-id typed into a text box, admitted in its own comment as a stopgap because no thema list existed. That box is gone: `DoelsuggestieReview.tsx` is deleted along with its four stopgap catalogue keys, and E2's components now render against the thema already open. `matching.titel`/`matching.uitleg` are reused rather than duplicated under a `themabeheer.*` name.
  > *Two things the copy forced out, both bigger than the copy.* The goal picker could only answer *"geen leerplandoel gevonden voor die zoekterm"* at a school whose register is **empty**; it now asks the unfiltered total separately and says nothing is imported yet. That is the **E1-16 finding-1 defect one screen over**, and what surfaced it was a new `themabeheer.*` dead-key guard (the `doelen.*` one, widened) which also found one genuinely unused label. **A dead key is a question, not clutter:** either copy nobody needs, or a state nobody built.
  > *And one defect only the browser found:* after a successful delete the screen refetched the **deleted** thema's class-scoped read and got a 404, because the shared mutation wrapper invalidates the whole `["thema"]` prefix. Invisible locally; one slow request from telling a teacher their successful delete failed to load. The **first fix was wrong** in a way worth repeating: `removeQueries` reproduced it, because removing a query that still has a mounted observer makes that observer fetch again at once. Full pass and the measured contrast table: [`worklogs/E1-14/browser-pass-landing-1.md`](worklogs/E1-14/browser-pass-landing-1.md).
  > ### Landing 2 (2026-08-04): the class-scoped half became writable
  > Subthema's and activiteiten can be created, edited and deleted, and leerplandoelen can be linked at both levels. The read-only sentence that pointed at Import is gone with its catalogue key: it existed to be honest about an absence that is now filled.
  > **The level rule ended up visible as three different form shapes**, which is the clearest thing this landing does. A thema carries no scope. A subthema requires a klas and an age, and the klas is deliberately **not a field**: it comes from the section the form was opened in, so a teacher cannot create, from L3's section, a subthema that belongs elsewhere and vanishes from the screen they are looking at. The age **is** a field, because Art. IX.2 scopes per class *and* age, which is what makes a graadklas expressible. An activiteit has no scope fields at all: it inherits its subthema's.
  > **Each subthema and each activiteit owns its own open/closed state, in its own component.** That is a direct answer to landing 1's MAJOR 1: state held one level up is exactly what let a form opened on one record write to another. Here it cannot happen by construction rather than by a `key`.
  > *Two findings, both surfaced while writing the tests rather than after:* **four of the seven controls on one card shared an accessible name** (a subthema and each of its activiteiten all offer *Wijzigen*, *Verwijderen* and *Leerdoel koppelen*, and on screen the row disambiguates them while a screen reader got four identical names) — six aria labels now name what each acts on, verified by reading them out of the live DOM; and **axe caught a heading-order jump** (`h3` section, `h5` form) the moment a form opened, so subthema-level headings are `h4` and activiteit-level `h5`.
  > *The test fake is stateful for this half on purpose:* a canned response would let a missing invalidation pass, because the rows would look identical before and after a write. Several assertions therefore check that the new row **appears**, which only happens if the component refetched.
  > *Gates:* **354** frontend tests in 18 files, 0 skipped, `tsc -b` + eslint clean, and the backend suite re-run for the message sweep below (**534 unit + 178 integration** against real PostgreSQL, 0 skipped). Four load-bearing assertions mutation-checked; every break failed the suite, and the files were then **diffed** against their backups rather than checksummed against themselves, which is a measurement error I made and caught in the same round.
  > *One honest gap:* the first full-suite run after adding `Klaslaag.test.tsx` reported a single failure whose name I did not capture, and it has not recurred in four subsequent runs (two full, two scoped). Recorded rather than dismissed. Browser pass: [`worklogs/E1-14/browser-pass-landing-2.md`](worklogs/E1-14/browser-pass-landing-2.md).
  > *Also swept, because landing 2 is the first screen to render them (raised as a QUESTION by the landing-1 audit):* three `SchoolcontentBeheerService` messages cited a constitution article at a teacher, one of those carried an em dash, and one printed a raw klas GUID. Each rule now sits in a comment beside its throw and each sentence says what the reader can do. No test pinned any of the three, which is worth noting rather than celebrating.
  >
  > ### Antagonist round 4 (2026-08-04, on the round-3 fix round): VIOLATIONS FOUND, 3 MAJOR + 6 MINOR. **The pattern held a third consecutive time, and two of the three came from one decision of mine.**
  > **The decision, and why it is now reverted.** Round 3 asked who owns the sentence *"Dit subthema bestaat niet meer"*, duplicated byte-for-byte in `nl.json` and in the service. I ruled *the UI owns it*, translated three server messages to English and special-cased a 404 in the two class-scoped forms. Round 4 shows that ruling caused two of its three MAJORs, so the fix is to **undo it** rather than add a third branch: the three sentences are Dutch again, no form special-cases a 404, and the duplication it was meant to remove goes back to being the MINOR it always was. *The lesson is not about Dutch: I answered a MINOR with an architectural decision, and it produced two MAJORs.*
  > 1. **MAJOR: a teacher read English.** My claim was *"the two forms stop rendering a 404 detail, so no screen renders one any more"*. There are **three** forms with fields, and my own round-3 correction names all three twenty lines higher in this very entry. `Themaformulier` never got the guard, so once the server sentences were English, editing a thema a colleague had just deleted rendered *"Thema not found: it was deleted by another writer."* to a Dutch-speaking teacher. It also contradicted an assertion this suite already makes elsewhere (*"nothing may render an English server title"*). **No fixture could reach it:** the fake answered every thema `PUT` with 200. There is now a fixture and a test.
  > 2. **MAJOR: every non-404 delete failure went silent.** My `onError` ran `setVerwijderen(false)` unconditionally, and the failure message renders **inside** that panel. So a 500, a proxy's HTML page or a dropped connection closed the panel, left the row on screen, and said nothing at all. Only a 404 closes it now. The thema level, which I never touched, had been correct all along.
  > 3. **MAJOR: the self-contradiction moved instead of going away.** The 404 copy I added to the two forms sits in a form that stays open with an enabled *Bewaren* which can only reproduce the same 404: round 3's MAJOR 1, relocated to the sibling path by the fix for round 3's MAJOR 1. Worse on the **create** paths, where the missing record is the *parent*: a teacher typing a new subthema was told *"Dit subthema bestaat niet meer"* about something that never existed, while my own guard suppressed the one sentence that named the right record. Both are gone with the revert.
  > *Also fixed:* the `key={alWeg ?? "nieuw"}` was **inert on the path it was written for** (the button handler already cleared the notice) and its only reachable effect was harmful: it remounted the create form and wiped five typed fields when a delete elsewhere 404ed, which is a small instance of this story's own round-1 MAJOR. And the section notice was cleared on exactly one event, so after a 404 on subthema A a teacher could delete B successfully and still read *"iemand anders heeft het verwijderd"* about their own action; it also survived a class switch. It now clears on any later successful write, and `Klaslaag` is keyed on the klas.
  > *Round 4 also corrected this file:* the claim *"9 MAJOR, all fixed"* was false as written, because its own findings show round 3's MAJOR 1 reproduced on the save paths and round 2's MAJOR 2 partly undone. The headline now says 12 across four rounds, and *"addressed"* rather than *"fixed"* where a later round reopened one.
  > *Two MINORs judged and deliberately left:* `ongekoppeld.uitleg`'s **first** clause still says *"aan een thema gekoppeld"* while a doel can now leave that list on the strength of one class's activiteit link. Loose rather than false, since the activiteit lives inside the thema aggregate, and round 4 graded it MINOR itself. Left because patching copy under time pressure at the end of a long round is the exact failure mode this story has now demonstrated four times. Same for a doc-comment redundancy in `SchoolcontentImportDiff`.
  > *Gates:* **358** frontend tests in 18 files and **535 unit + 178 integration** against real PostgreSQL, 0 skipped, `tsc -b` + eslint + `dotnet format` clean.
  >
  > ### Antagonist round 3 (2026-08-04, on the round-2 fix round): VIOLATIONS FOUND, 2 MAJOR + 7 MINOR + 1 QUESTION. Both MAJORs were **new work by the fix round or work it should have swept**, which is exactly what the owner asked this pass to look for.
  > 1. **MAJOR: the "iemand anders heeft het verwijderd" branch left the record on screen with a live delete button.** Round 2's fix suppressed the failure message and did not *act*: the card kept its name, its subdoelen, its activiteiten, the heading *"Dit subthema verwijderen?"* and an **enabled** confirm that could only reproduce the same 404. One panel asserting a record is gone beside a control offering to delete it is the contradiction that reopened **E3-07** by owner ruling. Landing 1 got this right (a 404 at thema level navigates back to the list) and the copied comment claimed to match it. **Fixed:** a 404 on either class-level delete now invalidates the thema queries, so the row disappears exactly as after a successful delete, and the panel closes.
  >    *Fixing it moved the message, which is worth recording as a design consequence rather than a detail:* reconciling the screen unmounts the card, and with it any notice inside it. The sentence therefore lives on the **section**, which outlives the row, and the card reports the fact upward. The test fake now models the deletion too, because a fake that kept the row would let a screen that never refetches pass the test that says the row disappears.
  > 2. **MAJOR: E1-18's falsified premise was still live on `/themas`, the screen this story built.** `ongekoppeld.uitleg` said a doel counts as linked *"wanneer een suggestie aanvaard is of je manueel een koppeling maakt"*. After the ruling an **import** is the third route, and importing school content silently removes goals from that very gap list. Round 2's fix searched the import feature and not the catalogue, so it removed the same false premise from one screen and left it on another, 100 lines away in the same file. **Fixed**, and `matching.manueelUitleg` checked.
  > *Fixed from the MINORs:* SC 2.5.3 failed on an **eighth** control, the picker's link button in its *"Bezig met koppelen"* state, which breaks Label in Name for every row at once because `bezig` is one flag (now dropped in that state, like the *"Annuleren"* toggles); the two class-level **unlink** paths rendered no failure at all, so a failed click was silent while the row stayed; `role="status"` on an injected notice is generally not announced, so it is `role="alert"` (**not** verified with a real screen reader, and this file says so rather than implying otherwise); *"uit een eerdere import **van dit bestand**"* claimed provenance the tool does not track; a comment the same commit had falsified now says what the fixture is; and the API contract's own docs (`SchoolcontentImportDiff`) still described the pre-E1-18 population in **six** places.
  > *One duplication resolved by deciding rather than noting:* the sentence *"Dit subthema bestaat niet meer…"* existed byte-identically in `nl.json` and in the service. **The UI owns it.** The two forms stop rendering a 404 `detail`, so no screen renders one any more, and the three server sentences went back to being **English operator diagnostics** under Art. II.3's audience test. That removes the drift instead of documenting it.
  > *Gates:* **357** frontend tests in 18 files and **535 unit + 178 integration** against real PostgreSQL, 0 skipped, `tsc -b` + eslint + `dotnet format` clean, **0 act warnings**. The reconcile fix was mutation-checked (disabling the 404 refresh fails the test that asserts the card is gone).
  > *Routed to the owner, not decided here:* whether *"Manueel"* is the right badge on the import's threatened-decisions panel now that it covers import-created links, and whether the E1-17/E8 routing of the status-less class-level link lists is the owner's call. Round 3 also confirmed the seven reworded server sentences are **not pinned by any backend test**, so re-adding a GUID would fail nothing.
  >
  > ### The disposition of every round-2 MINOR, because Art. X.7 asks for one each
  > **Fixed:** the `h4` on the subthema card; `subthema.klasId` on the edit path; the eighth control's aria label; the three stale cross-references; the flake's mechanism; the two GUID-bearing not-found messages that were only reachable via the edit paths. **Routed onward:** the status-less class-level link lists (E1-17/E8). **Explicitly waived:** none. *(Round 3 found this accounting incomplete in the first draft of this section: it claimed 7 MINORs and gave an outcome for four. Two of the missing three were fixed in the same commit and simply not listed; the third was the `role="status"` announcement, which round 3 then raised again on its own terms.)*
  >
  > ### Antagonist round 2 (2026-08-04, on landing 2 + E1-18): VIOLATIONS FOUND, 4 MAJOR + 7 MINOR + 1 QUESTION. All four MAJORs fixed; it falsified part of four of the five claims I asked it to attack.
  > 1. **MAJOR: two open forms collided on DOM ids.** The create form under the list and an edit form on a card can be open together, and with literal field ids the second form's five `<label htmlFor>` resolved to the **first** form's inputs: five fields with no label, and clicking *"Naam"* focusing the wrong box. **The gate could not see it, twice over:** axe reports `duplicate-id-aria` and `form-field-multiple-labels` as *incomplete* rather than violations while `toHaveNoViolations` reads only violations, and no test had two forms open because the fixture holds one subthema. Fixed with `useId()` in **four components**: the three forms that have fields (`Themaformulier`, `Subthemaformulier`, `Activiteitformulier`) plus the picker, whose id keyed on the *level*, so two cards' subdoel pickers collided too. *(An earlier revision of this line said "all four forms and the picker", which counts five forms where there are three. Corrected by round 3, and it is the kind of figure this file has had to retract before.)* A test now opens two forms and asserts each label reaches its own input.
  > 2. **MAJOR: landing 2 rendered raw-GUID server messages** on exactly the three paths it added, which is what the sweep in `0a13672` had just removed one level up. A colleague deleting a record is the ordinary way to reach `"Subthema <guid> bestaat niet."`, and both new levels showed it to a teacher **and** called it a failure, where landing 1 treats a 404 on delete as the outcome the teacher wanted. Fixed at both ends: **six** server messages lost their GUIDs and say what happened in words, and both new delete paths now report *"iemand anders heeft het verwijderd"*.
  > 3. **MAJOR, and the sharpest: E1-18 falsified three live strings in a screen that already shipped.** `import.bedreigd.*` (E1-13) told a teacher these were *"koppelingen die jij zelf beslist hebt"* and that the tool never discards *"een beslissing van een leerkracht"*. After the ruling the dominant population is links a **previous import of the same spreadsheet** created, which no teacher decided in the app, and that sentence gates an **irreversible deletion**. My E1-18 entry disclosed the behaviour change at length and missed the copy, which is exactly the difference between a knock-on that was disclosed and one that was discovered. Reworded to what is true of both origins: *vastgelegd*, by a teacher or by an earlier import.
  > 4. **MAJOR: SC 2.5.3 Label in Name (Level A)** — *"Ontkoppelen"* was not in its own accessible name, at three levels. Fixed; the other six labels hold. **Why the gate was silent, and it generalises beyond this story:** axe's `label-content-name-mismatch` returns *incomplete* under jsdom, because without a canvas it cannot rule out icon ligatures. So **a jsdom axe run structurally cannot catch this class**, and my browser pass read accessible names without running axe in the browser. The next browser pass on this screen should run axe there.
  > *Also fixed from the MINORs:* the subthema card renders its name as an `h4`, because the earlier heading fix only held while a subthema-level form happened to be open and an activiteit-level form on its own jumped `h3` to `h5` again (a second axe test covers that state now); the subthema **edit** path sends the record's own klas instead of the section's, which was safe only because a klas switch currently unmounts the card and would have silently moved a subthema to another class the day someone adds `keepPreviousData`; the **eighth** control on the card got the label the other seven have; and three cross-references pointed at a helper landing 2's rewrite had moved.
  > **The unexplained flake now has a mechanism, found by the audit rather than by me.** The axe test left the picker's unfiltered-total query in flight, so axe walked a DOM re-rendering under it and the update could land after the test returned. React's `act(...)` warning had been naming that test on **every** run, green or not. Settled, and one synchronous query that raced a refetch became `findAllByRole`; the warning count is now 0. *The lesson worth keeping: a green test that prints an act warning is not a green test, it is a race that happened to win.*
  > *Routed onward rather than fixed here:* after E1-18 nothing writes `Voorgesteld` at themadoel or subdoel level, and the class-level link lists show no status and no motivation. Harmless today; the day **E8** adds AI matching at those levels, a `voorgesteld` link would render identically to a `manueel` one with no accept control (Art. IV.2/IV.3). Filed against E1-17/E8 on the audit's own recommendation.
  > *Gates after the fixes:* **357** frontend tests in 18 files and **535 unit + 178 integration** against real PostgreSQL, 0 skipped, `tsc -b` + eslint + `dotnet format` clean. Three round-2 fixes were mutation-checked (literal ids restored, the 404 branch disabled, the `h4` demoted to a `span`) and each break failed the suite.
  > **Not re-audited.** These fixes have had no independent pass, which on this project's record is where the next defect lives: round 1's own fix round produced two of round 2's findings.
  >
  > ### Antagonist round 1 (2026-08-04): VIOLATIONS FOUND, 3 MAJOR + 8 MINOR + 3 QUESTION. All three MAJORs fixed; two findings became other stories.
  > The audit verified rather than accepted three of my claims (the level boundary, that nothing E2 shipped was lost, that the curriculum stays read-only) and **falsified two**:
  > 1. **MAJOR, and it was data loss.** `Themadetail` sits at one position in the router tree, so changing `:themaId` remounted nothing and `Themaformulier` seeds its fields at mount. Reachable at ≥1024px, where the list stays beside the detail: open thema A, click *Wijzigen*, click thema B in the list, click *Bewaren*, and **A's naam, duur and both woordenschat lists were written over B**, school-wide, with no confirmation and no undo. The same cause let a confirmation panel opened about A delete B. **Fixed** with a `key={themaId}` remount that discards every piece of per-thema state, plus three tests that navigate between two detail routes. A third instance of the same class: with the create form open, a click in the list moved the URL and `aria-current` while the pane kept the empty form, so the click did nothing visible (the E3-06 rule). The click now wins.
  > 2. **MAJOR: the 2-of-3 guideline is two rules, and I treated both as advice.** The lower bound is advisory (`HeeftVoldoendeThemadoelen` is `count >= 2`, nothing blocks), but **`Thema.VoegThemadoelToe` throws at 3** and the API answers 400. So at three themadoelen the koppel control could not succeed and the teacher got a bare *"kon niet gekoppeld worden"* with no reason, while `adviesUitleg` said they could *"gewoon verder werken zoals het nu staat"*. **Fixed:** at the cap the control is replaced by a sentence naming what to do, the advice line is scoped to the lower bound, and the cap sentence is ours from `nl.json` rather than the server's `detail` (which names Art. IX.2, and an article reference is not something a teacher can act on). **The browser pass had never reached this state**, which is the honest lesson: it went 0 → 1 themadoelen on a fresh thema, so the one state where the server refuses a write from this screen went untested.
  > 3. **MAJOR, and not mine to fix: a `Voorgesteld` themadoel is undecidable.** Filed as **[E1-18](#e1-18)**, blocked on an owner ruling. The import writes every themadoel as `Voorgesteld`, dekking counts only `Aanvaard`/`Manueel`, and no operation exists anywhere to accept one. Landing 1 is simply the first screen that shows the status.
  > *Also fixed from the MINORs:* `statusSleutel` took a `string` while its comment claimed a compile-time guarantee (now typed, like its sibling `typeSleutel`); the class half dropped `probleemstelling`, `onderzoeksvraag` and `verwachteUitkomsten`, so an onderzoeksvraag could exist in the database and be visible nowhere in the product (now rendered when filled, and the dead `verwachteUitkomstenLabel` was the second kind of dead key my own guard describes); two label+value sentences were composed in TSX (now `hoekWaarde`/`leeftijdWaarde` in the catalogue); a **404 on delete** rendered `"Thema <guid> bestaat niet."` under *"Wat de tool meldt"*, which is a raw GUID rather than a sentence, and is now treated as what it is: a colleague already deleted it, so the teacher's goal is met and the screen returns to the list; and the no-`detail` fallback was correct by construction but **untested**, which in this repo is how a finding is earned, so both error branches now have tests. `routes.ts` still described the old `/themas` and is corrected, and the browser-pass worklog carried a quoted string the screen does not render.
  > *Accepted rather than changed, with the reason:* landing 2's client wrappers and hooks ship in landing 1 with tests and no surface, which is the mirror image of this project's reachable-vs-tested gap. They stay, because the endpoints they wrap are E1-10's and fixed; if landing 2's design departs from that shape, they are to be reshaped rather than kept. Recorded here so it is a decision, not an oversight.
  > *Two QUESTIONs routed onward:* the class-scoped read is served to whoever picks a klas, which is the open **FR-10.2 visibility** decision rather than something this story introduced (E0-10's selector and the kalender already do it); and two backend strings that landing 2's link paths will render carry an em dash and an article reference (`SchoolcontentBeheerService` `:468`/`:479`), so they need sweeping **before** landing 2 renders them.
  > *Gates after the fixes:* **342** frontend tests in 17 files, 0 skipped, `tsc -b` + eslint clean; the three MAJOR fixes were each mutation-checked (removing the `key`, forcing the cap flag false, dropping the form-closing effect) and every mutation failed the suite, files md5-restored.
  > *Gates on `64021d9`:* 335 frontend tests in 17 files, **0 skipped**; `tsc -b` and eslint clean (`tsc -b`, because `pnpm lint`'s bare `tsc --noEmit` type-checks nothing here, per **E7-17**). Five load-bearing assertions plus the new catalogue guard were mutation-checked, every deliberate break failed the suite, and the files were md5-restored afterwards. Browser pass ran against a real API and a real PostgreSQL at 1440 px and 390 px. **No antagonist audit yet** — landing 1 has not been audited, so this story stays `[~]` on more than the missing half.
  The teacher-facing screens for managing school content: list/inspect thema's, and add, edit and delete at thema / subthema / activiteit level, including the 2–3 themadoelen and the goal links with their status.
  *Why this story exists:* the audit that closed E1-07 found the same defect in E1-10 — a `[x]` retiring FR-3.1 while *"leerkrachten kunnen … toevoegen, wijzigen en verwijderen"* had no surface a leerkracht could use. E1-13 was scoped to the import flow and does not cover ongoing content management.
  *Done when:* a teacher can create, edit and delete a thema, a subthema and an activiteit from the UI; **link an activiteit or subthema to one or more leerplandoelen, and manage a thema's 2–3 themadoelen (FR-3.2)** — the link-*creation* action, distinct from deciding on an AI-proposed link, which is E2-05's; level scoping is visible and respected (thema/themadoel/kernwoordenschat school-wide, subthema/subdoel/activiteit per klas & leeftijd, Art. IX.2); the 2–3 themadoel guideline is surfaced rather than silently enforced; and a link's status persists (Art. IV). Ref: FR-3.1/3.2 (display halves, deferred from E1-10), FR-3.3, Art. IX.2, ADR-0017.
  *Ownership note (2026-07-28):* FR-3.2 reads *"Een activiteit **kan aan** één of meerdere leerdoelen **gekoppeld worden**"* — creating a link. The endpoints exist (`ThemasController`/`SubthemasController`/`ActiviteitenController`, E1-10) with no UI; this story owns that UI. E2 owns only the *review* of AI-proposed links.
  *Relationship to E6-05:* E6-05 is the opinionated 10-step wizard for *building a thema from scratch* (Gap A.7, Art. IV.8). E1-14 is the plain CRUD surface for content that already exists. Build E1-14 first — the wizard needs somewhere to land its output.
  *Also done when (four items handed over by the E1-13 gates, 2026-08-03; E1-13 is the story that made these reachable, E1-14 is the one that gives them a screen):*
  - **Sweep `SchoolcontentBeheerService`'s two validation messages before rendering them.** `:468` reads `"Onbekende leerdoelcode '{code}' — koppeling geweigerd (Art. III.5)."` (an em dash **and** an article reference) and `:479` reads `"Een subthema is klas-gebonden; een klas is verplicht (Art. IX.2)."` Both are **pre-existing** (E1-10, `aab4df0`) and currently unrendered, which is the only reason they are not already violations of Art. II.3/II.5. **This screen renders them.** Recorded here because E1-13 fixed the near-identical sentence in the *import* service one file over and did not sweep this one, so `backlog/README.md` currently credits a fix whose sibling survives. That is the selective-fix pattern this repo has now recorded five times, and writing it down is the only thing that stops the sixth being a rediscovery on this screen.
  - **The themadoel cap notice wants a screen this story provides, and its advice should be re-read once that exists.** E1-13's reconcile notice tells a teacher that occupied themadoel slots held by a human decision cannot be freed by editing the file. The honest remedy is *"remove that themadoel"*, which E1-13 deliberately does not say because no screen offers it. When this story ships one, revisit `SchoolcontentImportService.PasThemadoelCapToe`: the safe lever should come **before** the globally destructive opt-in (it is both cheaper and sufficient whenever the file itself holds a slot), and the notice should **name the occupying codes** rather than only counting them. Today a teacher can read *"2 plaatsen zijn bezet"* with only one of the two codes identifiable anywhere on screen, so the instruction *"zorg dat die codes niet in de kolom staan"* cannot be carried out for the other. The service already has the codes (`behoudenKoppelingen`); `ThemaWijziging` does not carry them, so this is a payload question as well as a copy one.
  - **One untested inflection.** `SchoolcontentImportOpmerkingenTests` asserts the singular of `behouden`, `bezet` and `overgeslagen` but never of `inkomend` (*"1 nieuwe code"*), which needs `reedsAanwezig == 3` and is reachable by importing the same file twice. The class's own stated rule is a case per grammatical form; three of four is not that.
  - **A `Themadoel`'s status must be visible.** The cap logic distinguishes a `Voorgesteld` link (the file can drop it) from a human decision (it cannot), and that distinction now drives what a teacher is told. A beheer screen that shows themadoelen without their status hides the reason the tool gave them advice.
  *Depends on:* E1-10 `[x]` (the endpoints + level-scoping rules), and — recorded 2026-07-31 — on **E1-16** for the goal-link picker. FR-3.2's *koppelen* needs a searchable list of leerplandoelen, and the only list endpoint that existed was `GET /api/leerplandoelen/ongekoppeld`, which is the wrong set: it hides goals already linked elsewhere, while a leerplandoel may legitimately hang off several activiteiten. E1-16 adds `GET /api/leerplandoelen` with filters, so this story consumes that rather than building a second query. **Merge order: E1-16 first.** *(Owner, 2026-07-31: build E1-14 once the E1-16 session closes rather than branching off ungated work.)*

<a id="e1-18"></a>

- [x] **E1-18 — Een `Voorgesteld` themadoel is een beslissing die niemand kan nemen** — *added 2026-08-04 by the E1-14 landing-1 antagonist audit (MAJOR 3); **ruled and implemented the same day.** Owner, 2026-08-04: **"de school kan besliste koppelingen importeren"** — option 1 of the two the story put up. The import now writes `Manueel` for every themadoel and subdoel it creates, so imported content counts for dekking.*
  > **Why `Manueel` and not `Aanvaard`.** `Aanvaard` means a teacher accepted an *AI suggestion* (Art. IV.2), and nothing was suggested here: the file is the school's own content, so the link is the school's own, which is what `Manueel` records. Both count identically under Art. V.1, so this is about the story the status tells rather than about the number. It lives in one named constant (`IngelezenKoppelingStatus`) so the four creation sites cannot drift apart.
  > **⚠️ The knock-on the owner should know about, because it changes E1-13's import flow and nobody asked for that part.** `Manueel` satisfies `IsMenselijkeBeslissing`, so an imported link that **disappears from a later version of the file** is now *preserved and reported as bedreigd* instead of being dropped, and removing it takes E1-13's explicit destructive opt-in. That follows from the ruling rather than sitting beside it: if the school decided a link, the tool does not un-decide it because a later spreadsheet forgot it. But it does mean a school that treats its Excel as the single source of truth has to tick a box to make a deletion stick. **If that is not wanted, the fix is not to revert this story** but to distinguish "decided by import" from "decided by hand", which is a fifth status and a materially bigger change.
  > *One unit test asserted the opposite and was rewritten rather than deleted:* `Overwrite_freely_replaces_an_ai_only_voorgesteld_link` became `Overwrite_preserves_an_imported_link_the_file_no_longer_carries`, with the ruling and its consequence in the test's own comment, plus a new test asserting the statuses directly (`An_imported_link_counts_for_dekking_because_it_is_a_decision`).
  > *No data migration ships with this,* stated so it is a decision: there is no deployed environment (E7-11 is the deployment gate), so the only rows carrying the old value are demo and test data. A real database that has already imported would need its existing `Voorgesteld` themadoelen and subdoelen moved over, which is a one-statement update and is deliberately not written blind.
  > *Gates:* **535 unit + 178 integration** against real PostgreSQL, 0 skipped, `dotnet format` clean.
  > **Marked `[x]` in the same commit that wrote it, which was too early, and the audit said so (round 2).** Its review then found a **MAJOR**: the ruling made three live strings in E1-13's import screen untrue, in front of an irreversible delete. Those are fixed (`2dcc4f0`) and recorded on E1-14's round-2 section. The checkbox stands now on the strength of that review rather than on the day it was written, and the fix itself has had no independent pass.
  A `Themadoel`'s `DoelKoppeling` can sit at `KoppelingStatus.Voorgesteld` with **no operation anywhere in the product that moves it to `Aanvaard`**, while `EfDekkingOpslag` counts only `Aanvaard`/`Manueel`. So such a link contributes nothing to dekking, permanently.
  *Why this is not E1-14's to fix:* the state is created by the **import** (`SchoolcontentImportService` writes `KoppelingStatus.Voorgesteld` for themadoelen), and `ISchoolcontentBeheerService` has no themadoel-status operation to call, so closing it needs a server change. E1-14 landing 1 is merely the first screen that **shows** the status, which is how it was found.
  *Why it matters more than it looks:* a school that imports its thema's through the FR-1 import screen gets **every** themadoel as `Voorgesteld`. On the day E5-02 puts a dekkingspercentage on screen, those thema's contribute zero, and nothing in the product explains why or offers a way to fix it. The only route to a counting link today is *ontkoppelen en opnieuw koppelen by hand* (a manual link is `Manueel`), which is undiscoverable and collides with the 3-themadoel cap.
  *The question that was asked, kept because the answer only means something next to it:* when a school imports its own thema's with their goal links, has the school **decided** those links, or is the AI **proposing** them? The owner ruled the first. The alternative was an aanvaard/weiger action on a themadoel, which is now not needed and not built. Ref: FR-3.2, FR-1.1, Art. IV.1/IV.2, Art. V.1/V.2.
  > *Recorded for whoever picks this up:* E1-14's `Themadetail` deliberately renders the status as a **word** from the shared `suggestieStatus.*` vocabulary and offers only *Ontkoppelen*. That is honest about what exists but it is not a resolution, which is exactly why this story is filed rather than patched into that screen. The fixture in `frontend/src/features/themas/testdata.ts` already carries a themadoel with an `aiMotivatie`, so option 2's UI has its test data waiting.

- [x] **E1-11 — Gedeelde thema-bibliotheek (school-wide thema's)**
  Thema + themadoelen + kernwoordenschat owned at school level; per-class derivation of subthema's/subdoelen without cross-class bleed.
  *Done when:* editing a class's subthema does not mutate the shared thema. Ref: FR-3.3 (resolved per-level), Art. IX.2, Gap A.5.

### FR-2 — Decreed minimumdoelen (added 2026-07-28)

- [!] **E1-12 — Decreed-minimumdoelen import** — *blocked: needs the source file from directie*
  Import the decreed eindtermen as `Minimumdoel` rows (`ref`, `leeftijd` K-/4-/6-, `nr`, `omschrijving`) from a dedicated source, separate from the per-discipline goal Excel. Read-only reference data (Art. III.1); identity is `ref`, which is the concordance key leerplandoelen already point at.
  *Done when:* a decreed source imports, `minimumdoelen` is populated, MD-concorded leerplandoelen commit without FK failure, and minimumdoel-level coverage returns results. Ref: FR-2.2/2.3, Art. IX.1, Art. V.2.
  *Background:* the Art. XIV "minimumdoel source" decision was **resolved 2026-07-28 in favour of a separate import** (over making `omschrijving` nullable, which would have required amending Art. IX.1). **Unblocks E1-03 and E1-04**, and E5's inspectie-facing coverage depends on it — the minimumdoel level is what the onderwijsinspectie tests.
  *Blocked on:* the actual decreed source (Excel/CSV of the eindtermen). The column mapping cannot be written against a file that has not been supplied.
  > **A fabricated stand-in exists, and it does not unblock this story's checkbox (added 2026-08-03 at the technical lead's request; the E1-13 session holds this file).** Directie is on leave, so `assets/minimumdoelen-PLAATSHOUDER-niet-decretaal.{xlsx,csv}` plus [`assets/minimumdoelen-PLAATSHOUDER-LEESMIJ.md`](../assets/minimumdoelen-PLAATSHOUDER-LEESMIJ.md) provide 627 fabricated rows (`Leeftijd`/`Nr`/`Ref`/`Omschrijving`; K-/4-/6-, nrs 1..200) so that this story can be *built* at all. Every `omschrijving` reads `PLAATSHOUDER <ref>: geen decretale tekst…`, deliberately, because `omschrijving` is the field that reaches a teacher's screen and every export, so the warning travels with the data instead of living in a README nobody opens.
  > **What it unblocks:** the import path itself, the column mapping, E1-03/E1-04's failing concordance FK, and E5-04's computation. **What it does not:** close this story, reach M1 or M5, or justify any dekkingscijfer (Art. V.2).
  > **This story therefore stays `[!]` and may not go `[x]` on placeholder rows.** Its *Done when* says *"minimumdoel-level coverage returns results"*, and on fabricated rows that sentence is **literally true and substantively empty**: coverage would report against 627 goals whose text says only that no decreed text is present. That is precisely the class of `[x]` this backlog has already retracted three times (M1 twice, M2 once), and each time the retraction was for the same reason: a criterion that a passing mechanism satisfies while the thing it exists to prove is absent.
  *Also done when (added 2026-08-03, third clause at the lead's request, first two from the E1-13 round-2 audit):*
  - **Refuse to report dekking as reliable while any `Minimumdoel.Omschrijving` starts with `PLAATSHOUDER`.** This mirrors point 4 of the directie ruling of 2026-07-28 on stale placements: a figure the application cannot stand behind is marked *onbetrouwbaar / te herzien* rather than shown as a number that would mislead an onderwijsinspectie (Art. V.2). The two cases are the same in substance, so they should not be different in behaviour.
  - **Normalise the two identity hazards on import, because both fail as *missing data* rather than as *formatting*.** (a) `jaarFase`: accept both orderings (`1K/2K/3K` and `JK/K2/K3`) and store the canonical `JK/K2/K3` + `L1`–`L6` form, so nothing downstream ever compares `3K` against `K3`. *(Owner ruled the canonical form 2026-08-03; recorded in [`README.md`](README.md).)* (b) `minimumdoelRef` padding: `OpstapImportService.ControleerVoorwaardenAsync` compares refs with `StringComparer.Ordinal` while `ClosedXmlOpstapParser.ResolveMinimumdoelRef` builds the ref as a **verbatim** concatenation of columns B+C, so `6-1` and `6-01` are two different minimumdoelen, and this repo's own fixtures already mix the forms (`4-07` beside `4-2`). If the decreed list and the Op.stap files pad differently, **every** concordance key fails and the import answers 409 with a message that reads like absent data. The placeholder emits both forms to dodge it; a real file will not. Decide the normalisation here rather than during a debugging session later.
  *Also done when (added 2026-08-03 by the E1-13 round-2 audit, so a backlog item owns it instead of a code comment):* **remove `import.opstap.voorwaarde` and its notice block** from `frontend/src/features/import/Opstapimport.tsx`. E1-13 states on screen that the decreed minimumdoelen are not loaded yet, as an unconditional sentence with no check behind it, because there is no `Minimumdoel` read endpoint to derive it from. The moment this story lands, that sentence says the opposite of the truth. Either delete it here or give this story a count endpoint the notice can derive from. *Why this is written here and not left as a comment:* this repo's documented failure mode is a claim going stale because the story that invalidated it never read the claim, and E1-13's own round-1 audit caught three instances of exactly that reaching it **through a merge**.

- [x] **E1-15 — Trigger the Op.stap import: an invocation surface for FR-2.1/2.5** — *added 2026-07-28 (third antagonist audit): the curriculum import is unreachable. **Done 2026-07-31**: built on `story/E1-15-opstap-import-trigger`, merged into `feature/e1-curriculum-content` (`a6941fc`). Two fix rounds; antagonist **COMPLIANT** on the final commit after 3 MAJOR + 4 MINOR findings, test-runner **PASS** three times, the last one independently on the merged tree (484 unit + 133 integration, 0 skipped, lint + `pnpm test` clean). Picked ahead of **E1-13** deliberately: E1-13 clause 6 could not be built until this trigger existed.*
  > **What closes and what does not.** `POST /api/opstap-import` and `POST /api/opstap-import/voorbeeld` (multipart `bestand` + `disciplineNummer`) make an initial import and a re-import reachable in a deployed app, the re-import returns its `OpstapHerimportDiff` review report, the curriculum stays read-only and existing jaarplannen and teacher-set link statuses survive, all proven HTTP → real service → real PostgreSQL rather than by calling the service directly. **This does not satisfy FR-2.1.** A real per-discipline Op.stap file mixes MD and G rows, so its concordance keys refuse until **E1-12** loads the decreed minimumdoelen: the endpoint answers 409, changes nothing, and says which refs are missing. So the *trigger* is done and the *import of real data* still waits on directie's source file. E1-03/E1-04 stay `[~]`, E1 stays incomplete, **M1 stays unreached.**
  > *Two things this story found that its text did not predict:* `IOpstapParser` had **no DI registration at all**, so the unreachability was one layer deeper than described, and the preview used to green-light imports that then failed, which would have made E1-13's FR-2.5 review step actively misleading. The preview now refuses exactly what the commit refuses.
  > *Recorded because it bears on how much the gates prove:* fix round 2 was written in two halves. The implementer wrote the two code fixes and their tests, then stopped on an org spend limit; the orchestrator verified those fixes, ran the gates and made the record corrections. The antagonist then audited that exact commit independently and the test-runner verified the merged tree independently, so the fixed code did get outside eyes. Contrast **E1-16**, held at `[~]` precisely because it lacks that.
  > ### ⚠️ Defect found after closing (2026-07-31, by the E1-16 round-2 gates) — this story counts one link layer too few. **Owned by [E1-17](#e1-17).**
  > `OpstapImportService.KoppelingAantallenAsync` unions **three** layers (`Themadoelen`, `Subdoelen`, `Activiteiten.Doelkoppelingen`) and **omits `Thema.Doelsuggesties`**, which shares the very same `DoelKoppelingMapping` and therefore the same `Restrict` FK to `Leerplandoel.Code`. E1-16's doel detail reads **four** layers, and that disagreement is what surfaced it: the two stories report different link counts for the same doel (3 versus 4).
  > **The two consequences that hold.** (1) The report **does not disclose the still-referencing suggestion**: the code lands in `verdwenen` instead of `verdwenenMaarGekoppeld(code, 1)`, so a teacher is not told that a suggestion they are about to accept points at a goal that has left Op.stap, which is exactly what Art. III.4's review report exists to say and what **E1-13** clause 6 will render. (2) With the opt-in purge (`_verwijderVerweesdeNietGekoppelde`) enabled, `Remove(oud)` hits that `Restrict` FK at `SaveChangesAsync`. **The second is the serious one.**
  > > *Corrected 2026-07-31, third audit.* This entry first claimed *"`vereistReview` can stay `false`"*. **That is wrong:** `VereistReview` ORs `Verdwenen.Count > 0`, and the mis-bucketed code is added to `verdwenen`, so review always fires; nor is the row-level flag lost, since `ZetReviewVlag(…, true)` is set in **both** branches, so the *nakijken* marker does appear. Two of three consequences held and the loudest one did not. Kept visible because this is the same record-accuracy defect the same audit round had just found elsewhere, committed while writing up that finding.
  > **Root cause, which is the part worth fixing:** *two* places define which layers hold a `DoelKoppeling`, and the newer one is more complete. One shared definition, used by both, is the fix; adding a fourth `Concat` is the patch. **Neither audit of this story caught it, nor did E1-16's first audit:** it took comparing two stories' outputs on the same doel.
  Give `IOpstapImportService` a way to be called in a running application — an endpoint (or an explicitly-documented admin/CLI path) to run an initial import and a re-import per discipline, returning the `OpstapHerimportDiff` review report.
  *Why this story exists:* `IOpstapImportService` is DI-registered at `Infrastructure/DependencyInjection.cs` but referenced by **no controller** and by no test outside `OpstapImportServiceTests`. The parser, the concordance logic, the non-destructive re-import and the review diff are all built and tested — and **none of it can be run.** FR-2.1 (*"De leerplandoelen … worden ingeladen"*) and FR-2.5 (*"de doelen kunnen **opnieuw ingeladen** worden"*) both fail on the trigger, not the logic. This is the same defect as E2-08, in the other importer, and it went unnoticed for the same reason: unit tests reach a service directly, so "tested" and "reachable" look identical from the test report.
  *Done when:* an initial Op.stap import and a re-import can both be triggered in a deployed app; the re-import returns its review report; the curriculum stays read-only (Art. III.1) and existing jaarplannen are untouched (Art. III.4). Ref: FR-2.1, FR-2.5, Art. III.1/III.4.
  *Interacts with:* **E1-12** (the decreed-minimumdoelen import needs a trigger too — decide whether it shares this surface); **E1-13** clause 6 (rendering the review notice). Note this story does **not** unblock E1-03/E1-04 — those wait on the source file.
  *Weigh a narrower matching default when this ships (recorded here by E2-08, 2026-07-29).* The FR-4.1
  matching prompt's candidate set defaults to `LeerdoelSelectie.Alles` — every loaded leerplandoel, with no
  cap. That is harmless while the database holds only the demo seeder's `DEMO-*` goals, and it stops being
  harmless the moment this story makes a real per-discipline import runnable: prompt size (and AI cost) then
  grows with the curriculum. No cap was invented in E2-08 on purpose — deciding which goals are silently
  withheld from the model is pedagogical, not technical. The levers that exist are the endpoint's optional
  `selectie` and the panel's two filters.
  > **Weighed and ruled (owner, 2026-07-31, on this story shipping): the default stays `Alles`; no cap, no
  > code change.** The reasoning is the one this note already gave: choosing which goals are silently withheld
  > from the model is pedagogical. So this clause is discharged, not outstanding. The consequences now live
  > where the next author meets them rather than in a worklog: **E2-08 item 2** (treat `selectie` as the
  > per-run scoping lever; expect prompt size to scale with imported disciplines; do not add a cap later on
  > cost grounds without asking again) and **E7-11** (an uncapped, billable candidate list behind an
  > anonymous endpoint now has authentication as its only remaining mitigation).
  *Open (Art. XIV):* who may run an import, and from where? This is reference-data administration, so it likely belongs behind a directie-only role (Art. VI, FR-10) rather than a teacher-facing screen — which is a permissions decision, not just a routing one. Do not hard-assume; if E6-02's role matrix has not landed, put the endpoint behind a single authorisation seam.

### FR-2 — Doelen doorzoeken (added 2026-07-29)

- [x] **E1-16 — Doelen-UI: doorzoeken, filteren en één doel inspecteren** — *added 2026-07-29: the nav has a "Doelen" item and no story owned the screen behind it. **Done 2026-07-31** after three audit rounds and two verification passes.*
  **How it closed, because the path is the useful part.** Three audit rounds and two verification passes, on `fix/e1-16-audit-2` (`08acb8c`) on top of the merged `f0330ed`:
  | Round | `test-runner` | `antagonist` |
  | --- | --- | --- |
  | 1 | **FAIL** — the paging count bypassed `tAantal` | **VIOLATIONS FOUND** — 3 MAJOR, 7 MINOR, 2 QUESTION |
  | 2 | **PASS**, with the browser pass that had been missing all story | **VIOLATIONS FOUND** — 6 MINOR, 1 QUESTION, and *"no previously reported finding is falsely claimed as fixed"* |
  | 3 (fix diff only) | — | **VIOLATIONS FOUND** — 5 MINOR, all new; the seven round-2 findings verified closed, mutation checks reproduced independently |
  All findings from all three rounds are fixed; **nothing was waived**. The round-3 MINORs were: a comment that was *still* off by one about its own decomposition (now pinned by a test instead of prose), a page that told the teacher "de doelen konden niet geladen worden" while showing them, two record defects in this very entry and in the audit transcription, and a misleading indent.
  *Recorded because it shaped the whole story:* the implementer was cut off **three** times — twice by API stalls and finally by an **org spend limit**, mid browser check — and the Playwright MCP server was down from start to finish. So the round-1 fixes were self-verified, which is why this story sat at `[~]` until round 2 could run; and the browser evidence that finally closed it came from the test-runner driving Edge through the `playwright-core` in the npx cache, not from the MCP tools. Full record in [worklogs/E1-16](worklogs/E1-16/implementation.md), verdicts in [antagonist.md](worklogs/E1-16/antagonist.md) and [test-report.md](worklogs/E1-16/test-report.md), browser evidence in `docs/ux/wireframes/e1-16-r2-*.png`.
  > **One extra item for that re-run, found from outside this story (E1-15's test-runner, 2026-07-31).** Two of E1-16's error responses are **English and user-facing**, which is an Art. II.3 question under the ruling of 2026-07-30 (the language of a message follows who it is for): `GET /api/leerplandoelen` answers a validation failure with a **bare English string** rather than a `ProblemDetails`, and a missing code returns the framework's default 404 with the English title `"Not Found"`. Neither is caused by E1-15's `Probleemtitels` consolidation, which is why it is filed here rather than fixed there: both predate that merge and belong to whoever closes this story. Decide per message who can act on it. A malformed filter is arguably an operator diagnostic; a teacher opening a stale bookmark to a code that no longer exists is the exact case the Dutch side of the split is for. While you are there, the bare string is also an envelope inconsistency: every other endpoint answers `ProblemDetails`, and E1-13 will have to parse both.
  *Built:* `GET /api/leerplandoelen` (filter, search, server paging, total), `GET /api/leerplandoelen/{code}`, and a facets endpoint the filter lists are built from; `ILeerplandoelenQuery` in Application with the EF adapter in Infrastructure (deliberately *not* a second instance of E7-13); `frontend/src/features/doelen/` as a dense register with the detail on its own deep-linkable route.
  *Gates on the merged tree, re-run by the orchestrator:* `dotnet format` clean, **475 unit + 115 integration against real PostgreSQL, 0 failed, 0 skipped**, **174 frontend tests / 12 files**, lint and build clean. Browser-checked at 1440px and exactly 390px against the live database: no horizontal overflow, body text 15.42:1, muted labels 6.08:1, the six doelsoort badges 5.08–7.46:1, status badges 4.70–7.35:1, all alpha-composited.
  *The audit findings worth carrying forward, because each names a pattern rather than a bug:*
  1. **The screen told a teacher the curriculum had never been imported** — briefly on every cold visit, permanently after a facets error — because one boolean stood in for three query states. The story's brief had demanded three empty states never be collapsed, and a *fourth* state appeared underneath that rule. Every test walked past it via `await findByText`.
  2. **Two comments asserted a server guard that did not exist** (a bare `?subdomein=` mixed unrelated domeinen into one total), and the covering test was *named* for the invariant its first assertion disproved. Third instance in this repo of that exact failure mode. Ruled: make the comment true, so the controller now 400s (Art. VII.0).
  3. **The detail published class-scoped Subdoel/Activiteit links school-wide with no seam**, i.e. it answered the open FR-10.2 visibility decision with the widest option. Ruled: keep the information, add `Koppelingzichtbaarheid` passed from one place, and make every row state *hele school* or name its klas. Narrowing was refused because a doel used only through one klas's activiteit would then have read as used nowhere.
  4. **The plural bug shipped for the fifth time** ("Volgende 1 doelen laden"), and for the second time in a commit whose own log claimed it was fixed. Each earlier fix was applied to the instance that had been noticed, which is why a fifth was there to find. Now closed as a class: `frontend/src/i18n/catalogus.test.ts` fails on any `{aantal}` string with no singular, with a checkable reason per exemption and a second test keeping the exemption list from rotting. It immediately exposed an undocumented coupling: `kalender.teVol` is only grammatical because `VOORLOPIGE_TE_VOL_DREMPEL` is 3.
  *Deliberately not built:* no minimumdoel tab or screen (E1-12 blocks it, so the concordance is shown per doel with an honest line instead of a destination that renders nothing), and no import control on the empty state (E1-15 owns the trigger, so it says loading Op.stap is beheerderswerk and links to nothing).
  *Open for directie:* whether a zero-count filter option should disappear rather than show `(0)`. The counts now scope to the active filter and the option sets stay stable, so no number lies and no keuzelijst shifts mid-task.
  The teacher-facing screen for the imported curriculum: browse and search all `Leerplandoel`s (and, once **E1-12** lands, the `Minimumdoel`s), filter by discipline / domein / subdomein / doelsoort / jaarFase, and open **one doel** to read its full text, voorbeelden, toelichting, woordenschat and its concordance to a minimumdoel.
  *Why this story exists:* found the way E0-10 and E2-08 were found. The information architecture in [`docs/ux/ui-ux-approach.md` §3](../docs/ux/ui-ux-approach.md) lists **Doelen** as the first primary destination — *"browse/filter Op.stap leerplandoelen + minimumdoelen (read-only)"* — and **no story in E0–E8 owned it.** The neighbours only look like they cover it and do not: **E2-06** lists solely the *ongekoppelde* doelen (FR-4.4), **E5-03** filters by doelsoort inside the *coverage* view (FR-9.2), and **E1-03/E1-04** import the data without giving anyone a way to read it. So the app can hold the entire Op.stap curriculum and a teacher cannot look up a single doel. E0-10 shipped the nav item pointing at a placeholder, which is honest but leaves the destination unbuilt.
  *Done when:* a teacher can, on screen —
  1. see a paginated or virtualised list of leerplandoelen with code, doelsoort badge, jaarFase and text — **at real volume**, which after a full import is thousands of rows, not the handful E2-06 renders;
  2. search on code and on free text, and filter by discipline / domein / subdomein / doelsoort / jaarFase, with the taxonomy grouped by `(domein, subdomein)` because subdomein names are **not** globally unique (Art. VII.0);
  3. open one doel and read every imported field, plus its `minimumdoelRef` concordance and which thema's it is linked to (with each link's status);
  4. see that it is **read-only** — the curriculum is official reference data and the UI must offer no edit affordance whatsoever (Art. III.1). A teacher may only add internal labels/ordering, and that is not this story.
  Ref: FR-2.1/2.2/2.3/2.4, Art. III.1, Art. VII.0/VII.1, Art. XII (doelsoort colour + label, never colour alone), ADR-0017; `ui-ux-approach.md` §3.
  *Depends on:* **E0-10** (the route and nav item exist and currently render a placeholder). Server side: a list/search endpoint does **not** exist yet — `OngekoppeldeDoelenController` answers only the gap query — so this story includes the read API, or an explicit split into a sibling story if directie wants it separated.
  *Blocked in part by:* **E1-12** for the minimumdoel half. The leerplandoel half is buildable today; the *"+ minimumdoelen"* of §3 cannot be shown while no `Minimumdoel` row can exist.
  *Open (Art. XIV) — do not hard-assume:* which disciplines are in scope (the "disciplines first" decision), whether `leergebied`/Wereldoriëntatie needs surfacing, and the `jaarFase` code form (1K/2K/3K ↔ JK/K2/K3). Each of those changes what the filters offer, so build the filter list from the data rather than from a hard-coded enum.

### FR-2 — One definition of a doelkoppeling (added 2026-07-31)

<a id="e1-17"></a>

- [ ] **E1-17 — One shared definition of "which layers hold a `DoelKoppeling`"** — *added 2026-07-31, from a defect the E1-16 round-2 gates found in **E1-15** after it had closed*
  Give the four `DoelKoppeling` layers (`Themadoel`, `Thema.Doelsuggesties`, `Subdoel`, `Activiteit.Doelkoppelingen`) **one** definition that both the re-import's reference count and the register's detail read use, and cover the layer that is currently missed.
  *Why this story exists:* `OpstapImportService.KoppelingAantallenAsync` counts three layers and omits `Thema.Doelsuggesties`; `LeerplandoelenQuery.HaalKoppelingenAsync` reads four. The two disagree about the same doel, which is how this was found: not by either story's audit, but by comparing their outputs. The count is load-bearing in the importer, so the omission has consequences (see the ⚠️ note under **E1-15**): the review report does not disclose that a disappeared goal is still referenced by a suggestion, and with the opt-in purge enabled the delete hits a `Restrict` FK. A fourth `Concat` is the patch; one definition is the fix, because the same divergence will recur the next time a layer is added.
  *Done when:* a re-import that drops a goal referenced **only** by a `Thema.Doelsuggestie` reports it as still linked and never deletes it (asserted against real PostgreSQL, since the FK is the thing being relied on); the layer set lives in one place that both callers consume; and adding a fifth layer in future cannot update one caller without the other. Ref: FR-2.5, Art. III.4 (a re-import flags what to review and destroys nothing), Art. IV.2, Art. VIII.
  *Ownership note:* this reopens no checkbox. **E1-15** stays `[x]` — its acceptance criteria are met and its trigger works; what is wrong is a count inside it, and pretending the whole story regressed would be as inaccurate as leaving the defect unowned. This story owns the fix.
  *Do not fold into E1-13.* E1-13 renders the review report; if this is unfixed when it ships, E1-13 will render an accurate view of an incomplete report, which is the harder defect to notice.

- [ ] **E1-19 — Een subthema herschalen sleept elke activiteit mee over de klasgrens** — *filed 2026-08-05 by owner ruling, on the antagonist QUESTION against E4-08: leave the route as it is and file it, rather than widen that story or leave the finding in a code comment*
  `PUT /api/subthemas/{subthemaId}` accepts a `klasId` and `SchoolcontentBeheerService.WijzigSubthemaAsync` passes it to `Subthema.WijzigScope`, which re-points the subthema at another klas. Its **subdoelen and activiteiten inherit the scope structurally** (they carry no klas of their own, Art. IX.2), so one request moves a whole branch of one class's content into another class, including every `Manueel` `DoelKoppeling` a teacher made and every doel those links contribute to **both** classes' dekking.
  *Why this is filed now and not before:* **E4-08** established, by owner ruling, that *an activiteit never crosses a klas*. Its own verb enforces that as a domain invariant (`Subthema.VerplaatsActiviteitNaar` refuses, `Activiteit.VerhuisNaar` is `internal`), and its antagonist audit then asked the right question: the invariant binds **the verb**, not the system, because this route reaches the same outcome. The comment on that invariant is deliberately narrowed to say so.
  *What is genuinely not a defect here:* no screen offers it. `Subthemaformulier` takes the klas from the section it was opened in and E4-08's audit verified there is no UI path, so this is API-only, pre-existing E1-10 behaviour. The class-crossing is reachable only by a hand-written request.
  *What has to be decided rather than coded:* whether re-scoping a subthema to another klas is a legitimate operation at all. Two readings, and this is the ruling the story needs first:
  1. **It is a mistake the API should refuse.** Then `WijzigScope` keeps the leeftijd half and loses the klas half, `SubthemaWijzigingInvoer.KlasId` goes away or is verified against the stored value, and the refusal is a Dutch sentence a teacher can act on. Consistent with E4-08's rule and with Art. IX.2 being *structural*.
  2. **It is a legitimate correction** (a subthema entered under the wrong klas). Then it must be explicit and must disclose its consequences, the way E4-08's move panel does: how many activiteiten and doelkoppelingen travel, and that **two** classes' dekking figures change. Under Art. III.4's spirit the non-destructive option is to refuse and let a human redo it, which is the reading the leerplandoel-discipline case was ruled on (see `README.md`, 2026-07-31).
  *Done when:* the ruling is recorded here, the endpoint implements it, and a test against **real PostgreSQL** pins the chosen behaviour (E7-16: this is a write path whose whole content is an FK reassignment, so the in-memory provider cannot show it). If option 1, also assert that no request can move class-scoped content between klassen by any route, which is the property E4-08 could only claim for its own verb. Ref: FR-3.1, [Art. IX.2](../CONSTITUTION.md#article-ix--core-data-model-functional), Art. V.1.
  > *Related and deliberately separate:* **E7-19** is about the *message* `WijzigSubthemaAsync` can emit (an English `'naam' is required.` with `(Parameter 'naam')` appended, rendered verbatim by the same forms). Same endpoint, different defect; fixing one does not fix the other.

- [ ] **E1-20 — Een melding op de klaslaag blijft staan na een geslaagde schrijfactie die er niets mee te maken heeft** — *filed 2026-08-05 by E4-08's antagonist round 2, which found that E1-14's round-4 fix never fixed the case its own comment described, and that E4-08 then deleted the repo's only record of it*
  `Klaslaag` raises *"Dit subthema bestaat niet meer. Iemand anders heeft het verwijderd."* when a delete answers 404, and the notice lives at section level deliberately (the row it belongs to is refetched away). It is cleared on exactly one event: a **successful subthema create**. So the sequence E1-14's round 4 wrote down still holds: 404 on subthema A, then a *successful* delete of subthema B, and the teacher reads that someone else deleted something while looking at the result of their own action.
  *Why it is E1's and not E4-08's:* the only write `Klaslaag` owns is the create. Every other write on that screen lives in `Subthemakaart` (subthema edit and delete, activiteit create/edit/delete, both link operations), so clearing on "any later successful write" means a callback threaded through five call sites in E1-14's component. E4-08 replaced the **mechanism** (a render-phase read of a latched `isSuccess`, which cleared notices on the render that raised them) and deliberately did not widen into the call sites.
  *What E4-08 did leave behind:* a test asserting a notice does not survive a later successful **create**, so the mechanism itself is pinned. Nothing pins the delete case, because nothing implements it.
  *Done when:* a notice on the klaslaag does not survive any later successful write on that section, and a test asserts it for at least one non-create write. Ref: FR-3.1, Art. X.3. **Related:** `Klaslaag.tsx`'s comment names this story; keep them in sync or delete both.
  > *The transferable part, and the reason this is a story rather than a note:* E1-14's round 4 described this defect **in the comment of the code that did not fix it**, and it survived four audit rounds on that story plus two on E4-08 because every test in the file performs exactly one write. A defect described in a comment beside an unrelated fix is a defect nobody owns.
